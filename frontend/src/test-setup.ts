/*
 * Vitest global test setup.
 *
 * Imports @testing-library/jest-dom so all tests have access to custom
 * matchers (toBeInTheDocument, toHaveTextContent, etc.) without per-file
 * imports.
 */
import "@testing-library/jest-dom";
