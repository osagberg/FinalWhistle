[package]
name = "fw-memory"
description = "Event-sourced career ledger — careers that remember. ADR-0005 schema accepted 2026-05-18 (T3-1)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lints.clippy]
# Canonical-state crate. The ledger is append-only and replays
# deterministically; floats are forbidden.
float_arithmetic = "deny"

[dependencies]
fw-core      = { workspace = true }
rand         = { workspace = true }
rand_chacha  = { workspace = true }
serde        = { workspace = true }
thiserror    = { workspace = true }

[dev-dependencies]
proptest   = { workspace = true }
serde_json = { workspace = true }
insta      = { workspace = true }
