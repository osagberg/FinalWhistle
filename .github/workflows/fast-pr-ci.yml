# Fast PR CI (Tier A) — Final Whistle
#
# Per design/production-pipeline.md:
# - Runs on every PR + push to main/develop
# - GitHub-hosted Linux only (cheapest minute rate)
# - Budget: ≤5 minutes per run
# - No Unity Editor boot, no builds, no macOS/Windows runners
#
# SECURITY NOTE: this workflow does NOT interpolate any untrusted input
# (github.event.*.title, github.event.*.body, github.head_ref, commit
# messages, etc.) into run: steps. All step commands are static shell
# invocations. See:
# https://github.blog/security/vulnerability-research/how-to-catch-github-actions-workflow-injections-before-attackers-do/
#
# Extension model: Tier-A checks are wired inside `scripts/fw verify`.
# The single job below calls `fw verify`, which currently runs:
#   - verify-docs  (frontmatter + unsubstituted-placeholder check)
#   - banned-terms (Category-A + Category-B vocab lint per design/ui-vocabulary.md)
#   - dotnet test  (MatchSim + MatchSim.Tests)
# Future Phase-3/6 checks (determinism replay hash, content-pack schema,
# save-migration fixtures) wire into `fw verify` as they land —
# no workflow file edit required. The commented jobs below are a reference
# shape for when a check needs to run as a parallel job instead of inside
# the umbrella (e.g., when it's slow enough to want its own failure surface).

name: Fast PR CI (Tier A)

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

# Cancel in-progress runs for the same branch when a new push arrives —
# saves Actions minutes on rapid iteration.
concurrency:
  group: fast-pr-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    name: Verify (Tier A umbrella)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up .NET SDK
        # Required for `fw verify`'s embedded `dotnet test` step
        # (Phase-3 Week-1 onward — MatchSim + MatchSim.Tests).
        # MatchSim.csproj targets netstandard2.1 (Unity-compat);
        # MatchSim.Tests.csproj targets net10.0. global.json pins the
        # local SDK feature band; CI installs the matching major/minor SDK.
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Run fw verify
        # Umbrella runs every Tier-A check currently implemented
        # (verify-docs + banned-terms + dotnet test as of 2026-04-26).
        # New checks wire into scripts/fw verify as Phase-3/6 deliverables land.
        run: ./scripts/fw verify

  # ------------------------------------------------------------------
  # Phase-gated TODOs — reference shapes only, NOT a backlog.
  # Tier-A checks are expected to land inside `scripts/fw verify`
  # (umbrella). Uncomment below ONLY if a specific check needs its own
  # parallel job (e.g., separate failure surface, independent timeout,
  # or heavier setup like dotnet/python install that the umbrella
  # shouldn't carry). As of 2026-04-26, verify-docs + banned-terms +
  # dotnet test run inside the umbrella with no standalone-job need.
  # ------------------------------------------------------------------

  # banned-terms-lint (SUPERSEDED by umbrella — kept as reference shape):
  #   name: Banned-terms lint (Phase 1, parallel-job shape)
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 5
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'
  #     - name: Run lint-banned-terms.py
  #       run: python scripts/lint-banned-terms.py

  # matchsim-tests-matrix (DEFERRED; umbrella covers current Linux-only run;
  # promoted to Win/Mac/Linux matrix when Tick + Seed expand the determinism
  # corpus enough to justify the added Actions cost).
  # Per SPEC Phase-3 task `GitHub dotnet-test matrix (Tier A subset —
  # Win/Mac/Linux)` and design/production-pipeline.md §Cost discipline
  # (macOS minutes ~10× Linux; matrix activates only when the test surface
  # warrants the cost):
  #   strategy:
  #     matrix:
  #       os: [ubuntu-latest, windows-latest, macos-latest]
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: '10.0.x'
  #     - name: dotnet test
  #       run: dotnet test FinalWhistle.slnx --logger "console;verbosity=normal"

  # determinism-smoke:
  #   name: Deterministic replay hash smoke (Phase 3)
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 5
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: '10.0.x'
  #     - name: fw replay canonical-seed
  #       run: ./scripts/fw replay 0xDEADBEEF

  # content-pack-schema:
  #   name: Content-pack schema + ID stability (Phase 6)
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 5
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: fw content-lint
  #       run: ./scripts/fw content-lint

  # save-migration-smoke:
  #   name: Save migration fixture smoke (Phase 6)
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 5
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: '10.0.x'
  #     - name: Run save migration fixture tests
  #       run: dotnet test MatchSim.Tests --filter Category=SaveMigration
