# STATUS — Final Whistle

**Last updated**: 2026-06-03

## Phase

**Phase T4 — Pillar Wiring + Polish — IN PROGRESS.** T4-1/2/3/4/5a/6a + T4-2.5a + T4-sim-halt + T4-2.5b + T4-2.5c (+T1-25/T1-26) + T4-2.5e + T4-2.5d + T4-2.5f + T4-F1 + T4-F2 + T4-2.5g + T4-2.5h + T4-F4 + T4-I1 + QA-3 + QA-5 + T4-2.5i + T4-2.5j + T4-2.5k + T4-2.5L + **T4-7** DONE. **PHASE-T4 IMPLEMENTATION COMPLETE — the autonomous /loop HARD-STOPPED here** (terminus condition: all selectable T4 rows done; only the T4-8 phase gate + the DEFERRED T4-2b remain). The loop did NOT run `/done`, did NOT start T4-8, did NOT enter T4.5 — awaiting the user to drive the phase gate. The game-shell now looks like a finished product (branded main menu, splash, OS-theme-aware window, committed per-OS app-icon). All five pillars are wired AND have a falsifiable proof: T4-2.5L proves Pillar 2 cross-decade (a season-1 event survives the 5-season compaction boundary + still renders in season 8), emits RegressiveCollapse at a terminal `CAREER_END_SEASON`, + a 100-ledger compaction-retention corpus. T4-2.5k also fixed a pre-existing silent failure: memory callbacks now render REAL prose instead of a generic fallback. The 600-tick canonical pin was rebaselined at T4-2.5j (user-authorized, envelope-verified); the 60-tick smoke pin is UNCHANGED. T4.5 (World Scale + Content Bake) is the EA-critical phase between T4 and T5.

**2026-06-02 external dual review** correct → persistent → visible all landed; all T4-2.5x pillar-polish + QA + game-shell rows done. **Phase-T4 implementation is complete.** Remaining: **T4-8** (phase-gate Codex review — the user runs `/done` → `gh pr create` when ready) + DEFERRED T4-2b. F5/F6 (match-engine) second; C1 at private-flip. **Filed follow-ups:** roster Squad → Player navigation; match-sim `team_width` flake; T4-I1 `import type` reorder; T4-2.5i loader error-distinction + `commentary_line_bank_id`; T4-2.5j inert `trigger: NoOpStub` + body_shield vacuous test + hash-pins fw-tauri pin; T4-2.5k PressTopic::as_dto_str + repo-wide season-display indexing.

## Active task

**Phase T4 SHIPPED + TAGGED (`v0.4.0-polish` @ `c7ef7dcf`), pushed; CI + Determinism + Win/Linux Release green** (macOS Release codesign deferred to T5-1 — unconfigured Apple signing + the empty-`APPLE_*`-env unsigned-fallback workflow bug). Now in a user-directed **post-T4 interlude before T4.5**: DONE so far — **DX-1** (dev play-harness: `fw-dev-server` HTTP bridge + `?backend=http`, lets an agent drive the REAL backend in a browser), **fun-pivot** (MASTER_PLAN **Tier F** + `drama-model.md` + `fun-evaluation-harness.md` + DECISIONS — schedule + AUTOMATE fun via deterministic drama metrics + an agent LLM-judge), **feature-backlog** research (`docs/design/feature-backlog.md` — the management-depth breadth axis), and **DX-2** (match-quality inspection: `inspect_frames` 7 glitch-detectors + `render_contact_sheet`; full match = 5400 ticks; diagnosed the broken match = **32,737 ImpossiblePlayerVelocity flags / 54-53**, the velocity-clamp bypass = the FUN-0 target). **FUN-0 velocity slice DONE 2026-06-04** (first match-feel fix): the velocity-cap bypass DX-2 diagnosed is FIXED — `dispatch.rs` 2D-magnitude cap (was per-component → √2×MAX diagonal) + `separation` EPSILON 0.001→0.2m; **ImpossiblePlayerVelocity 32,183→12**, score 54-53→43-43; movement is now physical. **Canonical hash REBASELINED** (authorized, multi-pin, main-thread envelope-verified): 60-tick `85f45bf8…`→`a490489b…`, 600-tick `12ce5ab7…`→`3efd5623…` across all 6 pin sites; self-review triple ACCEPT. **NOT yet watchable** — 43-43 is the residual goal-rate / ball-physics issue (`BallPhasingPlayer` 844 + `BallTeleport` 237 + shot-decision frequency). **FUN-H1 (drama-sweep) DONE 2026-06-04** — the last Tier-F instrument; diagnosed the 43-43 as BIMODAL (½ seeds 0-goal, ½ 80-109; mean 38.85 vs 2.3-3.2) = a deterministic POSSESSION-LOCK + 100%-on-target shots that always convert (no keeper block); realism violation is M1 (goal-rate), not timing. CI fix landed (exclude dev-only `fw-dev-server` from the cross-OS matrix + timeout 20→30; Windows was timing out on the axum/tokio+image build bloat). **NEXT (the watchable-match fix): the possession-lock + shot-conversion slice** — systems-designer (shot-utility/xG-conversion/keeper-block) + gameplay-programmer (the possession-lock BT), measured before/after with `drama-sweep`, AUTHORIZED rebaseline. Then FUN-1 (drama tuning) + archetype-distinctness probe, FUN-2/3 (commentary/callback), T4.5 (World Scale — EA-critical).

## Blockers

None blocking the next task. **KNOWN (pre-existing, NOT T4-2.5h): a match-sim proptest flake** — `fw-match-sim` `team_width_when_in_possession_within_band` occasionally fails (in-possession outfield Y-width ~23.98m vs the [24,70]m band floor, ~1.8cm under) on a random proptest seed. Proven unrelated to the career-layer work (zero fw-match-sim code touched; both canonical pins byte-identical). FILED as its own match-engine task (F5/T5-5b territory) — it makes `scripts/fw verify` probabilistically red until the band/behaviour is calibrated. T4-sim-halt closed the desktop FullTime/goal SPAM (2026-06-02). OPEN: match-goal-RATE realism (T5-5b).

Previously: T2-1d2 (xg_utility honesty / cross-band oscillation) re-anchored to Deferred section of MASTER_PLAN. Open EA-scope question resolved 2026-05-29 per DECISIONS.md — DESIGN_DOC `§MVP-scope` anchor added at §8 `### MVP scope`.

## Last green verify

2026-06-04 (FUN-0 velocity slice — REBASELINE): `scripts/fw verify` exit 0. Player velocity-cap bypass fixed (`dispatch.rs` 2D-magnitude cap + `separation` EPSILON 0.001→0.2m); ImpossiblePlayerVelocity 32,183→12 on the 5400-tick `0xfeedbeefcafefade` run; the 600-tick exit-gate goal envelope still holds (main-thread verified BEFORE re-pin per the multi-pin discipline). Canonical hash REBASELINED across all 6 sites: 60-tick `a490489b…`, 600-tick `3efd5623…`. Self-review triple → all three ACCEPT + 3 test-quality fixes (vacuous integration test rewritten; `MAX_PLAYER_SPEED_SQ` const-desync guard; two-tick separation cascade guard). 420 fw-match-sim tests; clippy/fmt/determinism-audit/banned-terms/cargo-deny clean. Prior (2026-06-04, both pins UNCHANGED): DX-2 (inspection tooling), DX-1 (play-harness), fun-pivot + feature-backlog (docs). Prior (2026-06-03): QA-T4H, T4-8-CR1, T4-7 close — Phase T4 shipped + tagged `v0.4.0-polish`. KNOWN: the match-sim `team_width` proptest flake remains filed (passed this run).

## Last canonical hash

`blake3:a490489b534ecd16dde46bd5a8f2022b7a3fd127778b8f73050712bcd1b99dba` (60-tick) + `blake3:3efd5623b1e85d67d1b18cc350c86ea97f6e5740208651132e97e78fcd35b2d0` (600-tick). **BOTH REBASELINED at FUN-0 (2026-06-04)** — authorized (Tier-F + user "go"); the player velocity-cap fix (2D-magnitude cap + separation EPSILON) changed per-tick movement across all 22 players, so BOTH pins drifted (unlike T4-2.5j which was 600-tick-only). Main-thread independently verified (`inspect_frames` ImpossiblePlayerVelocity 32,183→12 + the 5-seed 600-tick goal envelope still in band) BEFORE re-pinning, per the post-T1-15 multi-pin discipline. Pin sites (6): 60-tick in `canonical_hash.rs` + `0xdeadbeefdeadbeef.ron` + `fw-content/tests/fixtures_load.rs`; 600-tick in `canonical_hash.rs` + `0xfeedbeefcafefade.ron`; + the hand-synced `fw-tauri` `t4_2_5e_canonical_pins_unchanged`. Prior 60-tick: `85f45bf8…` (T4-sim-halt). Prior 600-tick: `12ce5ab7…` (T4-2.5j), `206bddae…` (T4-2.5c), `856a7fed…` (T4-sim-halt). Save wire bytes: SaveEnvelope V0–V4 (V4 = `0x04`, T4-2.5g, current production schema); SettingsEnvelope V0. Next save bump: SaveV5 at T4.5-H / T4.5-E1.
