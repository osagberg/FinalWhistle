# STATUS — Final Whistle

**Last updated**: 2026-06-04

## Phase

**Phase T4 — Pillar Wiring + Polish — IN PROGRESS.** T4-1/2/3/4/5a/6a + T4-2.5a + T4-sim-halt + T4-2.5b + T4-2.5c (+T1-25/T1-26) + T4-2.5e + T4-2.5d + T4-2.5f + T4-F1 + T4-F2 + T4-2.5g + T4-2.5h + T4-F4 + T4-I1 + QA-3 + QA-5 + T4-2.5i + T4-2.5j + T4-2.5k + T4-2.5L + **T4-7** DONE. **PHASE-T4 IMPLEMENTATION COMPLETE — the autonomous /loop HARD-STOPPED here** (terminus condition: all selectable T4 rows done; only the T4-8 phase gate + the DEFERRED T4-2b remain). The loop did NOT run `/done`, did NOT start T4-8, did NOT enter T4.5 — awaiting the user to drive the phase gate. The game-shell now looks like a finished product (branded main menu, splash, OS-theme-aware window, committed per-OS app-icon). All five pillars are wired AND have a falsifiable proof: T4-2.5L proves Pillar 2 cross-decade (a season-1 event survives the 5-season compaction boundary + still renders in season 8), emits RegressiveCollapse at a terminal `CAREER_END_SEASON`, + a 100-ledger compaction-retention corpus. T4-2.5k also fixed a pre-existing silent failure: memory callbacks now render REAL prose instead of a generic fallback. The 600-tick canonical pin was rebaselined at T4-2.5j (user-authorized, envelope-verified); the 60-tick smoke pin is UNCHANGED. T4.5 (World Scale + Content Bake) is the EA-critical phase between T4 and T5.

**2026-06-02 external dual review** correct → persistent → visible all landed; all T4-2.5x pillar-polish + QA + game-shell rows done. **Phase-T4 implementation is complete.** Remaining: **T4-8** (phase-gate Codex review — the user runs `/done` → `gh pr create` when ready) + DEFERRED T4-2b. F5/F6 (match-engine) second; C1 at private-flip. **Filed follow-ups:** roster Squad → Player navigation; match-sim `team_width` flake; T4-I1 `import type` reorder; T4-2.5i loader error-distinction + `commentary_line_bank_id`; T4-2.5j inert `trigger: NoOpStub` + body_shield vacuous test + hash-pins fw-tauri pin; T4-2.5k PressTopic::as_dto_str + repo-wide season-display indexing.

## Active task

**On the no-EA dynamic, best-game-first believability spine** (DECISIONS 2026-06-04 "No EA milestone" — believability + depth lead; scope/timeline are not gates; the roadmap is dynamic). Spine order: **FUN-TS1 ✓ → FUN-TS2 ✓ → FUN-TS3 → FUN-TS4 → FUN-LAW1..4 → FUN-LM/CB/TI/PI**; T4.5 world-scale sequences when it serves the game.

**FUN-TS1 + FUN-TS2 SHIPPED.** FUN-TS1 gave each defending team a held zonal block (per-team `TeamShape` sidecar, ADR-0013; the fix was gating the `RunningOffBall` arm on `is_defending` so FWDs hold the line). FUN-TS2 (commit `11330476`) added coordinated pressing lines (`PressRole`: 1 Primary + 2 Cover + rest HoldShape), offside (new `MatchEvent::Offside` disc 6 + Tracery grammar + IFK restart; no offside from throw-in/corner/goal-kick), made `enforce_hold_zonal` bias-dominant (the FUN-TS1 P1), and shot-quality tuning that pulled on-target 63%→44.5% while holding goals in band. A 5-agent verification + re-verify caught + fixed 2 bugs pre-commit (CRITICAL press-role off-by-one; HIGH frontend-offside-missing-from-validator). Independent re-verify: `scripts/fw verify` GREEN; 20-seed drama-sweep M1=2.60 / on-target 44.5% / shots 10.4 / max 5 — all PASS; contact-sheet reads as football; pnpm green. Canonical 600-tick REBASELINED `6805c105…`→`f139c76a…`; 60-tick `d1170bfc…` UNCHANGED.

**NEXT = FUN-TS3** (the in-possession slice: CB1 passes-can-fail FIRST, then TS3 build-up geometry) — the implementation blueprint is ready at `docs/design/fun-ts3-cb1-implementation-brief.md`. Four design docs committed this session: `docs/design/{player-stats-presentation.md, gene-attribute-compiler.md, player-identity-descriptors.md, fun-ts3-cb1-implementation-brief.md}`.

**Pre-existing follow-ups (NOT TS2):** sigs/match=26 (signature-threshold artifact, the M8 ALL-GUARDS-FAIL driver → `systems-designer`); HighPress fires rarely within match windows (`HIGH_PRESS_REENTRY_COOLDOWN`) → verify/tune press-trigger frequency at FUN-TS4.

## Blockers

None blocking the next task. **KNOWN (pre-existing, NOT T4-2.5h): a match-sim proptest flake** — `fw-match-sim` `team_width_when_in_possession_within_band` occasionally fails (in-possession outfield Y-width ~23.98m vs the [24,70]m band floor, ~1.8cm under) on a random proptest seed. Proven unrelated to the career-layer work (zero fw-match-sim code touched; both canonical pins byte-identical). FILED as its own match-engine task (F5/T5-5b territory) — it makes `scripts/fw verify` probabilistically red until the band/behaviour is calibrated. T4-sim-halt closed the desktop FullTime/goal SPAM (2026-06-02). OPEN: match-goal-RATE realism (T5-5b).

Previously: T2-1d2 (xg_utility honesty / cross-band oscillation) re-anchored to Deferred section of MASTER_PLAN. Open EA-scope question resolved 2026-05-29 per DECISIONS.md — DESIGN_DOC `§MVP-scope` anchor added at §8 `### MVP scope`.

## Last green verify

2026-06-04 (autonomous believability-first session — NO rebaseline): targeted verify green — fw-match-sim 617 tests + clippy `-D warnings` clean; fw-tauri full suite + clippy clean; `cargo fmt --check` clean workspace-wide; banned-terms clean; canonical pins UNCHANGED (`e56562f8…`/`6805c105…`) — the drama-sweep hardening is bin-local, the scout fix is off the match canonical-hash path, the rest is docs/comments/tests. The fw-match-sim commit's `canonical-hash-guard` re-confirmed pins at commit time. Full `scripts/fw verify` + cross-OS CI NOT re-run this session (no canonical/sim-behavior change to gate — that's the push/phase-gate step). Prior — 2026-06-04 (FUN-0b+c watchable-match — REBASELINE): `scripts/fw verify` exit 0. Shot-quality xG gate + SS2 dispersion + SS3 GK save (gated on `last_shot_xg>0`) + `resolve_tackles` dispossession + carrier-aware Press/Mark + GK post-save distribution + 14-round tuning → drama-sweep M1 = 3.15 goals/match (in band). Canonical hash REBASELINED across all 6 sites: 60-tick `e56562f8…`, 600-tick `6805c105…` (main-thread envelope-verified the new full-match goal guard + drama-sweep M1 BEFORE re-pin per the multi-pin discipline; VERSION 9→11 for `last_shot_xg` + `tackle_cooldown_until`). Self-review triple → all three ACCEPT (P1 stale-xG clear fixed in-place; 2 P2: stale comment fixed, GK-carrier-tackle filed). fw-match-sim full suite green; clippy/fmt/determinism-audit/banned-terms/cargo-deny clean. Prior (2026-06-04): FUN-0 velocity slice (`a490489b`/`3efd5623`), FUN-H1, DX-2, DX-1, fun-pivot + feature-backlog. Prior (2026-06-03): QA-T4H, T4-8-CR1, T4-7 close — Phase T4 shipped + tagged `v0.4.0-polish`. KNOWN: the match-sim `team_width` proptest flake remains filed (passed this run).

## Last canonical hash

`blake3:d1170bfc6075ce825130f815b1dd7540bfb29e8cad7194010399681883170880` (60-tick, UNCHANGED since FUN-TS1) + `blake3:f139c76a631b7eb104e9d7619e9fb94ac9502e9afa5477fca9eaae6ec8c96d1c` (600-tick). **600-tick REBASELINED at FUN-TS2 (2026-06-04, `6805c105…`→`f139c76a…`)** — authorized (believability-first team-shape track); the coordinated press lines + offside + shot-quality tuning change per-tick player trajectories + shot selection over the 600-tick window. The **60-tick `d1170bfc…` is UNCHANGED** (HighPress doesn't fire within the pinned short-window, so the press + offside leave that state byte-identical). Independent re-verify (drama-sweep M1=2.60 in band + contact-sheet) BEFORE re-pinning, per the multi-pin discipline. Pin sites (6): 60-tick in `canonical_hash.rs` + `0xdeadbeefdeadbeef.ron` + `fw-content/tests/fixtures_load.rs`; 600-tick in `canonical_hash.rs` + `0xfeedbeefcafefade.ron`; + the hand-synced `fw-tauri` `t4_2_5e_canonical_pins_unchanged`. Prior 600-tick: `6805c105…` (FUN-0b+c watchable-match), `3efd5623…` (FUN-0 velocity), `12ce5ab7…` (T4-2.5j). Prior 60-tick: `e56562f8…` (FUN-0b+c), `a490489b…` (FUN-0 velocity), `85f45bf8…` (T4-sim-halt). Save wire bytes: SaveEnvelope V0–V4 (V4 = `0x04`, T4-2.5g, current production schema); SettingsEnvelope V0. Next save bump: SaveV5 at T4.5-H / T4.5-E1.
