# STATUS — Final Whistle

**Last updated**: 2026-05-17

## Phase

**T2 in progress. T2-3 (`fw-content-baker` validator-as-one-class refactor + offline-deterministic `bake-names` end-to-end + manifest emission) closed 2026-05-17.** Total T2 rows DONE: T2-1a/b/c/d-infra + T2-1-codex-fix + T2-2 + T2-3 (6 of 10 MVP rows). T2-1d2 (utility_shoot rewire + coefficient apply) DEFERRED to end-of-T2 cadence. T2-3 ships the FW v1 `IdentityPacketValidator.cs` carry-forward pattern via 4 dedicated `<Kind>Validator` structs in `fw-content-baker/validators.rs` (each with `Default + ::new()` + chained private check methods + `pub fn validate(&self, &<Kind>) -> Result<(), ValidationError>` returning structured error variants), the new `bake.rs` module with `BakeNamesOffline` worker (seeded ChaCha8Rng samples `Culture.first_name_bank × Culture.last_name_bank` deterministically), `BakeManifest` JSON sidecar (`model_id` / `prompt_hash` / `seed` / `output_path` bare-filename / `output_blake3` / `count`), and the `BakeNames` CLI subcommand wired end-to-end. Real Claude API path deferred to T2-4 (`model_id = "offline-v1"` for the deterministic path). Self-review triple verdicts: silent-failure-hunter REVISE (1 P0 + 3 P1 + 1 P1-edge → ALL fixed in-place: patronymic-pattern fail-loud; slug-collision `AlreadyExists` guard; `validate_fragment` returns own `NotImplemented`; CLI `--culture` `required = true`; `team_name_bank` doc-honesty); code-reviewer 3 P1s (2 fixed in-place: manifest `output_path` bare filename for cross-machine reproducibility; `Command::Manifest` milestone `T2-3` → `T2-4`; 1 deferred P2: workspace-hoist clap/tokio/reqwest/jsonschema — requires root Cargo.toml touch); type-design-analyzer Accept-with-revisions (4 P2s deferred to T2-4). 22 tests green (17 unit + 5 integration). **Canonical hashes UNCHANGED on both pins** — `fw-content-baker` is a non-canonical-path bin crate.

## Active task

(none — T2-3 closed at this commit; `scripts/fw verify` exit 0; **canonical hashes UNCHANGED on both pins** — bin-crate work; sim crates untouched. Next `/next` picks **T2-4** (`PlayerBio` generation — 500 players + phenotype-label catalog + 22-field gene model port from `design/player-generation.md`) per declared order + skip-DEFERRED rule (T1-17, T1-25..T1-28, T2-1d2, T4-9 all DEFERRED). 12 commits ahead of origin/main waiting to push.)

## Phase pointer

- **Just landed:** **T2-3** — `crates/fw-content-baker/src/bake.rs` (NEW ~310 LoC) with `BakeManifest` JSON sidecar + `BakeNamesOffline<'a>` worker. `crates/fw-content-baker/src/validators.rs` (heavy refactor ~726 LoC) with 4 dedicated `<Kind>Validator` structs + 9 new structured `ValidationError` variants + 13 unit tests (4 NotImplemented-honesty + 1 four-validators-exist smoke + 4 malformed-fixture-rejection + 4 secondary-check-after-primary-passes). `crates/fw-content-baker/src/main.rs` refactored: `run_validate_structural` consumes the 4 Validators; `BakeNames` subcommand wired end-to-end to `BakeNamesOffline`; `--culture` flag promoted to `#[arg(long, required = true)]`. NEW `crates/fw-content-baker/src/lib.rs` (9 LoC) re-exports `bake` + `validators` for integration-test access. NEW `crates/fw-content-baker/tests/bake_names_offline_test.rs` (~245 LoC) with 5 integration tests (AC3 100-entries + manifest filename-not-path assertions; AC4 same-seed byte-identical RON + manifest JSON; AC4-sanity different-seeds-differ; AC5 committed-cultures pass `CultureValidator`; AC5 committed-archetypes pass `TacticalArchetypeValidator`). All 5 silent-failure-hunter P0/P1 + 2 code-reviewer P1 findings fixed in-place pre-commit.
- **Next:** **T2-4** — `fw-content`: `PlayerBio` generation — names per cultural region (will consume `Culture` + the post-T2-3 `BakeNamesOffline` work), basic personalities, 22-field gene model (port from `design/player-generation.md`). Deps T2-3 (DONE). Likely a `narrative-director` task class (gene model + personality + bio templates). 500 players target; phenotype-label catalog covers all; ID-stability test passes round-trip. **Deferred follow-ups (status `DEFERRED` — `/next` skips)**: T1-17, T1-25, T1-26, T1-27, T1-28, T2-1d2, T4-9. **Carry-forward known follow-ups from T2-3 self-review (P2/P3)**: type-design F1 `BakeManifest.output_path: PathBuf` over String (deferred to T2-4 when API path adds a second hash-as-string site); type-design F2 `model_id` as enum with `OfflineV1 | Claude(String)` fallback (cheap and load-bearing for audit); type-design F4 single `CultureValidator::MIN_NAME_BANK_SIZE` const + runtime `minimum: usize` field in the two `Culture*BankTooSmall` variants so the error message can't drift from the constant; type-design F5 `Culture` invariant lives only in `CultureValidator` not the type — `ValidatedCulture(Culture)` wrapper or `Culture::try_new` constructor would centralize it (crosses crate boundaries, file for T2-4); code-reviewer P2 workspace-hoist `clap` / `tokio` / `reqwest` / `jsonschema` from inline version strings into `[workspace.dependencies]` (drop `tokio` + `reqwest` until first API consumer at T2-4); plus T2-1d-infra carry-forwards (`compute_opponent_shape_broken` wrapping_add→checked_add §11 hardening; direct unit-test matrix for `emit_possession_transition_events`'s 4 transition classes; `auto_exit_setpiece` eager-fire timing revisit at restart-timing landing; `setpiece_kind_for` corner-flag priority unit test; calibrate goal back-fill nearest-unmatched + determinism-audit per-rule exemption — both for T2-1d2).

## Blockers

None.

## Last green verify

2026-05-17 (T2-3 close): `scripts/fw verify` exit 0 (cargo fmt + clippy + cargo test --workspace + pnpm test 56 frontend + banned-terms + canonical-hash regression on both pins UNCHANGED + content-pack validate-structural + hash-pins atomicity test + cargo audit + cargo deny). 22 baker tests added (17 unit + 5 integration); 0 sim-test regressions.

## Last canonical hash

`blake3:eaf842ac3d19651d38dc7ce45d0763cc62b4d571ce2c2a5d56f1ee3c6ddead46` (60-tick smoke seed; UNCHANGED from T2-1b rebaseline — bin-crate work; sim crates untouched).

**Second corpus pin:** `blake3:aa7efe9b2a567d5e87d12c7da6a4ea928271429729884f38819baed85c3be5ae` (600-tick extended seed; UNCHANGED from T2-1-codex-fix rebaseline; T2-3 is non-canonical-path bin-crate work).
