# Status

**Last updated**: 2026-05-11 (Phase 3 🟡 ACTIVE — pure-C# MatchSim core feature-complete + deterministic-gate-locked; `unity-project/` on Unity 6.4 tech-stream `6000.4.4f1`; semantic-slice 5/5 shipped; dots-adapter **Slices 1-7 of 7 SHIPPED** — adapter complete. **Match-replay skill shipped 2026-05-11 (commit `3cae4d4`)** via 3-round `/duo-implement` cycle on topic `2026-05-11-match-replay-skill` (rounds 1+2 caught false-tick-evidence + seed-gate exit-code semantics bugs; round 3 acked). SPEC line 152 closed. **Phase-3 enforcement skeletons + Local MatchSim CI scripts already shipped** (SPEC lines 151 + 155 closed via commit `229da19` after the 2026-05-11 task-spec `dc0d1d05` discovered the work was already in place from the Codex round-4 batch). **Slice 7 shipped 2026-05-11 (commit `950f906`)** as the first real Tier-2 `/duo-implement` dogfood end-to-end: revised task-spec `fc1d4956` → round-1 commit-proposal `1d5e0ed7` → Codex counter `d353cbae` (4 P1 findings on motion-line wiring / pressure-tint wiring / SelectionRing retire-at-shot-end / scene-author + L2 evidence) → round-2 commit-proposal `de130051` → Codex ack `ba40da2b`. EditMode 206/206 + MatchSim 644/644; pinned 60-tick `MatchCanonicalState` hash UNCHANGED; `fw verify` GREEN; `fw shader-audit` clean (NO new shaders — blueprint sprite-emitter pattern). L2 evidence at `docs/screenshots/slice-7-{baseline-tactical-wide,selection-ring-pressure-tint,motion-line-burst-mid-fade,tension-cadence-player-isolation}.png`. Decision-5 compliant (NO IdentityCueRenderer; NO per-dot jersey-number text rendering). **Autonomous Tier-2 implementation protocol FULLY WIRED 2026-05-11** per ADR-0012 (amended with §Component 6 cascade-prevention). Four skills ready-to-use, agent-facing: `/duo-debate` (Tier-1 architectural debate, proven via `2026-05-09-mcp-migration-debate`), `/duo-implement` (Claude-side Tier-2 orchestrator), `/codex-review-loop` (Codex-CLI-side continuous-review mode), `/check-reviews` (Claude-side review-inbox with cascade-prevention). `scripts/agent-bus` extended with `pending-reviews` + `cascade-check` subcommands. **Unity MCP migration shipped 2026-05-09** per [ADR-0011](design/adr/adr-0011-unity-ai-assistant-mcp-migration.md) — official `com.unity.ai.assistant 2.7.0-pre.3` (server name `UnityAIAssistant`, stdio relay) is now the PRIMARY editor automation surface; CoplayDev retained as FALLBACK; routing matrix at [`docs/tooling/unity-mcp-routing.md`](docs/tooling/unity-mcp-routing.md) row-scoped. **Slice 6 of 7 shipped 2026-05-08** (commits `67c0905` + `47e7403`) — UI Toolkit overlay rewritten to UGUI fallback per Codex round-1 P1; 133/133 EditMode + 644/644 MatchSim; L2 evidence at `docs/screenshots/slice-6-{all-3-regions,reduce-motion-away}.png`. **Slice 5 shipped 2026-05-01** — URP custom passes; 120/120 EditMode at slice 5. Pinned 60-tick `MatchCanonicalState` hash UNCHANGED across the entire MCP migration + Tier-2 protocol scaffolding + tonight's wire-up.)

## Currently working on

**Phase 3 — Unity Bootstrap + MatchSim Prototype** 🟡 ACTIVE.

Phase 2 shipped 15 design docs + 4 specs + 7 ADRs; `/audit` green; bundle committed `3226c78`. Phase 3 is the first real code phase — MatchSim pure-C# skeleton with Q32.32 fixed-point determinism, 22-player ball physics, Unity + URP + dots-phase viewer prototype with 3 of 7 shot types, match-replay skill end-to-end. Gate = Month-3 match-engine gate (stranger watches match, understands drama in 3 minutes).

**Phase-3 Week-1 priority order** (gates Week-2 fixture authoring per corpus spec):
1. `MatchSim.csproj` pure-C# class library + `MatchSim.Tests.csproj` xUnit skeleton
2. `Fixed` struct (Q32.32 canonical format)
3. `Tick` deterministic 60Hz timestep loop
4. `Seed` (match + event seed derivation)
5. `SerializationContract.cs` — stable order for entities / events / Q32.32 fields. **Gates golden-replay-corpus fixture authoring** per `design/specs/golden-replay-corpus.md`
6. `scripts/fw shader-audit` — greps FW viewer adapter shaders for banned `_Time` references per ADR-0008/ADR-0009 determinism discipline inherited from superseded ADR-0002
7. ~~ADR-0008 ShotPresentationContract + ADR-0009 dots-phase render adapter — flip Proposed → Accepted~~ **shipped 2026-04-27** (cross-model rhythm: Claude drafted → GPT-5.5 reviewed (4 P1 + 4 P2 Concerns; applied) → Codex hardened (stale-reference cleanup; "no remaining blocking findings") → both ADRs **Accepted**. Phase-3 Week-2 viewer authoring is pre-cleared on the contract side)

**Weeks 2-4:** Ball custom-deterministic physics + Player state machine + 2 BT archetypes in YAML + xUnit determinism tests (Win/Mac/Linux hashes) + Unity project creation via Unity Hub + URP packages + dots-phase semantic-cinema prototype with 3 shot types (`tactical-wide` / `diagonal-attack-lane` / `pass-shot-impact`) + match-replay skill + unity-mcp handshake + devlog clips.

**Month-3 gate posture (2026-04-30 update):** observer-pool recruitment **scrapped per user direction** — solo-dev eye-test substituted for the cold-observer rubric (SPEC line 146 `[x]` + decisions-log entry 2026-04-30). Gate question and 6-task rubric in `design/month-3-vertical-slice.md` unchanged; only the answer-source flipped. Re-promotable via new SPEC decision if author self-assessment proves insufficient at gate time.

## Blockers

- None currently blocking Phase 3. User-action triggers now firing per `SETUP.md §10 trigger table`: Blender install (was Phase-3 trigger), VS Code / Rider editor install, CoplayDev unity-mcp install post-Unity-project-creation. **Plugin install ✅ verified 2026-04-30** — `feature-dev`, `pr-review-toolkit`, `hookify` all active at `~/.claude/plugins/installed_plugins.json`; CLAUDE.md §6.3 mandates the toolkit on substantial code commits + `pr-review-reminder.sh` reminds at PreToolUse-on-git-commit time. None gate ongoing Phase-3 MatchSim/viewer work.

## Pending async

- ~~GitHub remote creation~~ — **done 2026-04-24**: `osagberg/FinalWhistle` private; `vibelogic` org deferred to Phase 8 transfer if publisher branding wants it.
- User creates Steam Direct account at Phase 8 ($100 one-time).
- Phase-8 prep: formal trademark + Steam-name clearance for "Final Whistle" (existing non-AAA uses: `finalwhistle.es`, `finalwhistle.club`).

## Open questions for user

- None currently blocking. All Phase-2 design-doc open-question resolutions have a doc home or phase-deferred tracker row; user gets each architecture-bearing change for sign-off before it is marked Accepted.

## Next action

**Phase 2 ✅ COMPLETE (2026-04-25). Phase 3 🟡 ACTIVE.** Bundle committed `3226c78`; `/audit` green on Phase-2 scope (checks 1/17/18 ✅; 2-16 phase-deferred ⚪; 19 ✅ post-commit). Gate condition *"design bible complete; ADRs for every system that locks architecture"* satisfied.

**Phase-3 Week-1 priorities #1 + #2 + #3 + #4 + #5 + #6 + #7 `[x]` (full priority list shipped) + Week-2 Ball physics `[x]` + Player state machine `[x]` + BT archetypes `[x]` + determinism gate `[x]` + unity-project bootstrap `[x]`** (#1-3 2026-04-26; #4-#5 + #7 + Ball + Player + BT + determinism 2026-04-27; unity-project + Codex audit cycle + #6 shader-audit 2026-04-28):

- **#1 MatchSim.csproj + MatchSim.Tests.csproj skeleton** — netstandard2.1 + net10.0, xUnit 2.9.2, FinalWhistle.slnx + global.json SDK pin. fw test wired into fw verify umbrella.
- **#2 Fixed Q32.32 struct** — `MatchSim/Sim/Fixed.cs`. 32-bit-split allocation-free multiply, BigInteger division, banker's rounding, FW-VAL-A-018-compliant serialization with min/max round-trip. 141 Fixed tests; 144 total through priority #2 (incl. Codex review-pass corrections for boundary round-trip + parse hardening + BigInteger oracle for multiply path).
- **#3 Tick deterministic 60Hz timestep loop** — `MatchSim/Sim/Tick.cs`. `readonly struct Tick` over `long _value`; TicksPerSecond=60 / TicksPerMinute=3600 locked. Factories: FromSeconds / FromMinutes (checked). ToSeconds uses direct raw Q32.32 conversion `(tick << 32) / 60` with range-checking (exact at integer seconds within Fixed range, ≤1 ULP otherwise). **Type-distinguished `Tick - Tick → long` delta** vs `Tick + long → Tick`. Long-storage so multi-season corpus replays don't overflow. 45 new tests; 189 total through #3.
- **#4 Seed (match + event seed derivation)** — `MatchSim/Sim/Seed.cs`. `readonly struct Seed` over `ulong _value`. Canonical-triple derivation `Seed.Derive(matchSeed, Tick, eventId)` via SplitMix64 mixer (Java SplittableRandom-finalizer / xoshiro-author-recommended). Pure integer math, allocation-free, well-distributed avalanche (~50% bit-flip rate on 1-bit input change). Composition non-commutative on (matchSeed, eventId). Canonical string form `0xdeadbeefdeadbeef` matches `golden-replay-corpus.md` smoke-seed format. 45 new tests (incl. ≥16/64-bit avalanche assertions on each of the 3 inputs + 1000-eventId-HashSet distinctness + literal pinned value `0xddac907bb053edbb` plus oracle locking the SplitMix64 derivation against future regression). **234 total tests through #4**. Per-tick deterministic randomness primitive locked.
- **#5 SerializationContract + CanonicalEncoder** — `MatchSim/Sim/CanonicalEncoder.cs` (production code) + `MatchSim.Tests/Sim/SerializationContract.cs` (executable spec). Locks the on-disk byte representation that `golden-replay-corpus.md` hashes depend on. Encoding rules: little-endian via `BinaryPrimitives.Write*LittleEndian` (platform-independent; `BitConverter` forbidden); primitives 8 bytes LE over underlying 64-bit value; strings 4-byte LE byte-count prefix + strict UTF-8 (malformed UTF-16 rejected); counts 4-byte LE non-negative `int`; booleans 1 byte (0x00/0x01). Hash format `"sha256:<lowercase-hex>"` matching corpus spec. 43 new tests pinning literal expected `byte[]` for every primitive write + literal SHA256 hex for known inputs (independently computed via openssl) + malformed surrogate rejection. Caller responsibility per ADR-0008 §Determinism contract: collection elements MUST be sorted by ordinal comparison before encoding; encoder preserves write order, never sorts. **277 total tests passing**. Phase-3 Week-2 golden-replay-corpus fixture authoring now unblocked.
- **#7 ADR-0008 + ADR-0009 Accepted** — both ADRs flipped Proposed → Accepted via cross-model rhythm (Claude drafted → GPT-5.5 reviewed (4 P1 + 4 P2 Concerns; applied) → Codex hardened (stale-reference cleanup; "no remaining blocking findings")). Append-only from this point; supersession only via new ADR. ViewerEvent schema gained stable identity + ordering + base/effective shot split + bridge-resolves-once reduce-motion boundary; minimum `PitchView` + `ActiveViewerEvent` shapes locked; aftermath-freeze viewer-vs-sim time boundary fixed; corpus `pass_activation_log_hashes` adapter-keyed from v1; polish bar gained operational 6-task observer rubric; CI smoke split into synthetic + corpus fixtures.
- **Week-2 Ball physics** — `MatchSim/Sim/Vector3Fixed.cs` + `MatchSim/Sim/BallState.cs` + `MatchSim/Sim/BallPhysics.cs` (with `BallPhysicsCoefficients` co-located). Pure-deterministic ball physics integrator per `design/match-engine.md §Q2`. Q32.32 fixed-point throughout; semi-implicit Euler at fixed 60Hz step; gravity (continuous SI g·dt) + linear drag (per-step) + Magnus stub (per-step; skipped on zero spin) + ground bounce (clamp Y + flip+scale vertical velocity only when the ball crossed into the ground from above) + rolling friction (only when ball settled, NOT bouncing upward). `BallPhysicsCoefficients.Phase3Seeds` pinned to design-doc Phase-3 starting tuning seeds (g=9.81, C_d=0.02, C_m=0.0004, e=0.55, μ=0.25) and cached behind the property. Magnus stub policy honored: structure stays even when `MagnusCoupling = Fixed.Zero`. `CanonicalEncoder` gained `WriteVector3Fixed` (24 bytes; X/Y/Z order locked); `BallState.WriteCanonical` writes 72 bytes in P/V/S order. 56 new xUnit tests across 3 files after Codex hardening (31 Vector3FixedTests + 23 BallPhysicsTests + 2 SerializationContract additions), including grounded-at-rest / grounded-rolling regressions proving gravity cannot create a spurious bounce. **333 total tests passing**. NOT in scope: kick API, goal detection, touchline transitions — those wait for Player + Pitch.
- **Week-2 Player state machine** — `MatchSim/Sim/TeamSide.cs` + `MatchSim/Sim/PlayerState.cs` + `MatchSim/Sim/PlayerActuator.cs` (with `PlayerKinematics` co-located). Pure-deterministic steering-target actuator per match-engine.md §Q3 literal API (`desired_position` + `desired_speed`); pitch-plane projection preserves `Y=0` ground-player invariant even when a BT targets an airborne ball; semi-implicit Euler matching BallPhysics; clamps velocity-delta magnitude to `MaxAcceleration·dt`; defensive post-step MaxSpeed clamp. `PlayerState` enforces jersey 1-99 and Home/Away side on construction; `WriteCanonical` rejects default/uninitialized states. `PlayerKinematics` rejects negative max-speed / acceleration / radius. 64 actual Player-task xUnit cases after Codex hardening (26 FixedSqrtTests + 11 Vector3Fixed extension additions + 27 PlayerActuator/PlayerState cases). **397 total tests passing**. NOT in scope: behavior-tree archetypes, per-player gene-driven kinematics, roster / 22-player composition logic.
- **Week-2 BT archetypes** — `direct-pressing.yaml` + `low-block-counter.yaml` embedded resources; `PlayerCommand`, `BehaviorTreeArchetype`, `BehaviorTreeArchetypes.Parse/Load`, and `BehaviorTreeRunner.Tick` shipped. Month-3 scope is tactical heuristic with formation + PressRadius + BuildupSpeedFactor, not full sequence/selector BT. Parser now uses strict YAML schema validation (unknown fields rejected; missing `x`/`z` rejected); formation slots must stay on pitch plane; runner projects airborne ball positions to pitch plane for possession/press/nearest-player logic and commands. 47 current BT tests after Codex hardening. **444 total tests passing**. NOT in scope: sequence/selector nodes, gene-driven kinematics.
- **Week-2 determinism gate** — `MatchCanonicalState`, production `MatchSimulationState`, and production `MatchSimulationRunner` now own the canonical Month-3 loop: BT.Tick × 2 → PlayerActuator.Step × 22 → BallPhysics.Step → Tick+1. Pinned 60-tick smoke hash remains `sha256:299cdb0cbbc9606e141db278a14585780d0e3b5dbfb8815f634af89be7f6118a`; `MatchCanonicalState.EncodedByteCount = 1188` and `PlayersPerTeam = 11` are public constants. Smoke fixtures fill arrays by `FormationSlot.RosterSlot`, not authored-list order. `.github/workflows/fast-pr-ci.yml` now runs `fw verify` on Ubuntu / Windows / macOS with `bash` shell, so the claimed cross-platform gate is actually wired. **457 total tests passing**.
- **Week-2 unity-project bootstrap** (2026-04-28) — `unity-project/` created via headless Unity Editor CLI on **Unity 6 tech-stream `6000.4.4f1`** (chosen over strict LTS 6000.0; renderer-agnostic ADR-0008/0009 architecture decouples engine version from sim correctness; revisit at Phase 7 polish when next LTS lands; SPEC decisions-log entry 2026-04-28). `Packages/manifest.json` resolves URP 17.4.0 (manifest aligned post-Codex-audit) + UniTask 2.5.10 (OpenUPM scoped registry) + Addressables 2.3.16 + Recorder 5.1.2 + Input System 1.13.1 + Localization 1.5.5 + Timeline 1.8.7 + UGUI 2.0.0 + Cinemachine 3.1.6 + UnityMCP (CoplayDev `com.coplaydev.unity-mcp` pinned to commit SHA `b92c05a25820cfc9f59ce4094eb46aaec8632ea2`). ProBuilder + VFX Graph removed (post-Codex-audit; out of Phase-3 scope). `.gitignore` already aligned from bootstrap. **URP is active** (URP-Default + UniversalRenderer assets bound via UnityMCP-driven `GraphicsSettings` + Quality-level wiring; Linear colorspace; Input System enabled). 457 total tests still passing — MatchSim core untouched.
- **Codex audit pass round 1** (2026-04-28) — addressed 4 P1 + 3 P2 + 1 P3 findings on commits `81bcf33 → abb10d0`: UnityMCP server name + endpoint (Pascal `UnityMCP` on `localhost:8080/mcp`); Roslyn DLLs moved under `Assets/Plugins/Editor/Roslyn/` (auto-Editor-only); manifest hygiene (URP 17.4.0 + ProBuilder/VFX-Graph removed + unity-mcp SHA-pinned); CLAUDE.md path correction; SPEC decisions-log entry for tech-stream choice. Repair script `Assets/Scripts/Editor/Setup/RepairUniversalRenderer.cs` ships for the `UniversalRenderer.asset` `postProcessData` null repair — runs once via menu after Editor next-open.
- **Codex audit pass round 2** (2026-04-28) — addressed 3 of 4 round-2 findings in commit `b8857dc`: repair script gained explicit `PostProcessData` SerializedProperty assignment (P1 #1); `fw verify` excludes Unity-generated dirs at find-walk + case-statement levels (P1 #2); TOOLING.md row renamed to `UnityMCP` + STATUS URP version corrected (P3 #4). 1 deferred to user-action (P2 #3 packages-lock regen — Editor was open, blocked headless re-resolve).
- **Codex audit pass round 3** (2026-04-28) — multi-agent thorough-review handoff returned 5 P1 + 13 P2 + 5 P3 findings. **Audit cycle now closed end-to-end** across 6 commits (`47997fc` + `e9d9656` + `771c9fe` + `61f9c86` + `afd1c84` + `de331f2`) + 3 Codex round-trip re-verifications. Code fixes: `Fixed.Ceiling` / `Fixed.Round` upper-Q32.32-boundary overflow (P1-01; new `FromIntegerPart` helper + 6 regression tests); URP repair script `ResourceReloader` namespace fix + direct compile-time call (P1-02); MCP refresh hook removed as redundant (P2-04 — `manage_script` already auto-refreshes); `BallPhysicsCoefficients` validation (P3-01; 10 tests). Architecture decisions logged in append-only SPEC decisions log: P1-04 Month-3 semantic-slice scope (Option A keep the gate; tiny + fixture-driven), P1-05 PitchRules layer (folds in P2-03 seed-input refactor), P2-05 Forward renderer for dots, P2-09 enforcement-skeleton rollout (validator Phase-6, save-migration Phase-3 placeholder, fw replay Phase-3), P2-08 Tier-A CI carve-out. Doc sync across 12+ files (P2-01 + P2-06 + P2-10 + P2-11 + P2-12 + P2-13 + P3-03 + P3-04 + P3-05). User-action artefacts committed in `de331f2`: `UniversalRenderer.asset` (`postProcessData` populated via Final Whistle → Setup → Repair URP Renderer menu), `packages-lock.json` (unity-mcp SHA-pinned, ProBuilder + VFX Graph entries removed), 6 Unity-generated `.meta` files for Editor-only / Setup paths. 473 total tests passing (was 457; +16 new). Codex round-3 final sign-off: clean. Audit handoff + verification prompts at `docs/reviews/` deleted alongside this STATUS update.

**Phase-3 semantic slice ✅ COMPLETE (2026-04-30, all 5 ladder items shipped + hardened across multiple Codex review rounds).** Last commit `1a3ab6f` (Codex round-3 follow-up: SignatureRecipeMetadata struct → sealed class) clean on `origin/main`. Tests: **642/642 MatchSim + 38/38 Unity Test Framework EditMode**, pinned 60-tick `MatchCanonicalState` hash unchanged.

**Dots-phase render adapter Slice 1 of 7 ✅ shipped 2026-04-30 + Codex round-1 + round-2 hardened** (SPEC line 149 partially advanced; full task stays `[ ]` until Slices 2-7 land per `docs/plans/dots-adapter-blueprint.md`). `IShotPresentationAdapter` interface + `PitchView` Q32.32→`UnityEngine.Vector3` boundary + `ActiveViewerEvent` runtime wrapper landed in `Viewer.Core` per blueprint Decision 1 (locked in `Viewer.Core` not `Viewer.Contracts` because `PitchView.FixedToWorld` returns `UnityEngine.Vector3` and Contracts has `noEngineReferences: true`). 3 new files (~250 LoC code) + 20 EditMode tests (after round-1: was 19; +1) covering PitchView constructor invariants + FixedToWorld precision (incl. fractional-corner sub-mm pin and non-unity-scale corner-magnitude pin that actually exercise the float-cliff guard + 0.5× shrink + 2.0× enlarge brackets locking units-per-metre direction) + ActiveViewerEvent ShotTypeCatalog projection. PitchView's scale field is **`WorldUnitsPerMeter`** (default 1f means 1 metre → 1 Unity unit; 0.5f shrinks; 2.0f enlarges) — original Slice-1 draft used the inverted name `MetersPerUnit` which the math contradicted; Codex round-1 P2 caught + closed the rename before Slice 2 wired the field into pitch-quad mesh size. `ActiveViewerEvent.ElapsedTicks` upper-bound is **director-owned** per Codex round-1 P3 (Slice 3 `DotsMatchDirector` retires events when ElapsedTicks reaches `ShotDef.DurationTicks`; ActiveViewerEvent stays renderer-agnostic). Verified end-to-end via UnityMCP: 61/61 EditMode passing post-round-1 (was 21 baseline pre-slice; +40 new). MatchSim regression unchanged (642/642; pinned hash `sha256:7e851976...50e`). pr-review-toolkit triple ran pre-commit on the initial `b8d400f` (P1/P2 findings applied: NaN/Inf-before-positivity ordering + double-precision intermediate across the canonical-metre→Unity-unit multiply + fractional/non-unity-scale precision tests + KeyNotFoundException loud-fail doc-comment + RULES.md doc-drift fix). `feature-dev:code-architect` produced the 7-slice blueprint at `docs/plans/dots-adapter-blueprint.md` with Decisions 1-5 captured for SPEC append-only.

**Dots-phase render adapter Slice 4 of 7 ✅ shipped 2026-05-01** (SPEC line 149 partially advanced; full task stays `[ ]` until Slices 5-7 land per `docs/plans/dots-adapter-blueprint.md`). Camera shot framing for `tactical-wide` / `diagonal-attack-lane` / `pass-shot-impact`. New `ShotTypeSO` ScriptableObject (data-driven framing: orthographicSize / tilt / target anchor / transitionTicks); 3 `.asset` instances under `ShotTypes/`. New `ShotCamera` MonoBehaviour drives the orthographic Camera with constant-velocity Lerp from captured-from-state to target-state over `ShotTypeSO.TransitionDurationSeconds` (default 0.2s = 12 ticks); auto-returns to default tactical-wide framing after the active event's `EndTick` elapses. `DotsAdapterRoot.PresentShot` routes by `ShotCategory` via a `Dictionary<ShotCategory, ShotTypeSO>` built from the inspector-wired `shotCatalog` array. `DotsMatchDirector.CheckDiagonalAttackLaneHeuristic` triggers `ShotCamera.BeginAdapterShot(diagonalLane)` when ball Z-velocity magnitude > `diagonalAttackLaneZVelocityThreshold` (default 8 m/s; SerializeField for inspector tuning). `ShotTypeCatalog` gained a `ShotDiagonalAttackLane` entry (closes blueprint Q2; Phase-3 the bridge does NOT emit DiagonalAttackLane events — adapter-local only). **Verified end-to-end via UnityMCP**: 89/89 EditMode (was 88; +1 ShotTypeCatalog regression test) + 644/644 MatchSim. **L2 multi-screenshot** at `docs/screenshots/slice-4-tactical-wide.png` (full pitch ortho 38f + 23 dots + boundary), `slice-4-diagonal-attack-lane.png` (medium zoom ortho 20f + tilt 75° + ~12 dots visible), `slice-4-pass-shot-impact.png` (close-in ortho 12f + tilt 80° + 4 dots visible centred on ball) — three framings visually distinct. **pr-review-toolkit triple ran in parallel pre-commit**; 5 P1 + 6 P2 + 1 P3 findings applied: P1 — **real Lerp-drift bug** in ShotCamera transitions (was using `Lerp(current, target, alpha)` per-frame which produces exponential decay because `current` advances toward `target` each frame; fixed by capturing `from*` state at BeginShot/BeginAdapterShot/auto-return time + `Lerp(from, target, t)`); **camera frame-lag bug** in `DotPool.BallWorldPosition` (was returning post-tick `currentPositions[BallIndex]` snapshot; camera in LateUpdate consumed it but the rendered ball had been Lerped to an interpolated position by Update — fixed by reading `dots[BallIndex].transform.position` instead, which is the actual rendered position); `DotsMatchDirector.shotCamera?.Initialize` null-prop chain elevated to throw on null (was incoherent with the loud-fail discipline `dotPool` and `pitchQuad` already had); `DotPool.TryGetFocalWorldPosition` warn-once on first non-null focal-subject miss (doc said loud-fail but implementation was silent return false); `DotsAdapterRoot.Initialize` validates `TacticalWide` is registered (was deferring throw to first PresentShot call). P2 — `[SerializeField] diagonalAttackLaneZVelocityThreshold` for inspector tuning; `ShotTypeSO.TransitionDurationSeconds` derived property; `ShotCamera.OnSimTick(Tick)` parameter tightening from `long`; dead `transitionDurationSeconds = 0.2f` initializer removed; one-shot Debug.Log on `BeginAdapterShot` bail when an event-driven shot is active (telemetry for "heuristic never fires" debugging); runtime gate `ValidateShotResolves` in `BeginShot`/`BeginAdapterShot`/`Initialize` (catches misauthored SO that OnValidate's editor-only LogError can't block from saving); `Fixed.Abs(velocity.Z)` instead of `Fixed.Zero - velocity.Z` (uses canonical helper). P3 — clarifying comment on EndTick half-open interval semantics. Pinned 60-tick `MatchCanonicalState` hash `sha256:7e851976...50e` UNCHANGED — Slice 4 added a `ShotTypeCatalog` entry in Viewer.Contracts (`noEngineReferences: true`) but did NOT touch MatchSim source.

**Dots-phase render adapter Slice 3 of 7 ✅ shipped 2026-04-30** (SPEC line 149 partially advanced; full task stays `[ ]` until Slices 4-7 land per `docs/plans/dots-adapter-blueprint.md`). Live sim playback: 22 players + ball move in real time. `DotsMatchDirector.FixedUpdate` calls `MatchSimulationRunner.RunTicks(ticks=1)` at 60Hz (`Time.fixedDeltaTime` overridden in `OnEnable` + restored in `OnDisable`; cache fields `[NonSerialized]` so domain reload doesn't poison the prior-value cache); `EventBridge.Derive` projects newly-emitted canonical events into `ViewerEvent`s; a `ulong?` high-water-mark dispatches new events through a new `DotsAdapterRoot` MonoBehaviour that implements `IShotPresentationAdapter` with `PresentShot` no-op stub for the slice. `DotPool` gains `prevPositions` / `currentPositions` double-buffer + `PushTickSnapshot(ReadOnlySpan<PlayerState>, ReadOnlySpan<PlayerState>, BallState)` (zero-alloc; archetypes cached on Initialize per pr-review-toolkit type-design-analyzer Slice-3 P2) + `Update` linearly interpolates each dot via `Vector3.Lerp` with alpha `(Time.time - lastFixedTime) / Time.fixedDeltaTime` clamped to `[0,1]` (presentation-only — never feeds back to canonical state). Pinned 60-tick `MatchCanonicalState` hash `sha256:7e851976...50e` UNCHANGED — the chunked-vs-single-call invariant is already pinned by `Match_ChunkedRunTicksWithSignatures_ProducesIdenticalHashAndEventStream` in `MatchSim.Tests` per the Codex round-9/10 closure. **Verified end-to-end via UnityMCP**: 88/88 EditMode + 642/642 MatchSim. **L2 visual artifacts** at `docs/screenshots/slice-3-tick-001.png` + `slice-3-tick-180.png` — football-shaped pressing reads at full pitch scale: home midfielders + 2 strikers converge on ball at centre per direct-pressing 4-4-2; away holds low-block-counter shape with single striker advanced. **`gameplay-programmer` pre-implementation consult** asked 6 numbered questions (initial-state choice / `Time.fixedDeltaTime` placement / `SignatureCooldownState` invariant under FixedUpdate / `ViewerEventId` stability across re-derives / `PlayerState`-`BallState` snapshot safety / `Time.time`-`Time.deltaTime` determinism risk); answered all six + flagged a P0 "broader chunked-vs-single fixture test" prerequisite — but the test ALREADY EXISTS at `MatchDeterminismTests.cs:265-311` (the consult agent flagged it without reading the file). **pr-review-toolkit triple ran in parallel pre-commit**; 4 P1 + 3 P2 + 2 P3 findings applied: P1 — **real off-by-one bug fix** in `ElapsedTicks` calculation (was reading `state.CurrentTick` AFTER `RunTicks` advanced it; brand-new event firing at tick N got dispatched with `elapsed=1` instead of `0`; cached `preAdvanceTick` BEFORE `RunTicks`); `if (adapterRoot != null) Dispatch()` → warn-once when bridge has events but adapterRoot null (Slice-4 silent-fail trap); `DotsAdapterRoot.initialized` bool → `pitch != null` gate (same Slice-2 domain-reload anti-pattern); `lastProcessedViewerEventId` two-field shape collapsed to `ulong?`. P2 — `[NonSerialized]` on fixedDeltaTime cache fields; `ApplyTintAndScaleOnly` + `currentScales` array deleted (speculative Phase-4 dead code per CLAUDE.md §6.1); `PushTickSnapshot` signature trimmed 5→3 args. P3 — Time.time-vs-Time.fixedTime clarification comment; `OnValidate` on `matchSeedHex` for inspector-edit-time feedback.

**Dots-phase render adapter Slice 2 of 7 ✅ shipped 2026-04-30** (SPEC line 149 partially advanced; full task stays `[ ]` until Slices 3-7 land per `docs/plans/dots-adapter-blueprint.md`). Static pitch quad + DotPool rendering 22 players + ball visible on screen — first runtime-rendering deliverable for the dots adapter. 5 new C# files under `unity-project/Assets/Viewer/Adapters/Dots/`: `IdentityTintTable.cs` (ScriptableObject, 8 role families × 2 sides → Color, with editor-only `OnValidate` asserting documented band merges so an inspector author can't silently fragment the formation by drifting one half of a CB ≡ FB / DM ≡ CM / AM ≡ Winger pair); `ArchetypeRoleParser.cs` (static string→RoleFamily helper extracted from the SO per pr-review-toolkit type-design-analyzer Slice-2 P1 — keeps the YAML-conventions parser out of the render asset); `PitchQuad.cs` (runtime 105×68m green mesh + URP/Unlit material + LineRenderer hairline boundary; loud-fails on missing URP shader; OnDestroy cleans owned mesh + both materials); `DotPool.cs` (pool of 23 SpriteRenderer children pre-instantiated under the pool transform; serialized `[SerializeField]` sprite refs + tint table; `dots == null` initialization gate replaces the prior bool that would have re-deserialized incorrectly across domain reload); `DotsMatchDirector.cs` (`[DefaultExecutionOrder(-100)]` orchestrator; throws on missing inspector wiring rather than `Debug.LogError + return`; `[SerializeField]` archetype slug names so Phase-4 doesn't need code edits to swap formations). Plus: `Scenes/DotsViewer.unity` scene with orthographic top-down camera at `(0,50,0)` rotation Euler `(90,0,0)` ortho size 38f (hand-rolled — NO Cinemachine for Phase-3 per blueprint Decision 2); `IdentityTintTable.asset` with default palette pre-populated; 3 dot sprite PNGs (`dot_player_home.png` + `dot_player_away.png` 64×64 + `dot_ball.png` 32×32; alphaIsTransparency, mipmaps off, sprite import settings authored via .meta patch); `IdentityTintTableTests.cs` 27 EditMode tests with `[SetUp]/[TearDown]` cleanup. Test asmdef gained `FinalWhistle.Viewer.Adapters.Dots` reference. **Verified end-to-end via UnityMCP**: 88/88 EditMode passing in 0.05s (was 61 baseline post-Slice-1; +27 Slice-2 tests). MatchSim regression UNCHANGED (642/642; pinned hash `sha256:7e851976...50e`). **L2 visual at `docs/screenshots/slice-2-formation.png`**: pitch + 11 home cool blues + orange GK + 11 away warm reds + violet GK + white ball at centre; outfield colours discriminate at full pitch scale; GK colours visually distinct per ADR-0009 polish-bar §"identity legibility". **pr-review-toolkit triple ran in parallel pre-commit** (silent-failure-hunter + type-design-analyzer + feature-dev:code-reviewer) — 5 P1 + 4 P2 + 3 P3 findings applied. **Subagent failure note**: original `unity-specialist` delegation confabulated a polished narrative report claiming files were created and tests ran 64/64 green, but filesystem showed zero new files — the entire transcript was placeholder-rendered output that didn't actually invoke tools; recovered by authoring directly in main thread with the agent failure documented in the round-1 commit body.

**Dots-phase render adapter Slice 6 of 7 ✅ shipped 2026-05-08** (commits `67c0905` + `47e7403`; Codex round-1 closed). **UI Toolkit → UGUI fallback per Codex round-1 P1**: original Slice-6 round-1 implementation used UI Toolkit (`UIDocument` + UXML/USS) per the dots-adapter blueprint, but on Unity 6.0.4 + URP 17.4 + Mac/Metal the UI Toolkit runtime panel did not composite painted pixels into the framebuffer despite a verified-correct tree (8 labels populated, fonts resolved, PanelSettings cloned from a known-good Cinemachine reference, console clean, Slice-5 RenderGraph features ruled out). Codex round-1 review against `67c0905` flagged this as a P1 acceptance blocker. UGUI is the documented fallback per `.claude/rules/Scripts/Viewer/RULES.md` "UGUI fallback only when UI Toolkit lacks the surface" — Slice 6 ships UGUI (`OverlayController.cs` drives `Canvas` + `Text` + `Image` children); the authored UXML/USS stays on disk as a Phase-4+ migration scaffold once the UI Toolkit composition issue is diagnosed. L2 evidence at `docs/screenshots/slice-6-all-3-regions.png` + `slice-6-reduce-motion-away.png`; occlusion ~9% (under <10% acceptance budget per Codex round-1 round-2 P2 closure). Round-1 commit also closed: focal-prefix fix in `Viewer.EventBridge` (was `playerName.StartsWith("home")`; corrected to match bridge's `viewer.focal:home.{NN}` format), count-neutral breakthrough commentary (was "Third time today" but BlindSide+DiagonalSwitch cap at 2; replaced with "Repeat performance — that's a player finding his level."), OFL.txt trailing-whitespace cleanup, `m_RefreshImportMode` 0→1 (InProcess→OutOfProcess) for Editor focus issue. 133/133 EditMode + 644/644 MatchSim; pinned 60-tick `MatchCanonicalState` hash UNCHANGED.

**Next `/next` picks up:** **Slice 7 of 7 — identity cues + selection ring + motion lines + observer-rubric pass** per `docs/plans/dots-adapter-blueprint.md §B Slice 7`. The final dots-adapter slice. Anime-presentation-budget surfaces #4 (identity cues) + #5 (selection ring) + #6 (motion lines). Files to create: `IdentityCueRenderer.cs` (kit-stripe + jersey-number sprite-overlay per `IdentityTintTable` band), `SelectionRing.cs` (focal-subject highlighter; consumes `ActiveViewerEvent.FocalSubject`), `MotionLineFeature.cs` (URP scriptable-renderer-feature drawing speed-line trails behind fast-moving sprites; reduce-motion variant strips them per `ViewerEvent.ReduceMotionApplied`). Acceptance: ADR-0009 6-task observer rubric passes solo-eye-test (drama / momentum / identity / signature-impact / commentary-sync / camera-rhythm read in 3 minutes); zero banned terms. **Required agents:** `unity-specialist` + `art-director` framing aesthetics review post-L2 multi-screenshot. Verification: L1 + L2 multi-screenshot + L3 observer-rubric self-assessment per `design/month-3-vertical-slice.md`. LoC estimate ~350. Subsequent Phase-3 backlog (lighter-lift, can interleave): `fw replay <seed>` minimal compare + first corpus fixture (SPEC line 151); match-replay skill end-to-end (SPEC line 152); devlog clips Month 2-3 (SPEC line 154); local CI scripts (SPEC line 155).

## Open architectural questions

_None blocking._ Audit-cycle Editor user-actions (`P1-02` + `P1-03`) are closed by `de331f2`. The next architectural decision (MatchSim consumption strategy — DLL drop vs UPM local) is the first foundation-first task at the top of the next-up list above; will become its own SPEC `[ ]` task once driven.

**Phase-3 Week-1 priority #7 ✅ shipped 2026-04-27.** ADR-0008 ShotPresentationContract + ADR-0009 dots-phase render adapter both **Accepted** after GPT-5.5 review pass (4 P1 + 4 P2 Concerns findings applied across both ADRs + `golden-replay-corpus.md`) + Codex review pass (stale-reference cleanup; "no remaining blocking findings"). Schema landed: `ViewerEventId` / `SourceEventId` / `BaseShotTypeId` / `EffectiveShotTypeId` / `ReduceMotionApplied` / `FocalSubject`; minimum `PitchView` + `ActiveViewerEvent` shapes locked; aftermath-freeze viewer-vs-sim time boundary fixed; ordering rules locked (StringComparer.Ordinal + InvariantCulture + EntityId-preferred); corpus `pass_activation_log_hashes` adapter-keyed from v1 (no future migration); polish bar gained operational 6-task observer rubric; CI smoke split into synthetic ViewerEvent fixture (Week 2) + corpus seed (Week 3+). AdapterId enum gained code-owned-registry clarification. Append-only from this point; supersession only via new ADR. Phase-3 Week-2 viewer authoring is pre-cleared on the contract side.

## Phase-1 carryover (non-gating user-actions)

Phase 1 closed 2026-04-24 with these items intentionally open. None gate Phase 2 — they're timed by user convenience / phase triggers:

- Actions $0 spending cap (1-min GH UI step; runbook at `docs/ops/actions-budget.md §2b`). Recommended soon; not urgent
- Blender install (Phase-3 explicit trigger per `SETUP.md §4`)
- VS Code / Rider (user editor preference)
- ~~Slash-command smoke + plugin install~~ ✅ closed 2026-04-30 — `feature-dev` / `pr-review-toolkit` / `hookify` verified active at `~/.claude/plugins/installed_plugins.json`; CLAUDE.md §6.3 + `pr-review-reminder.sh` operationalize them.
- Branch protection (blocked on plan upgrade — GitHub Free constraint; revisit Phase 4 closed-itch OR Phase 8 EA)

## Recent milestones

- 2026-05-01: **Dots-phase render adapter Slice 4 of 7 shipped** (per `docs/plans/dots-adapter-blueprint.md` 7-slice ladder). Camera shot framing for `tactical-wide` / `diagonal-attack-lane` / `pass-shot-impact`. New `ShotTypeSO` ScriptableObject + 3 `.asset` instances; new `ShotCamera` MonoBehaviour Lerps orthographicSize + tilt + target world-position from a CAPTURED from-state to the active shot's to-state over `ShotTypeSO.TransitionDurationSeconds` (default 0.2s); auto-returns to default after the active event's EndTick elapses. `DotsAdapterRoot.PresentShot` dispatches by `ShotCategory` via `Dictionary<ShotCategory, ShotTypeSO>`. `DotsMatchDirector.CheckDiagonalAttackLaneHeuristic` triggers diagonal-attack-lane when ball Z-velocity magnitude > inspector-tunable threshold (default 8 m/s) AND no event-driven shot is active. `ShotTypeCatalog` gained `ShotDiagonalAttackLane` entry (Phase-3 closes blueprint Q2; bridge does NOT emit DiagonalAttackLane events — adapter-local only per Decision 3). **89/89 EditMode + 644/644 MatchSim**, pinned 60-tick `MatchCanonicalState` hash `sha256:7e851976...50e` UNCHANGED. **L2 multi-screenshot** at `docs/screenshots/slice-4-{tactical-wide,diagonal-attack-lane,pass-shot-impact}.png` — 3 visually distinct framings (full pitch / medium tilt-zoom / close-in tilt-zoom). **pr-review-toolkit triple ran in parallel pre-commit**; 5 P1 + 6 P2 + 1 P3 findings applied: P1 — **real Lerp-drift bug** in ShotCamera (was using `Lerp(current, target, alpha)` per-frame which produces exponential decay; fixed by capturing `from*` state at transition start + `Lerp(from, target, t)` for constant-velocity); **camera frame-lag bug** (was reading post-tick ball snapshot; fixed to read interpolated `transform.position`); director throws on null shotCamera (was `?.` chain — incoherent with loud-fail discipline); DotPool warn-once on focal-subject miss (doc said loud-fail; impl was silent); Initialize validates TacticalWide present. P2 — `[SerializeField]` ball-velocity threshold; derived `TransitionDurationSeconds`; `OnSimTick(Tick)` parameter tightening; dead initializer removed; one-shot Debug.Log on adapter-shot suppression; runtime gate validates SO id resolves; `Fixed.Abs` instead of negation pattern. P3 — EndTick half-open-interval comment. **Subagent rotation per CLAUDE.md §6.3**: pr-review-toolkit triple ran pre-commit; `unity-check` L1 + L2 satisfied via UnityMCP. Slice 5 (URP custom passes — screen-tone + impact-frame flash) is the next `/next` pickup.
- 2026-04-30: **Dots-phase render adapter Slice 3 of 7 shipped** (per `docs/plans/dots-adapter-blueprint.md` 7-slice ladder). Live sim playback: 22 players + ball move in real time. New `DotsAdapterRoot` MonoBehaviour implements `IShotPresentationAdapter` with `PresentShot` no-op stub (Slice 4+ wires shot-camera dispatch). `DotsMatchDirector.FixedUpdate` drives `MatchSimulationRunner.RunTicks(ticks=1)` at 60Hz (`Time.fixedDeltaTime` set in `OnEnable` + restored in `OnDisable`; `[NonSerialized]` cache fields prevent domain-reload poisoning) + consumes `EventBridge.Derive` + dispatches new events through a `ulong?` high-water-mark. `DotPool` double-buffers `prevPositions` / `currentPositions` + interpolates each dot via `Vector3.Lerp` in `Update` (presentation-only — never feeds back to canonical state). Pinned 60-tick `MatchCanonicalState` hash `sha256:7e851976...50e` UNCHANGED — chunked-vs-single-call invariant pinned by `Match_ChunkedRunTicksWithSignatures_ProducesIdenticalHashAndEventStream` in MatchSim.Tests per Codex round-9/10. **Verified end-to-end via UnityMCP**: 88/88 EditMode + 642/642 MatchSim. **L2 visual** at `docs/screenshots/slice-3-tick-001.png` + `slice-3-tick-180.png` — football-shaped pressing reads at full pitch scale (home midfielders + 2 strikers converge on ball at centre per direct-pressing 4-4-2; away holds low-block-counter shape with single striker advanced). **`gameplay-programmer` pre-implementation consult** answered 6 numbered questions on FixedUpdate timing + chunked-RunTicks invariant + ViewerEventId stability + snapshot safety + Time.time determinism risk. **pr-review-toolkit triple ran in parallel pre-commit**; 4 P1 + 3 P2 + 2 P3 findings applied: P1 — **real off-by-one bug fix** in `ElapsedTicks` calculation (was reading `state.CurrentTick` AFTER `RunTicks` advanced it; brand-new event firing at tick N got `elapsed=1` instead of `0`; cached `preAdvanceTick` BEFORE `RunTicks`); `if (adapterRoot != null) Dispatch()` upgraded to warn-once when bridge has events but adapterRoot null (Slice-4 silent-fail trap); `DotsAdapterRoot.initialized` bool → `pitch != null` gate (same Slice-2 domain-reload anti-pattern); `lastProcessedViewerEventId` two-field shape collapsed to `ulong?` (self-documenting one-field state). P2 — `[NonSerialized]` on fixedDeltaTime cache fields; `ApplyTintAndScaleOnly` + `currentScales` array deleted (speculative Phase-4 dead code per CLAUDE.md §6.1); `PushTickSnapshot` signature trimmed 5→3 args by caching archetypes on `DotPool.Initialize`. P3 — `Time.time` vs `Time.fixedTime` clarification comment; `OnValidate` on `matchSeedHex` for inspector-edit-time feedback. **Subagent rotation per CLAUDE.md §6.3**: `gameplay-programmer` consulted pre-implementation; pr-review-toolkit triple ran pre-commit; `unity-check` L1 + L2 satisfied via UnityMCP refresh + run_tests + screenshot. Slice 4 (camera shot framing for `tactical-wide` + `diagonal-attack-lane` + `pass-shot-impact`) is the next `/next` pickup.
- 2026-04-30: **Dots-phase render adapter Slice 2 of 7 shipped** (per `docs/plans/dots-adapter-blueprint.md` 7-slice ladder). First runtime-rendering deliverable: green pitch quad + DotPool of 23 SpriteRenderer children (11 home + 11 away + 1 ball) + DotsMatchDirector orchestrator + IdentityTintTable SO + ArchetypeRoleParser helper + DotsViewer scene with hand-rolled orthographic top-down camera. **88/88 EditMode tests** (was 61 baseline; +27 Slice-2). MatchSim regression UNCHANGED (642/642; pinned hash `sha256:7e851976...50e`). **L2 visual** at `docs/screenshots/slice-2-formation.png` — formation reads cleanly at full pitch scale: home cool-blue gradient defending left + orange GK; away warm-red/coral gradient defending right + violet GK; white ball at centre; pitch boundary visible. **pr-review-toolkit triple ran in parallel pre-commit**; 5 P1 + 4 P2 + 3 P3 findings applied: P1 — `DotsMatchDirector.Awake` throws on missing inspector wiring (was `Debug.LogError + return` half-built scene); `RoleFamilyForArchetypeLabel` extracted to dedicated `ArchetypeRoleParser` static class (was on the render SO; YAML-conventions parser doesn't belong on render assets); `IdentityTintTable.OnValidate` asserts documented band merges (CB ≡ FB / DM ≡ CM / AM ≡ Winger) so inspector authors can't silently fragment the formation; `DotPool` initialization gate switched from `private bool initialized` to `dots == null` because the bool re-deserializes incorrectly across domain reload while spawned children persist; `PitchQuad` boundary `Material` now stored in field + destroyed in `OnDestroy` (was leaking one Material per Initialize call); both materials route through a single `CreateUnlitMaterial` helper that loud-fails on missing URP shader (boundary path previously made the `Shader.Find` call without a guard). P2 — archetype slug names `[SerializeField]` instead of literals; `IdentityTintTableTests` uses `[SetUp]/[TearDown]` so SO cleanup runs even when assertions throw. P3 — `Lookup` ArgumentException paramName changed from single `nameof(role)` to joint `"side, role"`; added 5 missing `[TestCase]` rows for `DM`/`AM`/`RW`/`LW`/`CF` labels + new lowercase-throws regression (parser is case-sensitive per content-pack lint contract). **Subagent failure note**: original `unity-specialist` delegation confabulated a long narrative report claiming 64/64 tests passed and files were created, but filesystem showed zero new files — the entire transcript was rendered as `[Tool: ...]` placeholders that didn't actually invoke tools; recovered by authoring directly in main thread (CLAUDE.md §6.3 mandate satisfied via the pr-review-toolkit triple still running pre-commit + the failure documented in the commit body). Slice 3 (live sim playback) is the next `/next` pickup.
- 2026-04-30: **Dots-phase render adapter Slice 1 of 7 shipped + Codex round-1 + round-2 hardened** (per `docs/plans/dots-adapter-blueprint.md` 7-slice ladder authored by `feature-dev:code-architect`). `IShotPresentationAdapter` interface + `PitchView` Q32.32→`UnityEngine.Vector3` boundary + `ActiveViewerEvent` runtime wrapper landed in `Viewer.Core` per blueprint Decision 1 (PitchView depends on UnityEngine.Vector3 → Viewer.Contracts' `noEngineReferences: true` forbids it there → Viewer.Core is the correct home). 3 new files (~250 LoC code) + 20 EditMode tests after round-1 (was 19; +1) covering constructor invariants + FixedToWorld precision (incl. **fractional-corner sub-mm pin + non-unity-scale corner-magnitude pin** that actually exercise the float-cliff guard the double-precision intermediate exists to defend against — the prior integer-only corner test would have passed even without the double intermediate; **0.5× shrink + 2.0× enlarge brackets** lock the units-per-metre direction). Test-asmdef `Viewer.Tests.EditMode` gained `FinalWhistle.Viewer.Core` reference. `.claude/rules/Scripts/Viewer/RULES.md` doc-drift fix: PitchView + ActiveViewerEvent now correctly enumerated under `Viewer.Core` (was stale, said Contracts) with the UnityEngine.Vector3 rationale captured. **Verified end-to-end via UnityMCP `run_tests`**: 61/61 EditMode passing post-round-1 (was 21 baseline pre-slice; +40 new across PitchView + EventBridge harness expansion). MatchSim regression baseline UNCHANGED (642/642; pinned 60-tick `sha256:7e851976...50e`). **pr-review-toolkit triple ran in parallel pre-commit** on the initial `b8d400f` (silent-failure-hunter / type-design-analyzer / feature-dev:code-reviewer); P1/P2 findings applied: PitchView NaN/Inf check before positivity (closes IEEE 754 `NaN <= 0f` false-evaluation footgun yielding wrong exception type for NaN) + double-precision intermediate kept across the canonical-metre→Unity-unit multiply (was casting to float before scaling, defeating the double-side divide's precision benefit) + fractional/non-unity-scale precision tests added + ActiveViewerEvent KeyNotFoundException doc-comment for loud-fail discipline (silent fallback to TacticalWide would mask Phase-4+ ScriptableObject authoring drift) + RULES.md doc-drift fix. **Codex round-1 follow-up against `b8d400f`** closed 1 P2 + 1 P3: P2 — `MetersPerUnit` field renamed to `WorldUnitsPerMeter` (the math multiplies, which is units-per-metre semantics; the original name would have inverted Phase-4 non-default scales — mobile port at 0.5× would have rendered the pitch DOUBLE-size); P3 — `ActiveViewerEvent.ElapsedTicks` doc reworded as director-owned (Slice 3 `DotsMatchDirector` retires events when ElapsedTicks reaches `ShotDef.DurationTicks`; ActiveViewerEvent stays renderer-agnostic so a future 3D adapter can pick its own overrun behaviour). **Codex round-2 follow-up against `579edf3`** closed 1 P3: stale `MetersPerUnit` references in active SPEC closure prose + STATUS current-status paragraphs updated to the renamed field so future grep/readback audits don't chase the wrong API name (CHANGELOG entries kept verbatim — historical timeline). **Subagent rotation per CLAUDE.md §6.3**: `feature-dev:code-architect` produced the 7-slice blueprint with Decisions 1-5 captured for SPEC append-only; pr-review-toolkit triple ran pre-commit; `unity-check` L1+L2 satisfied via UnityMCP refresh + run_tests (L3 N/A — no rendering surface yet). Slice 2 (static pitch quad + DotPool) is the next `/next` pickup.
- 2026-04-30: **Phase-3 foundation #3 — `PitchRules` / `MatchRules` layer shipped**. Closes Codex P1-05 + folds in P2-03 seed-input refactor per the 2026-04-28 decisions-log entry. New types: `OutOfPlay` enum + `KeyEventKind` enum + `KeyEvent` record (35-byte canonical layout) + `MatchSimulationConfig` (Seed input) + `MatchRules.Step` orchestrator (linear-interp Q32.32 crossing detection; per-side goal/restart classification; transient OutOfPlay reset; HomeScore overflow guard). `MatchSimulationState` extends with score / OutOfPlay / append-only KeyEvents. `MatchSimulationRunner` accepts the config + runs `MatchRules.Step` after `BallPhysics.Step`. Canonical-state encoding adds 7 base bytes + variable KeyEvent body (35 bytes each); `EncodedByteCount` renamed to `EncodedBaseByteCount = 1195` + `EncodedByteCountFor(state)` for variable totals. **Pinned smoke-fixture hash re-baselined** (v0 → v1 schema bump per the 2026-04-28 decisions-log entry — intentional): `sha256:7e851976f6a5eea467797e90400ca030c6ab955e21c2f92466cffa00c880f50e`. **22 new tests** (495 total passing; was 473). Phase-3 simplifications: GoalKick / CornerKick disambiguation deferred to Phase 4 (no last-touched tracking); ThrowIn side defaults to Home; `KeyEvent.JerseyNumber=0` for all Phase-3 emissions (no scorer attribution). MatchSim DLL republished + Unity asmdef skeleton recompiled cleanly against new symbols (UnityMCP `read_console` zero errors / zero warnings).
- 2026-04-29: **Assembly Definitions skeleton shipped** (Phase-3 foundation #2; closes the second-up SPEC task in the foundation-first ladder). Three asmdefs at `unity-project/Assets/Viewer/{Contracts,Core,Adapters/Dots}/` per ADR-0008/0009. **Viewer.Contracts** is `noEngineReferences: true` — asmdef-level enforcement of the same UnityEngine-free rule MatchSim follows; references the `FinalWhistle.MatchSim.dll` plugin via `precompiledReferences`. **Viewer.Core** references Contracts + MatchSim. **Viewer.Adapters.Dots** references Core + Contracts + `Unity.RenderPipelines.Universal.Runtime` (URP asmdef name verified against package cache for URP 17.4.0). Reference graph: Dots → Core → Contracts (one-way; Contracts is the apex). Each asmdef ships a single `*AssemblyMarker.cs` public stub class proving the asmdef compiles + downstream resolution works. Compile verification via UnityMCP `read_console`: zero errors, zero warnings. 17 Unity-generated `.meta` files. 473/473 tests still pass. Real schema (ViewerEvent / ShotTypeSO / Viewer.EventBridge / dots adapter) lands in subsequent SPEC tasks; this skeleton is the consumer-anchor for foundation #3 (PitchRules) and the semantic-slice work that follows.
- 2026-04-29: **MatchSim consumption strategy locked + `scripts/fw build-unity-plugins` shipped** (closes the foundation-first task at the top of the next-up ladder; SPEC 2026-04-29 decisions-log entry). DLL drop chosen over UPM local package: `dotnet publish MatchSim/MatchSim.csproj -c Release` → copy `FinalWhistle.MatchSim.dll` + `.pdb` + `YamlDotNet.dll` into `unity-project/Assets/Plugins/MatchSim/`. Plugin DLLs check into git (reproducibility floor; matches the existing `Assets/Plugins/Editor/Roslyn/` pattern). Existing `.meta` files NEVER overwritten — Unity owns GUID stability. Idempotent re-runs (incremental dotnet publish + byte-equal cp). First-run user-action: open Unity → Editor generates `.meta` files → commit. Phase-4 revisit trigger: pivot to UPM local package if dev-loop friction emerges (forget-to-rebuild pattern). Tier-A DLLs-current freshness check deferred to Phase 4. 473/473 tests still pass.
- 2026-04-28: **`scripts/fw shader-audit` shipped** (Phase-3 Week-1 priority #6 + Codex round-3 follow-up scope-narrowing). Adapter validation criterion *"no `_Time` references in viewer shaders"* per ADR-0008/0009 determinism discipline (inherited from superseded ADR-0002). **Scope: viewer-adapter shaders only.** Scans `unity-project/Assets/Viewer/` for `*.{shader,hlsl,cginc,shadergraph,subshader}`; UI / debug / loading-screen shaders elsewhere in `Assets/` are intentionally NOT audited because ADR-0008 explicitly exempts visual-only effects from the determinism hash (they legitimately use `_Time` for breathing-glow / blinking cursors / animated loading indicators). Catches HLSL `_Time` / `_SinTime` / `_CosTime` / `_DeltaTime` / `_TimeParameters` + ShaderGraph `TimeNode`. POSIX-portable trailing-boundary regex (`[^A-Za-z0-9_]` — BSD grep on macOS doesn't support `\b`) excludes false-positives like `_TimeOfDay`. **Phase-3 stub-active mode**: exits 0 when `Assets/Viewer/` doesn't exist yet (current state — asmdef skeleton task hasn't landed); real catch begins the moment the dots adapter authors its first shader under the viewer tree. Wired into `fw verify` umbrella between `banned-terms` and `test`. Smoke-tested all four paths (no-Viewer-tree / positive-test / false-positive / scope-narrowing-confirm). 473/473 tests still pass.
- 2026-04-28: **Codex round-3 audit cycle CLOSED end-to-end** (commits `47997fc` + `e9d9656` + `771c9fe` + `61f9c86` + `afd1c84` + `de331f2` + this cleanup). 5 P1 + 13 P2 + 5 P3 findings + 3 round-trip Codex re-verifications, all closed. Code fixes (Fixed-overflow, URP repair, MCP hook removal, BallPhysics validation; +16 tests, 473/473). 5 architecture decisions logged in append-only SPEC decisions log (Month-3 semantic slice / PitchRules + seed input / Forward renderer / enforcement-skeleton rollout / Tier-A CI carve-out). 12+ doc files reframed (visual-target supersession completion / Unity-version wording / accessibility ADR-0002→0008/0009 / branch-protection / identity-compiler scope / rating-TacticalPreset Phase-3 caveats / phenotype-label drift / spec-frontmatter alignment). Editor user-action artefacts committed (`UniversalRenderer.asset` postProcessData populated; `packages-lock.json` SHA-pinned + cleaned; 6 Unity-generated `.meta` files). Temp review files at `docs/reviews/` deleted in this cleanup commit. **Phase-3 foundation is now stable for the dependency-ordered semantic-slice + dots-adapter work.**
- 2026-04-28: **Codex round-3 multi-agent audit — code fixes shipped + scope/architecture decisions captured** (commit `47997fc` + earlier rounds). Code: `Fixed.Ceiling`/`Round` Q32.32 upper-boundary overflow (P1-01; `FromIntegerPart` helper); URP repair script `ResourceReloader` namespace correction (P1-02); redundant MCP refresh hook removed (P2-04); `BallPhysicsCoefficients` validation guards (P3-01). Architecture: Month-3 gate stays narrative-bearing (P1-04 Option A); minimal `PitchRules`/`MatchRules` layer added to Phase 3 (P1-05); Forward renderer locked for dots adapter, Forward+ stays 3D-spike-conditional (P2-05); enforcement-skeleton rollout — `fw replay` Phase-3, save-migration fixture Phase-3 placeholder, content-pack validator Phase-6 (P2-09); Tier-A CI carve-out clarified (P2-08). 473/473 tests pass. Phase-3 SPEC task list rewired with 8 new bullets covering the semantic slice + PitchRules + MatchSim consumption strategy + asmdef skeleton + enforcement skeletons. User-action follow-up: Editor packages-lock regen + Repair URP Renderer menu + commit Unity-generated `.meta` files (closes P1-02 + P1-03 end-to-end). Next-up rescoped per dependency order: consumption strategy → asmdef skeleton → PitchRules → semantic slice → dots adapter.
- 2026-04-28: **Codex audit fixes shipped** (commits `7c72991` + `5f0bf06` + `76aa454`). 4 P1 + 3 P2 + 1 P3 + 1 other-audit recommendation from the post-bootstrap Codex review applied: UnityMCP server name + hook endpoint aligned (`mcp__UnityMCP__manage_script` + `localhost:8080/mcp`); Roslyn DLLs moved under `Assets/Plugins/Editor/Roslyn/` (Unity-convention Editor-only); manifest hygiene (URP `17.4.0`, unity-mcp pinned to commit SHA, ProBuilder + VFX Graph removed); CLAUDE.md `.claude/bootstrap/scripts/install-plugins.txt` path corrected; SPEC.md decisions-log entry for `6000.4.4f1` tech-stream choice + LTS-migration trigger; new Editor menu `Final Whistle → Setup → Repair URP Renderer` for the `postProcessData` null repair (run once after Editor next-open). 457 total tests still passing.
- 2026-04-28: **Phase-3 Week-2 — `unity-project/` created on Unity 6.4 tech stream.** First pure-Unity Phase-3 deliverable. Headless Editor CLI created the default 3D project at `unity-project/`; `Packages/manifest.json` rewritten to pull URP 17.4.0 (post-audit; was authored as 17.0.4) + UniTask 2.5.10 (OpenUPM) + Addressables + Recorder + Input System + Localization + Timeline + UGUI + Cinemachine; trimmed unneeded modules; UnityMCP added (CoplayDev `com.coplaydev.unity-mcp` pinned to SHA). Unity version pinned `6000.4.4f1` (tech-stream over strict LTS — renderer-agnostic ADR-0008/0009 architecture decouples engine version from sim correctness; trade-off documented in CHANGELOG + SPEC decisions-log; re-evaluate at Phase 7 polish when next LTS lands ~Q4 2026). `.gitignore` already aligned from bootstrap. URP active via UnityMCP-driven setup (URP-Default + UniversalRenderer bound via `GraphicsSettings`; Linear colorspace; Input System enabled). 457 total tests still passing — MatchSim core untouched. Open architectural question for next /next: MatchSim consumption strategy (DLL drop vs UPM local package; recommendation = DLL drop for Phase 3 simplicity)
- 2026-04-27: **Codex review pass on determinism gate** — corrected three load-bearing mismatches before Unity work: the match-loop composition moved out of test-only helper code into production `MatchSimulationState` + `MatchSimulationRunner`, `.github/workflows/fast-pr-ci.yml` now runs the Tier-A umbrella on Ubuntu / Windows / macOS instead of Linux-only, and formation-derived smoke fixtures fill by `RosterSlot` rather than YAML/list order. `MatchCanonicalState` exposes `PlayersPerTeam = 11` + `EncodedByteCount = 1188` constants and overloads for production simulation state. Pinned hash unchanged. **457 total tests passing**.
- 2026-04-27: **Phase-3 Week-2 — Cross-platform determinism gate shipped.** `MatchSim/Sim/MatchCanonicalState.cs` (production helper: 1188-byte locked-order encoding of Tick + Ball + 22 PlayerStates) + `MatchSim.Tests/Sim/MatchDeterminismTests.cs` (composition + pinned-hash gate). The cross-platform-determinism gate per match-engine.md §Prototype gate. **Pinned smoke-fixture hash:** `sha256:299cdb0cbbc9606e141db278a14585780d0e3b5dbfb8815f634af89be7f6118a` (computed on macOS 2026-04-27): direct-pressing (Home) vs low-block-counter (Away) at formation positions; ball at centre; 60 ticks. Seed not included in canonical-state hash (current sim has no per-event randomness; Phase-4+ stochastic events enter state through emitted events). 10 new tests at initial ship. **454 total tests passing at initial ship**. The full BT + Player + Ball + CanonicalEncoder stack now exercised end-to-end.
- 2026-04-27: **Codex review pass on BT archetypes** — caught YAML strictness gaps (unknown fields ignored; missing `x`/`z` defaulted to zero) and airborne-ball pitch-plane leak in press logic. Parser now rejects unknown fields and missing coordinates; `FormationSlot` rejects nonzero-Y base positions; runner uses pitch-projected ball position for possession/press/nearest-player logic and press commands. **444 total tests passing**.
- 2026-04-27: **Phase-3 Week-2 — 2 BT manager archetypes in YAML shipped.** `MatchSim/Content/archetypes/direct-pressing.yaml` (4-4-2 high press; PressRadius=25m; BuildupSpeedFactor=0.95) + `low-block-counter.yaml` (4-5-1 deep block; PressRadius=12m; BuildupSpeedFactor=1.0) embedded as resources. `BehaviorTreeArchetype` data type + `BehaviorTreeArchetypes.Parse/Load` (YamlDotNet 17.0.1) + `BehaviorTreeRunner.Tick` three-mode heuristic (Press / Build-up / Hold-shape) + allocation-free hot path. Coordinate convention: HOME orientation in YAML; runner mirrors X for Away. Possession resolution: nearest-player-to-ball; ties → opponent (defensive default). Scope decision: tactical heuristic with formation + 2 knobs, NOT full BT engine with sequence/selector nodes (those land Phase 4 if observers find tactics too thin). 42 new tests at initial ship. **439 total tests passing at initial ship**. BT + Player + Ball stack composes deterministically end-to-end via integration test. Next /next picks up xUnit determinism tests for canonical-state hash (cross-platform CI matrix gate)
- 2026-04-27: **Codex review pass on `PlayerActuator`** — caught ground-player invariant bug where a BT targeting an airborne ball position could make players gain Y velocity. Added regression tests for airborne desired-position projection and vertical-drift cleanup; actuator now projects state/velocity/target onto the pitch plane before steering. `PlayerState` now rejects invalid jersey/team identity and refuses to serialize default/uninitialized state; `PlayerKinematics` rejects negative tuning. **397 total tests passing**.
- 2026-04-27: **Phase-3 Week-2 — `Player` state machine + `Fixed.Sqrt` shipped.** `MatchSim/Sim/TeamSide.cs` + `MatchSim/Sim/PlayerState.cs` + `MatchSim/Sim/PlayerActuator.cs` (with `PlayerKinematics`). Pure-deterministic steering-target actuator per match-engine.md §Q3 literal API (BT outputs desired_position + desired_speed); semi-implicit Euler matching BallPhysics; clamps velocity-delta magnitude to `MaxAcceleration·dt` (caps turn rate implicitly without separate angular cap); defensive post-step MaxSpeed clamp; sqrt-free at-target shortcut via LengthSquared==0 check. **Pre-requisite: `Fixed.Sqrt`** via Newton's method on BigInteger (netstandard2.1-compat: GetByteCount-based bit-length over-estimate). Vector3Fixed gained Length / Distance / DistanceSquared / Normalize. TeamSide enum: byte (Home=1, Away=2; default 0 reserved as uninitialized sentinel). PlayerState.WriteCanonical writes 50 bytes in locked P/V/JerseyNumber/Side order. PlayerKinematics.Phase3Defaults: MaxSpeed=7 m/s, MaxAccel=6 m/s², Radius=0.5 m. HasPossession sqrt-free comparison against Radius²; boundary inclusive; multi-player possession allowed (match loop resolves contests). No separate Kick API — kick = `new BallState(ball.Position, kickVelocity, spin)` is trivial composition; integration test verifies player-runs-to-ball-then-kicks loop end-to-end. 56 new tests (reported before Codex hardening; current actual Player-task count is 64 xUnit cases). Test-strategy: bounded-property invariants for production paths going through Sqrt+division (ClampMagnitude doesn't compose cleanly with literal expected values); floor-of-sqrt uses closeness-margin invariant `(x - root²) ≤ 2·(root+1)·ε` instead of strict-floor (Q32.32 truncation can collapse adjacent ULPs to identical Fixed-multiply outputs). **389 total tests passing at initial ship**. Player + Ball can now compose end-to-end; next /next picks up the 2 BT manager archetypes in YAML
- 2026-04-27: **Codex review pass on `BallPhysics`** — caught grounded-ball contact bug where gravity made `BallState.AtRest` fall inside a tick, then bounce upward. Added two regression tests for grounded-at-rest and grounded-rolling under Phase-3 gravity; patched collision to bounce only when crossing into the ground from above, while grounded states clamp negative vertical velocity to zero and roll. Cached `BallPhysicsCoefficients.Phase3Seeds`. **333 total tests passing**.
- 2026-04-27: **Phase-3 Week-2 — `Ball` custom deterministic physics shipped.** `MatchSim/Sim/Vector3Fixed.cs` + `MatchSim/Sim/BallState.cs` + `MatchSim/Sim/BallPhysics.cs`. Pure-deterministic semi-implicit Euler integrator at fixed 60Hz step per `design/match-engine.md §Q2`. Vector3Fixed Q32.32 (X+Z = pitch plane; Y = altitude); BallState immutable (Position / Velocity / Spin); BallPhysics.Step pure-static function with gravity + linear drag + Magnus stub + ground bounce + rolling friction. BallPhysicsCoefficients.Phase3Seeds pinned to design-doc table (g=9.81, C_d=0.02, C_m=0.0004, e=0.55, μ=0.25). Magnus stub policy honored — structure stays even with zero coefficient. CanonicalEncoder gained WriteVector3Fixed helper; BallState.WriteCanonical writes 72 bytes in locked P/V/S order. 54 new tests (31 Vector3FixedTests + 21 BallPhysicsTests + 2 SerializationContract additions). Test-strategy: production-equivalent expected expressions for exact-byte determinism + combined-trajectory tests as independent oracles. **331 total tests passing** at initial ship. Ball is now ready to receive kicks once Player exists; next /next picks up `Player` state machine
- 2026-04-27: **Phase-3 Week-1 priority #5 shipped — `SerializationContract` + `CanonicalEncoder`.** `MatchSim/Sim/CanonicalEncoder.cs` (production code) + `MatchSim.Tests/Sim/SerializationContract.cs` (executable spec, 43 tests after Codex hardening). Locks byte-encoding rules: little-endian via `BinaryPrimitives.Write*LittleEndian` (platform-independent); primitives 8 bytes LE; strings 4-byte LE byte-count prefix + strict UTF-8 (malformed UTF-16 rejected); counts 4-byte LE non-negative; booleans 1 byte (0x00/0x01). Hash format `"sha256:<lowercase-hex>"`. Each primitive byte-encoding pinned to a literal expected `byte[]`; each well-known SHA256 hash pinned to a literal hex value (independently computed via openssl 2026-04-27). Order-matters test protects against accidental sort-on-write; static-vs-instance hash overloads agree. 277 total tests passing. **Phase-3 Week-2 golden-replay-corpus fixture authoring is now unblocked** (corpus spec OQ §1 "Exact sim-state serialization order" was the gating question)
- 2026-04-27: **Phase-3 Week-1 priority #7 shipped — ADR-0008 + ADR-0009 flipped Proposed → Accepted.** Cross-model rhythm complete: Claude drafted (2026-04-26) → GPT-5.5 reviewed (Concerns verdict; 4 P1 + 4 P2 findings applied: stable `ViewerEventId` + ordering; `BaseShotTypeId`/`EffectiveShotTypeId`/`ReduceMotionApplied` split; locked minimum `PitchView`+`ActiveViewerEvent` shapes; aftermath-freeze viewer-vs-sim time boundary; ordinal-sort + InvariantCulture + locale-exclusion rules; adapter-keyed `pass_activation_log_hashes` from corpus v1; operational 6-task observer rubric; CI smoke split into synthetic + corpus fixtures) → Codex hardened (stale-reference cleanup; sign-off "no remaining blocking findings") → both ADRs **Accepted**. Append-only from this point; supersession only via new ADR. AdapterId enum code-owned-registry clarification: community/mod adapters are code plugins via trusted-build channel, NOT Workshop content packs. Phase-3 Week-2 viewer authoring pre-cleared on the contract side
- 2026-04-27: **GPT-5.5 review pass on ADR-0008 + ADR-0009 + golden-replay-corpus.md.** Concerns verdict on each ADR; 4 P1 + 4 P2 findings applied across all three docs. Status remained Proposed at the time; superseded by the Codex review pass + Accept flip later same day. P1 fixes: stable `ViewerEventId` + ordering rule; explicit `BaseShotTypeId` / `EffectiveShotTypeId` / `ReduceMotionApplied` split (bridge resolves once; adapters never re-substitute); minimum `PitchView` + `ActiveViewerEvent` shapes locked into the contract; aftermath-freeze rewritten so viewer-time-hold doesn't pause MatchSim canonical time. P2 fixes: ordinal-sort + InvariantCulture + locale-exclusion rules for memory slots; `pass_activation_log_hashes` adapter-keyed from corpus v1 (no future schema bump); operational 6-task observer rubric replaces "drama legible" vibe-check; Tier-A CI smoke split into synthetic ViewerEvent fixture (Week 2; non-empty trace guaranteed) + corpus seed (Week 3+; end-to-end). AdapterId enum gained code-owned-registry clarification: community/mod adapters are code plugins via trusted-build channel, NOT Workshop content packs. ADR-0008 open questions reduced from 4 to 3 (deferred items narrowed). 234 dotnet tests still passing; `fw verify` green
- 2026-04-27: **Phase-3 Week-1 priority #4 shipped — `Seed` match + per-event derivation.** `MatchSim/Sim/Seed.cs`: `readonly struct Seed : IEquatable<Seed>, IComparable<Seed>, IComparable` over `ulong _value`. `Seed.Derive(ulong matchSeed, Tick tick, ulong eventId)` is the canonical-triple per-event derivation per ADR-0001 + ADR-0008 + TECH_APPROACH §3.2. SplitMix64 mixer (Java SplittableRandom-finalizer): pure integer math, allocation-free, ~50% bit-flip rate on 1-bit input change. Composition non-commutative on (matchSeed, eventId). Canonical `0xdeadbeefdeadbeef` lowercase 16-digit hex matches `golden-replay-corpus.md` smoke-seed format. 45 new tests covering determinism / sensitivity / avalanche (≥16/64 bits on each input) / 1000-eventId distinctness / equality / total-ordering / Parse + ToString round-trip + literal pinned value `0xddac907bb053edbb` plus oracle that trips on any future mixer change. 234 total passing. Per-tick deterministic randomness primitive complete; `SerializationContract.cs` is next + gates Week-2 golden-corpus fixture authoring
- 2026-04-27: **Codex review pass on Tick** (commit `2a453e5`) — caught a real architectural bug: `Tick.ToSeconds()` was narrowing through `Fixed.FromLong(_value)` which has ±2^31 integer-range check, defeating long-storage's purpose for multi-season replays. Fixed with direct raw Q32.32 conversion `((BigInteger)_value << 32) / 60` + range-check on final Fixed. 3 new tests (int.MaxValue / int.MinValue seconds round-trip + overflow-beyond-Fixed-range). CLAUDE.md + docs/ops/branch-protection.md aligned to direct-to-main solo-dev posture
- 2026-04-26: **Phase-3 Week-1 priority #3 shipped — `Tick` deterministic 60Hz timestep.** `MatchSim/Sim/Tick.cs`: `readonly struct Tick : IEquatable<Tick>, IComparable<Tick>, IComparable` over `long _value`. `TicksPerSecond = 60` locked per TECH_APPROACH §3.2; `TicksPerMinute = 3600`. Factories `FromSeconds(int)` / `FromMinutes(int)` checked-arithmetic. `ToSeconds() : Fixed` uses direct raw Q32.32 conversion with range-checking, so large long-backed tick values convert if their seconds value fits Fixed. **Type-distinguished `Tick - Tick → long` delta** vs `Tick + long → Tick` — type system enforces "absolute tick" vs "duration in ticks" semantics. Long-storage chosen because multi-season corpus replays exceed `int.MaxValue` ticks. 45 new tests across constants / factories / ToSeconds / additive / Tick-minus-Tick / equality / total-ordering / determinism / ToString. 189 total passing. Codex review pass on Fixed (commit `a276ba0`) caught + fixed a subtle MaxValue round-trip boundary bug pre-`Tick`
- 2026-04-26: **Phase-3 Week-1 priority #2 shipped — `Fixed` Q32.32 struct.** `MatchSim/Sim/Fixed.cs`: `readonly struct Fixed : IEquatable<Fixed>, IComparable<Fixed>, IComparable, IFormattable` with `long _raw` storage, top 32 bits signed integer + bottom 32 fractional, range ±2.147e9, precision ~2.328e-10. Constants (Zero / One / MinusOne / Half / MaxValue / MinValue / Epsilon), factories (FromRaw / FromInt / FromLong), full operator + comparison suite, Floor / Ceiling / Truncate / Round (banker's). Multiplication via allocation-free 32-bit-split unsigned multiply + explicit 128-bit-shift-back-to-Q32.32 + checked overflow. Division via BigInteger for correctness (profile-and-optimize if hot path at Phase 6). Canonical decimal-string serialization with 10 fractional digits + invariant culture matches FW-VAL-A-018; plain-decimal parser rejects scientific notation and round-trips through min/max bounds. 141 xUnit tests across 5 files all passing — first real determinism-math primitive. Win/Mac/Linux CI matrix activation now becomes a real candidate (deferred until Tick + Seed expand the corpus per cost-discipline)
- 2026-04-26: **Phase-3 Week-1 priority #1 shipped — MatchSim.csproj + MatchSim.Tests.csproj skeleton.** `MatchSim/MatchSim.csproj` (netstandard2.1, pure-C#, zero UnityEngine refs, C# 14 pinned) + `MatchSim.Tests/MatchSim.Tests.csproj` (net10.0, xUnit 2.9.2 + Microsoft.NET.Test.Sdk 17.11.1) + `FinalWhistle.slnx` solution + `global.json` SDK pin. `MatchSimAssemblyMarker.AssemblyMarkerVersion` skeleton class proves cross-project reference. 3 tests pass under `dotnet test FinalWhistle.slnx`, including no-Unity-reference guard. `fw test` subcommand authored + wired into `fw verify` umbrella; `fast-pr-ci.yml` Tier-A workflow adds `actions/setup-dotnet@v4` step (10.0.x). `.gitignore` extends with .NET artifacts (`**/bin/`, `**/obj/`, `*.user`, `.vs/`). First real code phase begins; next /next picks up `Fixed` Q32.32 struct
- 2026-04-26: **Codex correction pass on visual-target supersession** (commit 3516f7d). Per Codex/GPT-5.5 review of the ADR-0008 draft + supporting docs: MatchSim canonical-sim-only (Viewer.Contracts holds presentation contracts; Viewer.EventBridge derives ViewerEvents from canonical sim event stream + projects ShotTypeSO → ShotTypeDefinition pure DTOs); MemoryHit carries CallbackLineId + deterministic slots not rendered prose; replay determinism = semantic pass-activation traces not pixels; animation markers depict already-emitted events and cannot emit canonical records; FW-VAL-D-011 count drift fixed (21+11); stale Phase-9-only-3D language cleaned across 18 files; SPEC correction entry appended
- 2026-04-26: **Visual-target supersession + balance-harness pre-seed** from GPT-5.5 review pass (Concerns verdict + 9 P1 + 8 P2 findings applied), followed by Codex correction pass. Visual-target supersedes 2026-04-22 "2D-first MVP / 3D deferred" — new posture: renderer-agnostic ShotPresentationContract (ADR-0008 Proposed) + dots-phase render adapter (ADR-0009 Proposed) for Phase-3 validation + cel-shaded 3D as CANDIDATE shipping layer (ADR-0010 NOT pre-authored; gated on Phase-5/6 production-feasibility spike per `design/3d-pipeline.md` placeholder). Three-outcome EA contingency: spike-green → 3D ships; spike-yellow/red → dots ships if polish bar met (NO public 3D promise); dots-not-strong-enough → delay EA. ADR-0002 Superseded; original preserved. `FW-VAL-D-011` 3D-asset commercial-rights check added. Vendor-agnostic architecture; tooling catalogs may name candidate tools for cost/license tracking. Correction pass keeps MatchSim canonical-sim-only (`Viewer.EventBridge` derives ViewerEvents from pure ShotTypeDefinition DTOs), makes replay hashes semantic-trace-only, and blocks animation markers from emitting canonical events. Balance-harness pre-seed locks methodology (scale-agnostic internal score, stratified analysis, warning-bands-not-hard-fails, hybrid fuzzing legality grammar, NarrativeFlag-zero-rating-contribution invariant). Phase-3/5/6/8 SPEC tasks adjusted; decisions-log entries appended via footer-anchor pattern
- 2026-04-25: **Phase 4 pre-seed from cross-model design session** (Claude + GPT-5.5). Five commitments consolidated into SPEC decisions-log entry: *"Rate the job, not the highlight"* project rule + **centered-5.0 rating scale locked** (Par / Good / Excellent / Dominant labels; revisit clause tied to Month-3/4 observer feedback) + counterfactual defensive value derived-from-event-stream not emitted-as-EventClass + TacticalPreset system parallel to SignatureSO (NOT scope-expansion of ADR-0005) + IdentityPacket role-subtype schema-bump as implementation-pressure-driven. Two Phase-4 SPEC tasks added. Phase 2 not reopened; Phase 3 Week-1 priorities unchanged
- 2026-04-25: **Phase 2 ✅ COMPLETE.** Phase 3 🟡 ACTIVE. `/audit` green on Phase-2 scope; bundle committed `3226c78` (15 files: 3 new design docs + 2 new specs + 9 modified + .gitignore). `.gitignore` extended to exclude `.claude/session-snapshots/` (auto-generated pre-compact ephemera) + `.agents/` (not yet deliberately ported; contains absolute blueprint paths). Phase-3 first task: `MatchSim.csproj` pure-C# skeleton
- 2026-04-24: Phase 2 — **Artifact retention policy spec** authored at `design/specs/artifact-retention-policy.md`. 5-tier retention model (ephemeral / short / release-tied / permanent-in-repo / local-only). ~30 artifact classes catalogued with TTL + source + storage + rationale. RC bundle minimum-10-items rule locked for determinism-replay posture. Cost-discipline math projected through Phase 6 (Free 500MB cap tight; Pro 1GB comfortable). Playtest bug-bundle policy explicit. Phase-3 `fw artifact-cleanup` + Phase-6 `fw workflow-audit` added as SPEC tasks; `FW-WF-A-001` covers uploaded Actions artifact retention and `FW-WF-A-002` reports repo/org Actions artifact-and-log retention settings. Workflow logs are platform-managed, not 7-day uploaded artifacts. Release-tied bundles are verified as release assets, not `retention-days: 0`. `design/specs/` now holds 4 sibling specs
- 2026-04-24: Phase 2 — **`design/content_policy.md` authored.** PEGI 12 / ESRB T content boundaries locked. 12 in-scope mature themes enumerated with shipping-form + example; 13 out-of-scope categories enumerated; 6 edge-case rulings on drift-prone subjects (post-derby hostility / managerial dismissals / youth setback language / discrimination exclusion / injury severity prose / fan-sentiment targeting). AI-content disclosure at Steam 2025 policy compliance via FW-VAL-D-005 + pack-manifest `ai_content_disclosure` block sketch. Mod-pack content-safety review surface closes `design/modding.md` OQ#2 — banned-terms lint + Steam Workshop report-flow cover EA; in-game report flow deferred post-EA. Two prototype gates Phase 6/8. Four open questions. **All 15 Phase-2 design docs shipped** (12 original + modding + accessibility + content_policy)
- 2026-04-24: Phase 2 — **`design/accessibility.md` authored.** Five-item EA feature set locked (reduce-motion toggle / colorblind palette + color-never-sole-carrier / remappable controls / large-text UI / subtitles + post-match text-log). Synthesis of ADR-0001 `reduce_motion_variant` + ADR-0002 scene-load-time feature-disable + ADR-0006 default-OFF advanced details + corpus spec `reduce_motion` field; fresh commitments on colorblind palette + subtitle timing + text-scale + input-remap surface. Three prototype gates Phase 3/6/7. Missing reduce-motion variants block by Phase-6 content-pack v1 / EA lock; three open questions remain
- 2026-04-24: Phase 2 — **Content-pack validation contract spec** authored at `design/specs/content-pack-validation-contract.md`. 21 Tier-A checks (`FW-VAL-A-001..021`) + 11 Tier-D checks (`FW-VAL-D-001..011`, with D-011 added 2026-04-26 for 3D-asset commercial-rights manifests). Ownership decentralized across 5 validator asmdefs (mirrors Contracts/impl split). Red-team fixture per check + anti-red-team negative control + red-team CI self-check step. Binding failure-message convention + JSON output shape. Phase-6 SPEC tasks added: synthetic thin-mod-pack fixture (modding integration test) + red-team validator fixtures + content-pack validator full (cites spec)
- 2026-04-24: Phase 2 — **`design/modding.md` authored** as cross-ADR synthesis. 12 load-bearing mod-loadability constraints with per-constraint ADR citation; zero new architecture committed. Four open questions flagged for Phase-3/6 resolution. `design/README.md` index updated; SPEC task `[x]`
- 2026-04-24: Phase 2 — **ADR-authoring umbrella closed `[x]`** (SPEC.md line 91). Rollup of all 7 pre-seeded ADRs Accepted. Modding confirmed as design-doc-not-ADR (constraints woven across ADRs 0001/0004/0005/0006/0007). Phase-2 remaining work is concrete spec-authoring + 3 design docs + `/audit` green
- 2026-04-24: Phase 2 — `/refresh-docs` pass fixed 6 findings: TECH_APPROACH pack-id prefix drift (`finalwhistle.core.v1` → `fwh.core.v1`); TOOLING GitHub Account corrected (`Vibelogic` → `osagberg` w/ `vibelogic` org reserve note); SPEC + STATUS "11 design docs" → "12 design docs" (production-pipeline.md was the 12th); `scripts/fw verify-docs` now excludes `.claude/session-snapshots/` auto-generated artifacts
- 2026-04-24: Phase 2 — **ADR-0007 Accepted** after 5-finding review pass (P1 ScoutReport event-class-free / emitter owns EventClass selection; P1 Scouting.Runtime owns single post-gate path, Prototype carries pre-verdict selector; P2 LabelEstimate split from GeneCategoryEstimate; P2 subsection-header status discipline; P3 ADR-0006 stale subsection labels cleaned). **ALL 7 Phase-2 pre-seeded ADRs Accepted**
- 2026-04-24: Phase 2 — **ADR-0007 Scout archetype + ScoutReport + gate-fallback Proposed.** Both Month-4-gate paths committed architecturally (Path A Scout Disagreement 3-archetype; Path B BasicScoutUncertainty fallback with identical ScoutReport schema). `Scout.Biases.NarrativeFlag = 0` validator invariant. `Scouting.Contracts` / `.Runtime` / `.Prototype` asmdef split. Five rejected alternatives. All 7 pre-seeded Phase-2 ADRs now drafted
- 2026-04-24: Phase 2 — **ADR-0006 Accepted** after Codex/GPT-5.5 6-finding review pass. Fixes: (P1) LLM path explicitly outside byte-identical regeneration (compiler determinism = cohort + seed + checked-in name-bank artifact); (P1) canonical player-ID regex `^fwh\.core(?:\.v[0-9]+)?:player_[0-9]{5}$` aligned across ADR-0006 + player-generation.md + TECH_APPROACH; (P2) status discipline preserved (stayed Proposed until findings resolved); (P2) event-class count normalized to 42 starter / ~40 shorthand; (P2) `fw verify-docs` recursive frontmatter check; (P3) `.agents/` skill-port corrections. All 6 drafted ADRs now Accepted
- 2026-04-24: Phase 2 — **ADR-0006 IdentityPacket / AI Content Compiler Proposed**. Walled-off InternalGeneSnapshot (Q32.32 across 22 fields), 46-label phenotype enum with banned-term-lint on rendered strings, SignatureCandidate[] affinity storage (ADR-0005 handoff received), category-level scout bias + narrative-flag zero-visibility, advanced tooltip default OFF, canonical-JSON-artifact compiler discipline, 4-project split (Content.Contracts/.Compiler/.Validator/.UnityImport). Five rejected alternatives
- 2026-04-24: Phase 2 — **ADR-0005 Accepted** after 3 architectural tightenings: `SimBiasSnapshot` reframed as Unity-free `MatchSim.Contracts` DTO pre-baked BEFORE sim execution (no per-tick Unity push); `SimBiasFieldId` registry ownership moved to `MatchSim.Contracts` (MatchSim owns semantic meaning); continuous vs event-triggered bias application split per-field to avoid premature per-tick work. Asmdefs: MatchSim.Contracts (pure, owns field registry + DTO), Signatures.Authoring (Unity SO), Signatures.Baking (pure baker), MatchSim.csproj (pure consumer)
- 2026-04-24: Phase 2 — **ADR-0005 SignatureSO Proposed** with user's 6 pre-constraints baked in from first draft: event names via Memory.Contracts const references (not duplicate strings), explicit Scope enum, non-behavioral dependency metadata, field-level capped stacking with Id-tiebreak ordering, Display/ID lint-target separation, latent affinity explicitly not-here (lives in IdentityPacket per ADR-0006). Five rejected alternatives. Separate Signatures.Authoring / Runtime asmdefs; SimBiasSnapshot DTO boundary to MatchSim
- 2026-04-24: Phase 2 — **ADR-0004 Accepted** after five user-review tightenings (SalienceInputs + SalienceModelVersion persistence for audit; `FinalWhistle.Memory.Contracts` / impl split decouples MatchSim from persistence; quota rounding formula locked; event-class count corrected to ~40; float-salience added as 5th rejected alternative). Phase-6 content-pack validator SPEC task expanded to cover cross-doc event-class enum exact-match + `CallbackTag.ConsumingReaders ≥ 1`
- 2026-04-24: Phase 2 — **ADR-0002 Accepted** via self-review tightening pass (Knowledge Risk MEDIUM gate remains baked in; `fw shader-audit` promoted to explicit Phase-3 SPEC task). **ADR-0004 (MemoryEvent schema + CallbackTag registry + compaction tiers + migration framework) Proposed** — first real user of both save-migration-fixtures and golden-replay-corpus specs. Four rejected alternatives documented. Cross-doc exact-match discipline formalized for `SignatureAwakened`/`SignatureExecuted`/`ScoutReport*` enum names
- 2026-04-24: Phase 2 — Corpus spec tightened (Q1 converted to explicit Phase-3 SPEC task for `SerializationContract.cs`; seed policy locked at 1-seed Phase 3 / 3-seed Phase 6; generator owns key order). **Save-migration fixture policy spec** authored at `design/specs/save-migration-fixtures.md` — 4-test-per-schema-bump discipline (forward/callback-preservation/forward-incompat/round-trip); fixtures append-only; Phase 3 ~5 fixtures → Phase 8 ~15. `design/specs/` now holds 2 sibling specs
- 2026-04-24: Phase 2 — **ADR-0003 Accepted** after user tightenings (self-hosted-runner 4-condition acceptance gate, stale commit-hash removed). **Golden replay corpus spec** authored at `design/specs/golden-replay-corpus.md` — JSON fixture schema, Tier-A smoke seed `0xdeadbeefdeadbeef`, stable serialization rules, append-only growth policy, 3 Phase-3 open questions. New `design/specs/` subdirectory established
- 2026-04-24: Phase 2 — **ADR-0003 Proposed** (Production pipeline). 5-tier model + 5-channel build metadata + runner policy + cost discipline + release-gate rules formalized. Four rejected alternatives documented. Phase-1 validation criteria already satisfied (4 of 11)
- 2026-04-24: Phase 2 — **ADR-0001 Accepted** after user tightenings (ChainConditionId registry-backed / explicit deterministic-selection contract / per-content-pack Addressables grouping). **ADR-0002 Proposed** (Viewer rendering pipeline + URP custom-pass ordering). Knowledge Risk MEDIUM flagged on ADR-0002 — Unity 6 LTS URP 17+ Render Graph verification required at Phase 3 Week 1 spike. Two inline Category-B exemptions in place
- 2026-04-24: Phase 2 — ADR-0001 ShotTypeSO schema + Addressables grouping drafted as **Proposed** at `design/adr/adr-0001-shot-type-so-schema.md`. First Category-B inline exemption recorded (`term="domain"` ADR-template field). Awaiting user/GPT-5.5 sign-off before Accepted
- **2026-04-24: Phase 1 ✅ COMPLETE.** Phase 2 🟡 ACTIVE — Design Bible / ADR authoring. ADR order reprioritized per GPT-5.5 2026-04-24 guidance to feed Phase-3's playable slice (MatchSim + viewer) rather than tidy-doc order. Stale SPEC tasks cleaned up (Task 52 Unity CI stub superseded, Task 50 accounts done, 11 design-doc locks marked)
- 2026-04-24: Phase 1 — `scripts/lint-banned-terms.py` shipped. Category-A hard-ban across 5 subsections from ui-vocabulary.md + Category-B soft-ban with inline `ui-lint:allow term=/reason=/reviewer=` exemption audit. Sentinel-aware (ui-lint:ignore-start/end), both-forms matching (per GPT-5.5 feedback). Wired as `fw banned-terms`, integrated into `fw verify` umbrella → Tier-A CI auto-picks up. 11 files sentinel-wrapped for legitimate meta-references; repo lint-clean. Resolved the GPT-5.5 tech-debt note about spaced-placeholder hack via proper sentinel discipline
- 2026-04-24: Phase 1 — GPT-5.5 Codex review pass applied 5 findings: branch-protection reframed as local-discipline-only (Free plan doesn't allow protection on private repos; verified via `gh api`), approvals corrected to 0 (GitHub blocks author self-approval), JetBrains Mono license corrected to SIL OFL 1.1 (Apache-2.0 covers source code only), `fw verify` umbrella added + false broken-link claim removed, spaced-placeholder hack documented as tech-debt to resolve when `lint-banned-terms.py` lands
- 2026-04-24: Phase 1 — `osagberg/FinalWhistle` private remote created + initial push (2 commits on origin/main); `.github/workflows/fast-pr-ci.yml` Tier-A v0 authored (fw verify-docs only, phase-gated TODOs commented); `docs/ops/branch-protection.md` + `docs/ops/actions-budget.md` runbooks shipped. `vibelogic` org membership gap diagnosed + namespaced-around
- 2026-04-24: Phase 1 — first parallel-to-Unity-install batch shipped (6 tasks): Unity 6 LTS installed (user), asset-licensing tracker seeded (Anton/JetBrains Mono/Rajdhani/Magica Cloth 2), `scripts/fw` front-door with `help`/`status`/`verify-docs` implemented + 5 phase-gated stubs, PR template, 2× issue templates, `docs/ops/backup-restore.md`. `fw verify-docs` + `fw status` both green
- **2026-04-24: Phase 0 ✅ COMPLETE.** All 12 design docs resolved, 12 consolidated SPEC entries, 7 Phase-2 ADRs pre-seeded, ~33 pipeline-related SPEC tasks threaded across downstream phases, `/refresh-docs` green (6 findings fixed). Phase 1 🟡 Setup now active
<!-- ui-lint:ignore-start reason="historical /refresh-docs finding description" -->
- 2026-04-24: Phase 0 — `/refresh-docs` pass fixed 6 findings including user-visible `{{ PROJECT_NAME }}` placeholder leak in `.claude/hooks/session-start.sh` (was showing literal template token in SessionStart banner every new session), plus state-dump script + statusline comment + design/README.md stale date + missing production-pipeline row + production-pipeline.md verifier-trip. Intentionally left: bootstrap placeholder source-of-truth + TECH_APPROACH §8.5 non-standard numbering
<!-- ui-lint:ignore-end -->
- 2026-04-24: Phase 0 — production-pipeline planning pass landed (GPT-5.5 report). Authored `design/production-pipeline.md` with 5-tier workflow policy, 5-channel build policy, core-deliverable specs (golden replay corpus / save fixtures / content validator / `scripts/fw` / playtest ops / crash export / backup). Phase-2 production-pipeline ADR pre-seeded. ~33 pipeline-related SPEC tasks threaded across Phases 1/2/3/4/5/6/8. `TECH_APPROACH.md` §8.5 added
- 2026-04-24: Phase 0 — `design/ui-vocabulary.md` open questions resolved; **all 11 design docs now locked**. Lint scope (code + content + rendered output), sentinel-ignore mechanism (no whole-file self-whitelist), Category-A expanded to 5 subsections absorbing bans from all prior 2026-04-24 resolutions, Category-B audited inline-exemption mechanism (audit before EA lock + RCs, not quarterly), flatter ~140-template commentary pool structure, British-football tone register default. Phase-1 lint task upgraded to full `scripts/lint-banned-terms.py`. Template governance folds into AI Content Compiler ADR (no new ADR)
- 2026-04-24: Phase 0 — `design/worldbuilding.md` open questions resolved; nation locked as **Caldren** (Cresland fallback after lightweight clearance pass), 8 regions, pyramid 20/24/16/14/12/10=96 reframed as simulated-slice-not-entire-ecosystem, three cups (National all-tier + League Cup top-2 + Trophy tiers-3-6), compiler-only analogues with Phase-1 lint rule pre-seeded
- 2026-04-24: Phase 0 — `design/player-generation.md` open questions resolved; 22-field internal model locked, 46-label phenotype catalog with 3 targeted edits, default-off advanced tooltip, canonical-JSON reproducibility, **ID-stability correction** (no pack-minor in entity IDs), authoritative affinity P(k) tables materialized here, category-level scout biases, regional-priors integration. Sixth Phase-2 ADR pre-seeded
- 2026-04-24: Phase 0 — `design/breakthrough-moments.md` open questions resolved; cinema duration locked to 3-5s (default seed 3s), strict no-system-vocabulary rule, silent-first-near-miss anti-farming policy, regressive parity, pillar-tiebreaker live-fire-on-resolving-action-only rule. No new ADR (composes existing schemas)
- 2026-04-24: Phase 0 — `design/scout-disagreement.md` open questions resolved; Month-4 feel-prototype spec locked (3 archetypes, hand-authored packets, staged-time ledger feedback, user-excluded pass criterion, one-remediation-pass ceiling to prevent rescue-loop). Fifth Phase-2 ADR pre-seeded — architecture slot reserved regardless of gate outcome
- 2026-04-24: Phase 0 — `design/signatures.md` open questions resolved; Pillar-2 24-sig catalog locked with dependency metadata, #19 corrected to "stronger foot", #6 scoped as defensive_line. Affinity distribution tier-weighted. Multi-signature stacking uses field-level caps, not softmax. Counterplay via scout reports for observed sigs only. Fourth Phase-2 ADR pre-seeded
- 2026-04-24: Phase 0 — `design/event-sourced-memory.md` open questions resolved; Pillar-1 ledger architecture locked (salience structure, CallbackTag schema with consuming-reader metadata, ~40-entry PascalCase event enum, three-tier compaction with per-season quota cap, load-time migration). Third Phase-2 ADR pre-seeded. Callback-age + player-attention clarified as reader-side modifiers (not emission-time) to reconcile with 2026-04-22 SPEC seed
- 2026-04-24: Phase 0 — `design/semantic-cinema.md` open questions resolved; 7-shot vocabulary locked through Month-3 gate, ShotTypeSO schema drafted with chain_rules + reduce_motion_variant, rendering stack + typography locked (scoreline override: not Anton), two Phase-2 ADRs pre-seeded
- 2026-04-24: Phase 0 — `design/match-engine.md` open questions resolved; ball-physics structure + steering-target movement + Month-3 in-match event scope locked. Numeric coefficients explicitly kept out of SPEC as tuning seeds
- 2026-04-24: Phase 0 — `design/month-3-vertical-slice.md` open questions resolved; Month-3 gate parameters locked (match type / first-3 signatures / 3-min recording artifact / football-literate observer criterion / observer-pool fallback)
- 2026-04-24: Phase 0 — `design/overview.md` open questions resolved; 4 pillar-level decisions locked via consolidated SPEC entry (nation framing / title / quickstart archetypes / pillar tiebreaker)
- 2026-04-22: Project bootstrapped from blueprint v2; composed profile (sim-management + action-character + narrative trimmings); research scope active
- 2026-04-22: 19 initial decisions logged to `SPEC.md` decisions log (append-only)
- 2026-04-23: Codex review appended Q32.32 fixed-point decision and tightened Month-3 slice scope
- 2026-04-22: All 11 design docs scaffolded with real content under `design/`

---

*Timestamp auto-maintained by `.claude/hooks/update-status-timestamp.sh`. Everything else Claude-edited per `/done`.*
