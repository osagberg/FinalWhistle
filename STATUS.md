# STATUS — Final Whistle

**Last updated**: 2026-06-03

## Phase

**Phase T4 — Pillar Wiring + Polish — IN PROGRESS.** T4-1/2/3/4/5a/6a + T4-2.5a + T4-sim-halt + T4-2.5b + T4-2.5c (+T1-25/T1-26) + T4-2.5e + T4-2.5d + T4-2.5f + T4-F1 + T4-F2 + T4-2.5g + T4-2.5h + T4-F4 + T4-I1 + QA-3 + QA-5 + T4-2.5i + T4-2.5j + T4-2.5k + T4-2.5L + **T4-7** DONE. **PHASE-T4 IMPLEMENTATION COMPLETE — the autonomous /loop HARD-STOPPED here** (terminus condition: all selectable T4 rows done; only the T4-8 phase gate + the DEFERRED T4-2b remain). The loop did NOT run `/done`, did NOT start T4-8, did NOT enter T4.5 — awaiting the user to drive the phase gate. The game-shell now looks like a finished product (branded main menu, splash, OS-theme-aware window, committed per-OS app-icon). All five pillars are wired AND have a falsifiable proof: T4-2.5L proves Pillar 2 cross-decade (a season-1 event survives the 5-season compaction boundary + still renders in season 8), emits RegressiveCollapse at a terminal `CAREER_END_SEASON`, + a 100-ledger compaction-retention corpus. T4-2.5k also fixed a pre-existing silent failure: memory callbacks now render REAL prose instead of a generic fallback. The 600-tick canonical pin was rebaselined at T4-2.5j (user-authorized, envelope-verified); the 60-tick smoke pin is UNCHANGED. T4.5 (World Scale + Content Bake) is the EA-critical phase between T4 and T5.

**2026-06-02 external dual review** correct → persistent → visible all landed; all T4-2.5x pillar-polish + QA + game-shell rows done. **Phase-T4 implementation is complete.** Remaining: **T4-8** (phase-gate Codex review — the user runs `/done` → `gh pr create` when ready) + DEFERRED T4-2b. F5/F6 (match-engine) second; C1 at private-flip. **Filed follow-ups:** roster Squad → Player navigation; match-sim `team_width` flake; T4-I1 `import type` reorder; T4-2.5i loader error-distinction + `commentary_line_bank_id`; T4-2.5j inert `trigger: NoOpStub` + body_shield vacuous test + hash-pins fw-tauri pin; T4-2.5k PressTopic::as_dto_str + repo-wide season-display indexing.

## Active task

**Phase T4 SHIPPED + TAGGED (`v0.4.0-polish` at `c7ef7dcf`); pushed; CI green (one deferred gap).** Sequence this session: `/done` (4-track ultimate-review ACCEPT) → Codex T4-8 gate REVISE on two P1 save-integrity gaps → **T4-8-CR1** fixed both (`load_envelope` hard-rejects `watermark > ledger.len()` + non-contiguous ledger event-ids) → **QA-T4H** (12 tests + 2 fail-loud logs + `PressTopic::as_dto_str` + aggregate non-pinned envelope floor + timeless-anchor doc/test + save-spec doc-repoint) → pushed 38+ commits + `v0.4.0-polish`. **CI on push:** the tag fired the Release workflow, which caught a REAL regression — T4-7's `tauri.conf.json` `theme: "auto"` is not a valid Tauri 2 enum value → **T4-7-fix** removed it (absent = follow-OS; verified by a local `pnpm tauri build` that bundled `.app`+`.dmg`). After the fix + tag re-point: **CI ✅, Determinism Gate ✅, Release ✅ on Windows + Linux**; the SOLE remaining red is the **macOS Release codesign step** — Apple signing secrets unconfigured (the workflow's empty `APPLE_*` env defeats its own unsigned fallback) — **DEFERRED to T5-1** (user decision). Both canonical pins UNCHANGED all session. **2026-06-04 — `DX-1` DONE (dev play-harness):** the `fw-dev-server` HTTP bridge (`127.0.0.1:1422`) + frontend `?backend=http` mode now let an agent drive the REAL backend in a browser — live-validated (real squad/match/board rendered via the Preview MCP); self-review triple ACCEPT; both pins UNCHANGED; production build unaffected. The harness immediately surfaced (filed, NOT DX-1 bugs): broken match-feel (10 goals in minutes 4-14 of a 900-tick slice; busted tick→minute mapping; signatures stamped 0') and "No career active" in the dev-server's AppState. **NEXT (user-directed sequence):** `DX-2` (frame glitch-detectors + GIF filmstrip) → fun-roadmap pivot (deterministic drama metrics + LLM-judge; pull a watchable-match milestone forward) → feature-backlog research (multi-agent gap-analysis). Then **T4.5 (World Scale + Content Bake — EA-critical)**. macOS Release signing + the empty-env unsigned-fallback workflow bug remain T5-1.

## Blockers

None blocking the next task. **KNOWN (pre-existing, NOT T4-2.5h): a match-sim proptest flake** — `fw-match-sim` `team_width_when_in_possession_within_band` occasionally fails (in-possession outfield Y-width ~23.98m vs the [24,70]m band floor, ~1.8cm under) on a random proptest seed. Proven unrelated to the career-layer work (zero fw-match-sim code touched; both canonical pins byte-identical). FILED as its own match-engine task (F5/T5-5b territory) — it makes `scripts/fw verify` probabilistically red until the band/behaviour is calibrated. T4-sim-halt closed the desktop FullTime/goal SPAM (2026-06-02). OPEN: match-goal-RATE realism (T5-5b).

Previously: T2-1d2 (xg_utility honesty / cross-band oscillation) re-anchored to Deferred section of MASTER_PLAN. Open EA-scope question resolved 2026-05-29 per DECISIONS.md — DESIGN_DOC `§MVP-scope` anchor added at §8 `### MVP scope`.

## Last green verify

2026-06-03 (QA-T4H — post-T4 hardening batch): `scripts/fw verify` exit 0 — both canonical pins UNCHANGED (`85f45bf8…`/`12ce5ab7…`; test-only + fail-loud-log + behavior-preserving refactor + doc, zero sim/match-state touched). 12 new tests (incl. the convergent `select_career_end_collapse_player` identity pin, watermark advancement, poacher's-dart gate hole, body-shield exact-product + exclusivity, 5 fit-varies, compaction reader-order/timeless-anchor); 2 fail-loud logs; `PressTopic::as_dto_str` exhaustive match + source-pin test; aggregate non-pinned 5-seed envelope floor; save-spec doc created + dangling refs repointed. Self-review triple → all three ACCEPT (added the source-level `as_dto_str` pin two reviewers converged on). Prior, 2026-06-03 (T4-8-CR1 — Codex gate-blocker remediation): `scripts/fw verify` exit 0 — both pins UNCHANGED (save-boundary invariants only). `load_envelope` now hard-rejects `watermark > ledger.len()` + non-contiguous ledger event-ids before `restore_transient_state`; new `MemoryLedger::validate_for_load`; V4 golden regenerated to `watermark = 2`; self-review triple → all three ACCEPT. Prior, 2026-06-03 (T4-7 close — Phase-T4 terminus): `scripts/fw verify` exit 0 — both pins UNCHANGED (pure frontend/Tauri-shell + icon assets). Game-shell polish: window `theme: auto`; `Home.tsx` → branded main-menu (9-test rewrite); new `SplashOverlay` (boot overlay with a hard-timeout fallback so a hung `ready` can't lock the UI); real per-OS app-icon (FW monogram → `pnpm tauri icon` → committed icon.icns/.ico + PNGs, fixing a clean-clone build break). Self-review triple → Revise+fixed (splash hard-timeout + timer-leak + dead `reduce-motion:` class; main.tsx silent settings-catch → `console.error`; Home duplicate aria-label). 293 frontend tests + tsc + lint clean. Prior: T4-2.5L (cross-decade proof + corpus); T4-2.5k (PressReader IPC + render bugfix); T4-2.5j (signature catalogue, pin rebaselined). KNOWN unrelated: the match-sim `team_width` flake remains filed. Clippy + fmt + determinism-audit + banned-terms + cargo audit/deny clean throughout. **Phase-T4 implementation complete; next is the T4-8 Codex phase gate (user-driven `/done`).**

## Last canonical hash

`blake3:85f45bf8ae8821182a45a82969ec36bc5b2d70ba2518b8271de24782fd8064fa` (60-tick, UNCHANGED) + `blake3:12ce5ab79c53a3229f59d92ea61fd14b1c2d2a5cf52b71c111e403279c464c1c` (600-tick). **600-tick REBASELINED at T4-2.5j (2026-06-03)** — authorized (user present); 5 new cross-family signature predicates + all-22-slot candidate wiring entered the canonical match setup; SINGLE-pin (the 60-tick bare-`initial` path is untouched); main-thread re-verified the 5-seed envelope (pinned `0xfeedbeefcafefade` = 4 goals ∈ [2,5]; all 5 ∈ [0,7]) BEFORE re-pinning. The 600-tick pin lives in 2 registry sites (`canonical_hash.rs` + `0xfeedbeefcafefade.ron`) + a 3rd hand-synced site (`fw-tauri` `t4_2_5e_canonical_pins_unchanged`); the 60-tick pin in 3 sites (`canonical_hash.rs` + `0xdeadbeefdeadbeef.ron` + `fw-content/tests/fixtures_load.rs`) — unchanged. Prior 600-tick baselines: `206bddae…` (T4-2.5c), then `856a7fed…` (T4-sim-halt). Save wire bytes: SaveEnvelope V0–V4 (V4 = `0x04`, shipped T4-2.5g; current production schema); SettingsEnvelope V0. Next bump: SaveV5 at T4.5-H / T4.5-E1.
