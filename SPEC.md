# SPEC.md — Final Whistle

> Living work plan. Phase list, task checkboxes, decisions log. **Decisions log is append-only — enforced by hook.**
>
> Authored 2026-04-22. Updated every completed task (checkboxes) + every new decision (log, append-only).

---

## Current state

- **Active phase:** Phase 3 — Unity Bootstrap + MatchSim Prototype 🟡
- **Gate to next:** Month-3 match-engine gate — *"A stranger watches a dots-phase match for three minutes and understands drama, momentum, and player identity without reading a design doc."*
- **Active task:** Phase-3 semantic slice — 22 IdentityPacket fixtures + 3 active signatures (#13/#20/#22) + 1 MemoryEvent reader + 1 development event + Viewer.EventBridge minimum, gated on the upcoming Codex review of the round-4 hardening sweep + the football-rules-matrix spec. Foundation tasks 1–3 (asmdef skeleton, MatchSim consumption, PitchRules layer) plus the 6-commit Codex round-4 hardening batch (plugin importer, Unity bootstrap, CI split, enforcement skeleton, design-state sync, process discipline) all shipped between 2026-04-26 and 2026-04-30.

---

## Phases

### Phase 0 — Kickoff ✅ COMPLETE (2026-04-24)
**Goal**: design bible's open questions resolved; ready to set up engineering.

- [x] Fill `PROJECT_CONTEXT.md` — pitch, audience, tone, 4-bucket scope split
- [x] Fill `TECH_APPROACH.md` — MatchSim architecture, determinism discipline, content pipeline
- [x] Scaffold 11 design docs with real content (purpose / locked decisions / MVP boundary / deferred / open questions / prototype gate)
- [x] Seed 19 initial decisions into decisions log
- [x] MCP inventory + plugin queue + global config (tier-capabilities.json)
- [x] Review + resolve open questions in `design/overview.md`
- [x] Review + resolve open questions in `design/month-3-vertical-slice.md`
- [x] Review + resolve open questions in `design/match-engine.md` (fixed-point format, ball physics spec lock)
- [x] Review + resolve open questions in `design/semantic-cinema.md` (7 shot types authored specs)
- [x] Review + resolve open questions in `design/event-sourced-memory.md` (ledger schema lock)
- [x] Review + resolve open questions in `design/signatures.md` (24-signature catalog draft)
- [x] Review + resolve open questions in `design/scout-disagreement.md` (Month-4 prototype spec)
- [x] Review + resolve open questions in `design/breakthrough-moments.md` (trigger conditions)
- [x] Review + resolve open questions in `design/player-generation.md` (internal gene model finalize)
- [x] Review + resolve open questions in `design/worldbuilding.md` (fictional nation scope lock)
- [x] Review + resolve open questions in `design/ui-vocabulary.md` (banned-terms lint authored)
- [x] `/refresh-docs` green

**Gate to Phase 1**: every design doc's open-questions section resolved; Month-3 brutal vertical slice signed off.

---

### Phase 1 — Setup ✅ COMPLETE (2026-04-24)
**Goal**: machine ready, accounts ready, harness wired, first commit + remote pushed.

- [x] Install Unity 6 (currently pinned to tech-stream `6000.4.4f1` per SPEC 2026-04-28 decisions-log entry; LTS migration re-evaluated at Phase 7) via Unity Hub with Mac + Win + Linux Build Support
- [ ] Install Blender (deferred-3D pipeline ready)
- [ ] Install VS Code with C# extension (or Rider)
- [x] Account prerequisites: GitHub (exists), Steam Direct deferred to Phase 8 (2026-04-24 — GitHub remote created `osagberg/FinalWhistle` private; Steam Direct $100 tracked in SETUP.md §10 trigger table for Phase 8)
- [x] `gh repo create osagberg/FinalWhistle --private --source=. --remote=origin` (2026-04-24 — created under personal namespace; `vibelogic` org exists but dev accounts not members. Transfer to publisher org deferred to Phase 8 if needed for Steam branding)
- [x] CI stub from `~/dev/blueprint/ci-cd/github-actions-unity.yml.template` adapted for MatchSim.Tests matrix (Win/Mac/Linux) — **superseded 2026-04-24** by Task 58 Tier-A workflow (`.github/workflows/fast-pr-ci.yml`) per `design/production-pipeline.md` tiered approach. Unity CI moves to Phase 3 manual-dispatch Tier B; dotnet matrix lands in Phase 3 inside the umbrella
- [x] Asset licensing tracker initialized
- [x] Phase-1 lint rule: full `scripts/lint-banned-terms.py` implementing Category-A (hard-ban, no exemption) + Category-B (inline `ui-lint:allow` exemption with term/reason/reviewer audit) + sentinel-exemption blocks (`ui-lint:ignore-start` / `ui-lint:ignore-end`). Scope: UI code + runtime content packs + rendered player-facing outputs. CI emits exemption report reviewed before EA content lock + every RC. Banned-term source is `design/ui-vocabulary.md` Categories A.1-A.5 including real-world place-name analogues (2026-04-24 — wired as `fw banned-terms`, integrated into `fw verify` umbrella + Tier-A CI; lint green clean across repo; zero Category-B exemptions in current codebase)
- [ ] Smoke-test slash commands: `/status`, `/next`, `/log-decision`
- [x] Plugin install via slash commands (feature-dev / pr-review-toolkit / hookify) (2026-04-30 — verified installed at `~/.claude/plugins/installed_plugins.json`; reconciled across CLAUDE.md / TOOLING.md / STATUS.md during the 2026-04-30 process-enforcement sync. Codex round-4 sweep flagged this checkbox as the lone source-of-truth drift.)
- [ ] GitHub Actions budget cap set (stop on overage; Free 2k or Pro 3k included minutes per `design/production-pipeline.md`)
- [x] Fast PR CI (Tier A) workflow `.github/workflows/fast-pr-ci.yml` authored (2026-04-24 — single `Verify (Tier A umbrella)` job calls `scripts/fw verify`, which currently runs `verify-docs` + `banned-terms` sequentially. New checks (dotnet-test / determinism / content-lint / save-migration) wire into the `fw verify` umbrella as their Phase-3/6 deliverables land — no workflow edit needed per check)
- [x] `.github/PULL_REQUEST_TEMPLATE.md` authored (summary / why / test plan / breaking-changes / linked SPEC task)
- [x] `.github/ISSUE_TEMPLATE/bug_report.md` + `feature_request.md` authored
- [ ] Branch protection configured — **BLOCKED on plan upgrade**: GitHub Free does not allow branch protection on private repos (verified 2026-04-24 via `gh api`). Current posture: local-discipline-only per `docs/ops/branch-protection.md §0`. Unblocks on Pro/Team or making repo public; flip logged as SPEC entry when triggered
- [x] `scripts/fw` local command front-door skeleton (bash / makefile, no paid task runner) with verify / test / replay / content-lint / build-local / package-playtest stubs
- [x] `docs/ops/backup-restore.md` authored — GitHub + Time Machine + content-pack snapshot policy

**Gate to Phase 2**: machine + accounts + remote verified; `/next` picks up first Phase 2 task.

**Phase 1 ✅ COMPLETE (2026-04-24).** Machine (Unity installed), accounts (GitHub active + remote pushed, Steam deferred to Phase 8), remote (`osagberg/FinalWhistle` private, CI green end-to-end via Tier-A `fw verify` umbrella). Low-urgency user-actions carried over as open `[ ]` (Blender install is explicit Phase-3 trigger per SETUP.md §4; VS Code / Rider is user editor preference; slash-command smoke + plugin install happen in a fresh session; Actions $0 budget cap is a ~1-min GH UI step the user will handle soon). Branch protection is blocked on plan upgrade (GitHub Free constraint). None of these gate Phase 2.

---

### Phase 2 — Design Bible ✅ COMPLETE (2026-04-25)
**Goal**: every system design doc locked; ADRs authored for every load-bearing system decision.

**Phase 2 ✅ COMPLETE (2026-04-25).** 15 design docs + 4 specs + 7 ADRs shipped; `/audit` green on Phase-2 scope; Phase-2 bundle committed at `3226c78`. Gate condition *"design bible complete; ADRs for every system that locks architecture"* satisfied. Phase 3 🟡 promoted.

**Active posture (2026-04-24 promotion):** all 12 design docs are locked — 11 via Phase-0 open-questions resolution pass (12 consolidated SPEC entries), plus `design/production-pipeline.md` via the Phase-0 GPT-5.5 planning pass (same day). Phase 2 is now primarily about ADR authoring. Task ordering below prioritizes ADRs that unblock Phase-3's real risk — first deterministic MatchSim + watchable viewer — before tidying-only ADRs.

**Design-doc locks** — 11 substantively locked via Phase-0 2026-04-24 open-question resolutions; `design/production-pipeline.md` locked via the Phase-0 production-pipeline planning pass. Remaining Phase-2 work is the ADR authoring below (system-level architecture commitments) + the three new design docs (modding / accessibility / content_policy):

- [x] `design/overview.md` locked (Phase 0 / 2026-04-24 — pillar tiebreaker, title, quickstart archetypes, nation framing)
- [x] `design/match-engine.md` locked (Phase 0 / 2026-04-24 — Q32.32 fixed-point, ball-physics structure, steering-target movement, Month-3 in-match scope)
- [x] `design/semantic-cinema.md` locked (Phase 0 / 2026-04-24 — 7-shot vocabulary, ShotTypeSO draft schema, rendering stack, typography w/ scoreline override). **ADR below.**
- [x] `design/event-sourced-memory.md` locked (Phase 0 / 2026-04-24 — salience structure, CallbackTag schema, ~40-entry PascalCase event enum, three-tier compaction, load-time migration). **ADR below.**
- [x] `design/signatures.md` locked (Phase 0 / 2026-04-24 — 24-sig catalog with dependency metadata, tier-weighted affinity distribution, field-level capped stacking). **ADR below.**
- [x] `design/scout-disagreement.md` locked (Phase 0 / 2026-04-24 — Month-4 feel-prototype spec w/ 3 archetypes + one-remediation-pass ceiling). **ADR below (conditional-MVP).**
- [x] `design/breakthrough-moments.md` locked (Phase 0 / 2026-04-24 — cinema 3-5s, two-tier text, silent-first-near-miss, pillar-tiebreaker live-fire rule). No new ADR (composes existing schemas).
- [x] `design/player-generation.md` locked (Phase 0 / 2026-04-24 — 22-field gene model, 46-label phenotype catalog, ID-stability correction, affinity P(k) tier-weighted). **ADR below.**
- [x] `design/worldbuilding.md` locked (Phase 0 / 2026-04-24 — Caldren nation, 8 regions, 96-club pyramid, three-cup structure, compiler-only analogues). No new ADR.
- [x] `design/ui-vocabulary.md` locked (Phase 0 / 2026-04-24 — Categories A.1-A.5, sentinel exemptions, 140-template flatter pool, British-football tone default). No new ADR; lint shipped Phase 1.

**Phase-2 ADRs — ordered to unblock Phase-3 first (playable slice = first deterministic MatchSim + watchable viewer):**

- [x] ADRs written for every load-bearing system decision (2026-04-24 — rollup: ADR-0001 ShotTypeSO / ADR-0002 Viewer rendering [superseded by ADR-0008/0009 on 2026-04-26] / ADR-0003 Production pipeline / ADR-0004 MemoryEvent / ADR-0005 SignatureSO / ADR-0006 IdentityPacket + AI Content Compiler / ADR-0007 Scout archetype all Accepted. Modding constraints woven across ADRs 0001/0004/0005/0006/0007 via content-pack-qualified IDs + schema versioning + mod-pack-loadability — no separate modding ADR required; `design/modding.md` captures the consolidated data-architecture contract as a design doc. Accessibility + content_policy are scope commitments, not architecture locks)
- [x] **ADR (1): `ShotTypeSO` schema + Addressables grouping** (2026-04-24 — `design/adr/adr-0001-shot-type-so-schema.md` **Accepted** after user review pass: ChainConditionId registry-backed (no scripted predicates in content packs), explicit deterministic-selection contract (priority + Id-tiebreak + base-then-mod-pack precedence + forbidden nondeterminism sources + replay-seed variation path), per-content-pack Addressables grouping with 3-label convention)
- [x] **ADR (2): Viewer rendering pipeline + URP custom-pass ordering** (2026-04-24 — `design/adr/adr-0002-viewer-rendering-pipeline.md` **Accepted**, then superseded by ADR-0008/0009 on 2026-04-26. Historical value: Knowledge Risk MEDIUM gate and `fw shader-audit` no-`_Time` discipline inherited by the adapter model)
- [x] **ADR (3): Production pipeline** (2026-04-24 — `design/adr/adr-0003-production-pipeline.md` **Accepted** after user tightenings: self-hosted-runner acceptance gate (4 conditions — trigger-restricted, explicit labels, restricted label set, blast-radius declared — checked BEFORE any runner registers), stale commit-hash anchor removed. 5-tier model + 5-channel build metadata + runner policy + cost discipline + release-gate rules formalized. Four rejected alternatives)
- [x] **Golden replay corpus format specified** (2026-04-24 — `design/specs/golden-replay-corpus.md`. JSON fixture schema v1 with corpus_schema_version, match_seed, content_pack_version, archetype_ids, reduce_motion, sim_length_ticks, tick_rate_hz, expected block (final_score, key_event_hashes, final_canonical_state_hash, adapter-keyed pass_activation_log_hashes), verification_scope enum. Tier-A smoke seed pinned `0xdeadbeefdeadbeef`. Authoring runbook + CI contracts + growth policy included. 3 open questions deferred to Phase-3 Week 1. Unblocks Phase-3 determinism hash CI gate)
- [x] **Save migration fixture policy specified** (2026-04-24 — `design/specs/save-migration-fixtures.md`. Four-test-per-schema-bump discipline (forward-migration + callback-preservation + forward-incompat + round-trip); fixtures append-only at `MatchSim.Tests/fixtures/saves/`; Tier-A smoke + Tier-D full-matrix CI contracts; per-subject versioning; ~5 fixtures at Phase 3, ~15 at Phase 8. Schema-bump PR without fixture + 4 tests is unmergeable)
- [x] ADR (4): MemoryEvent schema, callback-tag enum, compaction tiers, and migration framework (2026-04-24 — `design/adr/adr-0004-memory-event-schema.md` **Accepted** after user tightenings: SalienceInputs + SalienceModelVersion persistence for Phase-6 audit; `FinalWhistle.Memory.Contracts` / impl project split (MatchSim depends on Contracts only, decoupling sim from ledger persistence); quota rounding formula locked (`min(N_quota, max(1, ceil(count*0.05)))`); event-class count corrected to ~40; float-salience added as 5th rejected alternative)
- [x] ADR (5): SignatureSO schema (2026-04-24 — `design/adr/adr-0005-signature-so-schema.md` **Accepted** after three architectural tightenings. Original 6 pre-draft constraints preserved. Added: (1) `SimBiasSnapshot` is a Unity-free immutable DTO in `MatchSim.Contracts`, pre-computed by `SignatureBaker` BEFORE sim execution — NOT a per-tick Unity callback; (2) `SimBiasFieldId` registry ownership moved from Signatures.Runtime to `MatchSim.Contracts` — MatchSim owns semantic meaning + units; signatures reference; (3) continuous vs event-triggered bias application modes, routed per-field, avoids premature per-tick work)
- [x] ADR (6): IdentityPacket / AI Content Compiler (2026-04-24 — `design/adr/adr-0006-identity-packet-compiler.md` **Accepted** after Codex/GPT-5.5 6-finding review. Fixes: LLM path explicitly outside byte-identical regeneration (compiler determinism = cohort + seed + checked-in name-bank artifact); canonical player-ID regex `^fwh\.core(?:\.v[0-9]+)?:player_[0-9]{5}$` aligned across ADR-0006 + player-generation.md + TECH_APPROACH; event-class count normalized to 42 starter / ~40 shorthand; `fw verify-docs` frontmatter check now recursive. Schema locks + 5 rejected alternatives preserved)
- [x] ADR (7): Scout archetype schema + ScoutReport schema + gate-fallback (2026-04-24 — `design/adr/adr-0007-scout-archetype-schema.md` **Accepted** after 5-finding review pass. Fixes: (P1) `ScoutReport` is event-class-free (emitter in `Scouting.Runtime` selects EventClass so Path B can drop `ScoutReportDisagreement` without breaking the contract); (P1) `Scouting.Runtime` owns ONE post-gate production path, NO dispatcher; Prototype carries pre-verdict selector + deleted post-gate; (P2) `LabelEstimate` split from `GeneCategoryEstimate` — label confidence vs per-category range are distinct measurements aligned with ADR-0006 §Q3 advanced-tooltip contract; (P2) subsection headers use plain naming until ADR-as-whole is Accepted. Both gate paths committed architecturally; five rejected alternatives)
- [x] Content-pack validation contract specified (2026-04-24 — `design/specs/content-pack-validation-contract.md`. 21 Tier-A checks + 11 Tier-D checks with IDs `FW-VAL-<tier>-<NNN>`, owner asmdefs decentralized across 5 validator packages (`Content.Validator` / `Viewer.Cinema.Validator` / `MatchSim.Validator` / `Memory.Validator` / `Scouting.Validator`), red-team fixture per check + anti-red-team negative-control fixture, binding failure-message convention + JSON output shape, CI wiring sketch for Tier-A (every PR) + Tier-D (RC only) + red-team self-check. Phase rollout: 5-8 Tier-A at Phase 3 → full Tier-A + partial Tier-D at Phase 6 → full Tier-D at Phase 8. Phase-6 implementation per ADR-0003. `FW-VAL-D-011` added 2026-04-26 for 3D-asset commercial-rights manifest coverage.)
- [x] Content-pack validator: cross-doc event-class enum exact-match check specified (2026-04-24 — spec-level contract satisfied by `design/specs/content-pack-validation-contract.md` FW-VAL-D-004 + FW-VAL-A-011. `EventClass.SignatureAwakened` / `SignatureExecuted` / `ScoutReportConfirmed` / `ScoutReportDisagreement` names and `CallbackTag.ConsumingReaders` validation (≥1 reader per tag) remain Phase-6 implementation work inside the Tier-A + Tier-D validator subsets.)
- [x] Phase-6 evaluation: Tier-A smoke-seed single-vs-rotation policy specified (2026-04-24 — policy locked in `design/specs/golden-replay-corpus.md`: measure Phase-3 `fw replay` runtime, expand to 3-seed rotation ONLY if per-run budget stays inside Tier-A's 5-minute ceiling. Phase-6 measurement task remains tracked under Phase 6.)
- [x] Artifact retention policy specified (2026-04-24 — `design/specs/artifact-retention-policy.md`. Five retention tiers locked: ephemeral (≤7d uploaded Actions artifacts) / short (14-30d Actions artifacts) / release-tied (GitHub release-asset, permanent) / permanent-in-repo (fixtures, forever) / local-only (never uploaded). ~30 artifact classes catalogued with source + storage + TTL + rationale. Cost-discipline math projected through Phase 6 (Free 500MB cap tight; Pro 1GB comfortable). RC / EA-launch bundle minimum-10-items rule locked. Determinism-replay posture: any historical build reproducible from bundle + tag. Phase-3 `fw artifact-cleanup` CLI commands + Phase-6 `fw workflow-audit` (`FW-WF-A-001` uploaded-artifact retention + `FW-WF-A-002` repo/org Actions retention-setting report) added as SPEC tasks owed; workflow logs are platform-managed and release-tied bundles are verified as release assets, not `retention-days: 0`. Playtest bug-bundle policy: local-only, gitignored, tester-consent-on-share, deletion-on-request honored ≤7d.)
- [x] `design/modding.md` — data architecture constraints every system must respect (2026-04-24 — authored as synthesis of ADRs 0001/0002/0004/0005/0006/0007 + TECH_APPROACH §3-4 + specs into 12 load-bearing mod-loadability constraints: content-pack-qualified stable IDs / schema-versioned forward migration / per-pack Addressables grouping / base-then-mod-pack precedence / registry-backed IDs / Contracts-asmdef split / canonical-JSON artifacts / lint-scanned rendered strings / bake-time-only AI / determinism boundaries / walled-off player internals / validator surface. Editor UX explicitly deferred post-EA. Four open questions flagged for Phase-3/6 resolution. No new architectural commitment — consolidation pass only)
- [x] `design/accessibility.md` — target accessibility features for EA (2026-04-24 — five-item EA surface locked per SPEC Phase-7 canonical list: reduce-motion toggle (synthesis of ADR-0001 `reduce_motion_variant` + ADR-0002 scene-load-time feature-disable) / colorblind palette with default + deuteranopia + protanopia + tritanopia + color-never-sole-carrier discipline / remappable controls via Unity Input System with keyboard+mouse parity + gamepad best-effort / large-text UI at 0.85× / 1.0× / 1.25× scales with Phase-7 reflow pass / subtitles covering crowd audio + match stings + tutorial with post-match text-log accumulator. Default-OFF advanced details already locked via ADR-0006. Three prototype gates at Phase 3/6/7. Missing `reduce_motion_variant` may warn during Phase-3 authoring but blocks by Phase-6 content-pack v1 / EA lock as `FW-VAL-A-021`; three open questions remain)
- [x] `design/content_policy.md` — PEGI 12 boundaries (2026-04-24 — consolidates 2026-04-22 bootstrap rating target + worldbuilding no-real-places + player-generation compiler-only discipline + ui-vocabulary banned-terms + SETUP §7 AI-disclosure + FW-VAL-D-005 into prescriptive content contract. Positive scope-in enumerated (12 PEGI-12-appropriate mature themes: dressing-room tension / ageing-star decline / relegation anxiety / derby hostility / press attacks / contract standoffs / injury-football-standard / career setbacks / manager confrontations / officiating controversy / transfer-market leverage). Scope-out enumerated (13 categories: violence-beyond-football / explicit sexual / substance / gambling / real-world-politics / religion references / hate speech / real-person-likenesses / real-club-league-venue names / real-brand sponsors / graphic crowd violence / self-harm-suicide / child endangerment). Six edge-case rulings on drift-prone subjects. AI-content disclosure wired to Steam 2025 policy + FW-VAL-D-005 + pack-manifest `ai_content_disclosure` block. Mod-pack content-safety review surface closes modding.md OQ#2 — banned-terms lint + Steam Workshop report-flow cover EA; dedicated in-game report flow deferred post-EA. Two prototype gates Phase 6/8. Four open questions)
- [x] `/audit` green on Phase-2 checks (2026-04-25 — Phase-2-scoped checks 1/17/18 green; 2-16 correctly phase-deferred; 19 green after Phase-2 bundle commit `3226c78`. `fw verify` plumbing green. Gate condition satisfied.)

**Gate to Phase 3**: design bible complete; ADRs for every system that locks architecture.

---

### Phase 3 — Unity Bootstrap + MatchSim Prototype 🟡 ACTIVE
**Goal**: MatchSim runs deterministic on 22 players with custom ball physics; Unity + URP + dots-phase viewer prototype with 3 of 7 shot types.

**Promoted 2026-04-25** after Phase 2 ✅ COMPLETE. Week-1 priority order: `MatchSim.csproj` skeleton → `Fixed` Q32.32 → `Tick` deterministic timestep → `Seed` derivation → `SerializationContract.cs` (gates Week-2 golden-corpus fixture authoring per corpus spec). Ball physics + Player state machine in Week 2. Unity project + URP + dots-phase viewer prototype in Weeks 3-4. Month-3 gate observer-pool recruitment starts Week 1 in parallel (per Phase-0 month-3-vertical-slice.md resolution fallback clause).

- [x] Create `MatchSim.csproj` as pure-C# class library (2026-04-26 — `MatchSim/MatchSim.csproj` targets `netstandard2.1` for Unity 6 Mono-runtime compat (currently pinned to tech-stream `6000.4.4f1` per SPEC 2026-04-28 decisions-log entry — `netstandard2.1` is the right floor for either tech-stream or LTS in the Unity 6 line) + modern .NET test-host compat; root namespace `FinalWhistle.MatchSim`; `LangVersion=14.0`; `Nullable=enable` + `TreatWarningsAsErrors=true`; `InternalsVisibleTo=FinalWhistle.MatchSim.Tests`. Skeleton class `MatchSimAssemblyMarker.AssemblyMarkerVersion` proves the assembly is referenceable from MatchSim.Tests. Zero UnityEngine references per TECH_APPROACH §3.1, guarded by a test over referenced assemblies)
- [x] Create `MatchSim.Tests.csproj` with xUnit (2026-04-26 — `MatchSim.Tests/MatchSim.Tests.csproj` targets `net10.0` (matches local + CI SDK 10.0.202 pinned by `global.json` with `latestFeature` roll-forward); `xunit 2.9.2` + `Microsoft.NET.Test.Sdk 17.11.1` + `xunit.runner.visualstudio 2.8.2` + `coverlet.collector 6.0.2`; references `MatchSim.csproj` via netstandard2.1 → net10.0 chain. `dotnet test FinalWhistle.slnx` runs green: 3 tests passed. `fw test` subcommand wired into `fw verify` umbrella; Tier-A CI workflow `fast-pr-ci.yml` adds `actions/setup-dotnet@v4` step. Win/Mac/Linux matrix deferred at skeleton time, then activated 2026-04-27 once the end-to-end `MatchCanonicalState` determinism hash landed)
- [x] Implement `Fixed` struct (Q32.32 canonical format) (2026-04-26 — `MatchSim/Sim/Fixed.cs`. `readonly struct Fixed : IEquatable<Fixed>, IComparable<Fixed>, IComparable, IFormattable` with `long _raw` storage; top 32 bits signed integer / bottom 32 bits fractional. Constants: Zero / One / MinusOne / Half / MaxValue / MinValue / Epsilon. Factories: FromRaw / FromInt / FromLong (range-checked). Operators: + - * / unary-+ comparison full set. Multiplication via 32-bit-split allocation-free unsigned multiply with explicit overflow check + long.MinValue special-case via BigInteger. Division via BigInteger (correctness over speed at Phase-3-prototype scale; profile-and-optimize if hot path). Rounding: Floor / Ceiling / Truncate / Round (banker's). Canonical decimal-string serialization with 10 fractional digits in invariant culture matches FW-VAL-A-018; round-trip stable through `MinValue` / `MaxValue`; rejects culture-specific formatting and scientific notation. Checked overflow throws `OverflowException` everywhere; zero silent wrap. 141 xUnit tests across 5 files (FixedConstantsTests / FixedArithmeticTests / FixedRoundingTests / FixedSerializationTests / FixedDeterminismTests) all passing. `fw verify` Tier-A umbrella green)
- [x] Implement `Tick` deterministic timestep loop (2026-04-26 — `MatchSim/Sim/Tick.cs`. `readonly struct Tick : IEquatable<Tick>, IComparable<Tick>, IComparable` over `long _value`. `TicksPerSecond = 60` locked per TECH_APPROACH §3.2 + 2026-04-22 SPEC entry; `TicksPerMinute = 3600`. Constants: Zero / One. Factories: `FromSeconds(int)` / `FromMinutes(int)` (both `checked` arithmetic). Conversion: `ToSeconds() : Fixed` uses direct raw Q32.32 conversion `(tick << 32) / 60` with range-checking, exact for integer-second multiples within Fixed range, within 1 Q32.32 ULP otherwise (1/60 isn't exactly representable in Q32.32; sim-side code stays in tick units, converts to seconds only at presentation boundaries). Arithmetic: `Tick + long → Tick` / `long + Tick → Tick` / `Tick - long → Tick` / **`Tick - Tick → long` (delta type distinct from absolute tick)** — all checked. Full operator + comparison surface; long-storage chosen over int because multi-season corpus replays exceed `int.MaxValue` ticks. 45 xUnit tests in `MatchSim.Tests/Sim/TickTests.cs` covering constants / factories / ToSeconds-conversion including large second values / additive arithmetic / Tick-minus-Tick-as-delta / equality / total-ordering / determinism / ToString. **189 total tests passing** (144 prior + 45 new). `fw verify` Tier-A umbrella green)
- [x] Implement `Seed` (match + event seed derivation) (2026-04-27 — `MatchSim/Sim/Seed.cs`. `readonly struct Seed : IEquatable<Seed>, IComparable<Seed>, IComparable` over `ulong _value` storage. Constants: `Zero` (default-constructible). Factories: `FromUInt64(ulong)` and **`Derive(ulong matchSeed, Tick tick, ulong eventId) → Seed`** — the canonical-triple per-event derivation per ADR-0001 + ADR-0008 + TECH_APPROACH §3.2. Derivation uses SplitMix64 mixer (Java SplittableRandom-finalizer / xoshiro-author-recommended seed mixer): pure integer math (deterministic across platforms), allocation-free, well-distributed avalanche (~50% bit-flip rate on a 1-bit input change). Composition is non-commutative on (matchSeed, eventId) — swapping them produces a different seed (matches spec intent that they're conceptually distinct identifiers). Canonical string form `"0xdeadbeefdeadbeef"` (lowercase, 16-digit fixed-width, `0x` prefix) matches `golden-replay-corpus.md` smoke-seed format. Parse / TryParse accept lowercase + uppercase hex with or without 0x prefix; reject 17-digit-or-longer + non-hex garbage. 45 xUnit tests in `MatchSim.Tests/Sim/SeedTests.cs` covering constants / construction / determinism (same-triple → same seed; literal pinned value `0xddac907bb053edbb` plus in-test SplitMix64 oracle so any future mixer change trips the test) / sensitivity (different matchSeed / different tick / different eventId → different seed; order-matters) / avalanche (≥16 of 64 bits flip on any 1-bit input change for matchSeed / tick / eventId) / 1000-eventId distinctness via HashSet / equality / total-ordering / ToString canonical lowercase + always-16-hex / Parse 8 accepted forms + 6 garbage rejection / TryParse no-throw + null + empty / round-trip stability across spread of values. **234 total tests passing** (189 prior + 45 new). `fw verify` Tier-A umbrella green)
- [x] Author `MatchSim.Tests/SerializationContract.cs` — stable order for entities / events / Q32.32 fields. **Gates Phase-3 Week-2 golden-replay-corpus fixture authoring** per `design/specs/golden-replay-corpus.md` open questions. Cross-platform hash parity depends on this (2026-04-27 — split into production `MatchSim/Sim/CanonicalEncoder.cs` + executable spec `MatchSim.Tests/Sim/SerializationContract.cs` per existing Fixed/Tick/Seed test-layout convention. **Encoding rules locked at v1:** little-endian for all multi-byte values via `BinaryPrimitives.Write*LittleEndian` (platform-independent; `BitConverter` forbidden); primitives `Fixed` / `Tick` / `Seed` write 8 bytes LE over their underlying 64-bit value (no type tag — schema is self-describing via caller-driven ordering); strings write 4-byte LE byte-count prefix + strict UTF-8 bytes (malformed UTF-16 surrogate sequences throw instead of replacement-encoding); counts write 4-byte LE non-negative `int`; booleans write 1 byte (0x00 / 0x01). Hash is SHA256, canonical form `"sha256:<lowercase-hex>"` matching `golden-replay-corpus.md`. **Caller responsibility:** ordering rules per ADR-0008 (collections must be sorted by `StringComparer.Ordinal` on a stable key before encoding; encoder preserves write order, never sorts). 43 xUnit tests in `MatchSim.Tests/Sim/SerializationContract.cs`: 17 primitive-byte-encoding rules with literal expected `byte[]` (Fixed.Zero/One/Max/Min, Tick.Zero/FromSeconds(1), Seed.Zero + corpus-smoke 0xDEADBEEFDEADBEEF, Int32 1/-1, Bool false+true, Byte, String empty/ASCII/non-ASCII like `"café"`, Count); 5 argument-validation tests (null string + malformed surrogate + negative count + negative capacity throw; zero capacity OK); 12 SHA256 reference-hash tests with literal pinned hashes independently computed via openssl 2026-04-27 (empty buffer = `e3b0c4...`; 8 zero bytes = `af5570...`; Fixed.One = `01acec...`; Fixed.Max = `6a69a6...`; Tick(1s) = `3fe8ad...`; corpus smoke seed = `743c76...`; canonical-triple zero = `9d908e...`; "café" = `1e5349...`; empty string = `df3f61...`); 7 lifecycle/composition tests (`WrittenCount` growth, `Reset` semantics, idempotent hash, order-matters, count-and-elements concatenation); 2 determinism tests (100 same-input → 1 hash, 1000 distinct seeds → 1000 hashes). **277 total tests passing.** `fw verify` Tier-A umbrella green. Phase-3 Week-2 golden-replay-corpus fixture authoring is now unblocked)
- [x] Author `scripts/fw shader-audit` — greps viewer adapter shaders for banned `_Time` references per ADR-0008/ADR-0009 determinism discipline inherited from superseded ADR-0002. Wires into `fw verify` umbrella once authored so Tier-A CI catches regressions immediately. **Gates adapter validation criterion "no `_Time` references in viewer shaders"** (2026-04-28 — `scripts/fw shader-audit` subcommand authored + scope-narrowed per Codex round-3 follow-up review. **Scope: viewer-adapter shaders only.** Scans `unity-project/Assets/Viewer/` (the canonical asmdef-rooted folder for `Viewer.Contracts` / `Viewer.Core` / `Viewer.Adapters.Dots` per ADR-0008/0009 asmdef plan) for `*.shader` / `*.hlsl` / `*.cginc` / `*.shadergraph` / `*.subshader` files. **UI / debug / loading-screen shaders elsewhere in `Assets/` are intentionally not audited** because ADR-0008 explicitly exempts visual-only effects from the determinism hash — those paths legitimately use `_Time` (breathing-glow on selected items, blinking cursors, animated loading indicators). Banned-pattern regex catches HLSL globals (`_Time` / `_SinTime` / `_CosTime` / `_DeltaTime` / `_TimeParameters`) + ShaderGraph `TimeNode` references. Trailing-boundary anchor uses POSIX-portable `[^A-Za-z0-9_]` instead of `\b` (which BSD `grep -E` on macOS doesn't support); excludes false-positives like `_TimeOfDay` while still catching `_Time.y` / `_Time;` / `_Time)`. **Phase-3 stub-active mode:** when `Assets/Viewer/` doesn't exist yet (current state — asmdef skeleton task hasn't landed), exits 0 with a friendly "no viewer-adapter shaders to audit yet" message. Real catch begins automatically the moment the dots adapter authors its first shader under the viewer tree. Wired into `fw verify` umbrella between `banned-terms` and `test`. Smoke-tested all four paths: (1) no-`Assets/Viewer/` path (current state, clean exit 0); (2) positive-test with planted `Viewer/_AuditSelfTest/banned-viewer.shader` containing `_Time.y` + `_SinTime.x` on the same line (FAIL with file:line + non-zero exit); (3) false-positive check with `_TimeOfDay` + `_MyTimer` properties (correctly silent); (4) **scope-narrowing check** — UI shader at `Assets/UI/_AuditSelfTest/ui-with-time.shader` containing `sin(_Time.y)` correctly silent because outside `Assets/Viewer/`. All 473 tests still pass. Remediation hint on FAIL lists three options: remove the `_Time`-driven visual / move the shader out of `Assets/Viewer/` if it's actually a UI/debug surface / raise an ADR revisiting the contract.)
- [x] Implement `Ball` custom deterministic physics (ground roll, air kick, bounce, friction; spin/Magnus stub acceptable for Month-3) (2026-04-27 — `MatchSim/Sim/Vector3Fixed.cs` + `MatchSim/Sim/BallState.cs` + `MatchSim/Sim/BallPhysics.cs` (with `BallPhysicsCoefficients` co-located). Q32.32 fixed-point per match-engine.md §Q2 structure-locked spec. **Vector3Fixed:** readonly struct over (X,Y,Z) Fixed components; coordinate convention X+Z = pitch plane, Y = altitude; full operator surface (+ - unary- scalar*); algebra (Dot / Cross / LengthSquared); zero/UnitX/UnitY/UnitZ constants; equality + GetHashCode + InvariantCulture ToString. **BallState:** readonly struct of (Position, Velocity, Spin) all Vector3Fixed; immutable; `WriteCanonical(CanonicalEncoder)` writes 72 bytes in locked order. **BallPhysics.Step:** pure-static deterministic function; semi-implicit Euler at fixed 60Hz step; gravity (continuous SI g·dt) + linear drag (per-step `v *= (1-C_d)`) + Magnus (skip if zero spin; otherwise `v += C_m·(spin × v)`); ground collision = clamp Y to 0 + bounce only when the ball crossed into ground from above (vertical-velocity flip × retention) + grounded clamp for gravity-induced negative Y velocity + rolling friction (only when ball settled, NOT bouncing upward). **BallPhysicsCoefficients.Phase3Seeds:** g=9.81, C_d=0.02, C_m=0.0004, e=0.55, μ=0.25 — pinned to match-engine.md §Q2 and cached behind the property to avoid repeated fixed-ratio divisions. **Magnus stub policy honored:** structure stays even when `MagnusCoupling = Fixed.Zero`. **CanonicalEncoder gained `WriteVector3Fixed`** (24 bytes; X/Y/Z order locked). 56 new xUnit tests across 3 files after Codex hardening: 31 Vector3FixedTests (construction / operators / dot / cross / length / equality / hashing / ToString); 23 BallPhysicsTests (determinism / gravity-only / drag-only / Magnus stub on/off / bounce edge cases including grounded-at-rest and grounded-rolling under gravity / rolling friction edge cases / kicked-ball + dropped-ball combined-trajectory sanity gates); 2 SerializationContract additions for the new encoder helper. **All-forces-off coefficient set + production-equivalent expected expressions** used to isolate one force per test (Fixed-point arithmetic is operation-order sensitive; tests use same expressions as production for exact-byte determinism while still verifying formula correctness; combined-trajectory tests serve as independent oracles). **333 total tests passing** (277 prior + 56 Ball-task tests). `fw verify` Tier-A umbrella green. **NOT in scope:** Ball-Player kick API, goal-line detection, touchline transitions — those wait for Player + Pitch entities)
- [x] Implement `Player` state machine (22 agents, basic movement + kick-ball) (2026-04-27 — `MatchSim/Sim/TeamSide.cs` + `MatchSim/Sim/PlayerState.cs` + `MatchSim/Sim/PlayerActuator.cs` (with `PlayerKinematics` co-located). Q32.32 fixed-point per match-engine.md §Q3 steering-target-actuator spec. **Pre-requisite added: `Fixed.Sqrt`** via Newton's method on BigInteger (netstandard2.1-compat: `BigInteger.GetByteCount() * 8` bit-length over-estimate as initial guess; iterates until `(x + n/x)/2 >= x`); throws on negative input. **Vector3Fixed gained:** `Length()` / `Distance(a,b)` / `DistanceSquared(a,b)` (sqrt-free; preferred for radius checks) / `Normalize()` (throws on zero vector). **TeamSide:** byte enum (Home=1, Away=2; default 0 reserved as "uninitialized" sentinel). **PlayerState:** readonly struct of (Position, Velocity, JerseyNumber, Side); Y=0 invariant for ground players in Month-3 slice (structure forward-compatible with future jumping headers); constructor rejects jersey numbers outside 1-99 and invalid TeamSide values; `WriteCanonical` writes 50 bytes in locked order P/V/JerseyNumber/Side and rejects default/uninitialized state. **PlayerKinematics.Phase3Defaults:** MaxSpeed=7 m/s, MaxAcceleration=6 m/s², Radius=0.5 m (homogeneous across all 22 players in Month-3; per-player gene-driven tuning is Phase 4+); constructor rejects negative values so bad tuning cannot invert movement/possession semantics. **PlayerActuator.Step:** pure-static deterministic function per design literal API (`desired_position` + `desired_speed`); projects current position/current velocity/desired position onto the pitch plane before steering so BTs targeting airborne balls cannot lift ground players; semi-implicit Euler matching BallPhysics; clamps velocity-delta magnitude to `MaxAcceleration · dt` (caps turn rate implicitly — at high speed, direction changes need many ticks; no separate angular cap needed); defensive post-step MaxSpeed clamp; sqrt-free at-target shortcut via `LengthSquared==0` check. **PlayerActuator.HasPossession:** sqrt-free `DistanceSquared` comparison against `Radius²`; boundary inclusive (distance == radius counts); both team-mates and opponents independently report possession of the same ball, match loop resolves contests. **No separate Kick API:** kick = `new BallState(ball.Position, kickVelocity, spin)` is trivial composition; integration test verifies player-runs-to-ball-then-kicks loop end-to-end. 64 actual Player-task xUnit cases across 3 test files after Codex hardening: 26 FixedSqrtTests (zero/one/negative + perfect-square Theory + quarter-half + non-perfect-square Theory using closeness-bound invariant + determinism + 100-distinct-inputs + MaxValue range + literal pinned values for Sqrt(2)/Sqrt(3)/Sqrt(5) computed via Python isqrt oracle); 11 Vector3Fixed Length/Distance/Normalize extension additions (3-4-5 + 3-4-12-13 Pythagorean triples; sqrt-free DistanceSquared invariant; Normalize-of-zero throws; Normalize preserves direction via cross-product=zero parallelism check; normalized length²-close-to-1 within 1e-6 tolerance); 27 PlayerActuatorTests (PlayerState construction / invalid identity rejection / equality / canonical-50-byte-order / default-state serialization rejection / TeamSide byte-pinning / PlayerKinematics.Phase3Defaults + negative-value rejection / Step determinism 100x / at-target stops / desired_speed=0 stops / airborne-target pitch-plane projection / vertical-drift cleanup / from-rest acceleration with structural-property bounds / per-step velocity-delta-magnitude bounded by `MaxAcceleration·dt` / never-exceeds-MaxSpeed over 600-tick run / diagonal target heading-ratio matches direction / HasPossession boundary cases + multi-player / integration test player-runs-to-ball-gains-possession-kicks-ball-away end-to-end). **Test-strategy lessons reapplied:** ClampMagnitude path goes through Sqrt + division; production-equivalent expected expressions don't compose cleanly across that path, so Step tests use bounded-property + close-to-target invariants. Floor-of-sqrt invariant is `root² ≤ x` + `(x - root²) ≤ 2·(root+1)·ε` (Q32.32 truncation can collapse adjacent ULPs to identical Fixed-multiply outputs, so strict integer-floor `(root+1)² > x` is NOT a clean Fixed inequality). **397 total tests passing** (333 prior + 64 actual Player-task cases). `fw verify` Tier-A umbrella green. **NOT in scope:** behavior-tree archetypes (separate SPEC task next); per-player gene-driven kinematics (Phase 4); roster / 22-player composition logic (downstream Match struct))
- [x] Author 2 behavior-tree manager archetypes in YAML (e.g., "Direct Pressing" + "Low-Block Counter") (2026-04-27 — `MatchSim/Content/archetypes/direct-pressing.yaml` + `low-block-counter.yaml` (embedded resources) + `MatchSim/Sim/PlayerCommand.cs` + `MatchSim/Sim/BehaviorTreeArchetype.cs` + `MatchSim/Sim/BehaviorTreeArchetypes.cs` (Parse/Load) + `MatchSim/Sim/BehaviorTreeRunner.cs` (Tick). YamlDotNet 17.0.1 added to MatchSim.csproj per CLAUDE.md tech-stack lock. **Scope decision** logged in commit: Month-3 archetype is a tactical heuristic with formation + PressRadius + BuildupSpeedFactor knobs — NOT a full BT engine with sequence/selector nodes (those land in Phase 4 if observers find Month-3 tactics too thin). **Coordinate convention:** YAML positions authored in HOME orientation (defending −X goal); runner mirrors X for Away. Pitch dimensions 105×68m; goal lines at X = ±52.5. **direct-pressing:** 4-4-2; PressRadius=25m; BuildupSpeedFactor=0.95. **low-block-counter:** 4-5-1 deeper; PressRadius=12m; BuildupSpeedFactor=1.0. **Three-mode runner heuristic:** (1) Press — opponents have possession AND own player within PressRadius of the ball's pitch projection → head to pitch-projected ball at MaxSpeed; (2) Build-up — own team has possession → ball-carrier heads to opponent goal at `MaxSpeed × BuildupSpeedFactor`, others hold formation; (3) Hold shape — otherwise, head to formation base position at `MaxSpeed × 0.5` jog. Possession resolution: nearest-player-to-ball-pitch-projection wins; ties resolve to opponent (defensive default). **Allocation-free hot path:** Tick takes `Span<PlayerCommand> commandsOut` buffer + writes in-order; no per-tick allocations. **Validation hardening:** archetype constructor rejects null/blank name, non-11-element formations, duplicate roster slots, missing roster slots 1-11, non-positive PressRadius/BuildupSpeedFactor; FormationSlot rejects out-of-range RosterSlot (must be 1-11) and nonzero-Y base positions; Parse rejects null/empty YAML, unknown fields, missing formation block, missing formation x/z coordinates, and malformed/non-positive numeric fields. 47 current xUnit cases across 2 test files after Codex hardening: 28 BehaviorTreeArchetypeTests (construction validation; FormationSlot validation including pitch-plane base positions; YAML parser strictness for unknown fields and missing coordinates; built-in loaders + cache); 19 BehaviorTreeRunnerTests (determinism; argument validation; press/build-up/hold-shape logic; airborne-ball pitch-projection; side mirroring; BT + PlayerActuator integration). **444 total tests passing** (397 prior + 47 current BT cases). `fw verify` Tier-A umbrella green. **NOT in scope:** sequence/selector BT nodes (Phase 4); per-player gene-driven kinematics (Phase 4); MatchState struct (emerges when match-loop layer needs it); roster / 22-player composition logic (downstream))
- [x] xUnit tests for determinism (hash canonical state after N ticks; compare Win/Mac/Linux via CI matrix) (2026-04-27 — `MatchSim/Sim/MatchCanonicalState.cs` + `MatchSim/Sim/MatchSimulationState.cs` + `MatchSim/Sim/MatchSimulationRunner.cs` + `MatchSim.Tests/Sim/MatchDeterminismTests.cs`. The cross-platform-determinism gate per match-engine.md §Prototype gate. **`MatchCanonicalState.Write`** writes (Tick + Ball + 22 PlayerStates) in locked v1 order: Tick (8B) + Ball (72B) + Home count (4B) + Home 11 PlayerStates (550B) + Away count (4B) + Away 11 PlayerStates (550B) = **1188 bytes per snapshot**. `PlayersPerTeam = 11` and `EncodedByteCount = 1188` are public constants so fixtures cannot drift behind magic numbers. **Production `MatchSimulationRunner` owns the canonical Month-3 loop order:** BT.Tick × 2 sides → PlayerActuator.Step × 22 → BallPhysics.Step → Tick+1. The tests exercise that production loop rather than a private test-only copy. `MatchSimulationState.FromArchetypeFormations` fills by `FormationSlot.RosterSlot`, not YAML/list order, preserving roster-order canonical arrays for valid shuffled authored data. **Pinned smoke-fixture hash** `sha256:299cdb0cbbc9606e141db278a14585780d0e3b5dbfb8815f634af89be7f6118a` (computed on macOS 2026-04-27): direct-pressing (Home) vs low-block-counter (Away) at their formation positions; ball at centre; 60 ticks (1 game-second). `.github/workflows/fast-pr-ci.yml` now runs `fw verify` on `ubuntu-latest`, `windows-latest`, and `macos-latest` with `shell: bash`; all platforms must produce the identical hash. Disagreement = a real determinism leak. **Seed not included in canonical-state hash** — current sim has no per-event randomness; when stochastic events land Phase-4+, they'll enter state through emitted events. 13 current xUnit tests in `MatchSim.Tests/Sim/MatchDeterminismTests.cs`: pinned-hash bedrock; determinism; sensitivity; order-stability; API surface including 1188-byte constant, null/wrong-team guards, roster-slot ordering, and negative tick validation. **457 total tests passing** (444 prior + 13 determinism tests). `fw verify` Tier-A umbrella green. **NOT in scope:** golden-replay-corpus fixture authoring (Week-3 task per corpus spec; depends on this test layer + serialization contract that just landed))
- [x] Create `unity-project/` via Unity Hub URP template (2026-04-28 — Unity 6 tech-stream version `6000.4.4f1` pinned per `unity-project/ProjectSettings/ProjectVersion.txt`. **Version-track decision** logged in commit message: tech-stream 6000.4 selected over strict LTS 6000.0 trading shorter Unity-side patch-support window for latest editor UX + faster URP cadence; defensible because our renderer-agnostic architecture (ADR-0008) decouples engine version from sim correctness — when Unity's next LTS lands (~Q4 2026 / early 2027 coincident with Phase 7 polish), we re-evaluate. **Project created** via headless Editor CLI: `Unity -createProject unity-project/ -batchmode -quit -nographics` produced default 3D project at `/Users/vibelogic/dev/football/unity-project/` with Assets/ + Library/ + Packages/ + ProjectSettings/ + UserSettings/ + Logs/. `.gitignore` already aligned from bootstrap (Library/ Temp/ Obj/ Build/ Logs/ UserSettings/ + auto-generated csproj/sln + crash reports). **Default 3D template chosen, NOT the URP template** — URP isn't a CLI-creatable template; instead `Packages/manifest.json` was rewritten to pull `com.unity.render-pipelines.universal 17.0.4` + framework packages (UniTask 2.5.10 via OpenUPM scoped registry; Addressables 2.3.16; Recorder 5.1.2; Input System 1.13.1; Localization 1.5.5; Timeline 1.8.7; UGUI 2.0.0). Removed unneeded modules (vr / xr / wind / vehicles / terrain / terrainphysics / cloth / androidjni / unityanalytics / 4 unitywebrequest sub-modules) from the default 3D module list — kept the relevant subset. **First-open ritual required** (user-action; documented in STATUS.md): open Editor once to trigger package resolution + URP wizard (Edit → Render Pipeline → Universal → Convert built-in to URP). After that, the project is healthy + the "Phase-3 Install Unity packages" SPEC task auto-completes. **Open architectural questions deferred to next /next**: (1) MatchSim consumption strategy (DLL drop in `Assets/Plugins/` vs source-reference via UPM local package) — affects YamlDotNet inclusion; (2) Assembly Definitions skeleton authoring (Viewer.Contracts ↔ Viewer.Core ↔ Viewer.Adapters.Dots split per ADR-0008/0009) is its own SPEC task — not pulled forward. **457 total tests still passing** (no MatchSim core changes); `fw verify` Tier-A umbrella green)
- [x] Install Unity packages: UniTask, Addressables, Recorder, Input System, Localization, UI Toolkit (built-in) (2026-04-28 — `unity-project/Packages/manifest.json` declares all + UnityMCP + Cinemachine + URP `17.4.0` + UGUI; `packages-lock.json` resolves them via OpenUPM scoped-registry (UniTask) + Unity registry (everything else). Manifest cleanup landed in commit `5f0bf06` per Codex round-1 audit: ProBuilder + VFX Graph removed (Phase-3 dots viewer doesn't need either); URP version aligned `17.0.4` → `17.4.0`; UnityMCP pinned to commit SHA `b92c05a25820cfc9f59ce4094eb46aaec8632ea2`. UI Toolkit (`com.unity.modules.uielements`) is a built-in module; auto-included)
- [x] Install CoplayDev unity-mcp via Packages/manifest.json (2026-04-28 — `com.coplaydev.unity-mcp` SHA-pinned in manifest; project-scoped MCP server registered as `UnityMCP` (Pascal) in `.mcp.json` on `http://localhost:8080/mcp`; `claude mcp list` confirms ✓ Connected. PostToolUse refresh hook removed in commit `47997fc` per Codex P2-04: redundant since `manage_script` already calls `AssetDatabase.ImportAsset` + `RequestScriptCompilation` internally)
- [x] **Repair URP `UniversalRenderer.asset` + commit `packages-lock.json` + Unity `.meta` files** (closes Codex audit P1-02 + P1-03; user-action when Editor is open) (2026-04-28 — closed end-to-end in commit `de331f2`. Editor user-action loop: user opened Unity → ran `Final Whistle → Setup → Repair URP Renderer` menu → `Window → Package Manager → Refresh`. Result: `UniversalRenderer.asset.postProcessData` now `{fileID: 11400000, guid: 41439944d30ece34e96484bdb6645b55, type: 2}` (was `{fileID: 0}`); `packages-lock.json` unity-mcp pinned to commit SHA `b92c05a25820cfc9f59ce4094eb46aaec8632ea2` (was `#main`); ProBuilder + VFX Graph entries removed from lock (matches manifest); 6 Unity-generated `.meta` files committed for Editor / Roslyn / Scripts / Editor / Setup / `RepairUniversalRenderer.cs`. Reproducibility floor for the Unity bootstrap is now stable. Codex round-3 verification clean sign-off in the same response.)
- [x] **MatchSim consumption strategy decision + implementation** — DLL drop in `unity-project/Assets/Plugins/MatchSim/` vs UPM local package. Recommendation: DLL drop for Phase 3 simplicity (`scripts/fw build-unity-plugins` copies `MatchSim.dll` + `YamlDotNet.dll` + .meta files into the Plugins dir; revisit at Phase 4 if dev-loop friction emerges). Strategy choice gates Viewer.Core asmdef references — author this before / alongside the asmdef skeleton task below (2026-04-29 — DLL drop chosen + implemented per SPEC 2026-04-29 decisions-log entry. `scripts/fw build-unity-plugins` authored: runs `dotnet publish MatchSim/MatchSim.csproj -c Release` to a temp staging dir (`mktemp -d` + bash `trap` cleanup), then copies `FinalWhistle.MatchSim.dll` + `FinalWhistle.MatchSim.pdb` + `YamlDotNet.dll` into `unity-project/Assets/Plugins/MatchSim/`. Skipped from copy: `.deps.json` (runtime-host manifest; Unity ignores) + `.xml` doc-comments (~87KB, no Editor benefit yet). Existing `.meta` files NEVER overwritten — Unity owns GUID stability. Idempotent: re-runs are no-ops at the dotnet-publish layer (incremental build) + cp overwrites bytes only on diff. First-run user-action: open Unity once → Editor generates `.meta` files for the new DLLs → commit them alongside. After that, the script's "First-run note" stops appearing. Plugin DLLs check into git (per the same reproducibility floor that closed P1-03 user-action; `git check-ignore` confirms none of the three files are gitignored). Wired into `scripts/fw help` "implemented" command list. **2026-04-30 hardening:** `fw verify` now runs `verify-unity-plugins`, which publishes MatchSim with the same stable relative project/PDB path as `build-unity-plugins` and byte-compares the three committed plugin files. Smoke-tested: plugin drop matches fresh publish; fresh clean-copy `fw verify` passes 501/501.)
- [x] **Assembly Definitions skeleton** — author per ADR-0008 / ADR-0009: `Viewer.Contracts` (pure-C# DTOs; UnityEngine-free; references MatchSim event types via the consumption-strategy choice above) + `Viewer.Core` (Unity-side adapter registry, `Viewer.EventBridge`, ShotTypeSO projection) + `Viewer.Adapters.Dots` (dots-prototype implementation; consumes Viewer.Core + URP). Asmdef references: Dots → Core → Contracts; cross-asmdef visibility honors the contract layer's UnityEngine-free constraint. Stub minimum types (1 file per asmdef) so the skeleton compiles; real implementations land in subsequent tasks. **Delegate to `unity-specialist` subagent per CLAUDE.md §6.3** (load-bearing architecture; Unity-side asmdef rules) (2026-04-29 — three asmdefs created at `unity-project/Assets/Viewer/{Contracts,Core,Adapters/Dots}/`. **Viewer.Contracts.asmdef**: `noEngineReferences: true` (asmdef-level enforcement of the UnityEngine-free rule, same architectural posture MatchSim uses); `overrideReferences: true` + `precompiledReferences: ["FinalWhistle.MatchSim.dll"]` for direct DLL consumption from `Assets/Plugins/MatchSim/`. **Viewer.Core.asmdef**: references Contracts + MatchSim DLL; UnityEngine-bearing. **Viewer.Adapters.Dots.asmdef**: references Core + Contracts + `Unity.RenderPipelines.Universal.Runtime` (URP runtime asmdef name verified against package cache at `Library/PackageCache/com.unity.render-pipelines.universal@18d0e59f18f1/Runtime/`). Each asmdef ships a single `*AssemblyMarker.cs` stub class proving (a) the asmdef compiles + (b) downstream asmdefs can resolve types across the boundary via fully-qualified `FinalWhistle.Viewer.<Layer>.<Type>` reads. **Cross-asmdef visibility decision**: marker classes are `public` (the `internal` first attempt failed compile with `CS0122 inaccessible due to its protection level` even with `public const` member — internal class hides public consts from other assemblies). Compile verification via UnityMCP `read_console`: zero errors, zero warnings after `refresh_unity force scripts compile=request`. Unity auto-generated 17 `.meta` files (5 folder metas + 3 asmdef metas + 3 cs metas + 3 stub-cs files + 3 asmdefs). 473/473 tests still pass; `fw verify` Tier-A umbrella green. Foundation #2 of the Phase-3 next-up ladder closed; foundation #3 (PitchRules / MatchRules layer) becomes the next /next.)
- [x] Addressables groups initialized (2026-04-30 — `Assets/AddressableAssetsData/` provisioned via `AddressableAssetSettingsDefaultObject.GetSettings(true)` driven through UnityMCP `execute_code`. Default state: 1 group (`Default Local Group`) + 2 schemas (`BundledAssetGroupSchema`, `ContentUpdateGroupSchema`) + 3 data builders (FastMode, PackedMode, PackedPlayMode) + 1 group template (`Packed Assets`) + `DefaultObject.asset` reference. Phase-3 minimum scope only — no custom group naming, no schema authoring, no preview cache config. Phase-4+ trigger when content packs land. UnityMCP `read_console` clean: zero code errors, zero code warnings (transport-level WebSocket blip is the pre-existing MCP-side noise, not a code regression). 23 net-new files (9 .asset + 14 .meta — count corrected per Codex round-5 review; the .meta count includes 5 directory metas Unity auto-emits) committed; `fw verify` 503/503 green; pinned 60-tick determinism hash unchanged.)
- [x] First scenes: `Boot.unity` + `MatchViewer.unity` (2026-04-30 — both scenes created via UnityMCP `manage_scene action=create template=3d_basic`. Three root objects per scene: `Main Camera` (tagged MainCamera) + `Directional Light` + `Ground` (template default plane; Phase-3 dots adapter will replace with deterministic-pitch geometry per ADR-0009). Registered in `EditorBuildSettings.m_Scenes` with Boot at index 0 (start scene) + MatchViewer at index 1 via `manage_build action=scenes`. `AssetDatabase.SaveAssets()` triggered explicitly so the build-settings change persists to disk. Phase-3 minimum scope only — no transition logic, no AppState singleton, no scene-load orchestration. Phase-4+ trigger when `Viewer.EventBridge` + dots adapter need a real entry-point flow. UnityMCP `read_console` clean: zero code errors / zero code warnings (only the pre-existing transport-level WebSocket blip). 6 net-new files (`Scenes.meta` + 2 .unity + 2 .unity.meta + `SceneTemplateSettings.json`) + 3 modified (CHANGELOG / SPEC / `EditorBuildSettings.asset`) per Codex round-6 review of `e93f138`. `fw verify` 503/503 green; `git diff --check` clean; pinned 60-tick determinism hash unchanged.)
- [x] **Phase-3 minimal `PitchRules` / `MatchRules` layer** (closes Codex P1-05 + folds in P2-03 seed-input refactor; see 2026-04-28 decisions-log entry). Field bounds (105×68m, goals X=±52.5, touchlines Z=±34); deterministic goal-plane detection (Y<2.44, |Z|<3.66); `OutOfPlay` enum {InPlay, GoalKick, ThrowIn, CornerKick} in `MatchSimulationState`; `(byte HomeScore, byte AwayScore)` score state; append-only `KeyEvent` record stream `{Tick, EventKind, Side, PlayerId, Position}` for goals + restarts; `MatchRules.Step` orchestrator runs after BallPhysics each tick; canonical-state encoding extends `MatchCanonicalState.Write` with KeyEvent-count + entries; pinned 60-tick smoke hash re-baselined intentionally. **Goal restarts are immediate respawn at center (no `KickOff` / `CenterRestart` state in the OutOfPlay enum)** — the 1-tick transition between goal-detected and ball-back-at-center with possession-flipped is implicit; if observers report the absence of a celebration tick range feels gamey, add a `KickOff` enum value via a new SPEC decision in Phase 3 Week 4 or Phase 4. **Closes Codex P2-03 (seed-input refactor):** introduce `MatchSimulationConfig` (or equivalent fixture-input record) that carries `Seed MatchSeed` into `MatchSimulationRunner`, even though Phase-3 code does not consume it yet — corpus fixtures record the seed; runner accepts it as input; canonical state hash unchanged (seed is not part of canonical state — it enters state through emitted events when stochastic events land Phase 4+). Out of scope for Phase 3: offside, set-piece taker behavior, fouls, cards, substitutions, stoppage (per match-engine resolution + 2026-04-28 PitchRules decisions entry) (2026-04-30 — full layer shipped. **5 new files**: `OutOfPlay.cs` (byte enum InPlay=0/GoalKick=1/ThrowIn=2/CornerKick=3), `KeyEventKind.cs` (byte enum None=0 sentinel/Goal=1/GoalKickRestart=2/ThrowInRestart=3/CornerKickRestart=4), `KeyEvent.cs` (35-byte locked v1 layout: Tick 8 + Kind 1 + Side 1 + JerseyNumber 1 + Position 24), `MatchSimulationConfig.cs` (Seed-input record per P2-03), `MatchRules.cs` (orchestrator with PitchBounds constants + linear-interp Q32.32 crossing detection + per-side goal/restart classification + transient OutOfPlay reset). **3 modified**: `MatchSimulationState.cs` (added HomeScore/AwayScore byte + OutOfPlay flag + KeyEvents append-only list), `MatchSimulationRunner.cs` (accepts MatchSimulationConfig + caches preStepBall + runs MatchRules.Step after BallPhysics.Step), `MatchCanonicalState.cs` (encoding extends with score 2 + OutOfPlay 1 + KeyEvent count 4 + variable KeyEvents body; renamed `EncodedByteCount` → `EncodedBaseByteCount = 1195`; added `EncodedByteCountFor(state)` for variable total; added convenience overloads defaulting score/OutOfPlay/KeyEvents). **Pinned smoke-fixture hash re-baselined** (intentional schema bump v0 → v1): `sha256:7e851976f6a5eea467797e90400ca030c6ab955e21c2f92466cffa00c880f50e` (was `sha256:299cdb0c...`); v0 hash preserved in test-doc comment for traceability. **22 new tests** in `MatchRulesTests.cs` (goal-mouth detection per side; goal-line crossings outside mouth → GoalKick; touchline crossings → ThrowIn; non-events; pre-out / post-out short-circuit; OutOfPlay per-tick reset; KeyEvent ordering across multi-tick scenarios; canonical-encoding includes KeyEvents; EncodedByteCountFor accounts for variable body; HomeScore overflow throws; MatchSimulationConfig round-trips Seed; runner accepts config + canonical hash unchanged when only seed differs (locks "seed is fixture input not canonical state" invariant); KeyEvent canonical encoding 35-byte width; KeyEvent rejects None/invalid-side/jersey>99; KeyEvent allows jersey=0 for unspecified). **Total tests: 495 passing** (was 473; +22 new). Phase-3 simplifications captured in code comments: GoalKick vs CornerKick disambiguation deferred to Phase 4 (no last-touched tracking yet); ThrowIn side defaults to Home (same reason); KeyEvent.JerseyNumber=0 for all Phase-3 emissions (no scorer attribution). MatchSim DLL republished via `scripts/fw build-unity-plugins`: new SHA `005d6b5c...0012be7a1`. Unity-side asmdef skeleton (foundation #2) recompiled cleanly against the new DLL — UnityMCP `read_console` reports zero errors / zero warnings. `fw verify` Tier-A umbrella green.)
- [x] **Football rules matrix authored** (Phase-3 guardrail spec — pre-empts ad-hoc football-law simplifications in code comments) (2026-04-30 — `design/specs/football-rules-matrix.md` shipped per Codex round-4 follow-up plan. 16 rule-surface rows covering goals / touchlines / goal kicks / corners / throw-ins / restart authority / kickoffs / offside / fouls / cards / substitutions / injuries / stoppage / advantage / penalties / GK handling. Each row pins real-football intent vs current Phase-3 behavior + simplification + player-visible risk + canonical impact + tests owed + promotion trigger. Cross-linked from `design/README.md` index. Acts as the umbrella contract; `MatchSim/Sim/MatchRules.cs` doc-comments stay as sub-contracts. Locked decision: any new MatchRules / PitchRules rule surface MUST add or update a matrix row before implementation can be marked done.)
- [x] **22 hand-authored `IdentityPacket` JSON fixtures** (Phase-3 semantic slice per 2026-04-28 decisions entry; foundation #4 first task) (2026-04-30 — Phase-3 minimum subset of the ADR-0006 IdentityPacket schema landed end-to-end. Embedded-resource location: `MatchSim/Content/identity-packets/<archetype>/<jersey>.json` (NOT testing-only) so the loader runs in both production + test contexts and the future Unity Addressables import can wrap the same JSON without a format migration. Same pattern as the existing BehaviorTree archetype YAMLs. **C# surface**: `MatchSim/Content/{IdentityPacket,IdentityPacketValidator,IdentityPackets,RoleFamily,IsExternalInitPolyfill}.cs` (~520 LoC schema/validator/loader) + `MatchSim/Content/Json/{JsonReader,IdentityPacketParser}.cs` (~470 LoC strict hand-rolled parser added in Codex round-7 refactor) collapsed into the `FinalWhistle.MatchSim.Content` + `FinalWhistle.MatchSim.Content.Json` namespaces; ADR-0006's 4-project split (Contracts/Compiler/Validator/UnityImport) is Phase-4+ refactor. **Phase-3 minimum subset** — IdentityPacket: PlayerId / DisplayNameFull / DisplayNameShort / RoleFamily / SignatureCandidates / Genes (6-field subset of the full 22) / SchemaVersion=1 / SourcePackVersion. Phase-4 schema-v2 bump adds the remaining 16 gene fields + phenotype labels + rivalry/lineage metadata via save-migration-fixtures.md 4-test discipline. **Validator**: ID-format regex (`^fwh\.core(?:\.v[0-9]+)?:player_[0-9]{5}$`); affinity weight in `[0, Fixed.One.RawValue]`; signature-candidate count ≤3; signature-ID format regex per ADR-0005; schema-version exact-match; role-family enum range; display-name presence. Returns `ValidationResult(bool IsValid, string[] Errors)` non-fail-fast. Phase-6 deferred: phenotype-label lint, real-player-name diff, SignatureCandidate.SignatureId resolution against loaded SignatureSO catalog. **Loader**: process-lifetime `ConcurrentDictionary` cache; `Load(archetype, jersey)` + `LoadAll()` + `Parse(json)` entry-points; throws on resource-missing / validation-failure with full error list in message. **22 fixtures** authored: 11 direct-pressing (Erik Halvarsson GK, Mateus Korhonen RB, Ardo Vermeer / Casper Linde CBs, Tobias Renno LB, Jonas Pielke / Felix Aydar wingers, Aleks Brennan / Niko Sandven CMs, Rafael Mendes-Cole / Liam Travers strikers) + 11 low-block-counter (Henrik Vasquez GK, Sami Lindahl RB, Marko Brennan / Joren Visser CBs, Petra Nordquist LB, Toma Velasco / Ryo Castellan wingers, Diego Hartmann / Yusuf Aren CMs, Anders Strom DM, Mikel Ostrowski lone striker). Caldren-flavored fictional names; no real-AAA-player collisions. Signature-affinity assignments role-coherent: low-cutback-from-byline only on Wingers (4 carriers); blind-side-near-post-run only on Strikers (3); first-time-diagonal-switch only on CMs (3). Gene values use 0.5/0.6/0.7/0.8 buckets with role-appropriate biasing (FastTwitch high on wingers/strikers, PatternRecognition high on CBs/CMs, etc.). **Tests**: 31 new tests across 3 files (`IdentityPacketRoundTripTests` 9 / `IdentityPacketValidatorTests` 14 / `IdentityPacketSignatureAffinityTests` 8) covering load + cache + JSON round-trip + 22-fixture validator sweep + per-fixture invariant checks (ID uniqueness, role-affinity coherence, weight bounds). **Total tests: 547 passing** (was 503 at the start of foundation #4; +44 new across 4 test files including 10 strict-parsing tests added in the Codex round-7 hand-rolled-parser refactor that closed P1#1 + P1#2 + P2). **Subagent rotation per CLAUDE.md §6.3**: `feature-dev:code-architect` produced the implementation blueprint (tool_uses=7; plugin subagent worked); `pr-review-toolkit:silent-failure-hunter` + `:type-design-analyzer` + `feature-dev:code-reviewer` ran in parallel before commit. Project-internal subagents (`narrative-director`, `gameplay-programmer`) skipped per the same invocation gap caught in the prior 2 /next pickups (Addressables init `0f420d7`, scenes `e93f138`); main-thread authoring with the architect's blueprint as guide. **Phase-3 dependencies for ADR-0006**: zero new runtime dependencies on the MatchSim side — System.Text.Json was added in `e0ecc5c` then REMOVED in the round-7 refactor (`62f378b`) because STJ + transitive deps don't ship in Unity 6's Mono runtime; replaced with the strict hand-rolled `Content/Json/IdentityPacketParser` which is also strict-by-design (rejects unknown fields, missing required fields, numeric RoleFamily, duplicate keys, leading-zero numbers, decimals/exponents, trailing junk). IsExternalInit polyfill added (records `init` setters require the marker type which ships net5.0+ but is absent on netstandard2.1). **csproj cleanup**: cref ambiguity in `MatchSim/Sim/CanonicalEncoder.cs` for `ArrayBufferWriter<T>` introduced when STJ landed (two-assembly type collision); kept as plain `<c>System.Buffers.ArrayBufferWriter<byte></c>` formatting after the round-7 STJ removal so the cref stays unambiguous if any future package re-introduces a duplicate definition. **Lint exemptions**: 3 inline `ui-lint:allow term="awakens"` exemptions for signature-mechanic vocabulary (matches catalog key, not match form); lowercase `jerseyNumber` in error message bypasses A.5 `\bJersey\b` regex naturally. Pinned 60-tick MatchCanonicalState determinism hash unchanged; cross-platform CI matrix unaffected.) <!-- ui-lint:allow term="awakens" reason="meta-reference to the inline ui-lint:allow exemption used in IdentityPacket.cs / IdentityPacketSignatureAffinityTests.cs to authorize signature-mechanic vocabulary; this SPEC closure note quotes the exemption attribute for traceability" reviewer="osagberg" -->
- [x] **3 active signatures end-to-end** (Phase-3 semantic slice per 2026-04-28 decisions entry; foundation #4 second task) (2026-04-30 — Phase-3 minimum signature-dispatch layer landed end-to-end; the 3 active signatures (#20 *Low cutback from the byline* / #22 *Blind-side near-post run* / #13 *First-time diagonal switch*) emit canonical `KeyEvent`s + parallel `SignaturePresentationRecipe` metadata. **Architecture per feature-dev:code-architect blueprint**: extended `KeyEventKind` with 3 new pinned values (5/6/7) so signature executions enter the canonical event stream + replay-corpus hash; added 5 new files under `MatchSim/Sim/` (`SignatureKind` enum / `SignatureCooldownState` per-match scratch / `SignatureConfig` threshold struct / `SignaturePresentationRecipe` DTO / `SignatureRules` orchestrator); `MatchSimulationState.SignatureRecipes` parallel list carries presentation metadata for the dots adapter (NOT in canonical hash; explicitly excluded from `MatchCanonicalState.Write` so overlay-text changes don't invalidate the corpus fixture). **Per-signature trigger detection** uses coarse spatial conditions per the architect's Phase-3 simplification policy: #20 = winger carrier near attacking byline + wide channel + lateral velocity; #22 = striker in attacking penalty area + ball wide for cross delivery + forward+curve velocity; #13 = CM in middle third + ball moving on both axes (proxy for "first-time touch" without a possession-transition tracker). Cooldown per signature (180/240/300 ticks) + per-match max-fires (3/2/2) prevent per-tick spam. **Sim bias is metadata-only at Phase 3** per ADR-0005 SimBiasSnapshot deferral; the recipe payload carries `SimBiasFieldId` + `SimBiasDeltaRawQ32` for the dots adapter's presentation-layer effects (camera cut, overlay text), with actual MatchSim canonical-state behavioral modification deferred to Phase 4. **Runner overload pattern**: `MatchSimulationRunner.RunTicks` keeps the legacy 7-arg signature for back-compat (delegates to the new signature-aware overload with `Array.Empty<IdentityPacket>()` × 2 → SignatureRules.Step short-circuits → byte-identical canonical hash). **Pinned 60-tick determinism hash unchanged** — the architect's spatial analysis proved the smoke fixture (ball at centre, formation positions) cannot satisfy any of the 3 trigger conditions; new `Match_SmokeFixture60TicksWithSignaturePackets_ProducesIdenticalPinnedHash` test pins this claim by running the smoke fixture WITH IdentityPackets and asserting both the hash + zero-signature-events. **19 new tests** (18 in `SignatureRulesTests`: per-signature fires + carrier-affinity gate + role-family gate + cooldown + max-fires + empty-packets short-circuit + 3 argument-validation + multi-fire-same-tick + BlindSideRun-not-carrier; 1 pinned-hash regression in `MatchDeterminismTests`). Total tests: 566 passing (was 547; +19). **Subagent rotation per CLAUDE.md §6.3**: `feature-dev:code-architect` produced the implementation blueprint (tool_uses=14); pr-review-toolkit triple ran in parallel before commit. <!-- ui-lint:allow term="awakens" reason="closure-note refers to ADR-0005 SignatureAwakened MemoryEvent class context referenced in code/blueprint; not player-facing copy" reviewer="osagberg" -->. **2026-04-30 Codex round-9 follow-up** closes one P1 + two P2 + two P3 findings against `bf2ac1e`: (P1) `SignatureCooldownState` now lives on `MatchSimulationState` (allocated once at construction; runner reads instead of re-allocating per `RunTicks` call) so chunked-tick callers — viewer/replay loops driving `ticks=1` in a hot loop — can no longer re-fire signatures past their per-match caps; (P2) `SignatureRules.Step` validates packet-array shape at the boundary and throws on partial rosters / mismatched lengths / single-null entries instead of silently dropping signature eligibility for misaligned slots via the prior `Math.Min` + null-skip defensive; (P2) the `MultiSignatureSameTick` regression test is rewritten to actually fire two signatures (CM jersey-7 #13 + Striker jersey-11 #22) on the same tick with sequential `KeyEventIndex` assertions, replacing the prior single-fire+`Single` assertion that would have masked a hypothetical "one-event-per-tick" cap regression; (P3) Q32.32 recipe deltas constructed via integer-ratio arithmetic on `Fixed.OneRaw` (`Fixed.OneRaw / 5L` / `Fixed.OneRaw / 4L` / `Fixed.OneRaw * 18L / 100L`) instead of `(long)(0.20 * (1L << 32))` double literals — keeps `double` arithmetic out of corpus-feeding metadata paths even though recipe deltas aren't currently in the canonical hash; (P3) SPEC + CHANGELOG test counts corrected from the at-`bf2ac1e` miscounted "+17 / 564" to the actual "+19 / 566". Round-9 follow-up adds 5 new tests (3 packet-validation + 2 chunked-tick regression) for a new total of 571 passing. **2026-04-30 Codex round-10 follow-up** closes one P2 finding against the round-9 commit — the prior chunked-tick regression tests (smoke-fixture hash + pre-saturated cooldown) would not have caught the original bug if it returned via "RunTicks switches back to a fresh local SignatureCooldownState while leaving the state property untouched" (smoke fires zero signatures; saturation pre-load doesn't depend on the runner consuming `state.SignatureCooldown`). Added `Match_ChunkedRunTicks_LowCutbackFiresOnceAcrossChunkedAndSingleCallRuns` which constructs a runner-driven trigger: home jersey 6 (Winger with #20 LowCutback affinity per the real loaded direct-pressing fixture packet) parked at the attacking byline (50, 0, 22) with lateral velocity (0, 0, 3); ball coincident; conditions persist 2+ ticks. Single-call `RunTicks(ticks=2)` fires #20 once (cooldown=180 blocks tick 1); two `RunTicks(ticks=1)` chunked calls also fire once with persistent cooldown — but would fire twice if the runner re-allocated cooldown per call (fresh `lastFiredTick = long.MinValue` makes `currentTick - lastFiredTick ≫ 180`). The test asserts chunked count == single count == 1 + canonical-hash equality + recipe-stream-count equality. Round-10 total: **572 passing**. Pinned 60-tick `MatchCanonicalState` determinism hash unchanged.)
- [x] **1 `MemoryEvent` reader callback** (Phase-3 semantic slice per 2026-04-28 decisions entry). Minimum viable demonstration of the event-sourced memory pattern. Recommend press/fan-callback flavor (lowest-friction); commentary-template overlay during a match minute. Schema-shaped to ADR-0004; extends to the full reader catalog in Phase 4-5 (2026-04-30 — Phase-3 minimum memory layer landed end-to-end. **Architecture per feature-dev:code-architect blueprint**: 13 contract types under `MatchSim/Memory/Contracts/` (`EventClass` / `EmitterKind` / `Emotion` / `SalienceBand` / `CareerDate` / `EventEmitter` / `Participant` / `SalienceInputs` / `CallbackTag` (+ `ExpiryPolicy` sealed-abstract closed-union) / `ReaderQuery` / `IMemoryReader` (+ `ReaderId`) / `CallbackCandidate` / `MemoryEvent`); 7 implementation types under `MatchSim/Memory/` (`SalienceWeights.Phase3Defaults` per `design/event-sourced-memory.md` Phase-6 tuning seeds + `Phase3ModelVersion=1`; `SalienceEngine.Compute` / `ClassifyBand` / `ApplyCallbackAgeModifier`; `EventClassRegistry` Phase-3 minimum subset {None=0, GoalScored=1}; `CallbackTagRegistry` with constructor-validated `PressFan`(MinBand=Notable, Expiry=Seasons(3) per narrative-director's authoring pass) + `ScoringMilestone` placeholder; `Ledger` append-only with snapshot-semantic `All` + `SinceSeason`; `MemoryEmissionRules.EmitForKeyEvents` bridge translating `KeyEventKind.Goal` → `EventClass.GoalScored` MemoryEvents; `PressFanReader` filtering by tag + season window + min-band, sorted by surfacing salience descending). **Determinism**: all numeric fields Q32.32; deterministic event-id `match:<matchId>:tick:<tick>:seq:<n>`; no `double` arithmetic in canonical paths; no `System.Collections.Immutable` (matches the Codex round-7 STJ-removal precedent — Unity 6 Mono doesn't ship it inboxed; uses `IReadOnlyList<T>` backed by `T[]` with defensive copies in `MemoryEvent` + `CallbackTag` constructors). **MatchSim never touches the ledger** per ADR-0004 hard line: bridge lives outside `MatchSimulationRunner`; runner adds zero new code paths; pinned 60-tick determinism hash UNCHANGED. **Phase-3 minimum scope** drops `Consequences` array + per-event `CallbackEligibility` override (Phase-4+ when contracts/attribute-deltas + per-event tag overrides land). **End-to-end Unity Mono load verified** via UnityMCP `execute_code`: synthetic Goal KeyEvent → 1 MemoryEvent → ledger → 1 PressFanReader CallbackCandidate carrying `fwh.core:callback_template.goal_press_fan_milestone` template at Notable band — works in the actual deploy environment. **Phase-3 placeholder values**: Stakes=0.95 / Prominence=0.6 / EventClassBaseWeight=0.6 (lifted from architect's 0.9/0.6/0.6 to leave a ~2 ULP margin above NotableThreshold=0.60 so Q32.32 multiply-rounding can't flip the band classification at the boundary). **42 new tests** across 3 test files (`MemoryEventTests`: 21 schema/invariant tests; `SalienceEngineTests`: 10 compute/band/decay tests; `PressFanReaderEndToEndTests`: 11 end-to-end + reader-behavior tests). Total tests: 614 passing (was 572; +42). **pr-review-toolkit triple ran in parallel before commit** (silent-failure-hunter: clean; type-design-analyzer: 3 findings applied — defensive `MemoryEvent.Participants` copy + `CallbackTag` constructor-validated (no opt-in `Validate()`) + duplicate-id check deferred Phase-4+; feature-dev:code-reviewer: 4 findings applied — C1 `ReaderQuery.CurrentSeason` decoupled from `ToSeason` so historical-window queries compute decay correctly; CV3 `CareerDate` doc-vs-code mismatch fixed; I1 `Ledger.All` snapshot semantics for consistency with `SinceSeason`; I2 `ApplyCallbackAgeModifier` doc clarified to describe both `<` and `==` early-return cases). **Subagent rotation per CLAUDE.md §6.3**: `feature-dev:code-architect` produced the implementation blueprint; `narrative-director` authored the press-fan callback prose + tag-registry semantic spec; pr-review-toolkit triple ran pre-commit. <!-- ui-lint:allow term="awakens" reason="closure note refers to ADR-0004 SignatureAwakened MemoryEvent class context that lands Phase-4+; not Phase-3 player-facing copy" reviewer="osagberg" -->)
- [ ] **1 persistent development event** (Phase-3 semantic slice per 2026-04-28 decisions entry). Single-match version of the breakthrough-cinema pattern per `design/breakthrough-moments.md`; deterministic trigger condition; surfaces via `ViewerEvent` overlay at a designed beat. Post-match stat-card optional; defer if scope-pressure hits
- [ ] **Recruit Month-3 observer pool: 5 cold football-literate observers + 2 backups, named in STATUS.md.** Per `design/month-3-vertical-slice.md` §Observer-pool lockdown fallback + audit-07 P0 finding (2026-04-30 8-agent triage): observer recruitment is gate infrastructure, not polish. Locked criterion: observers must be football-literate, not previously briefed on Final Whistle's pillars, willing to watch a 3-minute dots-phase match and answer the rubric questions. Sources: trusted friends / Discord / private itch keys. STATUS.md `Pending async` carries the named list when recruited
- [x] **Author `design/anime-presentation-budget.md` placeholder** (Phase-3/4 guardrail per Codex round-4 follow-up plan + audit-07 P0 anime-coded-gameplay finding) (2026-04-30 — placeholder shipped at `design/anime-presentation-budget.md`. Eight anime-inflected presentation surfaces enumerated: impact frames / screen-tone + motion-line variants / signature title-card + cut-in language / camera rhythm / pressure indicator / aftermath-freeze emphasis / signature-specific commentary cadence / UI-Toolkit overlay typography rhythm. Phase-3 minimum cells locked per surface; Phase-4+ expansion paths documented. Explicit no-list bans mid-match QTE + capitalized state-nouns + screen-shake-spam + speed-line-overuse. Cross-linked from `design/README.md` index. Full per-surface implementation specs land alongside Phase-3 Viewer.EventBridge + Phase-4 dots-adapter polish-bar work.)
- [ ] **`Viewer.EventBridge` minimum implementation** (Phase-3 semantic slice per 2026-04-28 decisions entry; gates dots-adapter authoring). Derives `ViewerEvent`s from the canonical MatchSim event stream + `ShotTypeDefinition` DTOs projected from `ShotTypeSO` assets per ADR-0008. Synthetic-fixture path lands first (per ADR-0009 §Polish-bar observer rubric); real-corpus-driven path second
- [ ] Dots-phase render adapter (per ADR-0009) prototype: `Viewer.Adapters.Dots` asmdef + sprite-on-pitch + 3 of 7 shot types (`tactical-wide` + `diagonal-attack-lane` + `pass-shot-impact`) + reduce-motion variant substitution per ADR-0001 + UI-Toolkit overlay text + selection-ring + identity overlay. Held to ADR-0009 polish bar (kit discrimination / identity legibility / camera rhythm / signature presentation cues / commentary integration). NOT debug-quality; may ship at EA per 2026-04-26 supersession outcome (c2). **Depends on:** asmdef skeleton + Viewer.EventBridge + 3 signatures + IdentityPacket fixtures + PitchRules — the dots adapter is the consumer of the semantic-slice work above, not parallel to it
- [x] ADR-0008 ShotPresentationContract + ADR-0009 dots-phase render adapter — flip from Proposed to Accepted via user / GPT-5.5 review pass before Phase-3 Week-2 rendering work begins (2026-04-27 — both ADRs **Accepted** after GPT-5.5 review pass (4 P1 + 4 P2 findings applied: stable `ViewerEventId` + ordering rules; explicit `BaseShotTypeId` / `EffectiveShotTypeId` / `ReduceMotionApplied` split with bridge-resolves-once boundary; locked minimum `PitchView` + `ActiveViewerEvent` shapes; aftermath-freeze rewritten so viewer-time-hold doesn't pause MatchSim canonical time; ordinal-sort + InvariantCulture rules for memory slots; corpus `pass_activation_log_hashes` adapter-keyed from v1; operational 6-task observer rubric for the polish bar; CI smoke split into synthetic ViewerEvent fixture + corpus seed) + Codex review pass (stale-reference cleanup; no remaining blocking findings). AdapterId enum gained code-owned-registry clarification: community/mod adapters are code plugins via trusted-build channel, NOT Workshop content packs. ADR-0010 (3D adapter) remains NOT-pre-authored and conditional on Phase-5 spike outcome. Append-only from this point; supersession only via new ADR)
- [ ] **Phase-3 enforcement skeletons** (per 2026-04-28 decisions-log enforcement-skeleton-rollout entry). (a) `scripts/fw replay <seed>` minimal compare — runs `MatchSimulationRunner` + computes `MatchCanonicalState` hash at fixture-defined tick count + compares against checked-in fixture under `MatchSim.Tests/fixtures/replay-corpus/<seed>.json`; first fixture is the smoke seed `0xdeadbeefdeadbeef`. (b) Save-migration fixture skeleton at `MatchSim.Tests/fixtures/save-migration/` + `fw save-migration-test` CLI stub (exits 0 when no fixtures); first real fixture lands when MemoryEvent ships in Phase 4. (c) Content-pack validator skeleton stays Phase 6 (no Phase-3 content-authoring need). Also covers existing item *"Local MatchSim CI scripts implemented via `scripts/fw` — `fw test`, `fw replay <seed>`, `fw verify` match Tier-A behavior locally"*
- [ ] Match-replay skill end-to-end (seed → headless match → viewer capture)
- [x] Unity MCP handshake verified (2026-04-28 — `claude mcp list` confirms `UnityMCP: http://127.0.0.1:8080/mcp (HTTP) - ✓ Connected`; UnityMCP-driven Editor calls have been load-bearing for Phase-3 Week-2 work (URP asset creation, GraphicsSettings binding, Quality-level wiring, colorspace + Input System config). Server name + endpoint pinned in `.mcp.json` + the previously-redundant refresh hook removed in `47997fc`)
- [ ] Devlog clips Month 2-3 published (first external audience signal)
- [ ] Local MatchSim CI scripts implemented via `scripts/fw` — `fw test`, `fw replay <seed>`, `fw verify` match Tier-A behavior locally
- [x] GitHub dotnet-test matrix (Tier A subset — Win/Mac/Linux) wired for `MatchSim.Tests` determinism suite (2026-04-27 — `.github/workflows/fast-pr-ci.yml` `verify` job now runs the full `scripts/fw verify` umbrella on `ubuntu-latest`, `windows-latest`, and `macos-latest`, with setup-dotnet + setup-python and `shell: bash` so the repo script runs consistently on Windows GitHub-hosted runners. Matrix was activated after the 60-tick `MatchCanonicalState` pinned hash landed)
- [ ] Deterministic replay hash check: ONE canonical corpus seed runs green in Tier A (full corpus deferred to Tier C/D)
- [ ] Unity smoke build workflow `.github/workflows/unity-smoke.yml` exists as **manual-dispatch only** — no PR trigger. One target (Windows OR WebGL) for now
- [ ] `scripts/fw build-local` produces a local Unity build (current platform only) for solo-dev verification
- [ ] Author `scripts/fw artifact-cleanup` CLI — `--list` / `--delete-expired` / `--release-lock <tag>` / `--audit-local` subcommands per `design/specs/artifact-retention-policy.md §Cleanup automation posture`. Manual-cleanup bridge until Phase-6 `fw workflow-audit` automates. Bridges the retention-policy gap during Phase 3-5 when workflows may accumulate artifacts without declared `retention-days:`

**Gate to Phase 4 — MONTH 3 MATCH-ENGINE GATE**: *A stranger watches a dots-phase match for three minutes and understands drama, momentum, and player identity without reading a design doc.* FAIL = extend Phase 3 by one cycle; do not proceed.

---

### Phase 4 — Scout Disagreement Prototype + First Signatures ⚪ PENDING
**Goal**: Scout Disagreement feel-tested (gate for MVP inclusion); 3-6 signatures authored end-to-end.

- [ ] Implement internal player gene model + identity-packet compiler
- [ ] **Author `design/progression.md`** — Phase-4 owed per 2026-04-30 USP-4 honesty pass (Codex round-4 follow-up plan, commit #5 of 6). Documents: bounded gene model (0.0–1.0 clamped) + role-normalized soft-cap distribution + breakthrough-flag triggers (`peak_ceiling_high` etc., per `design/player-generation.md`) + how rare players cross typical ceilings via narrative breakthroughs + balance-harness contract (Phase 6) for verifying the curve doesn't allow runaway growth. Replaces the prior Phase-0 "unbounded RPG progression" framing with a doc that matches the actual implementation. Authoring trigger: Phase 4 gene-model implementation lands first; the doc is co-authored alongside (not before) so it reflects shipped code rather than aspirational text
- [ ] Scout Disagreement prototype: 3 scout archetypes, different biases, generate disagreeing reports on same player
- [ ] Feel prototype playtest (2 weeks max)
- [ ] MONTH-4 GATE: Scout Disagreement feel-test verdict logged as decision
- [ ] Author 3-6 signatures end-to-end: trigger conditions + sim bias + presentation recipe + counterplay
- [ ] Breakthrough moments prototype (match-flow cinematic emphasis, no pause)
- [ ] Closed itch build for ~10 trusted testers
- [ ] Retention data collected from itch testers
- [ ] `docs/ops/playtest-distribution.md` authored — itch.io private-restricted page flow, feedback collection (Google Form or in-build markdown), build signing/hashing
- [ ] In-build "Export bug bundle" button ships: save + last-N replay seeds + rolling logs + settings + content-pack version. Testers email/itch-message zip; no cloud ingest
- [ ] `scripts/fw package-playtest` produces signed build zip ready for itch upload
- [ ] Design player match-rating model — `design/match-rating.md` authored + ADR if architecture-bearing. Per 2026-04-25 decisions-log entry: role-normalized ratings (rate the job, not the highlight) + counterfactual defensive value derived post-match from existing event stream (not new EventClass emission unless signature/reader needs) + **centered-5.0 scale locked** (Par / Good / Excellent / Dominant labels surfaced alongside number) + IdentityPacket role-subtype schema-bump decision driven by implementation pressure (DM = destroyer vs ball-playing pivot vs registra differentiation). Phase-4 first-signatures authoring is when role-subtype pressure surfaces concretely. Revisit clause: centered-5.0 can be superseded via new decisions-log entry if Month-4 observer feedback says the scale is unreadable
- [ ] Design TacticalPreset system — `design/tactical-presets.md` authored + ADR. Per 2026-04-25 decisions-log entry: parallel to SignatureSO, NOT scope-expansion of ADR-0005 `SignatureScope`. Tactical presets = team architecture that unlocks when squad personnel match (e.g. *huge striker → crossing volume + near-post/far-post targeting*; *fast winger → space-behind + isolation-vs-fullback*; *elite DM-anchored counterpress*). Covers the "wild tactics around stars" fun-pillar gap the 24-signature catalog doesn't address. Personnel-affinity metadata triggers preset availability; each preset carries its own `SimBiasFieldId` set (reuses MatchSim.Contracts registry). Author in parallel with / after first-signatures task so implementation-pressure feedback loops between the two systems

**Gate to Phase 5**: Scout Disagreement verdict logged; first signatures playable; retention data says keep going.

---

### Phase 5 — Vertical Slice ⚪ PENDING
**Goal**: one full season playable end-to-end; all 7 shot types; ledger operational.

- [ ] All 7 semantic-cinema shot types implemented in the dots-phase render adapter (per ADR-0009). 3D adapter implementation is conditional on Phase-5/6 spike-green outcome, not blocking
- [ ] **3D-pipeline production-feasibility spike preparation** per `design/3d-pipeline.md` §The Phase-5/6 production-feasibility spike: full license-audit of 3D-tooling commercial-rights (each candidate tool — 3D-asset generator / AI-assisted animation tool / cloth simulation / cel-shader stack — has documented commercial-rights coverage for Steam EA + 1.0 release before any subscription activates). `FW-VAL-D-011` content-pack-validator check and asset-licensing-tracker schema bump (new columns: generator / plan_license_tier / prompt_source_refs / human_edit_steps / commercial_rights_proof / generated_asset_hash) land first. Phase-5 SPEC blocker for the spike itself
- [ ] **3D-pipeline spike — vertical slice** per `design/3d-pipeline.md`: ≥6-10 visible players in one Unity scene + two distinct kits + one body-type variant + locomotion + one duel + one signature with ball-contact markers + cel-shader + outline + Unity import + LOD + target FPS on minimum hardware + repeatable export/import + commercial-rights manifest complete. Three outcomes: spike-green → ADR-0010 authored + 3D adapter promoted to shipping; spike-yellow → extend OR commit dots-EA path; spike-red → engage `design/3d-pipeline.md §Alternatives if the spike fails`
- [ ] `design/3d-pipeline.md` full pipeline spec authored at spike kickoff (lock rig standard / clip format / event markers / ball-contact markers / retargeting rules / fallback animations / minimum-hardware spec / animation budget realism / generated-likeness audit / skin-body-age variation matrix / stadium-crowd-scope decision + open questions resolution)
- [ ] Full season schedule (league + cup)
- [ ] Transfer market prototype (direct negotiation, no agents)
- [ ] Event-sourced memory ledger operational
- [ ] 3 memory readers (Alumni DB + rival recall + big-match scars)
- [ ] Starting set of 12 signatures playable
- [ ] Post-match report generation from templates
- [ ] Press/fan sentiment text via templates (no LLM at runtime)
- [ ] Save/load v1 (schema version 1, content pack v1)
- [ ] Month-6 public demo (conditional on itch retention)
- [ ] Crash / log bundle exporter implemented — rotating local logs (bounded size), exportable diagnostics zip, no PII by default, no cloud endpoint
- [ ] `docs/ops/crash-logs-telemetry.md` authored — local-first discipline, optional anonymous playtest JSON opt-in with SQLite/CSV ingest

**Gate to Phase 6**: one full season plays end-to-end; players want to start a second season.

---

### Phase 6 — Core Systems ⚪ PENDING
**Goal**: all 24 signatures + ~20-30 manager archetypes + save migrations tested + content pack v1 compiled.

- [ ] All 24 signatures authored (3 per role family × 8 role families)
- [ ] 20-30 manager archetypes in YAML
- [ ] Manager AI tuning via balance harness
- [ ] Content pack v1 compiled via AI Content Compiler: ~96 clubs, ~2000-2400 players, regional-flavor naming + cultural priors
- [ ] Save schema v2 + `migrate_v1_to_v2` tested
- [ ] Balance harness produces 10K-season sweep per `design/balance-harness.md` (owed Phase-6 design doc + ADR-if-architecture-bearing). Methodology per 2026-04-26 decisions-log entry: scale-agnostic internal performance score / stratified attribute-correlation analysis (no raw correlation; physical-trait correlation requires stratification by role-subtype + team-strength + minutes + tactic + opposition) / `NarrativeFlag` zero rating contribution invariant / EventClass emission rates scenario-banded not all-must-emit / golden-balance-corpus uses metric bands + seed manifests not exact distribution hashes / tactical-fuzzing legality grammar (hybrid: legal structured grid + authored-archetype mutations + evolutionary exploit search) / thresholds start as warning bands not hard fails / win-rate exploit threshold uses Elo team-strength normalization / salience separate from ratings / Claude-assisted analysis consumes summaries+outlier-reports+replay-samples not raw events. Balance-corpus starter (5-10 configs): equal-strength / weak-vs-strong-counter / physical-bias-lab / role-rating-parity / youth-dev-toy / known-exploit. Methodology supplements: metamorphic tests / micro-sim lab / shadow ratings / manager adaptation harness
- [ ] 2 additional memory readers (promise tracking + press/fan callbacks)
- [ ] 5-8 salience-gated narrative event templates per season category
- [ ] Month-8 Steam Next Fest (conditional on first 10 minutes being sharp)
- [ ] Steam page draft: description, tags, first screenshots
- [ ] Balance harness Tier-C (local / self-hosted) produces 10K-sweep summary artifacts uploadable to GitHub; full replay-corpus regen + diff runnable locally
- [ ] Save compatibility fixtures checked into `MatchSim.Tests/fixtures/saves/` — v1 fixture + v1→v2 migration test + callback-preservation test + forward-incompat failure test (per Phase-2 policy)
- [ ] Content-pack validator (full) implemented — Tier-A subset already running; Tier-D full suite (legal-sensitive-names diff, AI-content disclosure check, cross-doc event-class enum exact-match, `CallbackTag.ConsumingReaders ≥ 1`) per `design/specs/content-pack-validation-contract.md`
- [ ] Synthetic thin-mod-pack CI fixture authored at `MatchSim.Tests/fixtures/mod-packs/thin-mod.fwh.mod.v1/` — 1 new signature + 1 new shot type + 5 new IdentityPackets / players using existing `PhenotypeLabelId` values + 1 new ScoutArchetype using an existing `ScoutArchetypeKind`; must not modify `Content.Contracts`, add registry values, or require a schema bump; loads cleanly alongside unchanged `fwh.core@1.0.0` at Tier-D. This is the Phase-6 integration test for `design/modding.md §Prototype gate` + `design/specs/content-pack-validation-contract.md`. Failure = content pack v1 not actually mod-ready
- [ ] Red-team validator fixtures in place at `MatchSim.Tests/fixtures/validator-red-team/` — one intentional-fail pack per `FW-VAL-<id>` check implemented at Phase 6; red-team self-check CI step wired per `design/specs/content-pack-validation-contract.md §CI wiring`
- [ ] Golden replay corpus v1 checked in — seeds + expected hashes + regeneration command in `scripts/fw`
- [ ] Tier-A smoke-seed single-vs-rotation evaluation — measure Phase-3 `fw replay` runtime, expand to 3-seed rotation ONLY if per-run budget stays inside Tier-A's 5-minute ceiling (policy locked in `design/specs/golden-replay-corpus.md`)
- [ ] Author `scripts/fw workflow-audit` — enforces every `.github/workflows/*.yml` `upload-artifact` step carries a `retention-days:` attribute matching the Actions-artifact tier declared in `design/specs/artifact-retention-policy.md`; Tier-A CI fails on missing/mismatched uploaded-artifact retention (`FW-WF-A-001`). Also reports the repo/org Actions artifact-and-log retention setting (`FW-WF-A-002`) so workflow-log retention is visible without pretending logs are upload-artifacts. Release-tied bundles are audited through release-asset presence/checksum checks in `fw artifact-cleanup --release-lock`, not through `retention-days: 0`

**Gate to Phase 7**: systems architecturally complete; tuning happens during content scaling.

---

### Phase 7 — Content Scaling + Polish ⚪ PENDING
**Goal**: content-complete at EA target; polished to anti-FM26-regression standard.

- [ ] UI polish pass: navigation depth ≤ 2 clicks to any common action
- [ ] Performance pass: MatchSim + viewer on mid-range 2026 hardware within frame budget
- [ ] Balance harness production passes tuned
- [ ] Localization: English at launch; extract all user-facing strings to tables
- [ ] Accessibility: subtitles, colorblind, remappable controls, large-text UI, reduce-motion toggle
- [ ] QA pass: full season playthrough, bug triage
- [ ] Telemetry hooks (opt-in only)
- [ ] Crash reporting integration

**Gate to Phase 8**: game plays start-to-end at EA scope without blockers.

---

### Phase 8 — EA Launch (Month 12) ⚪ PENDING
**Goal**: Steam EA release button pressed.

- [ ] Steamworks SDK integration (achievements, stats, cloud saves, Workshop-ready scaffolding)
- [ ] Steam page finalized: description, 8-12 screenshots, trailer
- [ ] Age rating questionnaire (PEGI 12 / ESRB T via Steam)
- [ ] Steam Direct $100 paid
- [ ] Release candidate build + smoke-tested on clean machines
- [ ] Launch trailer + marketing assets
- [ ] EA launch date locked + public
- [ ] Day-1 patch prepared
- [ ] Tier-D Release Candidate workflow `.github/workflows/release-candidate.yml` green end-to-end — full dotnet matrix + full Unity builds + content-pack validator full suite + save migration matrix + asset-license audit + banned-term exemption audit
- [ ] Tier-E Steam deploy workflow `.github/workflows/steam-deploy.yml` authored with **manual-approval gate** — never auto-deploys on tag
- [ ] `docs/ops/release-channels.md` authored — dev/tester-closed/demo/ea/hotfix channel metadata + validation tiers
- [ ] Release checklist copied to version-specific doc (e.g., `release/v0.1.0-ea-checklist.md`)
- [ ] Rollback build tested on clean machine — confirmed restores prior EA build
- [ ] AI-content disclosure metadata checked (manifest of bake-time LLM uses per Steam / regulatory requirements)
- [ ] **EA visual-target lock per Phase-5/6 spike outcome** (per 2026-04-26 visual-target supersession): three outcomes locked + acted on. **(c1) Spike-green** → 3D adapter (per ADR-0010 if authored) ships at EA; full Steam screenshots + trailer rendered through 3D adapter. **(c2) Spike-yellow/red** → ship dots-only IF dots adapter hits ADR-0009 polish bar at this Phase-8 RC build (independently re-verified); NO public "3D coming in 1.0" dated promise allowed in Steam page / trailer / EA marketing copy; "3D R&D ongoing post-EA" framing acceptable only if uncommitted-on-timing. **(c3) Dots-not-strong-enough** → delay EA; do not ship a weak first impression. Decision is RC-gate blocking
- [ ] Steam page screenshots + trailer reflect locked visual-target (dots-only OR 3D-adapter); marketing copy matches per (c1/c2/c3) outcome

**Gate to Phase 9**: game is live on Steam EA.

---

### Phase 9 — Post-EA ⚪ PENDING
**Goal**: sustainable support; 1.0 planning; continue or scale 3D work only if the Phase-8 visual lock + audience signal justify it.

- [ ] Hotfix cadence (week-1 critical)
- [ ] Community feedback triage
- [ ] Review-response strategy
- [ ] Audience-signal gate: does the game deserve continued / expanded 3D investment after EA?
- [ ] If yes: 3D adapter R&D continues under the Phase-5/6 licensing rules and SETUP trigger table; no tool lock-in from SPEC
- [ ] Coaching Lineage surfacing (data was seeded at bootstrap; now expose)
<!-- ui-lint:ignore-start reason="internal dev-tool feature name (proper noun), not player-facing verb usage" -->
- [ ] Manager Archetype Forge (Claude-generates BTs from English briefs)
<!-- ui-lint:ignore-end -->
- [ ] Counterfactual Development Lab (if/trained/as projections)
- [ ] Physical Load as Narrative Debt polish
- [ ] Workshop editor UX (data architecture was ready at bootstrap; now build)

---

## Backlog (unordered, for future phases)

- Multi-nation expansion (post-1.0 content push)
- Roguelike "Legend Run" condensed-career mode
- Second language pass: JP / ES / PT / DE
- Audio commentary voice-acting (ElevenLabs evaluation) — conditional on player demand
- Counterfactual Development Lab full UI
<!-- ui-lint:ignore-start reason="internal dev-tool feature name" -->
- Manager Archetype Forge English-to-YAML generator
<!-- ui-lint:ignore-end -->
- Physical Load as Narrative Debt (injury system polish)
- Dynasty / lineage mechanics (if audience retains + requests)
- Steam Deck Verified certification push
- Cross-save sharing / "Legend Exchange" async social
- Modding Workshop editor UX
- 3D match engine (scope after audience signal)

---

## Decisions log (append-only — hook-enforced)

> This section is immutable. Do NOT edit past entries. To supersede a prior
> decision, append a NEW entry citing the prior one. The
> `.claude/hooks/protect-decisions-log.sh` hook rejects Edits/Writes that mutate
> any existing `- **YYYY-MM-DD**` bullet. Use `/log-decision` to append.

<!-- ui-lint:ignore-start reason="append-only decisions log; meta-references to banned terms are intentional historical record" -->

- **<YYYY-MM-DD>** — **<Decision headline>**. Reasoning: <short why>.
- **2026-04-22** — **Project bootstrapped from blueprint v2.** Composed profile: 60% sim-management + 30% action-character + 10% narrative trimmings. Intake across 5 rounds (including GPT-5.5 design-partner rounds 3-4). Research scope active for Phase 0-2; contract to `rich` at Phase 2 lock.
- **2026-04-22** — **2D-first MVP committed; 3D explicitly deferred.** Reasoning: Rematch-tier 3D is 30-person-team work over years; solo dev in 12 months is fantasy. Commit 2D stylized manga-broadcast viewer as the final identity, not a waypoint. 3D only post-EA contingent on audience-signal gate.
- **2026-04-22** — **MatchSim architectural split.** Pure-C# `MatchSim.csproj` with zero UnityEngine references. Fixed-point canonical state (format Q16.16 vs Q24.8 decided at Phase 3 Week 1). Enables headless balance harness, xUnit tests, cross-platform deterministic replay.
- **2026-04-22** — **Custom deterministic ball physics (not Unity PhysX).** Rocket League lesson. Magnus force + air drag in fixed-point, lockstep with MatchSim. Allows controlled Tsubasa-curl signature trajectories while staying physically grounded.
- **2026-04-22** — **No capitalized state nouns in player-facing UI.** Banned visible names: "The Hush", "Weather", "Calling", "Canon", "Seven", "Kismet", "Soul", "The Author". Internal floats (`momentum`, `rhythm`, `pressure`, `team_cohesion`, `signature_readiness`) surface via football-native commentary only. See `design/ui-vocabulary.md` for lint.
- **2026-04-22** — **Event-sourced Career Memory as single architectural pattern.** Every meaningful event emits structured record to append-only ledger. Five reader subsystems (alumni / rival recall / promise tracking / big-match scars / press-fan callbacks) are readers, not separate systems. Compaction at 5-season boundary.
- **2026-04-22** — **Fully fictional football world with England-readable grammar.** No real places, no real clubs, no alternate-history framing. Fictional nation(s) with credible football culture, pyramid structure, regional rivalries. Avoids licensing risk + tonal uncanny-valley.
- **2026-04-22** — **24 pre-authored signatures, 3 per role family x 8 role families.** Not composable atoms. Each signature = role-specific football behavior + trigger conditions + sim bias + execution modifier + presentation recipe + counterplay. UI surfaces via football copy ("Looks for early crosses"), never power names.
- **2026-04-22** — **Breakthrough moments are match-flow cinematic, not pause-QTE.** Sim continues deterministically; viewer punches in with panel/impact beat; post-match report confirms development change. Manager influence is tactics/selection/training/promises/pressure — not mid-match pop-up choice.
- **2026-04-22** — **Coaching Lineage: data seeded at bootstrap; surfacing deferred post-MVP.** Alumni tactical-DNA fields exist in schema from Phase 3; rival-manager tactical inheritance becomes surfaced system only post-EA. Avoids 10-year payoff blocking 10-minute demo.
- **2026-04-22** — **Scout Disagreement: conditional MVP gated on Month-4 feel prototype.** Gate criterion: "Does disagreement create interesting decisions, or does it just obscure truth?" Pass means MVP inclusion. Fail means fall back to simpler scout uncertainty system.
- **2026-04-22** — **Narrative ceiling: 5-8 salience-gated events per season.** Not 30. Depth via remembering the right 5 things, not 50 shallow event spam. Salience scored on stakes x rarity x character involvement x rivalry x callback age x player attention.
- **2026-04-22** — **Tone anchor: Giant Killing + Aoashi + occasional anime exaggeration.** Grounded football first; heightened moments second. Not Ted Lasso (too warm), not Blue Lock (too extreme), not mythic-ancestry.
- **2026-04-22** — **Manager characterization minimal.** Doctrine + reputation + history through choices, not authored inner monologue. No "manager trauma system" at MVP.
- **2026-04-22** — **Mod-ready data architecture from day one; editor UX deferred.** Stable IDs (content-pack-qualified), schema versions, content packs, import validation, Workshop-ready assumptions baked in. Phase 0 Modding ADR constrains every system. Editor UX ships post-EA.
- **2026-04-22** — **AI-native content pipeline via bake-time compiler.** spec to JSON to validation to lint to sim sanity to content pack to import. No runtime LLMs. All player bios, match reports, press quotes rendered from templates with runtime slot-filling from event-ledger state.
- **2026-04-22** — **Behavior-tree manager archetypes (YAML), not ML-Agents.** Deterministic, debuggable, balance-harness reproducible. 20-30 hand-authored archetypes in MVP; Manager Archetype Forge (Claude-generates BTs from English) is post-EA content-scaling tool.
- **2026-04-22** — **EA scope: one fictional six-tier pyramid (~96 clubs, ~2000-2400 players).** Multi-nation deferred post-1.0. Depth over breadth.
- **2026-04-22** — **Price: $20 EA -> $30 1.0.** Solo indie RPG-management pricing. Not FM-institutional-tier. Steam Direct $100 at Phase 8.
- **2026-04-23** — **Q32.32 fixed-point is the canonical MatchSim format.** Reasoning: Q16.16 risks multiplication overflow and Q24.8 is too coarse for ball/player trajectory work. Q32.32 keeps deterministic math simple and precise; downgrade only if Phase 3 profiling proves fixed-point arithmetic is the bottleneck.

- **2026-04-24** — **Overview pillar questions resolved (design/overview.md Q1-Q4).** Consolidated entry per user preference. (1) **Nation framing:** single named fictional nation with England-readable football grammar; actual nation name owned by `design/worldbuilding.md`, not duplicated in overview. (2) **Product title:** "Final Whistle" locked as working / product title. Formal trademark + Steam-name clearance deferred to Phase 8 launch prep. Known existing non-AAA uses (`finalwhistle.es` daily-mini-game, `finalwhistle.club` community product) are clearance-pass flags, not blockers. (3) **Quickstart club archetypes — 4 locked for EA:** decaying-giant (tier 2), rising-academy (tier 3), mid-table-survivalist (tier 1), backs-against-the-wall (tier 5). (4) **Pillar tiebreaker (P1 Memory vs P3 Watchability):** Memory wins by default. If a callback would interrupt a high-leverage live match sequence, watchability temporarily wins and the callback is queued to the next natural surface (dead ball, half-time, full-time, or post-match report). Callbacks are deferred, never suppressed. High-leverage = score margin ≤ 1 in final 10 in-game minutes, or any cup / promotion / relegation / derby / title-deciding sequence.

- **2026-04-24** — **Month-3 vertical-slice gate parameters resolved (design/month-3-vertical-slice.md Q1-Q4 + observer-pool lockdown).** Consolidated entry. (1) **Match type:** opening-day league fixture, two stylistically distinct fictional teams; no cup final, no title decider, no derby — the gate tests baseline legibility and must not be flattered by rivalry-driven stakes. Derby / cup stress tests move to Phase 5. (2) **First 3 signatures (names exact per `design/signatures.md`):** #20 Low cutback from the byline (winger), #22 Blind-side near-post run (striker), #13 First-time diagonal switch (CM). (3) **Gate artifact:** local build OR one continuous ~3-minute recording shown privately to 5 cold observers; no public / itch build for the gate itself. Short 30-60s clips are for devlog / audience-signal posts and do not count as the gate artifact. (4) **Pass criterion:** ≥4 of 5 football-literate cold observers (casual fans ~10+ matches/year, not project collaborators) responding privately in writing before any discussion can describe both (a) the match's emotional arc and (b) at least one specific player's style in football-native language. **Fail modes:** "boring" (watchability failed) or "confusing" (legibility failed) — route the fix to the failing pillar; do not paper over by adding features. (5) **Observer-pool lockdown:** if 5 plausible football-literate cold observers cannot be named by end of Month 2, the gate is at risk. Fallback: recruit a tiny private test pool via trusted friends / Discord / private itch keys. Criterion is not weakened; the recruiting problem is solved separately.

- **2026-04-24** — **Match-engine open questions resolved.** Q32.32 remains canonical per 2026-04-23. Month-3 ball physics uses Q32.32 position/velocity/spin, semi-implicit Euler at 60Hz, gravity, linear drag, ground bounce, rolling friction, radius-based possession checks, goal-plane detection, and touchline transitions; Magnus structure exists but may run with zero coefficient for the Month-3 gate if curve reads noisy. Player movement uses steering-target BT output plus deterministic fixed-point actuator caps; switching to continuous force integration requires a superseding SPEC decision or ADR. Month-3 excludes substitutions, injuries, fouls, cards, stoppage time, and VAR; Phase 4 introduces fouls/set pieces, cards, substitutions, basic injuries, then stoppage time. VAR remains deferred indefinitely. Numeric coefficients (`g`, `C_d`, `C_m`, `e`, `μ_step`) are fixed-step tuning seeds in `design/match-engine.md`, NOT SPEC-locked values.

- **2026-04-24** — **Semantic Cinema open questions resolved.** The 7-shot vocabulary is locked through the Month-3 gate; expansion beyond 7 requires a superseding SPEC decision. Shot authoring uses Addressable ShotTypeSO assets with stable IDs, framing parameters, stakes/memory modulation, chain rules, fallback shot, overlay template set, and reduce-motion variant; Phase 2 must add an ADR for ShotTypeSO schema and grouping. Viewer effects use URP fullscreen passes for screen-tone and impact frames, per-player trail meshes/sprites for motion lines, and UI Toolkit overlays for panel/text composition with fallback if masking is brittle; Phase 2 must add an ADR for viewer rendering pipeline and pass ordering. Typography stack is Anton for display impact, JetBrains Mono for data/stat/scout/debug, and Rajdhani for body/commentary/menu text; persistent scoreline uses Rajdhani SemiBold or JetBrains Mono, not Anton. Font licensing is verified in the Phase-1 asset tracker.

- **2026-04-24** — **Event-sourced memory open questions resolved.** Salience locks its formula structure and semantic bands, while weights/cutoffs remain Phase-6 tuning seeds; callback age and player attention are reader-side surfacing modifiers, not emission-time salience inputs. Callback tags are a fixed MVP enum with consuming-reader metadata, extensible post-EA through schema/content-pack versioning. Event classes use a versioned PascalCase enum with a tight ~35-40 starter set; growth beyond MVP requires schema review. Compaction uses three tiers: season-defining events hard-preserved, notable events compact-preserved with callback-essential fields, lower events aggregated only; top 5% salience events per season are hard-preserved with a per-season cap. Save/load uses load-time forward migration of the save envelope and MemoryEvents; no downgrades. Every schema bump requires migration tests. Phase 2 must add an ADR for MemoryEvent schema, callback tags, compaction, and migration.

- **2026-04-24** — **Signature system open questions resolved.** The 24-signature catalog is locked with dependency metadata, including Phase-4+ scheduling flags for set-piece/foul-dependent signatures. #19 is corrected to "Cuts inside onto his stronger foot"; #6 is scoped as a defensive-line effect, not a global team buff. Signature affinity count follows a power-law tail including rare zero-affinity players, with distribution tuned by generation tier and balance sweeps. Multi-signature effects stack through field-level capped policies, not generic softmax. Readiness uses a default threshold with per-signature override. Counterplay surfaces through scout reports for known signatures. Phase 2 must author a SignatureSO/schema ADR covering content-pack IDs, dependencies, scope, stacking policy, and Identity Packet affinity integration.

- **2026-04-24** — **Scout Disagreement open questions resolved.** Month-4 feel prototype uses 3 scout archetypes: physical_profiler, technical_purist, and regional_expert. Reports are canonical structured data rendered into deterministic prose templates. The user facilitates but does not count toward the gate; pass requires at least 2 of 3 external management-game-literate testers to satisfy all three criteria: scout-specific trust attribution, at least one changed sign/avoid decision versus neutral aggregate, and affective response that frames scouts as interesting models rather than noise. Fail modes are RNG-fail, ignore-fail, and overload-fail, with one short remediation pass allowed before final fallback to Scout Uncertainty. The prototype uses 10 hand-authored Identity Packet stubs and minimal MemoryEvent writes for scout track-record feedback. Phase 2 must pre-seed an ADR for Scout archetype schema, ScoutReport schema, callback/event integration, and fallback behavior if the Month-4 gate fails.

- **2026-04-24** — **Breakthrough Moments open questions resolved.** Cinema beat duration locked to 3-5s range with default Phase-3 tuning seed 3s; 5s reserved for genuinely high-stakes beats. Overlay text is a two-tier observational pattern; all system/progression vocabulary ("Signature unlocked," "Awakened," "XP gained," mystical state nouns) is banned and enforced via `ui-vocabulary.md` lint — text describes football behavior, not progression mechanics. Near-miss handling: silent on first same-match occurrence, post-match stat-card after 2nd+ to prevent farming. Regressive triggers have equal gravity to positive breakthroughs (same duration, same shot chain, tone modulation through existing semantic-cinema channels). Pillar-tiebreaker interaction: during normal play, breakthrough cinema defers to the next natural surface (dead ball → half-time → post-match); during a high-leverage sequence, the cinema may fire immediately only if the triggering action is the resolving beat (shot / save / tackle / final pass). Live play is never interrupted mid-sequence; dead-ball breakthroughs fire immediately because the natural surface already exists. No new ADR — composes existing ShotTypeSO, SignatureSO, MemoryEvent, and UI-vocabulary schemas.

- **2026-04-24** — **Player-generation open questions resolved.** The internal model is locked at 22 fields across physical, mental, technical, and narrative-flag categories; growth requires a schema bump. Player-facing phenotype labels use a stable content-pack-qualified enum with an MVP ceiling of 50 labels; role-specific labels are expanded to cover all role families, while systemic or stigmatizing labels such as "Plateau Risk" are removed or moved into scout prose. Advanced numeric scout details default off and expose only scout-estimated ranges, never true internal values. Content generation is reproducible by canonical checked-in JSON/content-pack artifacts, not by assuming LLM bit determinism. Content-pack deltas are additive-only; stable player IDs do not encode minor pack versions. Signature affinity count uses cohort-weighted power-law tuning seeds, and Scout Disagreement reads category-level visibility weights only. Phase 2 must author an IdentityPacket / AI Content Compiler ADR covering schema, phenotype enum governance, affinity rolls, content-pack IDs, and scout visibility.

- **2026-04-24** — **Worldbuilding open questions resolved.** The fictional nation is named **Caldren** — preferred over Cresland because it reads as a grounded fictional football nation, supports clean league/cup naming (Caldren Premier Division, Caldren National Cup, Caldren Football Association), and avoids awkward demonym forms. Cresland remains the fallback if formal clearance fails during Phase-8 launch prep. Caldren is used as in-game setting context, not Steam-facing branding. MVP uses 8 regions with compiler-internal real-world analogue notes only; user-facing region names are fictionalised and analogue leakage is blocked by a Phase-1 lint rule. EA content pack v1 contains 96 fully simulated clubs across six tiers: 20 / 24 / 16 / 14 / 12 / 10. This represents the fully simulated slice, not the entire off-screen Caldren football ecosystem. Cup structure locks three competitions: all-tier National Cup, top-two-tier League Cup, and tier-3-to-6 Trophy. Small-tier season format (repeat fixtures vs cross-group phase) flagged as Phase-6 decision point. RegionPriors remain governed by the IdentityPacket / AI Content Compiler ADR.

- **2026-04-24** — **UI vocabulary open questions resolved.** Player-facing vocabulary lint covers UI code, runtime content, content-pack JSON, and rendered player-facing doc/content outputs. `design/ui-vocabulary.md` uses explicit ignore sentinels only around banned-term catalog sections; no whole-file self-whitelist. Category-A terms have no exemption path (expanded with 2026-04-24 additions: system/progression vocabulary, genetics/bloodline terms, stigmatizing phenotype framings, real-world place-name analogues). Category-B terms may use audited inline exemptions with exact term, specific reason, and reviewer handle; exemption reports are reviewed before EA content lock / release candidates. Commentary overlay templates use flatter per-shot-type pools of 15-30 templates, targeting ~140 MVP match-flow templates, with stake/memory filters and slot variables providing variation. Default English tone is British-football vernacular; other locales use native football idiom and locale-specific banned-term lists. Template governance folds into the Phase-2 AI Content Compiler ADR.

- **2026-04-24** — **Production pipeline planning pass (GPT-5.5 report).** Authored `design/production-pipeline.md` as authoritative CI/CD + release-ops plan. Core posture: GitHub is source-of-truth for code/docs/PRs and cheap PR-gate CI; Unity CI is slow, license-sensitive, expensive (macOS especially) and runs manual-dispatch only through Phase 7; heavy sim sweeps (10K-match, balance harness, replay corpus regen, full Unity matrix) run local or on self-hosted runner; release CI is manual-approval only. Five workflow tiers: A (fast PR, ≤5 min Linux), B (Unity smoke, manual dispatch), C (heavy local/self-hosted), D (release candidate, tagged + manual), E (Steam deploy, manual approval only). Build channels: dev / tester-closed / demo / ea / hotfix. Core deliverables owed by the pipeline: golden replay corpus (Phase-2 spec, Phase-3 implement), save migration fixtures (Phase-2 spec, Phase-6 implement), content-pack validator (Phase-2 spec, Phase-6 full), local `scripts/fw` command front-door (Phase-3), in-build bug-bundle export + itch.io distribution (Phase-4), local-first crash/log exporter with opt-in anonymous telemetry (Phase-5+), backup policy (Phase-1). No paid pipeline services through MVP. Phase-1/2/3/4/5/6/8 SPEC tasks pre-seeded. Phase-2 ADR pre-seeded for production pipeline itself. TECH_APPROACH.md to add Production Pipeline section cross-referencing this doc.

- **2026-04-24** — **Phase 1 ✅ COMPLETE; Phase 2 🟡 promoted; ADR order prioritized for Phase-3 playable-slice unblock.** Phase 1 closed with all Claude-actionable work shipped (Tier-A CI green, `fw verify` umbrella includes verify-docs + banned-terms lint, runbooks for branch protection + Actions budget cap written, repo public-committed on `osagberg/FinalWhistle`). Low-urgency user-actions (Blender install / VS Code editor / slash-smoke / plugin install / Actions $0 cap) roll over as open Phase-1 `[ ]` and do NOT gate Phase 2 per solo-dev convention. Stale cleanup: Task 52 (Unity CI stub from blueprint) marked `[x] (superseded)` by Tier-A workflow; Task 50 (account prerequisites) marked `[x]` on remote creation. Phase 2 design-doc locks marked `[x]` across all 11 docs (substantively locked via Phase-0 2026-04-24 open-question resolutions). Phase-2 ADR order reprioritized per GPT-5.5 2026-04-24 guidance to feed Phase-3's real risk (first deterministic MatchSim + watchable 2D viewer) rather than tidy-doc order: (1) ShotTypeSO, (2) Viewer rendering pipeline, (3) Production pipeline, (4) Golden replay corpus format, (5) Save migration fixture policy, then MemoryEvent / SignatureSO / IdentityPacket / Scout archetype. Phase-2 gate unchanged: design bible complete + ADRs for every load-bearing system decision.

- **2026-04-25** — **Player-rating model principles committed (centered-5.0 scale locked; role-normalization + counterfactual-defensive-value + TacticalPreset system all Phase-4 design-doc / ADR deliverables).** Cross-model design session (Claude + GPT-5.5) surfaced five commitments for the player-match-rating system that will be designed in Phase 4. (1) **Rate the job, not the highlight.** Project-level design rule. Ratings credit role-fulfillment + counterfactual defensive value (passing lanes denied, counterattacks killed before becoming shots, opponent xThreat suppressed while player was responsible), not highlight-action volume. Protects CDM / CB / pressing-forward / defensive-fullback / veteran-organizer roles from structural invisibility. (2) **Centered-5.0 rating scale locked.** `5.0 = par` / `6.0 = good` / `7.0 = excellent` / `8.0+ = match-defining`. Labels surfaced alongside the number in UI (Par / Good / Excellent / Dominant). Not A/B-tested against an FM-familiar ~6.7-avg scale at Phase 4; committed now to signal project identity from first impression. Revisit clause: if Month-3 or Month-4 observer feedback signals 5.0-as-par is unreadable to football-literate viewers, supersede via new decisions-log entry; the internal role-normalized scoring model is scale-agnostic so the scale can shift without rewriting the rating math. (3) **Counterfactual defensive value is derived, not emitted.** MatchSim emits actual events per the ADR-0004 event-class enum; a post-match analysis pass derives `threat-suppressed` / `lanes-denied` / `counterattack-averted` metrics from the existing event stream + player-position traces. Promote to first-class `EventClass` entries only if a Phase-6 signature or memory-reader needs to react to them; default posture avoids event-enum inflation. (4) **TacticalPreset system is parallel to SignatureSO, not a scope-expansion.** Per ADR-0005, signatures are player-identity moments; tactical presets are team-architecture amplifiers that unlock when squad personnel match (e.g. *huge striker → crossing volume + near-post/far-post targeting*; *fast winger → space-behind + isolation-vs-fullback*; *elite DM-anchored counterpress*). Owes its own Phase-4 design doc + ADR. `SignatureScope` from ADR-0005 is NOT extended; a new authoring asset class lands instead. (5) **IdentityPacket role-subtype metadata is implementation-pressure-driven, not pre-committed.** `role_family = DM` is too broad to role-normalize ratings cleanly (destroyer vs ball-playing-pivot vs registra are different rating rubrics). Schema-bump to ADR-0006 happens if-and-only-if Phase-4 rating implementation confirms the need; decided via new ADR + save-migration fixture set at that point. Avoids speculative schema churn. Two Phase-4 SPEC tasks added to capture the implementation owed.

- **2026-04-26** — **Visual-target supersession: dots-prototype as Phase-3 validation layer; cel-shaded 3D as candidate shipping layer (NOT committed); renderer-agnostic ShotPresentationContract introduced.** Supersedes 2026-04-22 *"2D-first MVP committed; 3D explicitly deferred (post-EA audience-signal gate)"* + the propagation across PROJECT_CONTEXT.md §5 / CLAUDE.md §1/§3/§7 / TECH_APPROACH.md §2 / design/semantic-cinema.md / SETUP.md §3 / TOOLING.md anti-patterns. New posture, after GPT-5.5 Concerns review (4 P1 findings) on the "dots-prototype → cel-shaded-3D shipping" pivot Claude drafted: **(a) Renderer-agnostic presentation contract first.** A new `ShotPresentationContract` / `ViewerEvent` layer (ADR-0008, owed) defines how MatchSim events + `ShotTypeSO` identities surface to ANY renderer adapter. Same shot identity drives dots-adapter and 3D-adapter; renderers consume the same contract. ADR-0002's stylized-2D-illustrated rendering pipeline is superseded by ADR-0008 (contract) + ADR-0009 (dots adapter, owed). 3D adapter ADR is NOT pre-authored; it lands only after the production-spike gate succeeds. **(b) Dots-prototype is the Phase-3 validation layer.** Sprite-on-pitch + minimal overlay; cheap to build; Month-3 gate becomes a sim-readability test, not a stylized-2D-feel test. Dots viewer is held to a real polish bar (tactical clarity, identity overlays, camera rhythm, readable possession/pressure, signature presentation) because dots may ship at EA per outcome (c2) below. **(c) 3D cel-shaded is a CANDIDATE shipping layer, gated on a hard production-spike.** Phase-5/6 spike must prove a vertical slice: ≥6-10 visible players, two kits, one body type + one variant, locomotion, one duel, one signature with ball-contact markers, cel shader + outline, Unity import, LOD, target FPS on minimum hardware, repeatable export/import + commercial-rights manifest. Three outcomes: **(c1) spike green** → 3D ships at EA or 1.0 per timing. **(c2) spike yellow/red** → ship dots only IF dots viewer hits the polish bar in (b); NO public "3D coming in 1.0" dated promise. **(c3) dots-not-strong-enough** → delay EA; do not ship a weak first impression. **(d) Vendor-agnostic core architecture.** PROJECT_CONTEXT / CLAUDE / TECH_APPROACH / ADRs use generic terms ("3D-asset generator," "AI-assisted animation tool," "retargeting tool"); specific tools (current candidates: Tripo / Rodin / Cascadeur / Hunyuan3D / Magica Cloth 2 / Blender / lilToon) live only in `design/3d-pipeline.md` implementation notes. Tooling can change without ADR churn. **(e) Licensing is a first-class gate.** AI-content-disclosure manifest extends with: generator name + version, plan/license tier, prompt/source refs, human-edit steps, commercial-rights proof, generated-asset hash. New `FW-VAL-D-011` content-pack-validator check enforces; asset-licensing-tracker schema bumps to add columns. Phase-5 task: verify all 3D-tooling commercial-licenses BEFORE the pipeline-feasibility spike begins. **(f) Animation contract surface owed.** 24 signatures × ball contact requires explicit rig standard + clip format + event markers + ball-contact markers + retargeting rules + fallback animations. Captured in `design/3d-pipeline.md §Animation contract`; promotes to dedicated ADR if it grows beyond a section. **(g) Reversibility preserved by floor invariants.** MatchSim stays renderer-free (already locked via Contracts/impl asmdef split); gameplay never depends on 3D-only information; dots viewer is maintained throughout 3D development; renderer adapters consume the same `ShotPresentationContract` layer. If the 3D spike fails terminally, the architecture absorbs it without rewrite. **Three new artifacts owed:** (1) `design/3d-pipeline.md` (placeholder + animation contract surface + spike gate criteria + licensing requirements + alternatives — dots-EA + 3D cut-ins only / low-poly mannequins before AI-generated chars / dots match + 3D replay moments — + open questions including asset-rights / rig-standard / retargeting fallback / minimum hardware / generated likeness risk / skin/body/age variation / trailer quality / stadium-crowd-scope decision); (2) ADR-0008 ShotPresentationContract / ViewerEvent (renderer-agnostic; supersedes ADR-0002); (3) ADR-0009 dots-phase render adapter (consumer of ADR-0008, hits dots-shippable polish bar). ADR-0002 marked Superseded-by ADR-0008/0009; original preserved per append-only ADR discipline. **SPEC adjustments:** Phase-3 viewer tasks rewrite to dots-prototype-with-polish-bar; Phase-5 pipeline-feasibility spike + multi-player-scene gate added; Phase-8 EA-launch contingency rewrites to the three-outcome model above (no dated 3D promise). **What does NOT change:** MatchSim architecture / event-sourced memory / modding contract / accessibility (reduce-motion still applies; adapter-aware) / content policy / banned-terms vocabulary / production pipeline / validation contract surface (extends, doesn't replace) / retention policy. Visual-target choice is a renderer-adapter decision; everything sim-side is unaffected.

- **2026-04-26** — **Balance-harness pre-seed: methodology-first, scale-agnostic internal score, warning-bands-not-hard-fails, hybrid tactical fuzzing (Phase-6 design doc owed).** Per GPT-5.5 Concerns review (5 P1 findings, 6 P2 findings) of Claude's balance-harness pre-seed for the 10K-season headless-sweep + FM-failure-mode-defense plan. New `design/balance-harness.md` design doc + ADR-if-architecture-bearing owed at Phase 6. Methodology locks: **(1) Internal performance score is scale-agnostic.** Balance-harness evaluates a normalized internal performance metric independent of the displayed rating scale. The 2026-04-25 centered-5.0 displayed-scale commitment stands at the UI layer; the internal metric the harness audits is unaffected by scale-choice — meaning if Month-3/4 observer feedback supersedes the displayed scale, the harness math doesn't change. **(2) Stratified attribute-correlation analysis only.** Raw correlation between gene fields and rating is invalid (physical traits correlate with role + minutes + team strength + tactic + league level → false "pace bias" alarms). Use stratified models by role-subtype, team strength, minutes, tactic, opposition quality. `NarrativeFlag` invariant = ZERO rating contribution (not "within X% category ceiling"). **(3) EventClass emission rates are scenario-banded, not "all-must-emit."** Rare / phase-gated / cup-specific / conditional-MVP events legitimately may not fire in a given sweep. Define expected-rate bands by scenario; "must-emit" only for core match events under fixtures designed to exercise them. **(4) Golden-balance corpus uses metric bands + seed manifests, NOT exact distribution hashes.** Balance tuning intentionally changes distributions; exact hashes would fight every legitimate tuning pass. Hash exact replay state for determinism (already done via `golden-replay-corpus.md`); for balance-corpus, use deterministic seed manifests + acceptable metric bands + versioned snapshots. **(5) Tactical fuzzing requires a legality grammar.** Random formation × roles produces nonsense and wastes compute. Define valid tactical schema; fuzz inside legal constraints. Hybrid strategy: legal structured grid for coverage + authored-archetype mutations for realism + evolutionary search for exploits (start from authored archetypes, mutate toward higher win-rates). **(6) Thresholds start as warning bands, not hard fails.** Per-category variance ceilings, win-rate exploit thresholds, archetype-dominance limits, role-rating parity bounds — all warning-only initially; promote to hard-fail invariants only after several sweeps establish baseline + confidence intervals. Win-rate exploit threshold uses Elo / team-strength normalization (raw 70% means nothing without strength normalization). **(7) Salience stays separate from ratings.** Memorable ≠ well-played. ADR-0004 salience scoring feeds memory-event surfacing, not balance-harness rating audits. **(8) Claude-assisted analysis consumes summaries + outlier reports + replay samples + suggested hypotheses, NOT raw 5M+ events.** Tier-C balance-harness emits compact JSON / CSV / Parquet summaries + seed manifests; the AI-tuning interface reads only the summaries. **Balance-corpus content (Phase-6 starter, ~5-10 configs):** equal-strength teams (parity sanity), weak-vs-strong counter-attacking (underdog tactical viability), physical-bias lab (same player + tactic, only physical genes boosted — bias detection harness), role-rating parity (scale-agnostic role-subtype audit; pairs with the IdentityPacket role-subtype schema-bump pre-seed), youth-development toy season (curve plausibility), known-exploit tactic (regression detection). **Methodology supplements:** metamorphic tests (name-swap shouldn't change results; cloned teams hover ~50/50); micro-sim lab (controlled matchups before 10K-season sweeps); shadow ratings (compute new rating model beside old before exposing); manager adaptation harness (test whether AI counters exploits over time). **Balance-harness scope coverage** explicit beyond the four headlines: economy + wages + financial fair play; home advantage modeling; late-season mental-pressure (cup-final chokes / relegation panic); referee/officiating variance; weather modeling; injury + fatigue + fixture congestion; transfer AI; promotion/relegation feedback loops. SPEC Phase-6 task replaces the vague "Balance harness produces 10K-season sweep; key distributions documented" with explicit `design/balance-harness.md` deliverable + the methodology bullets above as Phase-6 implementation contract. Phase-3 architectural floor preserved: MatchSim event stream + `Seed` derivation + tactical-config serialization must be balance-harness-friendly from day one (cheap to add; cheap to retrofit at Phase 6 only if MatchSim Week-1 work doesn't pre-empt the shape).

- **2026-04-26** — **Visual-target supersession correction pass (Codex review).** Clarifies the prior 2026-04-26 visual-target entry without changing the high-level pivot. MatchSim emits canonical sim events only; `Viewer.EventBridge` derives `ViewerEvent`s from ordered MatchSim events + pure `ShotTypeDefinition` DTOs projected from Unity `ShotTypeSO` assets. Renderer adapters consume `Viewer.Contracts` / `Viewer.Core`; they do not depend directly on `MatchSim.Contracts`. Golden replay corpus v1 stores adapter-keyed semantic `pass_activation_log_hashes` from day one (`"dots"` in Phase 3; `"celShaded3d"` only if ADR-0010 enters). Pixel output stays visual QA only, not replay-hashed. 3D animation markers depict already-emitted events; they never emit canonical MatchSim / MemoryEvent records. Vendor-agnostic means architecture docs avoid tool lock-in; SETUP / TOOLING / `design/3d-pipeline.md` may name candidate tools for license and cost tracking. Core docs updated to remove stale Phase-9/post-EA-only 3D subscription language: 3D spend starts only after the Phase-5 commercial-license audit + production-feasibility spike, and production subscriptions require spike-green outcome.

- **2026-04-28** — **Unity tech-stream `6000.4.4f1` chosen for Phase-3 bootstrap; LTS migration deferred to next 6000.0 LTS release.** The first `unity-project/` was created on the Unity 6 tech-stream release `6000.4.4f1`, not the strict LTS `6000.0.x` line. Rationale: (1) **Renderer-agnostic architecture absorbs the engine-version risk.** ADR-0008 `ShotPresentationContract` + ADR-0009 dots-phase render adapter mean MatchSim correctness is independent of the Unity engine version (canonical sim is pure C#, zero UnityEngine references); the Unity-side risk surface is the viewer-adapter layer + URP, both of which tech-stream supports earlier than LTS. (2) **Tech-stream gives latest URP / UI Toolkit / Editor UX** for the Phase-3 dots-prototype validation visual + Phase-4+ management-screen authoring, where iteration speed beats long-tail patch support. (3) **Trade-off accepted:** tech-stream has a shorter Unity-side patch-support window than LTS. We accept this for the Phase-3-through-Phase-6 stretch; the LTS migration becomes a planned Phase-7 task. (4) **Migration trigger:** re-evaluate at the next 6000.0 LTS release (~Q4 2026 / early 2027 per Unity's historical cadence — coincident with Phase 7 polish). Migration cost should be small if we stay on the 6000-stream and stick to URP-canonical APIs (no obscure tech-stream-only features used in production code paths). (5) **What is NOT changed by this decision:** the Phase-3 unity-project version pin (`ProjectVersion.txt = 6000.4.4f1`) governs all bootstrap/prototype work; engine-version changes go through a new SPEC entry with explicit migration plan; ADR-0008/0009 contracts stay engine-version-agnostic. Documented in CHANGELOG / STATUS / SPEC task text since 2026-04-28; logged here per Codex audit recommendation that engine-version choices belong in the append-only decisions log.

- **2026-04-28** — **Month-3 gate stays narrative-bearing; semantic slice tasks added to Phase 3 (Option A — keep the promise; tiny + fixture-driven).** Per Codex multi-agent audit P1-04 finding, the Phase-3 implementation path was on track to satisfy *kinematic* legibility (dots-on-pitch with deterministic movement / BT / ball physics) but NOT the *narrative* legibility required by the locked Month-3 observer rubric (*"a stranger watches a dots-phase match for three minutes and understands drama, momentum, and player identity"*). Decision: keep the locked gate language and add the missing semantic-slice deliverables to Phase 3 as fixture-driven minimal implementations rather than full Phase-4 systems. **Phase-3 semantic-slice scope (delta from prior Phase-3 task list):** (1) **22 hand-authored `IdentityPacket` JSON fixtures** — one per player on the two Month-3 archetype rosters. NOT the full `AI Content Compiler` pipeline (per ADR-0006, that lands in Phase 4); just structurally-shaped fixture JSON the validator round-trips, sufficient to exercise scout-prose templates + signature-affinity reads. (2) **3 active signatures from the locked catalog**: #20 *Low cutback from the byline* (winger), #22 *Blind-side near-post run* (striker), #13 *First-time diagonal switch* (CM) per the 2026-04-24 month-3-vertical-slice resolution. Implemented as deterministic event-emitting behaviors that surface as `ViewerEvent`s through the ADR-0008 contract. (3) **1 `MemoryEvent` reader callback** — minimum viable to demonstrate the event-sourced memory pattern. Recommend press/fan-callback flavor since it's lowest-friction; commentary-template overlay during a match minute satisfies the rubric criterion "stranger sees the team has history." (4) **1 persistent development event** — single-match version of the breakthrough-cinema pattern per `design/breakthroughs.md`; fires on a deterministic trigger condition; surfaces via `ViewerEvent` overlay at a designed beat. Post-match stat-card optional for Phase 3, deferred if scope-pressure hits. (5) **Canonical event stream + `Viewer.EventBridge` minimum implementation** — sufficient to derive `ViewerEvent`s from MatchSim's emitted events. Synthetic-fixture path lands first (per ADR-0009 §Polish-bar observer rubric), real-corpus-driven path second. **What stays out of Phase 3 per this decision:** the full content-compiler pipeline (Phase 4); full memory-system reader catalog (Phase 4-5); rating-system + TacticalPreset (Phase 4); per-player gene-driven kinematics tuning (Phase 4); the rest of the 24-signature catalog beyond the 3 above (Phase 4+). **Risk if Option A failed:** Phase-3 scope creep. **Mitigation:** the deliverables are explicitly fixture-driven, not full system implementations; if observer pool stress-testing shows the rubric is gettable with even fewer pieces, individual items can shed without re-superseding (the gate language is the binding contract, not this task list). **Linked SPEC tasks added below the active phase block.** Rejected alternative: Option B (narrow the gate to kinematic legibility + push identity/signature/memory to Phase 4) — would require superseding the locked 2026-04-24 month-3-vertical-slice resolution, weakens the Month-3 observer promise to "movement readability" only, and softens the Phase-4-promotion gate criterion. Not worth the de-scope at this point in Phase 3.

- **2026-04-28** — **Phase-3 minimal `PitchRules` / `MatchRules` layer added: field bounds, score, restart-state, key-event emission.** Per Codex multi-agent audit P1-05 finding. Current `MatchSimulationState` canonicalizes `Tick + Ball + 22 PlayerStates` (1188 bytes) but emits no score, no out-of-play state, and no goal/restart events — meaning the golden-replay-corpus spec's `final_score` + `key_event_hashes` fields cannot be populated, and the Month-3 viewer cannot show drama beats it doesn't know happened. Decision: add a minimal Phase-3 `PitchRules` layer that satisfies the corpus spec's Phase-3 expectations without bringing in fouls / cards / substitutions / stoppage (per the existing 2026-04-24 match-engine resolution, those stay deferred). **Phase-3 PitchRules scope:** (1) **Field bounds**: pitch is 105m × 68m, X+Z plane; goal lines at `X = ±52.5`; touchlines at `Z = ±34`. Coordinates already conventional (per BallPhysics + PlayerActuator). (2) **Goal-plane detection**: ball center crosses goal-line at `Y < 2.44` (crossbar height) and `|Z| < 3.66` (post-to-post half-width) → goal event. Deterministic; emits `GoalEvent` with scoring side, scorer player ID, scoring tick. (3) **Out-of-play state in `MatchSimulationState`**: enum {InPlay, GoalKick, ThrowIn, CornerKick} — Month-3 minimum; restart behavior is "ball respawns at standard restart spot for that out-of-play type, possession to opposite team" (heuristic; full set-piece authoring is Phase 4). (4) **Score state**: `(byte HomeScore, byte AwayScore)` in `MatchSimulationState`. Capacity for the Phase-3 max realistic score (~10-15 per side) without overflow concerns. (5) **`KeyEvent` record stream**: append-only deterministic list of `{Tick, EventKind, Side, PlayerId, Position}` for goals + restarts. Canonical-state encoding extends `MatchCanonicalState.Write` with KeyEvent-count + entries; the pinned 60-tick smoke hash gets re-baselined intentionally as part of this delta. (6) **`MatchRules.Step` orchestrator**: runs after `BallPhysics.Step` each tick — checks goal-plane crossings + touchline crossings, mutates `MatchSimulationState`, appends `KeyEvent`s. Pure-deterministic, integrator-style. **Test coverage:** at least one fixture exercising goal-plane detection, one exercising touchline-out detection (each side, each axis), determinism-replay over the new state, canonical-encoding extension for `KeyEvent` list. **Pinned hash refresh:** `MatchDeterminismTests` 60-tick smoke fixture re-baselines under the extended canonical state once `KeyEvent`-emitting code lands. The new pinned hash gets a CHANGELOG entry + `[x]` task closure note. **What stays out of Phase 3 per this decision:** offside (Phase 4); set-piece taker logic + wall-formation behavior (Phase 4); throw-in trajectory authoring (Phase 4 if Phase-3 heuristic feels too gamey to observers); fouls + cards (Phase 4 per match-engine resolution); substitutions + stoppage (Phase 4). **Linked SPEC tasks added below the active phase block.**

- **2026-04-28** — **Renderer locked at Forward for the dots-phase candidate; Forward+ stays a 3D-spike-conditional decision.** Per Codex multi-agent audit P2-05 finding — `TECH_APPROACH.md` claimed Forward+ as the locked render mode while `unity-project/Assets/Settings/UniversalRenderer.asset:m_RenderingMode = 0` (default Forward). Resolution: dots-phase doesn't need Forward+ (Forward+ is for many-light-source clustered shading; dots have minimal lighting + no shadow-stress scenes). Forward is the right Phase-3 default. Forward+ becomes a candidate decision IF the Phase-5 production-feasibility 3D-spike succeeds AND the cel-shaded 3D adapter's lighting model needs many-light support — at that point a new SPEC entry locks Forward+ for the 3D adapter only (the renderer-agnostic ADR-0008 contract allows different adapters to ship under different render-pipeline configurations). `TECH_APPROACH.md` to be amended in the same commit set: render mode is Forward for the dots adapter; Forward+ is conditional and gated on 3D-spike outcome.

- **2026-04-28** — **Phase-3 enforcement-skeleton rollout: `fw replay` Phase-3, save-migration fixture Phase-3 placeholder, content-pack validator Phase-6 full.** Per Codex multi-agent audit P2-09 finding — accepted specs (`golden-replay-corpus.md`, `save-migration-fixtures.md`, `content-pack-validation-contract.md`) name Phase-3 obligations that current SPEC + `scripts/fw` have left as stubs. Decision: rollout matches actual Phase-3 content-authoring need rather than spec-text aspiration. **(1) `fw replay <seed>` minimal compare**: ships in Phase 3 — runs `MatchSimulationRunner` with the seed, computes `MatchCanonicalState` hash at fixture-defined tick count, compares against checked-in fixture JSON; `--compare-corpus` flag against `MatchSim.Tests/fixtures/replay-corpus/<seed>.json`. First fixture is the smoke seed `0xdeadbeefdeadbeef` per corpus spec. Wires into `fw verify` only when runtime stays inside the Tier-A budget. **(2) Save-migration fixture skeleton**: ships in Phase 3 as a placeholder fixture-path under `MatchSim.Tests/fixtures/save-migration/` — first real fixture lands when `MemoryEvent` schema first ships (Phase 4), per the 2026-04-24 Phase-2 ADR plan. Phase-3 deliverable is the directory + the `.gitignore`-exempt sentinel + the `fw save-migration-test` CLI stub that exits 0 when no fixtures present. **(3) Content-pack validator skeleton**: stays Phase 6. Phase-3 doesn't author content packs (24 signatures + 50 phenotype labels + 10 IdentityPacket fixtures all live in code/JSON-in-MatchSim, not a Workshop-style external pack), so no validator runtime need until Phase 6 fixture authoring. The accepted contract spec at `design/specs/content-pack-validation-contract.md` stays unchanged; the implementation timeline is what's being clarified. **Linked SPEC tasks added below the active phase block.**

- **2026-04-28** — **Tier-A CI policy clarification: Linux-only baseline; deterministic-core dotnet runs on cross-platform matrix as explicit carve-out under <5min budget.** Per Codex multi-agent audit P2-08 finding — `ADR-0003` + `design/production-pipeline.md` describe Tier-A as Linux-only with ≤5 min ceiling, but `.github/workflows/fast-pr-ci.yml` runs the full `fw verify` on Ubuntu / Windows / macOS. Resolution: the matrix is correct + load-bearing (cross-platform determinism is the whole point of Q32.32 fixed-point) but the ADR/design-doc text needs to make the carve-out explicit so future audits don't re-flag. **Tier-A CI policy after this clarification:** baseline general checks (verify-docs / banned-terms / generic build) remain Linux-only ≤5 min; deterministic-core dotnet test suite (`MatchSim.Tests`) runs on Ubuntu + Windows + macOS as an explicit carve-out, justified by the determinism contract being the floor invariant the entire game depends on. Matrix expansion BEYOND `MatchSim.Tests` (e.g. adding Unity build-validation to Tier A, or adding dotnet test runs across more SDKs) requires a new SPEC decision. ADR-0003 + `design/production-pipeline.md` to be amended in a follow-up commit to land this carve-out language.

- **2026-04-29** — **MatchSim consumption strategy: DLL drop into `unity-project/Assets/Plugins/MatchSim/` via `scripts/fw build-unity-plugins`; UPM local package deferred to Phase 4 if dev-loop friction emerges.** Closes the open architectural question logged in STATUS.md after the Phase-3 unity-project bootstrap. Decision: Unity-side code will reference MatchSim types via a checked-in DLL drop at `unity-project/Assets/Plugins/MatchSim/`, populated by a new `scripts/fw build-unity-plugins` command that runs `dotnet publish MatchSim/MatchSim.csproj -c Release` and copies `FinalWhistle.MatchSim.dll` + `FinalWhistle.MatchSim.pdb` + `YamlDotNet.dll` into the plugin folder. Unity-generated `.meta` files for those DLLs check into git alongside (per the same reproducibility floor that closed P1-03). Rationale: (1) **Unity has no native "rebuild project DLLs at Editor open" workflow** — without an Editor-side orchestration script (which would itself be Phase-4-grade scope), the DLLs need to either ship checked-in or be a pre-Editor-open user-action; checked-in is simpler, avoids "I forgot to rebuild before opening" friction, and matches the existing pattern for `Assets/Plugins/Editor/Roslyn/` (third-party DLLs we vendor, also checked in). (2) **`scripts/fw build-unity-plugins` is the explicit rebuild gate** — every MatchSim source change runs through this command before commit; combined with the existing `dotnet test` + `fw verify` umbrella, the dev loop stays "edit C# → fw verify → fw build-unity-plugins → commit". (3) **YamlDotNet (NuGet dep) ships as part of the drop** — `dotnet publish` (NOT `dotnet build`; build doesn't copy transitive deps for class libraries) produces `YamlDotNet.dll` alongside MatchSim's own DLL, so Unity can resolve the BT-archetype YAML loaders at runtime. (4) **Phase-4 revisit trigger**: if dev-loop friction emerges (specifically: forgetting to rebuild before commit; a CI failure mode where Unity-side referenced API doesn't match latest MatchSim), pivot to UPM local package (`unity-project/Packages/manifest.json` adds `"com.finalwhistle.matchsim": "file:../../MatchSim"` + `MatchSim/` gains `package.json`). UPM local would let Unity's Roslyn workflow pick up MatchSim source directly — no DLL drop needed — but introduces ScriptCompilation-ordering complexity + requires Unity 6 + a UPM-compatible netstandard2.1 layout. Not worth the Phase-3 setup cost; revisit when actual friction appears. (5) **What stays in MatchSim solution layout**: nothing changes. `MatchSim.csproj` continues to target `netstandard2.1` (per CLAUDE.md tech-stack lock + 2026-04-28 decisions-log entry on Unity 6 tech-stream); `MatchSim.Tests.csproj` targets `net10.0` for modern xUnit test-host compat. The DLL drop is purely a Unity-side consumption mechanism, transparent to MatchSim's own build/test loop. (6) **What ships under `Assets/Plugins/MatchSim/`**: `FinalWhistle.MatchSim.dll` + `FinalWhistle.MatchSim.pdb` (managed-PDB for real stack traces in Editor + player builds) + `YamlDotNet.dll` + their Unity-generated `.meta` files. Skipped from copy: `.deps.json` (runtime-host manifest; not used by Unity) + `.xml` (XML doc comments; ~87KB bloat per-DLL; revisit if Editor tooltips become valuable). (7) **CI freshness check activated 2026-04-30**: Codex round-4 review showed the stale-DLL risk was already real enough to gate now, not Phase 4. `fw verify-unity-plugins` publishes via the same stable relative project/PDB path as `fw build-unity-plugins` and byte-diffs `FinalWhistle.MatchSim.dll`, `FinalWhistle.MatchSim.pdb`, and `YamlDotNet.dll` against the checked-in plugin drop inside the Tier-A `fw verify` umbrella.

- **2026-04-30** — **Codex round-4 review-driven hardening sweep — six fixes spanning fresh-clone reproducibility, decisions-log enforcement, sub-ULP boundary correctness, restart-semantics framing, EventBridge-purity asmdef boundary, and plugin-freshness verification.** Closes the Codex P1/P1/P2/P2/P2/P2 set logged 2026-04-30 against HEAD `9f762ec`. (1) **Fresh-clone embedded-resource fix**: `MatchSim/MatchSim.csproj` now pins `<EmbeddedResource>` `LogicalName="FinalWhistle.MatchSim.Content.archetypes.%(Filename)%(Extension)"`. Prior wildcard include relied on default MSBuild auto-naming, which flattened the relative path to `FinalWhistle.MatchSim.<name>.yaml` — local stale `obj/bin` masked the mismatch; a fresh clean-copy verification that excludes `bin/obj` now lands 501/501 instead of 472/500. (2) **Decisions-log removal made commit-blocking**: `.claude/hooks/protect-decisions-log.sh` PreToolUse hook now reads the on-disk SPEC pre-image and blocks any Write whose post-image would drop existing decision bullets; `.claude/hooks/validate-commit.sh` Check 3 promoted from non-blocking warning to BLOCKING (exit 2) when staged SPEC.md drops bullets present in HEAD. The earlier framing — "PreToolUse only inspects proposed content; commit-time only warns" — let a wholesale-rewrite Write slip through both layers. (3) **Exact-rational crossing comparison**: `MatchSim/Sim/MatchRules.Step` no longer compares Q32.32-divided `Fixed` t values for goal-line vs touchline crossings; instead computes `(absNumeratorRaw, absDenominatorRaw)` from raw long arithmetic and cross-multiplies via `BigInteger`, producing an exact ordering even when both rationals fall within the same Q32.32 ULP. New regression test `Step_DiagonalTouchlineFirst_SubUlpTouchEarlierThanGoal_EmitsThrowIn` constructs a sub-ULP collision (rounded `Fixed` t values exactly equal but exact rationals differ) and asserts ThrowIn — the prior rounded-compare implementation would have tied and emitted GoalKick. (4) **Restart semantics framed as event-only placeholders**: `MatchRules` doc-comment + `MatchSimulationRunner` doc-comment now state explicitly that the `KeyEvent.Side` recorded on `GoalKickRestart`/`ThrowInRestart`/`CornerKickRestart` is informational; the runner does NOT consume any restart-control state on the next tick (no possession lock, no taker assignment, no BT suppression). Phase 4 introduces real possession-lock + taker behavior. The docstring lock prevents future readers from inferring gameplay-authoritative semantics from the recorded side. (5) **EventBridge purity enforced at asmdef boundary**: `Viewer.EventBridge` SPEC-locked to live under `unity-project/Assets/Viewer/Contracts/` (`Viewer.Contracts.asmdef` already ships `noEngineReferences:true`). The compiler refuses any `UnityEngine.*` import at the source level, making determinism a build-time guarantee rather than reviewer discipline. Codex's concern was that Bridge living under `Viewer.Core` would inherit UnityEngine access; lock prevents drift. (6) **Unity plugin freshness gated by `fw verify`**: new `scripts/fw verify-unity-plugins` subcommand runs `dotnet publish` to a tempdir and diffs the produced DLLs against the committed `unity-project/Assets/Plugins/MatchSim/` bytes; mismatch → exit 2. The publish path is deliberately repo-root-relative and passes a relative `PdbFile` MSBuild property so fresh clones do not embed absolute checkout paths in the DLL's CodeView record. Wired into `fw verify` umbrella between `shader-audit` and `test`. **Smaller findings swept in same commit**: (a) `MatchRules.HandleTouchlineCrossing` uses `KeyEvent.JerseyUnspecified` instead of literal `0`; (b) `STATUS.md` plugin-install drift reconciled (already aligned 2026-04-30 process-enforcement sync; sweep confirms); (c) Viewer asmdefs ship `autoReferenced:false` for explicit graph discipline; (d) `IsInField` boundary semantics doc-locked (boundary line is OUT for Phase 3 simplification — football lines are nominally IN, but the center-point-only check + Phase-3 GoalKick-everything classification makes the boundary distinction observationally invisible; revisit Phase 4 with last-touched + true CornerKick); (e) `com.unity.multiplayer.center` removed from `Packages/manifest.json` (no current Phase-3 use); (f) `KeyEvent.GetHashCode` flagged as non-canonical for future Phase-4+ deterministic-grouping audit; (g) ProBuilder ProjectSettings remnants cleaned; (h) `global.json` SDK feature-band roll-forward narrowed (`latestFeature` → `latestPatch`) to tighten reproducibility floor under committed DLL/PDB.

- **2026-04-30** — **Football rules matrix authored as `design/specs/football-rules-matrix.md` — Phase-3 guardrail spec consolidating MatchSim football-law simplifications.** Per Codex round-4 follow-up plan: scattered `MatchRules.cs` doc-comments documenting which football laws Phase 3 simplifies / defers were making it hard for any reviewer to answer the question "what football do we model TODAY?" without reading the whole codebase. The new matrix at `design/specs/football-rules-matrix.md` consolidates that into 16 rule-surface rows (goals / touchlines / goal kicks / corners / throw-ins / restart authority / kickoffs / offside / fouls / cards / substitutions / injuries / stoppage / advantage / penalties / GK handling), each pinning real-football intent + current Phase-3 behavior + simplification + player-visible risk + canonical impact + tests owed + promotion trigger. **Locked decisions** introduced by the matrix: (1) **Matrix before expansion** — any new MatchRules / PitchRules rule surface MUST add or update a matrix row before its implementation can be marked done in SPEC. (2) **Phase-3 simplifications allowed but named** — Month-3 gate prioritizes legible continuous play; offside / fouls / cards / subs / stoppage stay out unless a new SPEC entry supersedes this. (3) **Player-visible restarts need a contract** — if the viewer can show a restart, the matrix must say whether gameplay authority matches the displayed restart or whether the displayed side is a placeholder ahead of last-touched tracking. (4) **Canonical impact is explicit** — rows mark whether a rule mutates `MatchCanonicalState`, `KeyEvent`, replay hashes, or is presentation-only. (5) **Football lines treated deliberately** — matrix states whole-ball-over-line vs ball-center approximation per surface (Phase 3 is ball-center + strict-`<` boundary). (6) **Promotion triggers are binding** — when a trigger lands, the simplification cannot remain hidden in code comments; it becomes a Phase task or ADR/spec update. **What stays out of scope of this entry**: implementation changes to `MatchSim/Sim/MatchRules.cs`. The matrix is doc-only authority; implementation moves through separate SPEC tasks (foundation #4 + Phase-4 fouls / set-pieces / possession). **Cross-links**: `design/README.md` index updated; `match-engine.md` / `month-3-vertical-slice.md` / `golden-replay-corpus.md` referenced from the matrix as the surrounding system contracts. **CHANGELOG entry**: 2026-04-30 (Football rules matrix spec). The matrix is intended to be the first stop for any future Codex / Claude review touching MatchRules / PitchRules / Viewer.EventBridge restart-presentation.

- **2026-04-30** — **USP #4 honesty pass: "Unbounded RPG progression" replaced with "Breakthrough-driven player development with soft caps + rare narrative exceptions" framing.** Per Codex round-4 follow-up plan, commit #5 of 6. The 2026-04-22 framing promised more than the bounded gene model + balance-harness can defensibly deliver: gene fields are 0.0–1.0 clamped, per-role soft-cap distributions are the steady-state, and "unbounded" reads as marketing copy that the implementation never matches. New framing locks the load-bearing claim — players grow because of what HAPPENED to them via the breakthrough-flag mechanism in `design/breakthrough-moments.md`, not because growth has no ceiling. Most players hit a soft cap appropriate to their role distribution; rare players with appropriate narrative-flag triggers (`peak_ceiling_high` etc., per `design/player-generation.md`) cross those ceilings, but not arbitrarily. **Implementation owings**: (a) `CLAUDE.md §1` USP #4 reworded inline (not the decisions log — the marketing-claim text edits in place per the docstring's nature); (b) `design/progression.md` Phase-4 owed task added — co-authored alongside the Phase-4 gene-model + identity-packet-compiler work so the doc reflects shipped code rather than aspirational text. **What is NOT changed by this decision**: the 24-signature catalog locked 2026-04-22 + the breakthrough-cinema pattern in `design/breakthrough-moments.md` + the bounded-gene-model design from `design/player-generation.md`. The de-promise is purely a framing fix; nothing in the actual mechanic surface changed.

- **2026-04-30** — **Anime-coded presentation budget locked as `design/anime-presentation-budget.md` placeholder spec. Authored 2026-04-30 per Codex round-4 follow-up plan + audit-07 P0 anime-coded-gameplay-drift finding (8-agent triage 2026-04-30).** Eight anime-inflected presentation surfaces named as the Phase-3/4 budget the dots adapter ships with so anime-curious players see the genre signal even if the 3D adapter slips per the Phase-5/6 production-feasibility spike outcome: (1) impact frames; (2) screen-tone + motion-line variants; (3) signature title-card + cut-in language; (4) camera rhythm; (5) pressure indicator; (6) aftermath-freeze emphasis; (7) signature-specific commentary cadence; (8) UI-Toolkit overlay typography rhythm. **Explicit no-list locks**: mid-match QTE HARD-BANNED (per `design/breakthrough-moments.md` football-correctness mandate); capitalized state-nouns (`The Hush` etc., per `design/ui-vocabulary.md` Category A.1); screen-shake-on-every-collision; speed-line-spam-outside-signature-executions. Phase-3 minimum cells per surface ship for the Month-3 gate (impact frames on `pass-shot-impact`, signature title-cards on the 3 active signatures, three-tier camera rhythm, stadium-tone pressure indicator, post-goal aftermath freeze). EA (Phase 8) ships all 8 surfaces; the 3D adapter inherits the same budget if spike-green. The placeholder spec locks the SHAPE of the budget; full per-surface implementation specs land alongside Phase-3 Viewer.EventBridge + Phase-4 dots-adapter polish-bar work. Cross-linked from `design/README.md` index. Surfaces not on the eight-row list require a new SPEC entry to add.

- **2026-04-30** — **CLAUDE.md §6.3 mandatory rotation table; `/next` discipline gate sharpened.** Per Codex round-4 follow-up plan, commit #6 of 6. Audit-06 caught that 8 of 15 project agents had ZERO invocations across 36 agent events (`qa-lead`, `lead-programmer`, `art-director`, `narrative-director`, `gameplay-programmer`, `engine-programmer`, `ui-programmer`, `unity-ui-specialist`); `feature-dev:code-architect` was never invoked despite design-bearing work; all 5 Unity skills were inactive. The capability under-utilization was the highest-leverage triage finding because activating any of these tightens future-cycle dynamics. **Resolution**: a 10-row mandatory rotation table now lives in `CLAUDE.md §6.3` mapping task classes → required agent(s) (MatchSim code → gameplay/engine-programmer; Unity/Viewer → unity-specialist + unity-check; Contracts/asmdefs/ADRs → lead-programmer + feature-dev:code-architect; Narrative/identity → narrative-director; Systems/balance → systems-designer; Tests-heavy → pr-review-toolkit:pr-test-analyzer; Architecture/design-docs → director-of-discipline; Codebase exploration → feature-dev:code-explorer; Cross-discipline coordination → producer; etc.). **`/next` discipline gate** updated in `.claude/commands/next.md` step 6: must NAME both the classification AND the required agent(s) before any code is written; skipping a row's mandate requires an explicit one-liner in the commit body. **First-session directive (CLAUDE.md §8 step 1)** updated to surface the rotation table at the top of the onboarding read. **What is NOT changed**: the §6.3 prose mandate (delegation discipline framing) is preserved; the rotation table FORMALIZES what was previously prose. The pr-review-toolkit hook-reminded posture stays as-is. Process discipline reinforced by reminder + rotation-table mandate, not full enforcement (per the established Codex audit 2026-04-30 honest framing).

- **2026-04-30** — **Month-3 observer-pool recruitment promoted to active SPEC task. 5 cold football-literate observers + 2 backups must be named in STATUS.md.** Per audit-07 P0 finding (8-agent triage 2026-04-30): observer recruitment is gate infrastructure, not polish — the Month-3 gate language is the binding contract. `design/month-3-vertical-slice.md §Observer-pool lockdown fallback` says "name 5 by end of Month 2"; Month 2 is past with no named observers. Decision: recruitment now lives as an explicit Phase-3 SPEC task (added below Phase-3 active block) with the named-list living in STATUS.md `Pending async`. Locked criteria: observers must be football-literate, not previously briefed on Final Whistle pillars, willing to watch a 3-minute dots-phase match and answer the rubric questions per `design/month-3-vertical-slice.md`. Sources: trusted friends / Discord / private itch keys. If 5+2 observers can't be named by the time the dots adapter ships its Phase-3 minimum, the gate is at risk and a new SPEC decision must adjudicate (delay-the-gate vs run-with-fewer-observers vs pivot-to-a-different-rubric). Recruitment is user-action.

<!-- ui-lint:ignore-end -->

---

*Authored 2026-04-22. Updated every completed task (checkboxes) + every new decision (log, append-only).*
