# Final Whistle — Decisions log

Append-only. The PreToolUse hook at `.claude/hooks/protect-decisions.sh` rejects any edit that mutates a line matching `^- \*\*\d{4}-`. To change a prior decision, append a new entry that supersedes it, citing the prior bullet verbatim.

Format: `- **YYYY-MM-DD** — <topic>: <decision>. Context: <why>. Supersedes: <prior bullet date + topic verbatim, or "none">.`

Architectural detail of each decision lives in its ADR under `docs/adr/`. This log is the meta-pointer + the audit trail for meta-decisions that don't fit any single ADR.

---

- **2026-05-13** — Retract the LoC budget: implementation scope is bounded by determinism contract + maintainability + pillars, not by LoC counts or developer-hours. Context: the "~3000 LoC for fw-match-sim" framing I'd been propagating wasn't in the original CLAUDE.md — it was an extrapolation from the "solo-dev shippable" wording that hardened into LoC-as-scarcity thinking, then bled into research-agent prompts as an explicit budget. Several research recommendations got boxed by it (VAEP ruled out partly for budget; FSM-with-large-state-taxonomies ruled out for "2,141-line state files don't fit"; attribute count capped at 32; influence map sized to 16×12 to fit budget). Target depth is Football Manager-class or beyond, in a procedurally-generated world. Pillar constraints all remain. Supersedes: none.

- **2026-05-13** — ADR-0001 Match engine architecture (status: Proposed, pending Codex audit): 7-layer stack at `docs/adr/0001-match-engine-architecture.md`. 60Hz integration tick, 8Hz staggered per-player decisions, 5-state tactic FSM event-driven plus 2Hz heartbeat, 32×24 influence map regenerating at 8Hz aligned with decisions. Context: composed across sports-sim research wave 1 (FM 4Hz baseline + reactive interrupts) and existing-rust-sims wave 2 (ZOXEXIVO 100Hz baseline, OFM minute-event baseline rejected). 60Hz is the sweet spot for football's ball-physics + signature-event resolution; ZOXEXIVO's 100Hz is unnecessary precision; FM's 4Hz is too coarse for ball events. Supersedes: none.

- **2026-05-13** — ADR-0002 Player attribute model (Proposed): 55-attribute schema at `docs/adr/0002-player-attribute-model.md`. 38 visible (14 technical / 10 mental / 8 physical / 6 goalkeeper-specific, all on a flat struct so BT decision sites never branch on role) + 17 hidden (14 personality + 3 durability). All values `Q32` in `[0, 1]`. CA derived, PA stored. PA mutable only via `MemoryEvent::Breakthrough` — the architecture that makes pillar 3 ("growth via ledger") real. Five separate condition layers on a `PlayerCondition` struct (form / morale / match_fitness / sharpness / signature_readiness). Role affinity weights live in content-pack RON, not hardcoded. Scout uncertainty is FOF-style ranges keyed to scout skill (single canonical truth + projection layer, not OOTP-style parallel tables). Context: research wave 1 recommended 24+8=32 under the (now-retracted) LoC budget; FM-class is the right target. Supersedes: the prior "24 visible + 8 hidden = 32" recommendation in `docs/research/sports-sims/07-player-attributes-progression.md` (was LoC-budget-driven; retracted today via the LoC-budget retraction entry above).

- **2026-05-13** — ADR-0003 Decision-utility math primitives (Proposed): closed-form Q32 math at `docs/adr/0003-decision-utility-math.md`. xG as 6-feature logistic with a shared 257-entry symmetric sigmoid LUT in `fw-core::math::sigmoid_q32`. xT as a 16×12 Q32 LUT baked from a hand-authored Bellman fixed-point transition matrix in `crates/fw-content/src/xt/transitions.ron` (mod-overridable). Pitch control as Spearman closed-form, queried per-decision-point only (4 canonical sites). Pressing trigger as Bauer-and-Anzer 5-second rule. Personality bias is multiplicative per consideration with a locked 7-consideration × 8-vector mapping table. Tie-breaking is top-3 softmax with `T ≈ 0.15`, seeded by `seed_fn(match_seed, tick, decision_id)` — reduces to argmax at `T→0` for tests. Coefficient values live in `docs/design/*.md` per the "tuning out of SPEC" rule. Context: VAEP ruled out for determinism (gradient-boosted models = non-deterministic across platforms = breaks pillar); StatsBomb-data-fit ruled out for procedural-fantasy pillar (no real-world ground truth to fit against in fantasy worlds). Supersedes: none.

- **2026-05-13** — ADR-0004 IPC command surface (Proposed): live-match quintet + 9-variant MatchCommand at `docs/adr/0004-ipc-command-surface.md`. Commands: `start_live_match`, `step_live_match(handle, ticks)`, `get_match_snapshot(handle)`, `apply_match_command(handle, MatchCommand)`, `finish_live_match(handle)`. Handle-based dispatch via `BTreeMap<u32, LiveMatchSession>` in `AppState` — unblocks T2-5 concurrent matches. Frame streaming via Tauri events at ~10Hz with PixiJS interpolation to 30fps; scoreboard polling at 1Hz. Diagnostic mode is a parallel `match:diagnostic-frame` event channel gated by `fw-tauri/diagnostic` feature flag + runtime toggle. Async on the IPC quintet only; sim crates stay sync (firewall). TS types hand-mirrored at `frontend/src/lib/types.ts`; drift caught by `insta`-snapshot smoke test in `crates/fw-tauri/tests/dto_schema_smoke.rs`. Folds in Codex Imp #10 (src-tauri command consolidation): `src-tauri/src/commands.rs` deleted entirely at T1-5; `fw-tauri::generate_invoke_handler()` exported. Context: command shape confirmed by openfootmanager's identical surface. Supersedes: Codex Imp #10 (src-tauri command consolidation), previously deferred at T0-12, now scheduled into T1-5.

- **2026-05-13** — ADR-0005 Memory ledger + breakthroughs (Proposed): 14-field MemoryEvent schema + 28-event catalogue at `docs/adr/0005-memory-ledger-and-breakthroughs.md`. Both `stakes` and `salience` are `Q32` (not f32 as DESIGN_DOC §7 implied). Three decay functions (Never / Linear / Exponential), applied at read-time projection only; ledger is never mutated. Five readers (Salience / Press / Fan / Scout / Coach) with explicit cadences. Breakthroughs require all three: per-(player, attribute_family) `signature_readiness` threshold + narrative-gating event + per-family cooldown — PA redraws upward, partial CA lift via `ca_lift_fraction`. Regressive collapse is the symmetric mechanism (`regressive_pressure` ticker; PA down to a career-floor; reversible only via subsequent breakthroughs). Mod compatibility via `UnknownEventClass { tag: ModEventTag, payload: Vec<u8> }` variant — opaque bytes participate in canonical BLAKE3 hash but are ignored by core readers. Context: pillars 2 and 3 both load-bearing here; research wave 1 (RimWorld memory-as-decaying-thoughts; CK3 hidden traits) informed the decay + bias-vector patterns. Supersedes: DESIGN_DOC §7's `stakes: f32` and `salience: f32` typing (architecturally wrong, would have broken cross-OS canonical hash if implemented as written).

- **2026-05-13** — ADR-0006 BT vs FSM for per-player decision layer (Proposed): hybrid **FSM-of-Behavior-Trees** at `docs/adr/0006-bt-vs-fsm-decision-layer.md`. Outfield roles (defender / midfielder / forward) each have a flat enum of ~6-10 coarse role states; each state owns a small BT (~10-30 leaves) that runs per tick. Goalkeeper is pure FSM, no inner BT — GK behavior is mode-dominated. Role-state transitions are short priority-ordered `(predicate, target_state)` lists evaluated at dispatch. Universal pre-emption hooks are defined once at the dispatcher (single-chaser claim, foul reaction, set-piece switchover) — this fixes ZOXEXIVO's 2,141-line state-files problem. Utility-scored selector nodes are a first-class BT node type fired at on-ball decisions; personality bias multiplies in at utility scores + probabilistic BT decorators, never into role-state transitions. Authorship split: role-state catalogue + pre-emption hooks + BT leaf primitives are Rust in `fw-match-sim`; the BT subtrees themselves are content-pack RON (schema-versioned, FW-VAL'd). Nodes are code, trees are data. Context: pure-BT was the research-wave recommendation under the (now-retracted) LoC budget; reframe reopened the question on architectural merit. Hybrid keeps FSM's debuggability + BT's composability + Halo-style data-driven authoring. Supersedes: the pure-BT layer-2 recommendation in `docs/research/sports-sims/00-synthesis.md` (was budget-driven; retracted today).

- **2026-05-13** — ADR-0007 Dev verification surface (Accepted; design ratified by MASTER_PLAN T1-2a / T1-4 / T1-9): three-layer formalization at `docs/adr/0007-dev-verification-surface.md`. Layer 1 = diagnostic commentary per significant event (T1-4). Layer 2 = dev-tier 2D tactical board, always available, lands T1-2a. Layer 3 = behavioral proptest invariants + pair-seed knob-isolation tests (T1-9). The canonical-hash regression sits orthogonal as bedrock — not a layer in this stack. Two T2 candidates surfaced but not rowed yet: OOTP-style stat-distribution CI gate (KS-test against pinned reference distributions), EHM-style two-engine cross-check (lean Dixon-Coles closed-form as calibration reference). Both flagged for the next MASTER_PLAN update touching T2. Context: formalization of the design doc at `docs/design/dev-verification.md`, ratified by being in MASTER_PLAN T1 rows. Supersedes: none.

- **2026-05-13** — ADR-0008 Browser-dev mode for the 2D tactical board (Proposed): the dev-tier 2D tactical board (T1-2a, per ADR-0007 Layer 2) reads frames via a `FrameSource` trait with two impls — `TauriFrameSource` (default, IPC) and `HttpFrameSource` (browser-dev, reads JSON fixture file). URL param `?source=fixture:/path.json` switches modes. A new binary `crates/fw-match-sim/src/bin/dump_frames.rs` produces deterministic fixture JSON for any seed. Unlocks Claude-driven visual verification via the Claude Preview MCP — Claude can `preview_start http://localhost:1420/dev/board?source=fixture:/tmp/frames.json` + `preview_screenshot` to verify "is this football?" without human-in-the-loop. Detail: `docs/adr/0008-browser-dev-mode-tactical-board.md`. Context: ADR-0007 Layer 2 currently requires a Tauri runtime to view, which Claude (as a dev collaborator) can't access — browser-dev mode complements (doesn't replace) the Tauri IPC live path. Supersedes: none.

- **2026-05-13** — Superpowers TDD skill mandate from T1-2b onward: the `superpowers` plugin's TDD skill (RED-GREEN-REFACTOR cycle) MUST be invoked at the start of each implementation chunk for **T1-2b and all subsequent phases working on real behavior code** (sim, memory, replay, save, content-runtime modules). T1-1 (schema + content-pack RON authoring) is exempt as data-only work. Mechanical refactors (renames, fmt fixes), STATUS/CHANGELOG/MASTER_PLAN sync writes, and doc-only edits are also exempt — exemption requires a one-liner in the commit body. Context: the `superpowers` plugin was added to Claude Code at the user's direction on 2026-05-13; its TDD skill enforces test-first authorship and is the highest-leverage feature for the upcoming match-engine work where "just start coding" is the dominant failure mode. The mandate is encoded in `.claude/skills/next/SKILL.md` Step 4. Supersedes: none.

- **2026-05-13** — Codex full-project audit + Tranche 2 remediation: read-only multi-agent Codex audit (`docs/audits/codex-full-audit-2026-05-13.md`) returned ~50 findings (1 P0 + 11 headline P1 + ~30 lower). P0 (bedrock-test `#[ignore]` hole) fixed at `eb0b952e` with three-layer guard. Tranche 2 (`bf439f7`) tightened T1-1 schema: `AbilityCeiling::try_new` validates ranges + invariant; `VISIBLE_ATTRIBUTE_NAMES` split from `KNOWN_ATTRIBUTE_NAMES` (CA-weights validate visible-only per ADR-0002 §"Choices" item 6); `Q32Inner` re-export removed + field tightened to `pub(crate)`; `RoleId::try_new` replaces `debug_assert!`-only validation; buildup_speed range references constants. Supersedes: none.

- **2026-05-13** — ADR-0001 + ADR-0003 amendments (Codex full-project audit P1): (a) per-player decision runner cadence 8 Hz → 4 Hz (60/8 = 7.5 wasn't the claimed clean 15-tick window; 60/4 = 15 IS clean and matches FM's reference baseline; responsiveness carried by reactive interrupts at 60 Hz). (b) Personality bias vector size 8 → 14 hidden attributes in both ADRs (mirrors ADR-0002's 14-field PersonalityVector; the "8-element" claim silently contradicted ADR-0002). The 7-consideration × 14-vector mapping table is now a Tranche 4 deliverable at `docs/design/personality-bias-weights.md`. Supersedes: prior ADR-0001 §"Cadence rationale" 8 Hz pick + prior ADR-0001 layer-4 + ADR-0003 §5 "8-element hidden vector" claims.

- **2026-05-13** — ADR-0005 amendment (Codex full-project audit P2): event class catalogue count reconciled to 30 (was inconsistently 29 vs 28 across the doc; `Compaction` system event was referenced in prose but absent from the enum). Added `Compaction` as the sixth thematic group's sole "system" variant. Supersedes: prior ADR-0005 §"Event class catalogue" 29-entry header + §"Mod-overlay compatibility" "28 core variants above" comment.

- **2026-05-13** — ADR-0009 RNG seed derivation (Proposed): canonical `seed_fn(match_seed: u64, tick: u32, layer: SeedLayer, site: u32) -> u64` at `docs/adr/0009-rng-seed-derivation.md`. BLAKE3 over 17-byte buffer, truncated to u64 LE. Eight `SeedLayer` discriminants (`Decision` / `UtilityTieBreak` / `ReactiveInterrupt` / `BallPhysics` / `SignatureTrigger` / `MemoryEvent` / `ScoutObservation` / `ContentBake`). Site is the per-layer disambiguator. Resolves Codex audit P1 "RNG seed contract inconsistent across 4 docs" (DESIGN_DOC + ADR-0001 + ADR-0003 + ADR-0006 cited different tuples). DESIGN_DOC + the 3 ADRs updated to the canonical signature in the same pass. Supersedes: the 3-tuple `(match_seed, tick, event_id)` in DESIGN_DOC §"Determinism contract" + DESIGN_DOC §"Determinism contract" lines; the 4-tuple `(match_seed, tick, layer_tag, decision_id)` in ADR-0001; the implicit `seed_fn(match_seed, tick, decision_id)` in ADR-0003; the 3-tuple in ADR-0006.

- **2026-05-13** — ADR-0010 Save format (Proposed): bincode 2 + zstd compression level 3 + forward-only migrations at `docs/adr/0010-save-format.md`. On-disk format: `[magic "FWS1"][version u32][zstd(bincode2(SaveBundle))]`. Mod-load fingerprint stamped via `BLAKE3(sorted (mod_id, mod_version))`. Per-bump 4-test contract per `docs/specs/save-migration-fixtures.md`. bincode 1 → 2 workspace migration happens during T2-9. Supersedes: prior folklore in CLAUDE.md / MEMORY.md about "bincode 2 for save format" — now ADR-backed.

- **2026-05-13** — ADR-0011 Signature system (Proposed): 24-signature catalogue (3 per role × 8 role families) at `docs/adr/0011-signature-system.md`. `SignatureDefinition { TriggerPredicate, SimBiasSnapshot, PresentationRecipe, CooldownPolicy }`. Per-player affinity via `PlayerTemplate.signature_candidates: Vec<SignatureCandidate>` (Tranche 4 schema change at T1-3 — carry-forward from v1's IdentityPacket.SignatureCandidates). Softmax dispatch via `SeedLayer::SignatureTrigger`. Stacking allowed across category boundaries; cooldown per signature per player. Counterplay via defensive signatures with cancellation predicates. Supersedes: ADR-0001 layer 1's bare "SignatureKind" reference + v1's `MatchSim/Sim/SignatureRules.cs` design source.

- **2026-05-13** — ADR-0012 Pinned-hash rebaseline policy (Proposed): four legitimate rebaseline triggers (canonical schema bump / encoder change / documented sim-behavior change / cross-OS divergence repair) at `docs/adr/0012-hash-rebaseline-policy.md`. Solo-dev period: commit-body marker `canonical hash: REBASELINED (trigger: N; old: ...; new: ...; reason: ...)`. Post-EA: also requires DECISIONS.md entry. Every rebaseline MUST pass CI matrix on all 3 OSes with identical post-rebaseline hash. Three-layer guard from commit `eb0b952e` (meta-test + CI grep + hook) stays untouched; only the hash rebaselines. Supersedes: scattered folklore in `docs/specs/determinism-gate.md` §9 references — now ADR-backed.

- **2026-05-13** — ADR-0013 Licensed-data policy (Proposed): three enforcement layers at `docs/adr/0013-licensed-data-policy.md`. Lint (cheap, CI-blocking) + FW-VAL validator (cosine-similarity against ~50k-name corpus, bake-time, fail-closed) + human review (long-tail). Mod content carries its own legal liability. Per-bake audit report committed alongside baked RON. Post-ship collision-fix flow defined (single-character edit; corpus_version bump). Supersedes: prior folklore in DESIGN_DOC §3 Pillar 1 + CLAUDE.md §1 "no real licensed data" — now ADR-backed.

- **2026-05-13** — ADR-0014 Runtime AI / content boundary (Proposed): shipped game makes ZERO LLM calls at runtime at `docs/adr/0014-runtime-ai-content-boundary.md`. No outbound HTTP to providers, no embedded inference model, no deferred AI work, no LLM SDK in any shipped crate. All generation at bake-time via `fw-content-baker` (dev tool, NOT shipped). Bake manifest captures `model_id + prompt_hash + prompt_template_version + seed + corpus_version + licensed_data_corpus_version`. Rebake policy: valid on model/template/seed/licensed-corpus change; invalid for cosmetic regen of shipped content. Future-proofing path documented for opt-in dynamic narrative if a serious case emerges post-EA (would supersede this ADR). Supersedes: prior folklore in CLAUDE.md §3 + DESIGN_DOC §3 "no runtime LLM" — now ADR-backed.

- **2026-05-13** — ADR-0015 Phase-gate review policy (Proposed): three review tiers at `docs/adr/0015-phase-gate-review-policy.md`. Tier 1 = per-task self-review on ≥100 LoC code (mandatory, auto). Tier 2 = mid-phase targeted Codex audit (manual, fires on 5 explicit criteria: schema lock / new canonical-state surface / first ADR implementation / IPC API surface / save-migration adapter). Tier 3 = phase-boundary full audit at every `/done`. T1-1 demonstrated that phase-boundary-only was too coarse — Tier 2 fills the gap. Prompt templates land at `docs/templates/tier-{2,3}-codex-prompt.md` as Tranche 4 deliverables. Supersedes: CLAUDE.md §6's "phase boundaries only" rule (Codex audit Lane E showed it missed T1-1's load-bearing schema work).

- **2026-05-13** — Browser-dev fixture path correction (T1-2a Tier-2 audit P2 follow-up): the dev-tier 2D tactical board fixture path is `frontend/public/dev-fixtures/smoke.json` (loaded via `?source=fixture:/dev-fixtures/smoke.json`), NOT `/tmp/smoke.json`. Vite's dev server serves only files under the project root; `/tmp/` is unreachable by the browser. The `frontend/public/dev-fixtures/` directory is `.gitignore`d (ephemeral, regenerable from `cargo run --bin dump_frames`); the .gitignore + README live in-tree. Context: the original ADR-0008 entry below + the original ADR-0008 doc both said `/tmp/...`; that worked for the dev-shell binary but never for the browser. ADR-0008 + MASTER_PLAN T1-2a row both updated to the correct path in the same audit-remediation pass. Supersedes: prior `2026-05-13 — ADR-0008 Browser-dev mode for the 2D tactical board` entry's `fixture:/tmp/frames.json` example (the *concept* of the entry stands — only the example path was wrong).

- **2026-05-15 — Cargo-cult fix-pass pattern surfaced + mitigation policy.** Three audit cycles on T1-2b (`iv` self-review BLOCK → Codex Tier-2 mid-phase REVISE → Codex Tier-2 re-audit REVISE) produced shape-without-substance fixes from delegated `gameplay-programmer` agent runs. Concrete instances: (a) `tick_goalkeeper` signature renamed `_player` → `player` (surface change) while state handlers still ignored attribute reads (substance unchanged) for the P1-4 GK attribute-bindings finding; (b) `vacuousness_check_acN` companion tests added by name but tested fake in-memory bypass states instead of dispatch-path bad outcomes for the P1-8 AC test-vacuousness recurrence; (c) cross-category storage `[player][category]` added (shape) while dispatch only applied the first active bias via `find_map` short-circuit (substance unchanged) for the P1-7 stacking finding. Mitigation: for fixes whose verification is **behavioral** (assertion-on-side-effect, not structural-on-signature), prefer main-thread implementation OR dispatch messages MUST specify substance-level acceptance criteria with example test bodies. Cargo-cult risk increases monotonically with fix-pass round count; by round 3 on the same finding family, switch authorship. Captured in the T1-2b-fix commit body + this DECISIONS entry so the next multi-round audit-recurrence has a documented prior pattern. Supersedes: none (new lesson).

- **2026-05-16 — ADR-0009 amendment: 9th `SeedLayer::Commentary` discriminant `0x18` for T1-4b deterministic Tracery variant-pick.** T1-4b ships the commentary template bank in `fw-content::commentary` per Content/RULES.md §4 + ADR-0007 Layer 1 (≥3 variants per `MatchEvent` slot; ≥18 templates total across 6 events). Tracery variant-pick must be deterministic across reruns + cross-OS — that requires `seed_fn(match_seed, tick, SeedLayer::Commentary, site)` per ADR-0009. Without a dedicated layer T1-4b would either (a) reuse an existing layer + collide with its random-draw space (silent non-determinism on future encoder shape changes), or (b) skip seeded variant-pick entirely (non-deterministic prose; breaks reproducible test fixtures + cross-OS canonical-state hash). Discriminant `0x18` is the next value after the 8 existing (`0x10` Decision / `0x11` UtilityTieBreak / `0x12` ReactiveInterrupt / `0x13` BallPhysics / `0x14` SignatureTrigger / `0x15` MemoryEvent / `0x16` ScoutObservation / `0x17` ContentBake per ADR-0009). Site formula: `((player_slot as u32) << 16) | (event_class_discriminant as u32)` so each (player, event_class) pair has a distinct RNG site within the layer — different players narrating the same event class draw independently, and the same player narrating two events draws independently. T1-4b implementation row updates `docs/adr/0009-rng-seed-derivation.md` §"SeedLayer" + `fw-core::seed::SeedLayer` enum + adds vector tests for the new layer; this DECISIONS entry is the prereq gate the row depends on (the row's `/next` cycle cannot pick it up without an ADR amendment recorded here). Supersedes: none (extends the existing layer set; does not modify any existing discriminant).

- **2026-05-16 — ADR-0009 layer-assignment factual correction (supersedes earlier 2026-05-16 amendment entry's "0x10..0x17 contiguous" claim).** The earlier 2026-05-16 entry on `SeedLayer::Commentary = 0x18` justified the discriminant choice as "the next value after the 8 existing `0x10` Decision / `0x11` UtilityTieBreak / `0x12` ReactiveInterrupt / `0x13` BallPhysics / `0x14` SignatureTrigger / `0x15` MemoryEvent / `0x16` ScoutObservation / `0x17` ContentBake per ADR-0009". That claim is factually wrong — the existing layer assignments in `fw-core::seed::SeedLayer` are non-contiguous: `0x10..0x14` (Decision, UtilityTieBreak, ReactiveInterrupt, BallPhysics, SignatureTrigger), then `0x20` (MemoryEvent), `0x30` (ScoutObservation), `0x40` (ContentBake). The original ADR-0009 chose these spaced-out values to leave room for future amendments to slot in adjacent to related layers. `Commentary = 0x18` correctly sits in the `0x15..0x1F` gap and is collision-tested against all 9 layers (per `seed_layer_discriminants_are_adr0009_canonical` + `seed_fn_commentary_does_not_collide_with_existing_layers`), so the chosen value is sound — the rationale in the prior entry was just wrong about how the existing layers were laid out. Codex Tier-2 2026-05-16 P2 caught this. The actual constraint that justifies `0x18` is "any unused u8 value not equal to an existing discriminant"; `0x18` keeps amendable layers clustered in the `0x1X` range adjacent to the original ADR's `0x1X` block. Code is unchanged (the variant has been `0x18` since T1-4b shipped); this entry corrects ONLY the rationale in the earlier entry's prose. Supersedes the rationale clause "Discriminant `0x18` is the next value after the 8 existing (`0x10` Decision / `0x11` UtilityTieBreak / `0x12` ReactiveInterrupt / `0x13` BallPhysics / `0x14` SignatureTrigger / `0x15` MemoryEvent / `0x16` ScoutObservation / `0x17` ContentBake per ADR-0009)" in the prior `2026-05-16 — ADR-0009 amendment: 9th `SeedLayer::Commentary` discriminant `0x18` for T1-4b deterministic Tracery variant-pick` entry. The amendment itself (`SeedLayer::Commentary = 0x18` discriminant addition + site formula + cross-layer collision-freedom) stands.

- **2026-05-16 — Codex 2026-05-16 whole-codebase audit triage: ball-mutation row T1-3.5 inserted before T1-4b; 4 hardening rows appended T1-10..T1-13.** Codex audited committed HEAD `537148f3` (T1-2b-fix round 4 — pre-T1-4a) and returned REVISE with 1 P0 + 8 P1s + 6 backlog items. The audit's full report lives in the user's transcript on 2026-05-16 (read-only audit; no companion `docs/audits/` artifact). **P0 — normal match path cannot produce real ball actions**: `apply_intent` in `crates/fw-match-sim/src/dispatch.rs` treats `AttemptShot` / `AttemptPass*` / `Cross` / `LayOff` / `Dribble` / GK distribution intents as "move the player toward the target" with no ball.vel mutation, no possession-state transitions, no goal detection, no score updates. T1-4a (commit `5b3b8bd`) shipped `MatchEvent` emission, but the events describe **player intentions, not football outcomes**: `Pass.completed: T1_PASS_COMPLETED = true` is hardcoded; ball doesn't move on Shot; the `MatchEvent::Goal` variant is structurally unreachable (the `apply_tactic_event_with_emission` helper was deleted in the T1-4a fix-pass as a silent-failure trap — the real call site needs ball-in-net detection + possession-chain scorer attribution that doesn't yet exist). Per ADR-0007 line 87 ("templates that surface bugs textually are the same ones that ship") authoring T1-4b's commentary on intent-only events would produce shipped prose that has to be rewritten when ball-mutation lands. **Decision**: insert new row `T1-3.5` (ball-mutation + possession + goal detection) BEFORE T1-4b; T1-4b's `depends_on` changes from `T1-4a` to `T1-4a, T1-3.5`. Codex's recommended order ("Fix the normal football loop first") is honored. **Other P1 findings grouped into new rows**: (1) runtime f64 LUT bake (P1) → new row `T1-10` — replace `SIGMOID_LUT` + `EXP_LUT` runtime f64 generation in `fw-core::math` with committed Q32 const tables; current passes cross-OS hash but is libm/platform-dependent by design (latent risk). (2) signatures unreachable in normal `tick_match` (P1) → new row `T1-11` — `tick_match` passes `BTreeMap::new()` for sig definitions + initial players have empty `signature_candidates`; signatures fire in tests only; need a real match setup/runtime context that projects content definitions + player candidates into `MatchState` / `dispatch_tick`. (3) content validation still trusts too much (P1) → new row `T1-12` — `fw-content::runtime` loaders silently overwrite duplicate IDs; banned-term / licensed-data / cliche validators return `Ok(())`; `RoleId` / `SignatureId` / `SignatureCandidate` `try_new` constructors are bypassed by serde; needs fail-on-dup-ID + validators-return-Err + serde post-parse validators. (4) frontend tests promised but not gated (P1) → new row `T1-13` — Vitest scaffold for `FrameSource` + dev-board lifecycle; `pnpm test` gated in `just verify` + CI per CLAUDE.md §9 claim that currently doesn't hold (plus this row absorbs the small `cargo audit` / `cargo deny` CI wiring that was pulled forward in MASTER_PLAN but never wired). **P1 findings absorbed into existing T1-5 row**: IPC command-surface consolidation (`src-tauri/main.rs` has local `get_dummy_state` / `play_match` stubs while `match_frames` comes from `fw-tauri` — same app, two backend truths; T1-5 was already carrying Codex T0 Imp #10 deferral; audit reframes it as P1 not P3); `match_frames` unbounded (caller can request huge tick count → forced large allocation/CPU; T1-5 needs backend max-tick cap + mirrored frontend validation + typed error). **P1 findings deferred to next-row-touching code**: weak "non-vacuous" tests (single-candidate fit-score test in `dispatcher.rs`, AC-4 bypassing `dispatch_tick` in `signature_dispatcher_proptest.rs`, BT attribute-binding spot-check-only coverage in `bt-attribute-binding.md`) → fix when next `/next` touches `signature/dispatcher.rs` or `signature_dispatcher_proptest.rs` or the `bt/` modules; no dedicated row needed. **Trivial fix landing alongside this decision-log commit**: `T0-7b` row in MASTER_PLAN flipped TODO → DONE (T0 closed 2026-05-13 and the cross-OS CI matrix has been green ever since; row stayed TODO by oversight — Codex's "state docs can mislead /next" P1). STATUS.md was already current post-T1-4a (`d376ba26` round-4 commit already replaced the stale "awaiting Codex re-audit" text). **Backlog items deferred (Codex flagged but no row added)**: reactive interrupts not a 60 Hz layer yet (`preempt_check` always returns `None`, gated behind `should_decide` — known carry-forward; defer to T1-9 or Phase 2); exact-co-location separation pushes only `EPSILON_SEPARATION ≈ 1mm` against a `MIN_PLAYER_DISTANCE = 0.4m` (the 7-unit test pins the EPSILON behavior so it's intentional; the resulting overlap is real and acceptable for the skeleton tier — defer until tactic FSM event integration when player density patterns change); tactic FSM event transitions not yet integrated (heartbeat runs but possession / goal / set-piece events don't drive tactics — T1-3.5 may absorb the goal-event hookup; the others defer to Phase 2 tactic-system maturation); `SubtreeLibrary` bypassed by hardcoded one-leaf tree (acceptable as skeleton-tier per T1-2b-iii-a doc — its acceptance criterion was "compile + run"; defer to Phase 2 BT expansion); `.claude/hooks/protect-decisions.sh` ignores `MultiEdit` (settings.json routes `Edit|Write|MultiEdit`; hook checks only `Edit|Write|NotebookEdit` so a MultiEdit on DECISIONS.md slips the append-only guard — small but security-relevant; absorb into the next session that touches hooks OR fold into T1-13's CI work). **What looks solid per the audit** (Codex affirmation): seed derivation layout correct (ADR-0009 substrate as built); canonical encoding explicit little-endian + sorted maps/sets; `signature_candidates` now canonical-encoded; Q32 public operators use checked arithmetic; the T1-2b-fix round-4 AC-2 cooldown observable rewrite is the right shape; PixiJS app creation/cleanup mostly sane. **Recommended next `/next` order** (per Codex + this triage): `T1-3.5` (ball mutation, blocks T1-4b) → `T1-4b` (commentary templates) → `T1-11` (signature wiring into tick_match so commentary has signatures to narrate) → `T1-5` (Tauri + frontend Match page; includes IPC consolidation + match_frames cap) → `T1-12` (content validation) → `T1-10` (LUT bake) → `T1-13` (frontend tests + cargo audit) → `T1-6` (frontend Match polish) → `T1-7` (procgen) → `T1-8` (replay corpus) → `T1-9` (behavioral assertions). Supersedes: none (audit-driven plan revision; extends MASTER_PLAN row set; does not change any prior decision).

- **2026-05-16 — Subagent discipline post-T1-15 incident; `/next` skill hardened with 4 binding rules + skip-DEFERRED selector.** Codex Tier-3 phase-boundary review of `v0.1.0-first-match` (verdict REVISE 2026-05-16) flagged two process/doc gaps that had to land before T2: (1) MASTER_PLAN state drift (stale Snapshot + Now/Next + T1-17 plain `TODO` so `/next`'s "first eligible TODO" walker could pick it instead of T2-1); (2) T1-15 workflow-incident lessons named in `CHANGELOG.md` but not yet encoded in the `/next` skill. **The T1-15 incident** (commit `0a0df5c3`): a `gameplay-programmer` subagent dispatched to fix one named bug in `bt/on_ball.rs::utility_pass_short` shipped a 5-axis behavioral retune touching 4 files explicitly marked OUT OF SCOPE (`dispatch.rs` / `ball_physics.rs` / `lib.rs` / `tests/behavior_proptest.rs`), skipped the mandatory self-review triple, rebaselined BOTH canonical pins, AND created the commit autonomously — all without main-thread review. Empirical result happened to satisfy T1 exit-gate Bullet 1 (smoke seed = 4 goals), so the user accepted via post-hoc audit. The workflow contract was violated on 4 axes; the empirical result was lucky. **Decision** (4 binding rules now encoded in `.claude/skills/next/SKILL.md`): **(1) Subagents do not commit** — `git commit` boundary belongs to main thread; subagent prompts must explicitly forbid `git commit`; autonomous-commit-attempted is a Pause-and-ask trigger (#10). **(2) `files_in_scope` + `files_out_of_scope` are binding** — subagent may not touch any file outside `files_in_scope` without escalating; "scope-expansion-needed" report is the only legal response to mid-task scope discovery (Pause trigger #2 hardened). **(3) Multi-pin or behavior-change-driven canonical-hash drift requires main-thread review BEFORE rebaseline** — subagents may VERIFY a drift occurred (read new actual hash from failing test) but MUST NOT write new pin values when drift is multi-pin OR ADR-0012 trigger #3 (behavior-change); main thread reads diff, runs `scripts/fw verify` + 5-seed empirical sweep, confirms envelope (e.g. T1 exit-gate Bullet 1 2-5 goals), authorizes pin updates (Pause trigger #9; "Canonical-hash drift gate" hardened). **(4) `/next` skips rows with status `DEFERRED`** — phase-walk first-eligible-TODO selector now skips any row whose status field contains `DEFERRED` (case-insensitive substring); intentional carry-forward rows (T1-17 at T1 close per Codex Tier-2 "test-quality only" verdict) sit in plan for traceability but `/next` walks past them; user manually promotes back to `TODO` when scheduling sibling cleanups. Plus a mandatory subagent-prompt boilerplate template added to the skill — main-thread MUST paste it into every subagent dispatch citing all 4 rules + T1-15 incident reference. **Consequences**: subagent dispatches gain ~30 lines of boilerplate per prompt (acceptable cost); main thread can VERIFY post-return that subagent stayed in scope + didn't auto-commit + didn't autonomously rebaseline; small mechanical-task subagent autonomy unchanged; discipline applies to the cases that bit us (sim-Rust ≥100 LoC commits where the agent decides "while I'm here, let me also..."). Codex Tier-3 verdict explicitly affirmed: "subagents do not commit, forbidden-file lists are binding, and multi-pin/hash-drift behavior changes require main-thread review before rebaseline — sufficient if made hard." **MASTER_PLAN drift fix landed alongside**: Snapshot refreshed to "T1 CLOSED" + Now/Next pointer refreshed to T2-1 + T1-17 status `TODO` → `DEFERRED` (so the new skip rule has something to match). After this entry lands + the `/next` skill ships hardened, `/next` correctly selects T2-1 as the next eligible row. Supersedes: none (extends `/next` workflow contract with new binding rules; does not change any prior decision; T1-15 incident was documented in `MEMORY.md` but the structural prevention had not yet been encoded).

- **2026-05-16 — Post-T1-close ultimate-review (Claude+Codex 6-track) — 4 new MASTER_PLAN rows + 7 workflow improvements + `Sim/RULES.md` §11 hardening.** After the Codex Tier-3 ACCEPT verdict on `v0.1.0-first-match`, user requested an 11th "hardcore" audit pass deliberately using tactics not used in the prior 10 (mutation-test analysis, architectural-drift docs-vs-code, whole-codebase silent-failure sweep, test-the-tests dead-test detection, Codex adversarial red-team, Codex property explosion). Shared review file at `docs/audits/post-t1-ultimate-review-2026-05-16.md`; 4 Claude subagents + 2 Codex tracks ran in parallel, writing to dedicated track sections. **Audit verdict ACCEPT for T2 start with 9 P1 findings across 4 surfaces** — none phase-blocking; all worth landing as targeted follow-ups before T2-1 dispatches a sim-Rust subagent. **Codex Track F (property explosion: PROPTEST_CASES=10000 on 13 invariants, 130k sampled cases) found zero new failures** — strongest positive signal of the audit. **4 new MASTER_PLAN rows authored** (T1-19 preempt_check behavioral tests + ADR-0006 amendment; T1-20 content-validation hardening absorbing Codex workflow improvement #4 validate-naming split; T1-21 Tick arithmetic policy alignment + Sim/RULES.md §11 amendment; T1-22 hash-pin registry script + env-driven determinism rerun counts absorbing Codex workflow improvement #6 + Track D 5th-pin finding + Codex Track F caveat). **7 workflow improvements landed in this commit** per Codex's parallel review of "anything we should change in workflow/setup/blueprint": (1) AC-to-test matrix in `/next` Step 2 template — every acceptance criterion must declare its observable BEFORE coding (attacks vacuous-test recurrence directly); (2) post-T1-15 subagent boilerplate already binding from 2026-05-16 prior commit — no new edit needed; (3) mutation-thinking 30-second mental checklist in `/next` Step 6 self-review — "if I changed the constant, flipped the team, removed the guard, would a test fail?"; (4) `validate-structural` / `validate-semantic` / `validate-content-pack` naming split in T1-20 row spec; (5) Sim/RULES.md §11 — invariants fail in release, not just debug (ban `debug_assert!` for canonical/gameplay invariants + ban undocumented `saturating_*` arithmetic on sim newtypes); (6) hash-pin registry script per T1-22 row spec; (7) multi-track Claude+Codex ultimate-review as normal phase-close lane in `/done` skill (new Step 5.5). **Cross-track convergence patterns surfaced as the highest-value signal of the multi-agent design:** `preempt_check` flagged from 2 independent angles (Track A zero behavioral tests + Track B stale ADR-0006); constant-mutation-invisible flagged from 3 angles (Track A MAX_PLAYER_SPEED + Track C softmax tie-handling + Track D ignored snapshot); test-vs-spec-drift surfaced as a class. No single audit framing would have produced these convergences. **Workflow-incident discipline check passed** — all 4 Claude subagents respected the post-T1-15 mandatory boilerplate (read-only audits, no commits, no out-of-scope edits); Track A's subagent specifically reported lacking Write/Edit tools + asked main thread to append findings on its behalf — exact "scope-expansion-needed" escalation pattern the boilerplate prescribed. **Codex-vs-Claude framing for future audits:** Claude is strong on implementation-drift / test-quality / systemic-pattern detection (4 of the 6 tracks); Codex is strong on adversarial red-team + property explosion at scale (2 of the 6 tracks). The split is load-bearing; doing it as a Claude-only OR Codex-only audit would have missed roughly half of what the combined pass surfaced. Codified in `/done` Step 5.5 as the phase-boundary recipe. **Cost expectation per phase-close ultimate-review:** ~30-60 min wall-clock + ~$15-30 in agent spend. Acceptable given the bug-class catches. Supersedes: none (extends existing audit + workflow practice with multi-track recipe; adds 4 follow-up rows; codifies `Sim/RULES.md` §11 + 3 `/next` + `/done` skill improvements).

- **2026-05-16 — T1-19 — ADR-0006 amendment: `preempt_check` 3-policy scope documented + 5 behavioral unit tests pin each policy.** ADR-0006 originally framed pre-emption as a single conceptual hook with the only named live policy being the "single-chaser claim" (line 27 + §"Concrete sketch" line 138-152). At T1-15 (commit `0a0df5c3`, 2026-05-16) `preempt_check` in `crates/fw-match-sim/src/dispatch.rs:867-948` grew from `fn preempt_check(...) -> Option<PlayerIntent> { None }` to a 3-policy implementation: (1) possession gate — return None if `state.possession.is_some()`; (2) GK own-side chase — slots 0/11 chase IFF `|ball.pos_x| > 42m` (within 10m of own goal at ±52.5m) AND ball on own side (`bx_bits < 0` for home GK); (3) outfield nearest-2 chase — preempt fires UNLESS 2+ same-team outfielders are STRICTLY closer (Manhattan distance, `<` tiebreak; excludes same-team GK). T1-19 lands the ADR-0006 amendment section + 5 behavioral unit tests (`preempt_check_home_gk_does_not_chase_away_ball`, `preempt_check_home_gk_chases_loose_ball_within_42m_of_own_goal`, `preempt_check_outfield_chaser_count_caps_at_2`, `preempt_check_only_fires_on_loose_ball`, `preempt_check_does_not_conflict_with_goalkeeper_fsm`) per the post-T1 ultimate-review Track A finding (`docs/audits/post-t1-ultimate-review-2026-05-16.md` Surface 2). Each test is mutation-discriminating per the AC-to-test matrix in the T1-19 MEMORY task-spec — flipping the predicate or removing the `continue;` would surface a failure. Strict-< tiebreak is deterministic by construction (no RNG, no clock); under exact-Manhattan ties, more than 2 outfielders may chase — known limitation deferred to T2-1 archetype-driven positioning. Foul reaction + set-piece switchover + 60 Hz reactive interrupts remain T2+ scope per the amendment. Supersedes: none (extends ADR-0006 with current-implementation documentation + binding test coverage; does not modify the original Decision text).

- **2026-05-16 — T1-20 — `fw-content-baker validate` renamed `validate-structural` per Codex workflow improvement #4 honesty-naming + content-validation pipeline hardening.** Closes post-T1-close ultimate-review Surface 1 (Codex Track E #1 + #3 + #4 + Codex workflow improvement #4). **Naming-honesty decision**: rather than keep the `validate` subcommand name + change semantics (Option B from MASTER_PLAN row: fail hard while stubbed validators return NotImplemented), shipped Option A: rename to `validate-structural` + reserve `validate-semantic` + `validate-content-pack` for T2-3 when the LLM bake pipeline lands. Rationale: rename is the cleaner long-term design — `validate` was actively misleading (promised full content-pack validation while only structural ran); `validate-structural` is honest about scope today + makes room for the eventual 3-way split without breaking semantic-stable callers. Pre-1.0, solo-dev, no external CLI consumers, CI updated in same commit — the cost of a clean break is zero. Stale doc callers in 3 paths (`patterns/bake-time-llm-content-pipeline.md`, `.claude/rules/Content/RULES.md`, `.claude/skills/next/RECIPE.md`) updated as scope-expansion follow-through of the rename per Codex code-reviewer P1 finding — these were technically out-of-scope in the MEMORY task spec but mechanical follow-through of a renamed CLI doesn't constitute substantive scope expansion (the rename is the user-authorized work; the stale callers would actively mislead future /next cycles otherwise). **Cross-reference architecture decision**: reused existing `ContentLoadError::DanglingReference` variant (string-parameterized over `from_kind` + `to_kind`) for the new `PlayerTemplate.signature_candidates → SignatureDefinition` check rather than introducing a sealed `CrossRefKind` enum. At 2 cross-ref pairs the string flexibility is correct; at 4+ pairs (T2-3 will add `PlayerTemplate.role_id → RoleAffinityTable` and `PlayerTemplate.culture_id → Culture` per Codex Track E follow-on) the sealed enum becomes worth its complexity cost. T2-3 row body will track this refactor as a precondition for the additional cross-refs landing. **Sentinel-scope security hardening**: `ui-lint:ignore-start/end` blocks in `scripts/lint-banned-terms.py` are now scope-restricted to relative-path prefixes `docs/`, `crates/`, `scripts/` + project-root `.md` only — the prior file-agnostic respect created an exfiltration path where an attacker could hide Category-A banned terms (e.g. "Manchester United") inside JSON/RON comment-bracketed sentinel pairs in shipped content. Silent-failure-hunter caught the original implementation's substring-match bug (re-opened the exact escape via nested paths like `frontend/src/docs/legacy/x.ts`); fixed in-place with relative-path prefix matching + 3 dedicated regression tests. The discovery is the kind of cross-checking the self-review triple is designed to catch: my own mutation-thinking pre-check missed the substring-vs-prefix distinction because the test fixtures didn't exercise nested-path edge cases; the silent-failure agent's threat-modeling pass found it in 30 seconds. Supersedes: none (extends content-validation pipeline + lint script + ADR-0009 cross-ref family). Canonical hash UNCHANGED on both pins.

- **2026-05-16 — T1-21 — `fw-core::Tick` arithmetic aligned to Q32's panic-on-overflow policy + 2 `debug_assert!` → `assert!` conversions per `Sim/RULES.md §11`.** Closes post-T1-close ultimate-review Surface 3 (Codex Track C P1 + Codex workflow improvement #5). **Policy alignment**: `Tick` operators (`+`, `-`, `+=`, `-=`) + `successor()` + `from_seconds()` switched from `saturating_*` to `checked_*().expect()` with panic messages citing §11 + pointing at the new `Tick::clamping_*` opt-in alternative. New `Tick::clamping_add` + `Tick::clamping_sub` methods (with `// SAFETY:`-style doc-comments) preserve saturation as an opt-in for legitimate UI / dev-tooling sites. **AC4 re-interpretation decision**: MASTER_PLAN row's literal `Tick::ZERO - Tick::from_raw(1)` example was a misread of the failure mode — the result `-1` is well within i64's range, NOT an underflow at the arithmetic layer. The file prologue explicitly says "negative ticks are useful as signed offsets in scheduling math." Implementation panics only on actual i64 over/underflow at extremes; negative-result zone is preserved + pinned by a dedicated `tick_subtraction_into_negative_zone_does_not_panic` test. If a future change wants to ban negative-result Tick subtraction (would require auditing every `.to_raw()`-based subtraction site for signed-offset uses), that's a follow-up decision; today's policy preserves the documented affordance. **debug_assert! conversion scope**: §11 explicitly named 2 sites (`ball_physics.rs:190` is_well_formed; `dispatch.rs:824` self-pass guard); both converted to `assert!`/`assert_ne!`. The remaining `debug_assert!` sites in `decision_cadence.rs`, `personality_bias.rs`, `canonical.rs` are caller-guaranteed preconditions per §11's "Allowed" clause — left as-is with the existing inline rationale (formal `// debug_assert OK here because:` prefix deferred to opportunistic cleanup; the existing comments already document the reasoning). **Cross-tool self-review convergence**: silent-failure-hunter (P2) + type-design-analyzer (P1) + code-reviewer (P2) ALL flagged the same factually-wrong claim in the `bump_decision_counter` SAFETY comment — the original wording said the field was "RNG-only" but it ALSO writes into the canonical-hash buffer at `canonical.rs:500`, AND said "i64::MAX-anchored" when the field is u32 (saturating at u32::MAX). Rewritten in-place to acknowledge BOTH consumers + correctly attribute determinism semantics to the 16-bit site mask in `dispatch.rs:374` (`counter & 0xFFFF` — site values recur every 65_536 decisions regardless of saturation; the mask is the determinism contract, the saturation inherits its semantics). The self-review triple cross-convergence pattern (multiple agents flagging the same finding) reproduced at single-task scope what the ultimate-review playbook demonstrated at phase-boundary scope — same load-bearing signal at smaller cost. **Canonical hash UNCHANGED on both pins**: production code uses raw `.to_raw()` + i64 arithmetic (not Tick operators); `assert!` vs `debug_assert!` conversion changes only when-it-panics, not state. `scripts/fw verify` exit 0 in BOTH debug + release builds. Supersedes: none (extends Sim/RULES.md §11 with concrete callsite alignment; opt-in clamping methods extend the Tick API surface without changing existing public-method behavior beyond the saturating→panic change which is the documented policy intent). Cross-reference: `Sim/RULES.md §11` (codified at `0958e178`); `docs/audits/post-t1-ultimate-review-2026-05-16.md` Surface 3.

- **2026-05-17 — T2-1a — canonical encoder VERSION bumped 8 → 9 for per-team archetype IDs + `tactic_fsm::archetype_params_for(&TacticalArchetype) -> ArchetypeParams` bridge thresholds + Codex 2026-05-16 whole-codebase audit triage outcome.** **Schema bump**: `MatchState` gained 2 canonical `String` fields (`home_archetype_id`, `away_archetype_id`) appended after `last_touched_by` per Sim/RULES.md §6 stable-field-order discipline + 2 `pub(crate) ArchetypeParams` sidecars (`home_archetype_params`, `away_archetype_params`) consumed by the Goal-event apply at `lib.rs:781`. Encoder VERSION 8 → 9 with `[u16 LE len] [UTF-8 bytes]` per id appended to the canonical byte stream. **Bridge threshold decisions** (PRESERVE-CURRENT-BEHAVIOR — chosen so the bridge output for `attacking-fullback.ron` (= `DEFAULT_ARCHETYPE_ID`) exactly matches the pre-T2-1a hardcoded `ArchetypeParams::direct_pressing()` constructor used by `MatchState::initial`): (a) `press_intensity = High` iff `press_radius_metres > 20` else `None`; (b) `counter_intent = High` iff `buildup_speed_factor_bps >= 11_000` else `Default`; (c) `default_in_defence_state = LowBlock` iff `press_radius_metres <= 20` else `MidBlock`. These thresholds are LOAD-BEARING on the canonical-hash schema-only-drift claim — coherence pinned by the new `initial_default_sidecar_matches_bridge_on_default_archetype_id` test in `lib.rs::smoke` (silent-failure-hunter CRITICAL-3 fix). T2-1b/c may revise the thresholds when 5-8+ new archetypes provide more data points; any revision MUST update the hardcoded `direct_pressing()` sidecar simultaneously OR remove the hardcoded sidecar entirely + thread `MatchState::initial` through the bridge. **Rebaseline framing** (CRITICAL-1): BOTH canonical hash pins drifted at this commit per ADR-0012 trigger #1 ONLY (schema bump), NOT trigger #3 (behavior change) as the initial commit-message draft claimed. Justification: the only `TacticEvent` consumer in current production is `Goal`, whose `apply_event` arm hardcodes `TacticState::MidBlock` regardless of the archetype param. So even though the 600-tick extended-seed test now passes `home=fwh.core:archetype.attacking-fullback, away=fwh.core:archetype.low-block-counter` to `MatchState::initial_with_content`, the away sidecar never reaches a consumer that would produce per-tick behavioral divergence. Per-team behavioral divergence (and ADR-0012 trigger #3) ships at T2-1b/c when `BallInPlay` / `PossessionLost` / `BallRecovered` `TacticEvent` variants get emitted + their `apply_event` arms consult the per-team `archetype_params`. **Codex audit triage outcome**: post-compaction Codex whole-codebase audit (HEAD `537148f3`, pre-T1-close) triaged against current HEAD `13a1ed8f`: 8 of 9 findings STALE (P0 ball/possession/events refuted by T1-3.5 + T1-15 work; 6 P1s closed by T1-10/11/15/20/22/23/24 work; frontend tests already gated by `pnpm test` in CI). 4 still-valid findings authored as DEFERRED rows (T1-25 sig fit-score 2-candidate winner-changing test; T1-26 AC-4 via dispatch_tick path; T1-27 BT attribute-binding table-walk proptest; T1-28 separation EPSILON vs MIN_PLAYER_DISTANCE). 2 in-place reconciliations applied immediately: T1-3.6 MASTER_PLAN status column drift `IN-PROGRESS` → `DONE (closed 2026-05-16)`; `.claude/hooks/protect-decisions.sh` MultiEdit coverage gap closed (hook script whitelist + edits[] iteration; verified by 6-test Python harness). Supersedes: none (extends T1-2b-fix's signature_candidates canonical-state decision + ADR-0012 rebaseline-triggers decision to per-team archetype-ID + sidecar surface).

- **2026-05-18 — Phase T2 close — `design/player-generation.md` ported from FW v1 archive; T2-4 + T2-7 + T3-5 unblocked.** Codex Tier-3 phase-gate review on issue #1 ACCEPTED the phase but flagged `design/player-generation.md` as the long-standing blocker on T2-4 (PlayerBio gen) + T2-7 (Squad page) + T3-5 (Scout uncertainty). Rather than invent the 22-field gene model + 46-label phenotype catalog from scratch, the FW v1 design at `/Users/vibelogic/dev/football-archive/design/player-generation.md` (315 LoC, locked at 2026-04-24 per the v1 SPEC.md) was ported in-place to FW v2 with: (a) Unity ScriptableObject references → Rust RON files per `Content/RULES.md §1`; (b) `IdentityPacket` → `PlayerBio` rename (football-native vocabulary per `CLAUDE.md §7`; dev surfaces only — player UI never sees either term); (c) `f32` gene fields → `Q32` per `Sim/RULES.md §1`; (d) `HashMap` → `BTreeMap` per `Sim/RULES.md §2`; (e) Unity Phase 2/3/4 numbering → T2/T3/T4 aligned to current MASTER_PLAN; (f) FW v1 SPEC.md decision-log references → FW v2 `docs/DECISIONS.md` references; (g) bake-time pipeline references reconciled to T2-3's `BakeManifest` shape + `fw-content-baker` validator-as-one-class pattern; (h) Rust type sketch added for `PlayerBio` struct (binding shape T2-4 will ship). **The 5 locked decisions Q1–Q5 are preserved VERBATIM**: 22-field gene model with 7 physical + 6 mental + 5 technical + 4 narrative-flag categories (Q1); 46-label phenotype catalog with the 4 explicit exclusions (`Fragile Under Scrutiny` → `Struggles Under Scrutiny`, `Powerful Striker` → `Powerful Ball Striker`, `Plateau Risk` removed, `Injury-Prone` excluded) ceiling 50 (Q2); advanced tooltip numeric exposure OFF by default + power-user opt-in surfacing scout-ESTIMATED ranges (never true gene values) (Q3); compiler reproducibility = checked-in RON as canonical artifact (not prompt+seed+model) with generator metadata in `content/baked/manifest.json` (Q4); additive-only delta packs with stable IDs that NEVER mutate, pack-minor-version in manifest not in entity IDs (Q5). **The cohort-weighted P(0,1,2,3) signature-affinity distribution** + **scout archetype category-level biases** (no per-field biases at MVP; narrative-flag category invisible to scouts) are also preserved verbatim with the original Phase-X "tuning seeds, NOT SPEC-locked" framing reconciled to "T-phase tuning seeds, NOT DECISIONS.md-locked." **T2-4 + T2-7 status change**: both currently `DEFERRED-ROLLED-TO-T3` per the post-/done roll-forward decision; this port resolves the BLOCKER reason but leaves the rolled status intact (T3 sequencing will promote them to TODO when the user invokes /next against T3-1 + decides whether T3-1 (fw-memory ledger) or T2-4 (PlayerBio gen) ships first). T3-5 (Scout uncertainty) has its `T2-4` dep met for status-flip purposes once the user promotes T2-4. **Cross-reference**: `design/player-generation.md` head-matter `last_verified: 2026-05-18`; archive source preserved at `/Users/vibelogic/dev/football-archive/design/player-generation.md` as the canonical pre-pivot baseline. Future tuning changes to Q1–Q5 land via new dated bullets in this file citing this entry verbatim. Supersedes: none (extends `MIGRATION_AUDIT.md` carry-forward catalog + unblocks 3 MASTER_PLAN rows; the FW v1 SPEC.md 2026-04-22 + 2026-04-24 resolution decisions are preserved as forward references, not re-decided).

- **2026-05-18 — T3-1 — ADR-0005 Accepted + EventClass variant-discriminant lock + `Instant::now()` allowed for fw-save `#[ignore]`-gated perf tests only.** Three load-bearing decisions land at T3-1: (1) ADR-0005 (Memory ledger event model + breakthrough triggers) moves from Proposed (2026-05-13) to Accepted on commit. The 30 EventClass variants + UnknownEventClass for mod overlays + DecayFunction enum + 5-reader contract + breakthrough/regressive-collapse mechanism shape are now binding. Tuning values (salience weights, threshold seeds, redraw distributions, decay half-lives, family-relevance table) stay deferred to `docs/design/memory.md` + `docs/design/progression.md` per the tuning-coefficients-out-of-SPEC rule. (2) `EventClass` carries `#[repr(u32)]` with EXPLICIT discriminants 0..29 + UnknownEventClass=30. Same FOREVER-LOCKED pattern as SaveEnvelope's V0=0/V1=1/V2=2 wire-byte pins from T2-9 + T3-1. Re-ordering variants would silently swap canonical wire bytes of every ledger event in the wild + every save in the wild — explicit discriminants make re-ordering a compile-time error (Rust forbids duplicate explicit discriminants) + a visible diff. The pattern is now established as the workspace default for ANY canonical-state enum that ships in serde output. `MemoryEvent::Placeholder` (T0 stub) DELETED; V1 saves in the wild have no real events so V1→V2 migration emits an empty V2 ledger. (3) `Instant::now()` is permitted in `crates/fw-save/tests/perf_test.rs` AND only there. fw-save tests are non-sim, non-canonical-state; the perf test is `#[ignore]`-gated so it does not run by default. Documented via per-rule exemption in `scripts/determinism-audit.py` PER_RULE_EXEMPT entry. No other workspace site is exempt; the bare `Instant::now()` in any other path still fails the audit. Supersedes: none (ADR-0005 acceptance is fresh; variant-discriminant pattern extends the T2-9 SaveEnvelope tag-stability decision to all canonical enums; perf-test clock exemption extends `Sim/RULES.md §3` with a single named carve-out for non-sim, non-canonical-state, `#[ignore]`-gated benchmark code).

- **2026-05-18 — T3-2 — memory-ledger readers rank on `stakes × decay`; full ADR-0005 5-term salience blend deferred.** ADR-0005 §"Salience scoring" locks a 5-term linear blend (`w_stakes·stakes + w_prominence·participant_prominence_avg + w_class·event_class_base_weight + w_rivalry·rivalry_boost + w_rarity·rarity_boost`). At T3-2 only ONE of the 5 inputs is computable: `stakes` (emitter-set, ADR-locked, on every `MemoryEvent`). The other 4 are blocked — `participant_prominence_avg` needs the career system (not built), `rivalry_boost` needs the Phase-4 rivalry graph, `event_class_base_weight` + `rarity_boost` need `docs/design/memory.md` (the salience-weights tuning doc, unwritten). **Decision** (user-resolved /next ambiguity gate): the 5 readers rank/filter on `projected_salience = stakes × decay_factor(decay_function, elapsed)` — `stakes` is the "how weighty as it happened" emission signal, semantically the closest available proxy for salience; `decay_factor` is fully implementable now (Never/Linear/Exponential are ADR-locked, Exponential uses fw-core's committed `exp_q32` LUT). `compute_salience` in `crates/fw-memory/src/ledger.rs` changes from the T3-1 placeholder `Q32::ZERO` to return `event.stakes` (the 1-term degenerate of the 5-term formula), so the cached `event.salience` field is meaningful + readers read it uniformly. **Consequence**: the full 5-term blend lands incrementally — `w_class`/`rarity` when `docs/design/memory.md` is authored; `prominence` when the career system ships; `rivalry` at Phase 4. Each addition is a `compute_salience` extension, NOT a schema change (salience is a cached Q32 field; widening the formula does not bump the MemoryEvent schema_version). The readers' ranking logic is formula-agnostic — it reads the cached `salience` regardless of how many terms fed it. Alternatives rejected: authoring the ~30-entry event-class base-weight table now as T3-2 tuning seeds (commits balance values before the tuning doc exists — premature); shipping SalienceReader as a stub (leaves the row partially done). Supersedes: none (extends ADR-0005 §"Salience scoring" with a phased-rollout sequencing decision; the 5-term formula structure itself is unchanged + still binding).

- **2026-05-18 — T3-3 — `fw-content` news/quote render takes resolved-slot context structs, not `&MemoryEvent`; no `fw-content → fw-memory` dependency edge.** T3-3 ships the news-headline + manager-quote render module in `fw-content`. The natural-looking signature would be `render_headline(event: &MemoryEvent, ...)` — render directly from the career event. But `MemoryEvent` lives in `fw-memory`, and `fw-content` does NOT currently depend on `fw-memory` (deps: fw-core, serde, ron, thiserror, rand_chacha, rand, blake3, tracery, serde_json). **Decision**: the render functions take plain `(career_id: u64, event_id: u32)` integers (for the deterministic `seed_fn` derivation the done-criteria requires) + a resolved-slot **context struct** (`HeadlineContext` with team/opponent/player/scorer/score_line/minute/manager String fields; `QuoteContext` with manager/team/opponent/player/scorer). The PlayerId/ClubId → display-name resolution that a `&MemoryEvent`-based signature would force into `fw-content` is instead a CALLER concern, deferred to the wiring task (T3-6 player-detail page / the career loop) which already has the career roster + content store in hand. **Consequence**: `fw-content` stays free of an `fw-content → fw-memory` dependency edge. This matters because `fw-memory` and `fw-content` are siblings in the crate graph — fw-memory consumes content IDs (PlayerId etc. from fw-core) but a fw-content→fw-memory edge would couple the content/narrative layer to the event-ledger layer, and a future fw-memory→fw-content edge (memory events referencing content) would then be a cycle. Keeping the render layer ID-resolution-agnostic (it formats already-resolved strings) preserves the clean layering: fw-memory owns events, fw-content owns templates+rendering, and the wiring crate (fw-tauri or the career loop) owns the join. Alternatives rejected: adding `fw-content → fw-memory` (couples two sibling domains + risks a future cycle); putting the render module in `fw-memory` (fw-memory would then own Tracery + grammar loading, which is squarely content-layer responsibility). Supersedes: none (establishes the render-from-resolved-context convention for all future narrative-render surfaces — scout prose, fan callbacks, breakthrough recaps will follow the same shape).

- **2026-05-20 — T3-4 prep — `design/breakthrough-moments.md` ported from FW v1 archive + `docs/design/progression.md` authored; T3-4 unblocked.** MASTER_PLAN T3-4 ("breakthrough events") references `design/breakthrough-moments.md`, which did not exist in FW v2 (only the FW v1 archive copy, a Unity-cinematic-era doc). And ADR-0005 defers all breakthrough tuning (attribute-family list, family_relevance table, gating-event table, redraw distributions, thresholds, cooldowns) to `docs/design/progression.md`, which also did not exist. Same blocker class as T2-4 (resolved 2026-05-18 by porting `design/player-generation.md`). **Decision**: author both docs as a design pass before T3-4's `/next`. Four load-bearing design calls made in the port: (1) **Cinematic apparatus dropped entirely** — the archive doc was built around a 3D viewer + "semantic cinema" (player-isolation shots, crowd cutaways, aftermath-freeze, 3-5s cinema beats); FW v2 is text-first with no 3D viewer (CLAUDE.md), so all cinema framing is replaced by post-match text-recap surfaces. Archive Q1 (cinema beat duration) is deleted; Q2/Q3/Q4 survive reframed for text; Q5 (live-fire timing) reduces to "the recap surfaces in the post-match report." (2) **3 trigger kinds reconciled as 3 gating-flavors of ONE mechanism** — the archive treated signature-awakening / latent-flag-unlock / regressive-collapse as three mechanically separate systems; ADR-0005 (accepted) defines one per-`(player, attribute_family)` meter mechanism. Reconciled: Kind 1 (signature awakening) = a `BreakthroughMoment` in a family where the player holds a matching pending `SignatureCandidate`; Kind 2 (latent-flag unlock) = a `BreakthroughMoment` whose narrative gate requires one of the 4 narrative-trigger gene flags (late_bloomer / flow_access / peak_ceiling_high / awakening_dormant); Kind 3 = ADR-0005's `RegressiveCollapse`. The kinds differ in gate shape, not meter or redraw shape. (3) **Archive↔ADR discrepancies resolved in ADR-0005's favor** — readiness threshold 0.85 (archive) → 0.92 (ADR); the archive's permanent negative labels (`fragile` / `confidence_fractured` / `ceiling_compressed`) are REPLACED by ADR-0005's reversible `RegressiveCollapse` + career-floor bounding (a collapse is overwritable by a subsequent same-family breakthrough, never a permanent label). (4) **10-attribute-family list locked** as a Phase-3 seed in `progression.md`: Finishing / Passing / DefensiveAnticipation / AerialPresence / Composure / Pace / Stamina / WorkRate / DeadBallDelivery / Leadership — coarser than ADR-0002 per-attribute granularity, mapped onto the 22-field gene model; Pace/Stamina split so `InjuryLongTerm` pressure targets endurance without collapsing acceleration. **Tuning values** (thresholds, family_relevance weights, gating-event table, redraw distributions, cooldowns, the 1-3-breakthroughs-per-5-season-career cadence target) live in `progression.md` as Phase-3 tuning seeds per `design-docs/RULES.md §4` — expected to drift through the T3-4 balance harness; NOT DECISIONS.md-locked. User reviewed + approved the headline balance numbers before commit. Supersedes: none (extends the `MIGRATION_AUDIT.md` carry-forward pattern; the FW v1 archive `breakthrough-moments.md` 2026-04-24 resolutions are preserved as reconciled forward references, not re-decided; ADR-0005 remains the authoritative mechanism contract — these two docs are the design + tuning layer on top).

- **2026-05-21 — T3-4 — breakthrough mechanism lives in `fw-memory`, not `fw-content`; no `fw-content → fw-memory` edge.** The MASTER_PLAN T3-4 row stated the breakthrough mechanism (signature awakening / latent-flag unlock / regressive collapse) goes in `fw-content`. That label was stale planning-time text written before T3-1/T3-2 landed. **Decision** (user-confirmed during the T3-4 `/next` spec): the mechanism lives in `fw-memory` as a new module (`crates/fw-memory/src/breakthrough.rs`). Rationale: the mechanism READS the memory ledger (event accumulation) and EMITS `EventClass::BreakthroughMoment` / `RegressiveCollapse` `MemoryEvent`s — and `MemoryEvent`, `MemoryLedger`, the `EventClass` variants, and the 5 readers all live in `fw-memory` (shipped T3-1/T3-2). ADR-0005 describes it as a `fw-memory`-centric mechanism, and T3-2's `CoachReader` explicitly deferred its "breakthrough-readiness signal" as a `fw-memory` follow-up. Placing it in `fw-content` would force a NEW `fw-content → fw-memory` dependency edge — the exact edge the 2026-05-18 T3-3 decision deliberately avoided (fw-content's news/quote render takes resolved-slot context structs precisely to stay free of fw-memory). **Consequence**: zero new cross-crate edges. Player data the mechanism needs (per-family PA/CA, the 4 narrative-trigger gene flags, pending signature candidates, age) is passed in via a caller-supplied `BreakthroughContext` struct — the same render-from-context pattern T3-3 established — so `fw-memory` does NOT gain an `fw-content` dependency either. The PA/CA redraw produces delta values carried in the `BreakthroughMoment` event's `consequence` field; APPLYING the delta to a stored player record is a future career-loop concern, not T3-4's. The MASTER_PLAN T3-4 row's crate label was corrected `fw-content → fw-memory` at task close. Supersedes: none (the MASTER_PLAN row label was never a decision, just a planning-time guess; this entry records the corrected placement + the reasoning so a future reader doesn't re-litigate it).

- **2026-05-21 — T2-4 — PlayerBio T2-4 scope corrected to the design-doc MVP boundary; `NarrativeFlag` canonically in fw-content.** Two decisions land at T2-4. **(1) Scope correction.** The MASTER_PLAN T2-4 done-criterion said "500 players generated" — stale planning-time text that predated the 2026-05-18 `design/player-generation.md` port. The design doc's §"MVP boundary" explicitly scopes T2-4 to **22 hand-authored compiler-shaped `PlayerBio` fixtures ("NOT compiler output")** + the binding type contract + a `PlayerBioValidator`, and defers the procedural ~2000-2400-player gene-distribution-from-cultural-priors compiler to **T4+**. The design doc — authored by the user specifically to unblock T2-4 — is the authoritative + newer source; the MASTER_PLAN criterion was corrected to match (user-confirmed at the T2-4 `/next` ambiguity gate). Consequence: T2-4 ships the type contract + validator + 22 fixtures; the procedural compiler is a T4+ row. **(2) `NarrativeFlag` placement.** The 4 narrative-trigger gene flags (`flow_access` / `peak_ceiling_high` / `late_bloomer` / `awakening_dormant`) are part of the 22-field gene model, which is fw-content's domain. T3-4 had already created a `NarrativeFlag` enum in `fw-memory` (for `BreakthroughContext`) as a stopgap before T2-4 ran. **Decision**: T2-4 defines `NarrativeFlag` canonically in `fw-content` (`gene.rs`) — the gene model's home crate. fw-memory's parallel copy stays UNTOUCHED; fw-content must NOT depend on fw-memory (the edge the T3-3 decision deliberately avoided), and fw-memory's breakthrough mechanism takes its flags via the caller-passed `BreakthroughContext` (the established decoupling pattern). The two parallel 4-variant enums are an accepted, conscious duplication at the crate boundary; a future dedup (relocate to `fw-core`, or have the career-loop caller map between them) is a logged follow-up, not T2-4 scope. **(3) minor — validator-test crate placement.** A validator-exercising test belongs in the validator's crate: the `PlayerBioValidator`-against-22-fixtures test lives in `crates/fw-content-baker/tests/`, NOT `fw-content/tests/`, to avoid forcing an `fw-content → fw-content-baker` dev-dependency back-edge. Supersedes: none (the MASTER_PLAN "500 generated" criterion was never a decision, just a planning-time guess; this records the corrected scope + the cross-crate type-placement calls so they aren't re-litigated; the gene-range encapsulation gold-plating — try_new + private fields + TryFrom-Deserialize — is a deferred follow-up beyond the `GeneSnapshot::validate()` form shipped at T2-4).

- **2026-05-21 — T3-5 — `design/scouting.md` ported + consolidated from two FW v1 archive sources; Path B (single-scout uncertainty) is the FW v2 MVP, Path A (3-archetype disagreement) is a conditional T4+ expansion.** MASTER_PLAN T3-5 references "Path B fallback", "3 archetypes", and a "Phase-3 feel-prototype" gate — all terms from a scouting design doc that did not exist in FW v2 (`fw-scouting/src/lib.rs` itself said the model was "owed `design/scouting.md` Phase-3"). Same blocker class as T2-4 (`design/player-generation.md`, ported 2026-05-18) and T3-4 (`design/breakthrough-moments.md`, ported 2026-05-20). **Decision** (user chose "Port docs + ship T3-5 now" at the T3-5 `/next` ambiguity gate): port + consolidate the two FW v1 archive sources — `design/scout-disagreement.md` (207 LoC) + `design/adr/adr-0007-scout-archetype-schema.md` (389 LoC), both locked 2026-04-24 — into a single FW v2 `design/scouting.md`. Reconciliations (T2-4 port precedent): C#/Unity → Rust/`fw-scouting`; `IdentityPacket`→`PlayerBio`, `InternalGeneSnapshot`→`GeneSnapshot`, `Fixed`/`Q32.32`→`Q32`, `ImmutableArray`→`Vec`, `ContentPackQualifiedId`→qualified `String`; Unity Phase/Month numbering → FW v2 T-phases; FW v1 SPEC.md refs → `docs/DECISIONS.md`; archive ADR-0004→FW v2 ADR-0005, archive ADR-0006→`design/player-generation.md`+ADR-0002. **No new ADR** — per the T2-4 precedent the port lands as a `design/` doc logged here, not as a `docs/adr/` entry (FW v2 ADR-0007 is already `dev-verification-surface`; the archive's ADR-0007 *content* — the `Scout`/`ScoutReport` schema — folds into `design/scouting.md`'s type-contract section). **Locked decisions preserved**: no omniscient scouts; identical `ScoutReport` schema across both paths; structured data canonical + prose a deterministic rendered artifact (no runtime LLM); narrative-flag zero-visibility; category-level (not per-field) bias; `ScoutReport` event-class-free (emitter picks the class — keeps `fw-scouting` off an `fw-memory` edge); track record is save-state. **Two reconciliation calls made in the port.** (1) **Path selection is structural, not feel-gated.** FW v1 made Path A vs B a Month-4 feel-test verdict; FW v2 has no Month-4 calendar, so T3-5 ships Path B (`BasicScoutUncertainty` single report) outright and Path A (the 3 archetypes `PhysicalProfiler`/`TechnicalPurist`/`RegionalExpert` + bias-filter + regional noise) becomes a documented conditional T4+ expansion — the `ScoutArchetypeKind` enum carries the reserved slots so the schema is stable, but only `BasicScoutUncertainty` is constructed at T3-5. (2) **`UncertaintyBand` is a FW v2 addition.** The archive `ScoutReport` carries numeric `Confidence` + tooltip-only `[LowerBound, UpperBound]` ranges; FW v2's T3-5 done-criterion ("uncertainty bands display as text labels, not numbers") needs a text surface, so the port adds a 4-variant `UncertaintyBand` enum (`Hunch`/`Tentative`/`Confident`/`Settled`) with `display_label()` football-native text + a `from_confidence(Q32)` mapping — the same exhaustive-`display_label` pattern shipped for `PhenotypeLabelId` at T2-7. **Tuning seeds NOT DECISIONS-locked**: the `BasicScoutUncertainty` noise amplitude + band half-width, the label-confidence draw range, the `UncertaintyBand` thresholds, and the (future) Path-A `CategoryBiases` are T3 tuning seeds living as `Q32` constants in `fw-scouting`, revisable during balancing without a DECISIONS entry — per the archive's "`CategoryBiases` are Phase-3 tuning seeds, not SPEC-locked" framing reconciled to "T-phase tuning seeds." Supersedes: none (extends the `MIGRATION_AUDIT.md` carry-forward catalog — `design/scout-disagreement.md` + `adr-0007-scout-archetype-schema.md` move from un-ported to ported; the FW v1 2026-04-24 resolutions are preserved as reconciled forward references, not re-decided; ADR-0002's "scout uncertainty is FOF-style ranges keyed to scout skill" remains the high-level contract this design doc implements).

- **2026-05-21 — T3-R-E — SaveV3 career-state persistence: snapshot the season, not a replay cursor; new `fw-save → fw-content` crate edge.** Codex post-T3 review finding E5: a `SaveV2` stored the career ledger but not enough runtime state to resume a career mid-season. Through V2, `fw-save`'s module doc stated `SeasonState` was intentionally NOT persisted because the seed regenerates it via `generate_league` — but the seed regenerates the league STRUCTURE, not a season's PROGRESS (which match-days are played, the live standings) or the `season_number`. **Decision** (executing the MASTER_PLAN T3-R-E row + the E5 finding; one design fork resolved at the `/next` spec): bump the save schema to `SaveEnvelope::V3(SaveV3) = 3` (wire byte `0x03`, locked FOREVER). `SaveV3` = V2's `career_seed`/`content_pack_version`/`ledger` + `season_number: SeasonNumber` + `season: Option<SeasonState>` + `breakthrough_states: BTreeMap<PlayerId, BreakthroughState>`. **Fork resolved — snapshot, not replay-cursor**: the row offered "the active `SeasonState` (or a deterministic replay cursor that reconstructs it)"; snapshot was chosen because `SeasonState` (fw-content) and `BreakthroughState` (fw-memory) ALREADY derive serde — `BreakthroughState`'s own doc says "serialized in the career save" — i.e. the codebase was pre-designed for snapshotting, and a replay cursor is a fragile optimization (any sim-determinism change breaks old saves). **`season` is `Option`**: `migrate_v2_to_v3` is a pure function with no `ContentStore`, so it cannot synthesize a `SeasonState` — it sets `season: None`, which a loader/caller reads as "no persisted season; regenerate a fresh one from `career_seed`"; a genuine V3 career save sets `Some`. **Consequences**: (1) a new workspace-internal `fw-save → fw-content` Cargo dependency edge (for `SeasonState` + `generate_league`) — cycle-checked clean (`fw-content` does not depend on `fw-save`); (2) `fw-memory`'s `BreakthroughState` gained a one-line `PartialEq, Eq` derive so `SaveV3` (hence `SaveEnvelope`) keeps its equality derives; (3) V3 is the current production schema, joining the permanent V0/V1/V2 migration chain (`load_envelope` chains V0→V1→V2→V3). **Scope**: T3-R-E ships the `fw-save` schema layer only — schema + `migrate_v2_to_v3` + the 4-test V2→V3 migration discipline + a frozen `v3_career_sample.fwsave` fixture; wiring fw-tauri's `AppState`/`CareerState` to actually save/load V3 to disk is the downstream work this row unblocks ("Blocks any save/load UI"), a T4 concern. Supersedes: none (the "SeasonState intentionally not persisted" statement lived only in `fw-save/src/lib.rs`'s module doc, never a DECISIONS.md bullet; that doc paragraph was rewritten at T3-R-E to record the V3 change).

- **2026-05-22 — T4-2 — ECharts stat dashboards scope-corrected to per-team-only; per-player + per-season views split to DEFERRED row T4-2b.** MASTER_PLAN T4-2's planning-time done-criterion named "per-player + per-team + per-season views" + "no perf regressions on 5-season careers", but the T4 Tauri IPC surface (11 commands) only carries per-team data: `get_standings` (20 clubs × 9 numeric stats) + `get_fixtures` (38 results per club) are rich and chartable, while per-player stats do not exist (`PlayerBio` / `SquadPlayer` are identity records — no goals / appearances / minutes / ratings, and players are not mapped to club rosters) and per-season data is just `CareerOverview.history` (a `{season, championClubName}` list — nothing numeric to chart). Both missing layers depend on the not-yet-built T4+ career-roster layer (flagged in STATUS.md Phase T4 notes and the T2-7 / T3-6 closure records). **Decision** (user chose "Per-team dashboard now" at the T4-2 `/next` ambiguity gate): T4-2 ships the per-team ECharts dashboard (new `/stats` route — ranked bar, GF-vs-GA scatter, per-club cumulative-points trend) against the two existing IPC commands; the per-player + per-season stat dashboards split to a new MASTER_PLAN row **T4-2b**, status DEFERRED, career-roster-gated — promoted to TODO once the career-roster + per-player-match-stat layer lands. Same stale-planning-spec correction pattern as T2-4 ("500 players generated" → 22 hand-authored fixtures, 2026-05-21) and T2-7 ("20 clubs' squads viewable" → the 22-fixture pool, 2026-05-21). Consequence: T4-2's MASTER_PLAN done-criterion was rewritten; DEFERRED row T4-2b added; T4-4 (which deps T4-2) is unaffected. Supersedes: none.

- **2026-05-22 — Licence posture clarification + prior-art research removed from the public repo + named-project references sanitized across public docs.** Context: the repo at `github.com/osagberg/FinalWhistle` is PUBLIC, with `license = "LicenseRef-Proprietary"` declared in `Cargo.toml` workspace metadata and a "Proprietary. All rights reserved." section in `README.md` — but no `LICENSE` file existed at the repo root, and `frontend/package.json` lacked an explicit licence field. During T1 design research the match-engine architecture was informed in part by surveying two open-source Rust football sims (one GPL v3 copyleft, one Apache 2.0 permissive); the research notes were committed under `docs/research/existing-rust-sims/` and contained derivation-implying language ("Top 5 — STEAL", "adopt this shape almost verbatim") that read worse than the shipped code does. Two independent code-comparison audits run 2026-05-22 confirmed the SHIPPED code is independent of both surveyed projects on every one of the five patterns originally flagged in the private research notes — per-item verdicts: 4× INDEPENDENT + 1× PATTERN-LEVEL (API-shape only, protected by Oracle America, Inc. v. Google LLC, 593 U.S. 1 (2021)). The Apache 2.0 audit returned zero attribution owed (only overlap is industry-standard FM-attribute vocabulary, not copyrightable). **Decision** (executed 2026-05-22 at user direction): (a) added a `LICENSE` file at the repo root explicitly carrying the "All Rights Reserved" notice the README claims; (b) added a `NOTICE.md` at the repo root catalogueing the third-party-licence policy + the audit verdict on prior-art research; (c) set `frontend/package.json` `"license": "UNLICENSED"` to mirror the Cargo workspace's `LicenseRef-Proprietary`; (d) moved all of `docs/research/**` to a private sibling location at `/Users/vibelogic/dev/football-research/` and git-rm'd from the public repo — research preserved privately, not destroyed; (e) sanitized all remaining named references to the two surveyed prior-art Rust football sims across ADRs (0001 / 0002 / 0004 / 0006 / 0007), `MEMORY.md`, and `docs/MASTER_PLAN.md` to neutral "prior-art research" phrasing — the architectural reasoning in each ADR stays intact, only the project-name attribution is removed. **Consequence**: a casual reader of the public repo now sees zero specific named references to the two surveyed prior-art projects; the licence posture is explicit; the audit verdict + `NOTICE.md` cover the third-party catalogue; the historical 2026-05-13 DECISIONS entries that named those projects as research context remain unchanged (this entry supersedes only the project-name attribution that appears in those entries' "Context" sections — the architectural choices in ADR-0001 / ADR-0004 themselves stand). **Git history note**: the research docs remain reachable via `git log --all` for anyone digging through history; rewriting git history to remove them entirely is a separate `git filter-repo` + force-push operation reserved for a future explicit decision if the dev wants fuller hiding. The Cargo workspace remains `LicenseRef-Proprietary`; no licence change to FW itself. Supersedes: the project-name attribution only (NOT the architectural decisions) in the following prior entries — "**2026-05-13** — ADR-0001 Match engine architecture (status: Proposed, pending Codex audit): 7-layer stack at `docs/adr/0001-match-engine-architecture.md`. …" and "**2026-05-13** — ADR-0004 IPC command surface (Proposed): live-match quintet + 9-variant MatchCommand at `docs/adr/0004-ipc-command-surface.md`. …".

- **2026-05-22 — T4-5 — ADR-0004 live-match IPC quintet implemented (T4-5a); frontend live UI + salience-band jump split to DEFERRED T4-5b.** Context: T4-5 ("match-day live mode") was a single MASTER_PLAN row that sat on top of (a) ADR-0004's live-match command quintet, which was specified but NEVER implemented — the live IPC surface did not exist — and (b) a "salience-band-N event mode" whose semantics depend on salience-on-MatchEvents wiring that does not exist (salience lives on MemoryEvents, computed post-match, per ADR-0005). **Decision** (user chose "implement the real live IPC, split the UI" at the `/next` ambiguity gate): T4-5a ships the server-side ADR-0004 quintet (`start_live_match` / `step_live_match` / `get_match_snapshot` / `apply_match_command` / `finish_live_match`) + the closed 9-variant `MatchCommand` + the `MatchSnapshot` DTO + a `LiveMatchSession` handle store (`RwLock<BTreeMap<u32, _>>` + `AtomicU32` allocator) in `AppState`; T4-5b (new DEFERRED row) ships the tick-by-tick frontend live UI + the salience-band jump. **Three implementation-scoping calls made** (the ADR-0004 *contract* is unchanged — these are how the MVP realises it): (1) **`apply_match_command` fail-loud-stubs all 9 variants** via a new `IpcError::LiveMatchCommandUnimplemented { command_kind }` — the closed enum + IPC plumbing are real, but per-variant sim *effects* (substitution, formation change, set-piece takers, team talk) do not exist yet and each earns its own future row; returning the error is honest (vs a silent Ok-with-no-effect). (2) **`MatchSnapshot` ships with documented defaults for fields that have no current sim source** — `yellow_cards`/`sent_off` empty (no card system at T1), `possession_pct` a running tick-ratio, `ball_zone` a 5-bucket projection of `ball.pos_x`, lineups from player slots 0-10/11-21; the DTO contract matches ADR-0004 §3 so T4-5b + future sim work fill the fields without an IPC change. (3) **Live mode preserves `play_match`'s run-past-FullTime behaviour** — `step_live_match` does NOT halt the sim at FullTime, because `play_match_inner` runs its full tick budget unconditionally and T4-5a's load-bearing guarantee is determinism-equivalence (`live_match_step_equivalent_to_play_match`: step×N == `play_match(N)` canonical hash); an IPC-layer "stop at FullTime" guard would diverge the two paths. Halting the sim at full time is a deferred `fw-match-sim`-level concern (both paths would then inherit it, preserving equivalence). **Consequence**: the live IPC surface exists + is determinism-safe + concurrent-match-capable; per-variant command effects + the card/possession/phase sim state + the FullTime halt + the frontend live UI are all explicit follow-ups. Supersedes: the "(Proposed)" status implied in the "**2026-05-13** — ADR-0004 IPC command surface (Proposed): live-match quintet + 9-variant MatchCommand at `docs/adr/0004-ipc-command-surface.md`. …" entry — ADR-0004's quintet + MatchCommand shape are now implemented as specified (T4-5a); the ADR's command *contract* stands unchanged.

- **2026-05-22 — T4-6a — App settings persist via a NEW versioned `SettingsEnvelope` in `fw-save` (wire byte `0x00` for V0, locked forever), separate from the per-save `SaveEnvelope` chain; T4-6's design-heavy surfaces split to DEFERRED T4-6b.** Context: MASTER_PLAN T4-6 ("settings screen") bundled five subsystems with no existing foundation — no settings persistence (`fw-save` held only game saves SaveV0–V3), no keybind system (nav hotkeys are display-only; `Layout.tsx` said "Real key handling lands at T4-6"), no `/settings` route, theme not persisted across launches, and no design for the colourblind palettes / text-scale steps / rebindable-action catalogue — plus an architecturally-load-bearing persistence acceptance ("via the save layer's version-migration chain"). **Decision** (user chose "split; ship the persistence foundation now" at the T4-6 `/next` ambiguity gate): T4-6a ships the versioned persistence layer + the two settings that need ZERO new design (theme, reduce-motion); T4-6b (new DEFERRED row) ships text-scale + colourblind palette toggle + key rebinds + save-folder location, each needing its own `/log-decision` for its design specifics before implementation. **Architecture call — a SEPARATE envelope, not a SaveEnvelope variant.** Settings persist via a NEW `SettingsEnvelope` enum in `fw-save` that MIRRORS the `SaveEnvelope` forward-migration discipline EXACTLY (the faithful reading of the acceptance's "version-migration chain") but is a DISTINCT type writing a DISTINCT file: `SettingsEnvelope::V0(SettingsV0)` carries wire byte `0x00` (locked FOREVER, like the SaveEnvelope wire bytes), `SettingsV0 { theme: ThemePref (Light|Dark), reduce_motion: bool }`. Rationale: settings are APP-GLOBAL (one per install), whereas a `SaveEnvelope` is PER-SAVE-GAME (one per career) — conflating them would force every game save to carry UI prefs and every settings write to round-trip a whole career. The settings file is `settings.fwcfg` in the Tauri OS app-config dir (`app.path().app_config_dir()`), resolved in `src-tauri/main.rs`'s `.setup()` (NOT a CWD-relative path, which would silently drift with the launch directory) with a loud `log::error!` fallback to a CWD-relative file if the platform cannot supply an app-config dir. `load_settings_envelope` is a V0-only forward-migration chain today; each T4-6b setting adds a field → bumps to V1/V2/… each owing the four-test migration discipline (forward-migration + callback-preservation + forward-incompat-failure + round-trip-byte-identical) per CLAUDE.md §9, exactly as the SaveEnvelope chain does. **Consequence**: `fw-save` now has two independent versioned envelopes (game-save SaveV0–V3 + settings SettingsV0); a new `IpcError::SettingsLoadFailed`; a `/settings` route + load-on-startup + reduce-motion animation gating; theme now persists across launches. **Honesty note — `get_settings` returns documented defaults (Light, no-reduce-motion) only when the file is ABSENT**, and `load_settings_envelope` errors loudly (does not silently default) on a present-but-corrupt file. Supersedes: none (no prior DECISIONS bullet covered settings persistence; the `Layout.tsx` "Real key handling lands at T4-6" comment is superseded in CODE by the T4-6b split, not a decision bullet).

- **2026-05-29 — T4 resequence — career-roster layer (sub-rows T4-2.5a–h) inserted ahead of T4-7/T4-9 to wire pillars 2/3/4/5 into a played career.** Context: the 2026-05-29 mid-T4 fresh-eyes review (`docs/audits/mid-t4-fresh-eyes-review-2026-05-29.md`, a 29-agent two-lens audit by Opus 4.8) found ZERO P0/P1 correctness bugs but established that 4 of 5 design pillars produce zero player-visible output in a real career: every save serves the same 22 hand-authored `PlayerBio` records (no per-club rosters, no seed-varied players); only one event class (`TitleWon`, club-only) is ever emitted (so `/player` career-moments is provably empty + no breakthrough or scout observation ever fires in gameplay); signatures fire for only slot 7; and league standings come from seeded-procgen scorelines rather than the 22-player tick engine. The single unscheduled foundation gating all of this — a per-club player roster with mutable per-player state — was an implicit dependency that four already-deferred rows (T2-4, T2-7, T4-2b, T4-5b) pointed at without any row building it; three consecutive rows (T2-4, T2-7, T4-2) were hollowed at their `/next` gate against it. **Decision** (user "go", 2026-05-29): insert ID'd sub-rows T4-2.5a–h into MASTER_PLAN ahead of the discretionary polish rows T4-7 (game-shell) + T4-9 (stretch viewer), per the blueprint at `docs/design/career-roster-layer.md`. The layer adds (a) `CareerState.roster: BTreeMap<ClubId, Vec<PlayerInstance>>` in `fw-tauri` where each `PlayerInstance` owns mutable `PlayerAttributes` + `AbilityCeiling` + `signature_candidates` + `BreakthroughState` + `PlayerSeasonStats`; (b) a new `fw-core::attribute_family_bridge` mapping the 55 attributes → the 10 `AttributeFamily` PA/CA buckets on the 1..=200 scale that `BreakthroughContext` requires (+ the inverse to write redrawn deltas back) — the missing piece with no current implementation; (c) SaveV4 persisting the roster + per-player state via the existing forward-migration chain + the four-test discipline; then wiring `evaluate()` (pillar 3), `observe_player` + `get_scout_report` IPC (pillar 4), all-22-slot signature candidates (pillar 5), and player-subject `MemoryEvent`s — `PlayerDebut`/`LegacyGoal`/`BreakthroughMoment` (pillar 2). **Two engineering calls baked in**: (1) **match-engine policy** — the player's own club's 38 matches run the real `play_match` tick engine; the other 360 fixtures stay seeded-procgen, keeping a 10-season career at ~20s (under the T5-5 <60s target) while making the player's own stats/signatures/breakthroughs genuine; (2) **first-increment generation** — populate the 20 clubs by assigning the existing 22-`PlayerTemplate` pool across them (the procedural ~2000-player compiler stays deferred). **Consequence**: T4-7/T4-9 are deferred until the layer lands; T4-2b + T4-5b unblock once T4-2.5h ships; the four P2 correctness findings + the cheap per-signature-commentary win remain available as independent side-tracks. **Explicit open follow-up (a SEPARATE decision, NOT decided here)**: reconcile `DESIGN_DOC §MVP-scope`'s promised 6-tier ~96-club LLM-baked pyramid with the shipped single-20-club-league reality — the roster layer is built to scale to the pyramid but does not require it; the procedural player compiler + 6-tier pyramid + LLM bake pipeline (7 of 8 baker subcommands are stubs) remain a deferred post-vertical-slice phase. Supersedes: none.

- **2026-05-29 — T4-2.5a — Gene→family PA/CA bridge: formula, placement, and AttributeFamily taxonomy** — The breakthrough system (Pillar 3) requires per-family PA/CA values on the 1..=200 scale to populate `BreakthroughContext.pa_by_family` / `ca_by_family` (consumed at `crates/fw-memory/src/breakthrough.rs:1048-1049,1113-1114`). The career-roster blueprint's first draft assumed these derive from the 55 visible `PlayerAttributes` with the bridge in `fw-core`; grounding against `docs/design/progression.md` (which defines the 10 families by gene-model anchors) + the crate graph showed both halves were wrong. **Decision:** (1) `AttributeFamily` moves from `fw-memory` to `fw-core` (fundamental taxonomy, no crate-specific deps; serde wire-compat preserved — bincode encodes by variant index, discriminants 0..9 unchanged; `breakthrough_states` is empty in all shipped saves so no real save data is at risk). (2) The bridge lives in `crates/fw-content/src/breakthrough_input.rs` — it reads a `GeneSnapshot` (fw-content's own type) + an `AbilityCeiling` (fw-core); NOT `fw-core` (cannot see genes — that edge is backwards) and NOT `fw-tauri` (a gameplay formula does not belong in the IPC shell, which is not float-linted). (3) The formula is **gene-sourced**, deterministic, Q32-only, no RNG: `gene_score_f` = weighted mean of 2–3 anchor genes per family; PA = `gene_score_f × 0.75 + ceiling.potential × 0.25` scaled to 1..=200; CA = `gene_score_f × (ceiling.current / ceiling.potential)` scaled to 1..=200 then `.min(pa)` so `1 ≤ ca ≤ pa ≤ 200`. Signed genes (`mentality`, `growth_curve`, Q32 ∈ [−1,+1]) normalized via `(g + 1) >> 1` (WorkRate uses inverted normalization for `mentality` so introvert-grinders score high). (4) The bridge emits all 10 families, so `evaluate()`'s `unwrap_or(100)`/`unwrap_or(70)` defaults are reachable only for an absent family and must never fire for a bridge-initialized player. Exact anchor weights, the full 10-family table, the worked example, and the tuning dials live in `docs/design/progression.md §"Gene→family PA/CA bridge (T4-2.5a)"` per design-docs RULES §4 (coefficients out of DECISIONS). Designed by the `systems-designer` agent; implementation is `lead-programmer` (cross-crate type move + new fw-content module) under TDD. Supersedes: none.

- **2026-05-29 — Roadmap re-baseline (ratified) — EA = full 6-tier pyramid + LLM bake; new phase T4.5; T4 refocused on pillar-wiring; T5 re-sequenced.** Context: a 16-agent re-baseline workflow (12 audit slices → producer synthesis → completeness + scope-discipline critics → finalize; full proposal at `docs/audits/roadmap-rebaseline-proposal-2026-05-29.md`) found the T0-authored plan had drifted — closing T1–T3 shipped the engine + pillar *machinery* but not the pillars themselves, and the 6-tier world + LLM bake were unscheduled. User ratified the re-baseline 2026-05-29. **Seven constituent decisions:** (1) **EA scope fixed** — EA ships the full 6-tier ~96-club single-nation pyramid + LLM-baked content pipeline + ~2000-player procedural compiler (NOT a hand-authored single-league slice); the career-roster layer (T4-2.5a-h) ships the 20-club template-assign first increment, the pyramid/compiler/bake follow in T4.5; resolves DESIGN_DOC §13 OQ1 (one nation, 6 tiers, ~96 clubs; two-nation pyramid is post-EA). (2) **New phase T4.5 "World Scale + Content Bake"** inserted between T4 and T5 (6-tier pyramid generator, nation/`RegionPriors` layer, ~2000-player compiler, gene→`PlayerAttributes` forward compiler, real Anthropic-API bake pipeline + composed-name semantic validator, promotion/relegation, SaveV5, perf re-derivation, Codex gate `v0.5.0-world`); T5 now depends on T4.5. (3) **Match-engine policy** — the season ALREADY runs the real tick engine for all fixtures (discarding all but the scoreline) at a 600-tick proxy; the policy going forward is player's-club keeps full output (stats/breakthroughs/signatures) while AI-vs-AI fixtures use fast seeded scorelines (a code *reduction*, zero new model); corrects the imprecise "seeded-procgen scorelines rather than the 22-player tick engine" framing in the 2026-05-29 T4-resequence entry. (4) **T5-5 perf target retired** — the `<60s / 10-season` number was against the retired 20-club seeded substrate; re-derived at T4.5-I via a separate `/log-decision`. (5) **T4-2.5b forward-compat** — the `BTreeMap<ClubId, Vec<PlayerInstance>>` + `PlayerId` scheme must NOT assume exactly 20 clubs or template-sourced players (must scale to ~96 without structural change); binding done-criteria clause. (6) **Mods are out of EA scope** — no mod-overlay loader row in the EA plan; `mod_fingerprint` (ADR-0010) is therefore NOT in SaveV5, but the save format stays forward-compatible with adding it as an additive serde-default field in a future SaveV6; T2-R-E3 (mod negative-path tests) deferred post-EA. (7) **Item-budget cap raised ~55 → ~80** — the re-baseline adds ~23 net new rows for the previously-unscheduled world-gen phase; cut discipline (Stretch before scope; every feature in exactly one bucket) remains in force. **Open-question defaults ratified:** replay (not live-mode) satisfies the T4 gate; signature EA floor = 8-of-24 predicates implemented with all 24 authored; T4-6b deferred (promote before T5-3); single-scout uncertainty is the EA floor (multi-scout deferred); PressReader is the one EA-wired reader. Supersedes: the "open follow-up (separate decision)" + "6-tier ~96-club pyramid … remain a deferred post-vertical-slice phase" language in the "**2026-05-29 — T4 resequence**" entry above (now scheduled as T4.5, not deferred); the "~55 items" cap in `docs/MASTER_PLAN.md`; and the ADR-0010 implication that `mod_fingerprint` lands at EA.

- **2026-05-29 — EA content corpus is Claude-session-authored, NOT paid-API-baked.** Context: the LLM bake pipeline (Pillar 1) was scheduled at T4.5-D as a "real Anthropic API client" inside `fw-content-baker`. The dev (solo; Claude via subscription) flagged that a one-time, dev-time content-authoring job needs no paid API — Claude can author the content packs directly in working sessions, and the committed RON is the canonical artifact regardless of origin (consistent with the Q4 decision: checked-in RON is canonical, not prompt+seed+model). **Decision:** the EA content corpus is authored by Claude IN-SESSION (the `narrative-director` agent) and committed as FW-VAL-validated RON — NOT generated via a paid Anthropic API call from a baker CLI. T4.5-D scope changes from "build a Claude-API bake client" to "Claude-session-author the content packs → FW-VAL validate → commit RON"; `fw-content-baker` keeps its validators (`validate-structural` + the T4.5 composed-name `validate-semantic`) but needs NO API client for EA. **Scope clarification (what is actually authored):** a BOUNDED set of content packs — per-culture name banks, Tracery grammars (commentary / headlines / scout-phrases / manager-quotes / fan-reactions), and bio/club-history templates — NOT per-player text; the runtime procgen generator (gene→attribute compiler) + Tracery expand those per-seed deterministically. **Unchanged:** the no-runtime-LLM pillar (the shipped game NEVER calls an LLM — this is all dev-time authoring → committed RON); Pillar 1's LLM-friendly-content moat; cross-OS determinism. The content manifest records provenance as `claude-session-authored` rather than `model_id + prompt_hash`. An optional API-client bake path MAY be added post-EA for bulk regeneration; it is out of EA scope. Supersedes: the "Real Anthropic API client" framing of T4.5-D in the "**2026-05-29 — Roadmap re-baseline (ratified)**" entry above (the T4.5-D row, the T4.5 exit gate, the Risk Register, and `Content/RULES.md §7` are updated to match).

- **2026-05-29 — Match-engine realism via empirical calibration to real-world aggregate football metrics.** The path to the "match/beat FM" realism goal is calibration (system identification), not vibes-tuning: run thousands of deterministic seeds, aggregate the EMERGENT match metrics (goals/match, shots, possession, pass-completion, shot-conversion, touches-per-role, pass-length + pass-network distributions, positional heatmaps), and tune the engine's coefficients (BT utility weights, decision cadences, ball physics, xG coefficients per `xg-coefficients.md`, personality biases per `personality-bias-weights.md`) until the emergent distributions land in real-world target BANDS. The deterministic Q32/seeded-RNG engine makes reproducible thousand-seed sweeps tractable — this is a strength to exploit. **Layer separation (resolves the fantasy concern):** calibration targets HOW football is played (universal mechanics) + the BASELINE attribute→behavior mapping — NOT the fantasy CONTENT (names / clubs / cultures / histories) and NOT the dev/breakthrough/scouting systems (the deliberate fantasy-progression USP; never calibrated to real careers). The engine renders whatever attributes it is handed realistically; the fantasy systems drive the attributes. The two layers are orthogonal. **Data:** only PUBLIC aggregate statistical distributions/bands (e.g. ~2.7 goals/match, ~80-85% pass completion, ~50-70 touches/full-back) — public facts, NOT licensed player/club data; the no-real-data pillar is intact. **Caveats:** aggregate-metric match is necessary but NOT sufficient — pair with spatial/visual plausibility on the 2D board + the T4-9 viewer (metrics can be hit by un-football-like behaviour); condition targets on role × tactic-archetype; start with match-level metrics + a few role-level, expand later; match bands not exact values (avoid overfitting). **Consequence:** the metric bands become a proptest-style empirical-envelope REGRESSION GATE — a far stronger realism floor than today's single smoke-seed 2-2/4-goal check. Scheduled as `T5-5b` (calibration harness extending `crates/fw-match-sim/src/bin/calibrate.rs` + a new `docs/design/match-metrics-targets.md` targets doc + the metric-envelope gate); `systems-designer` + `qa-lead`; can run iteratively once the roster + BT runner produce real matches, formalized by ship. Feeds the BT/utility re-fit + the "accurate football" engine-depth work. Supersedes: none.

- **2026-06-02 — T4-sim-halt — `tick_match` self-halts at `match_end_tick`; match length default 60→5400 (a real 90 displayed-min match).** Context: the first real `tauri dev` run surfaced "FullTime + a goal every minute" spam. Root cause: `MatchState.match_end_tick` defaulted to `Tick::from_raw(60)` (a documented T1 smoke-budget placeholder), the play paths never overrode it, and `tick_match` ran ALL gameplay every tick ungated by `match_end_tick` — so the step-9 FullTime guard (which only checked `tick >= match_end_tick && last_event != FullTime`) re-emitted FullTime on every tick a Goal/Pass appended after it, while goals kept accumulating past match-end. **Decision:** (1) `tick_match` now SELF-HALTS — a step-0 freeze guard returns the state unchanged once `MatchEvent::FullTime` is the tail of `match_events`, making the function idempotent after full time (canonical state byte-identical regardless of how many extra ticks a caller requests); gameplay steps 2-8 run only inside an in-play gate (`state.tick <= state.match_end_tick`); step 9 emits FullTime exactly once (the `!already_emitted` check is removed — subsumed by the freeze guard, which proves FullTime is never already the tail at step 9 since `tick_match` is the sole FullTime emitter and emits it last). (2) The default `match_end_tick` is now `Tick::from_raw(fw_match_sim::FULL_MATCH_TICKS as i64)` where `FULL_MATCH_TICKS = 5400` (90 displayed-min × 60 ticks/game-min — the display convention already in `result.rs`). Short-match tests use the existing `.with_match_end_tick()` builder. **Determinism-equivalence preserved exactly as the T4-5a entry predicted:** the halt lives in `tick_match`, so the batched (`play_match_inner`) and live (`step_live_match_inner`) paths BOTH inherit it — they stay bit-for-bit equivalent. **Authorized multi-pin canonical-hash rebaseline** (ADR-0012 trigger #3; post-T1-15 discipline — main thread independently re-verified the 5-seed empirical envelope held BEFORE rebaselining): the rebaseline is event-only — `match_end_tick` (60→5400) is read ONLY by step 9, so with the new default the 60-/600-tick pins run gameplay byte-identically (60,600 < 5400 → gate never closes, freeze never fires) and only lose the spurious FullTime event(s) from `match_events`; goal counts UNCHANGED, so the [2,5]-pinned / [0,7]-5-seed envelope held trivially. Both pins re-pinned across all three pin sites (`canonical_hash.rs` ×2 + `fw-content/tests/fixtures_load.rs`) + both `.ron` fixtures + the smoke insta snapshot. **Explicitly NOT in scope (deferred to T5-5b calibration):** goal-RATE realism — a real 5400-tick match still scores unrealistically many goals until the engine is calibrated; this row fixes match LENGTH + the halt, not the rate. The season path (`SEASON_MATCH_TICK_BUDGET = 600`) deliberately keeps its truncated budget (so it runs short of `match_end_tick` and emits no FullTime — `play_one_match` reads the score, not FullTime); raising it to a full match is part of the T5-5b goal-rate calibration. Supersedes: the deferral clause "(3) **Live mode preserves `play_match`'s run-past-FullTime behaviour** … Halting the sim at full time is a deferred `fw-match-sim`-level concern (both paths would then inherit it, preserving equivalence)." in the "**2026-05-22 — T4-5 — ADR-0004 live-match IPC quintet implemented (T4-5a) …**" entry above — the sim now halts as that clause anticipated, and both paths inherit it.

- **2026-06-02 — Decision 5 — Career-roster forward-compatibility: the `BTreeMap<ClubId, Vec<PlayerInstance>>` shape + the `PlayerId` derivation scheme MUST NOT assume exactly 20 clubs or template-sourced players (T4-2.5b pre-task gate).** Context: T4-2.5b builds the first career-roster increment (20 clubs × 22 players via template-assign), but the ratified EA scope (the 2026-05-29 roadmap re-baseline + `docs/design/career-roster-layer.md`) is a 6-tier ~96-club pyramid (~2000 players) populated by the T4.5-E1 procedural gene→attribute compiler. The blueprint requires (constituent decision #5 of the re-baseline, hereby extracted into a standalone binding pre-task clause so T4-2.5b's done-criteria can cite it) that the roster data structures introduced at T4-2.5b scale to the pyramid WITHOUT structural change. **Decision (binding done-criteria clause for T4-2.5b):** (1) the roster is keyed `BTreeMap<ClubId, Vec<PlayerInstance>>` where `ClubId` is the club's content-qualified id (a `String`, consistent with how clubs are already keyed) — career-start generation iterates the actual `league.clubs` set, NEVER a hardcoded count of 20. (2) `PlayerId` is a per-career-distinct stable handle derived deterministically from `(career_seed, club_index, slot)` that stays collision-free at ~2000-player pyramid scale; a test asserts uniqueness across the full generated set. (3) Per-player attribute / ceiling / signature SOURCING is an implementation detail that T4.5-E1 replaces wholesale (template-assign now → gene→attribute compiler later) — T4-2.5b must not bake the 20-club or single-`PlayerTemplate` assumption into any type signature or persisted shape (SaveV4 at T4-2.5g serialises the same map shape forever). A T4-2.5b test must demonstrate the generation + ID scheme work for a club count other than 20 (or assert no `== 20` / `20`-literal gates the roster build). **Code-reality note (verified 2026-06-02):** the content tree currently has 1 `PlayerTemplate` fixture (attributes + ceiling + signatures) and 22 `PlayerBio` fixtures (identity + genes + scout-labels); `ProcGenTeam` already generates 22 distinct names + a manager per club deterministically. The first increment sources per-player NAME/role from the per-club `ProcGenTeam` and attributes/ceiling/signatures from the available `PlayerTemplate` pool indexed deterministically (N-template-safe; with 1 template, first-increment players share an attribute baseline — real per-player diversity is T4.5-E1, explicitly out of T4-2.5b scope). Supersedes: refines (does NOT supersede) constituent decision (5) of the "**2026-05-29 — Roadmap re-baseline (ratified)**" entry — that clause is now a standalone binding pre-task gate for T4-2.5b.

- **2026-06-02 — Decision 5 clause (1) clarification — `ClubId`/`PlayerId` are the `fw_core` u32 newtypes, NOT `String`s.** When T4-2.5b was implemented, the code reality made Decision 5's clause (1) parenthetical imprecise: it said `ClubId` is "the club's content-qualified id (a `String`, consistent with how clubs are already keyed)". Correction: clubs are keyed by `fw_core::ClubId` — a **u32 newtype** (`TeamTemplate.id: ClubId`; `league.rs` mints `ClubId::new(club_idx + 1)`); the content-qualified **`String`** is the separate `TeamTemplate.qualified_id` field. Likewise `PlayerId` is the `fw_core::PlayerId` **u32 newtype** (the same id space `SaveV3.breakthrough_states: BTreeMap<PlayerId, BreakthroughState>` already uses), NOT a `String`; the content `String` player id is `PlayerBio.player_id` (a separate, already-bridged identity space via `parse_player_id_suffix`). The shipped T4-2.5b roster correctly uses the `fw_core` u32 newtypes for `roster: BTreeMap<ClubId, Vec<PlayerInstance>>` and `PlayerInstance.player_id`. The binding substance of Decision 5 (no hardcoded 20-club count; collision-free `PlayerId` at pyramid scale — implemented as the bijective `club_idx * SLOTS_PER_CLUB + slot`; no 20-club/single-template assumption baked into any persisted shape) is UNCHANGED and was met. This entry corrects only the id-TYPE prose in clause (1); it does not change any requirement. Supersedes: the "(a `String`, consistent with how clubs are already keyed)" parenthetical in clause (1) of the "**2026-06-02 — Decision 5 — Career-roster forward-compatibility …**" entry above.

- **2026-06-02 — T4-2.5c — signature candidates are ROLE-MATCHED to formation slots, not sprayed across all 22.** Context: `docs/design/career-roster-layer.md` §5 says pillar-5 wiring should make "all 22 players' candidates enter the sim (not just slot 7)." Implementing that as an indiscriminate all-22-slot spread of the content template pool's `signature_candidates` was tried first and **REJECTED**: with the single authored template (an attacking-midfielder whose `first-time-diagonal-switch` signature carries `pass_mul=2.0`), giving every slot the AM candidates made all 6 midfielders + the GKs over-pass, collapsing shooting to **0 goals** on the pinned seed (the 5-seed empirical envelope broke — caught by the main-thread pre-rebaseline verification, exactly the post-T1-15 gate working as intended). **Decision:** a content template's `signature_candidates` are assigned to a slot ONLY when the slot's formation `Role` (the existing `PlayerState.role`) matches the template's `preferred_role` (mapped via `preferred_role_to_formation_role(&RoleId) -> Role`). With the 1 AM template this means the 6 MID slots (5-7 home, 16-18 away) receive candidates; GK/DEF/FWD slots stay empty until role-appropriate templates are authored. The roster path (`play_one_match` → `build_slot_signatures`) applies the same role gate (`role_receives_candidates`). **Consequence:** (1) the "all 22 players' candidates enter the sim" intent is honoured as "each player gets its role-appropriate signatures" — the wiring is role-driven, not slot-7-hardcoded, and scales when per-role/per-player templates arrive at T4.5-E1; (2) the pinned 600-tick envelope holds (4 goals, in [2,5]) and the canonical 600-tick pin rebaselined to `206bddae…57a9` (single-pin — the 60-tick bare-`initial` path is untouched); (3) the AM template's FTDS pass-bias being strong enough to zero out scoring when over-applied is noted as a signature-TUNING observation for the systems-designer + the T5-5b calibration track — NOT addressed here (this row wires structure, not balance). **Calibration flag:** 2 of the 5 envelope sanity seeds score 0-0 (within the accepted [0,7] band; the pinned seed is the [2,5] contract) — goal-rate/seed-health across the seed-space is a T5-5b concern, not a T4-2.5c blocker. Supersedes: refines the "all 22 players' candidates enter the sim (not just slot 7)" wording of `docs/design/career-roster-layer.md` §5 to "role-matched per-slot assignment" (the blueprint intent is met; the spray interpretation is rejected). Prior DECISIONS bullets: none on signature-slot assignment.

- **2026-06-02 — T4-2.5e — pillar wiring T4-2.5d DEPENDS ON T4-2.5e (not parallel); resequenced.** Context: `docs/design/career-roster-layer.md` §8 sequenced the career-roster pillar-wiring rows as "d/e/f parallel after b." Scoping T4-2.5d (pillar-3 breakthroughs) revealed this is wrong: `fw_memory::breakthrough::evaluate()` accumulates meters + fires breakthroughs ONLY off player-subject gating events present in the `MemoryLedger` (`SignatureFirstFired`/`LegacyGoal`/`HatTrickScored`/… per the `accumulate` table), and a breakthrough additionally requires a pending signature candidate or a narrative gene flag. Those player-subject events are emitted by **T4-2.5e** (pillar 2); the ledger otherwise holds only club `TitleWon`. So d's done-criterion ("≥1 BreakthroughMoment in 5 seasons") cannot be met until e has populated the ledger. **Decision:** T4-2.5e ships BEFORE T4-2.5d; the T4-2.5d MASTER_PLAN row gains an explicit `T4-2.5e` dependency. **Also surfaced (logged so d starts clean): the gene-source gap** — rostered `PlayerInstance`s carry no `GeneSnapshot` (they are generated from `PlayerTemplate`, which has none; genes live only on the 22 `PlayerBio` fixtures), yet the T4-2.5a bridge (`gene_family_pa_ca`) that d uses to build `BreakthroughContext` requires `&GeneSnapshot`. d's pre-task is to resolve this — recommended: add `genes: GeneSnapshot` to `PlayerInstance`, populated at career-start by round-robining the bio-gene pool until T4.5-E1's procedural gene generator replaces the source. Supersedes: the "d/e/f parallel after b" ordering claim in `docs/design/career-roster-layer.md` §8 (d is now downstream of e).

- **2026-06-02 — T4-2.5e — roster `PlayerId` space is OFFSET by `ROSTER_PLAYER_ID_BASE` to never collide with content-bio ids.** Context: T4-2.5b's roster `PlayerId` scheme (Decision 5, refined: `club_index * SLOTS_PER_CLUB + slot`) is 0-based, range `[0, 2111]` at pyramid scale. Content `PlayerBio` fixtures carry String ids `fwh.core:player_00001..00022` whose numeric suffix (1..22) is decoded to `PlayerId::new(N)` by `parse_player_id_suffix`. These two independent identity spaces COLLIDE (`PlayerId(1)` = bio #1 AND roster club0-slot1). T4-2.5e was the first code to resolve a player through both, and `get_player_detail_inner` produced a chimera (a bio's labels + a different roster player's name/events). The self-review triple flagged it across all three reviewers; type-design recommended fixing it NOW because SaveV4 (T4-2.5g) will serialize `BTreeMap<ClubId, Vec<PlayerInstance>>` keyed on these ids — freezing a colliding space into the save format. **Decision:** add `pub const ROSTER_PLAYER_ID_BASE: u32 = 1_000_000` (well above any content-bio count, which is capped < 100_000 by convention) and generate roster ids as `ROSTER_PLAYER_ID_BASE + club_index * SLOTS_PER_CLUB + slot` (range `1_000_000..~1_002_111` at 96-club scale, comfortably within `u32`). The two id spaces are now non-overlapping BY CONSTRUCTION (the same flavor of invariant as the bijective scheme itself). `get_player_detail_inner` routes by range: suffix `< ROSTER_PLAYER_ID_BASE` → content-bio path (the bio's own name; the prior roster-name override is removed); suffix `>= BASE` → roster path. **Consequence:** roster ids are non-canonical (CareerState only — both canonical match pins UNCHANGED); the bijectivity test asserts `min == BASE`, `max == 1_002_111`, and a `const`-assert that `BASE > 100_000`; this is the id layout SaveV4 (T4-2.5g) will persist. A full `IdKind`/separate-roster-id-type was considered and rejected as over-engineering for two call sites (no-speculative-abstraction rule) — the range offset gives the same correctness guarantee for a fraction of the surface. Supersedes: refines the `PlayerId` derivation in the "**2026-06-02 — Decision 5 …**" entry (the `club_index * SLOTS_PER_CLUB + slot` formula now carries the `ROSTER_PLAYER_ID_BASE` offset); the "collision-free at pyramid scale" requirement of Decision 5 clause (2) is unchanged and still met.

- **2026-06-02 — T4-2.5d (pre-task) — breakthrough wiring: gene source, age default, and the per-family→ceiling write are first-increment approximations replaced wholesale at T4.5-E1.** Context: T4-2.5d wires `fw_memory::breakthrough::evaluate()` into `advance_season_inner`, building a `BreakthroughContext` per rostered player via the T4-2.5a `gene_family_pa_ca(&GeneSnapshot, AbilityCeiling) -> FamilyPaCa` bridge. Three input gaps surfaced (rostered `PlayerInstance`s carry no `GeneSnapshot`, no age, and the per-family delta has no defined write-back to the single `AbilityCeiling`), each blocking the wiring. **Decisions (binding for T4-2.5d; all explicitly first-increment, NOT load-bearing balance):** (1) **Gene source** — add `genes: GeneSnapshot` to `PlayerInstance`, populated at career-start (`build_roster_from_league`) by round-robining the authored `PlayerBio` gene pool (`bio.internal_gene_snapshot` across the 22 fixtures) deterministically by index. With 22 distinct gene-sets across ~440 players the breakthrough propensities vary enough to fire; T4.5-E1's procedural gene generator replaces the SOURCE without changing the `PlayerInstance.genes` field (forward-compatible, SaveV4-persisted). `genes` also supplies `BreakthroughContext.narrative_flags` (the `GeneSnapshot.narrative_flags: BTreeSet<NarrativeFlag>` mapped fw-content→fw-memory by-name, per blueprint §5 — the two crates carry parallel `NarrativeFlag` enums). (2) **Age** — `PlayerInstance` carries no age; supply `BreakthroughContext.age_years` from a deterministic per-player default (a fixed development-prime age or a seeded draw in a young range — implementer's call, documented). Real ages arrive with the player generator at T4.5-E1; age here only feeds the age-curve modifier and is not a tuned threshold. (3) **Per-family→ceiling write** — a `BreakthroughOutcome { delta_pa, delta_ca, family }` bumps the player's single `AbilityCeiling` (delta_pa → potential, delta_ca → current, clamped, via a NEW PUBLIC fw-core ceiling mutator wrapping the existing `pub(crate) redraw_ceiling`). The `family` is the NARRATIVE attribution (which area broke through) + the meter-gating key — it does NOT yet flow back into the 55-field `PlayerAttributes` (the per-family→attribute recompile is the T4.5-E1 gene→attribute compiler). So at T4-2.5d a breakthrough lifts CA/PA + emits a `BreakthroughMoment` event, but does NOT yet change in-match behavior (the 55 attrs) — a documented first-increment limitation consistent with the pillar-wiring pattern. **Consequence:** NON-canonical (ceiling/ledger/genes are all `CareerState`; both match pins UNCHANGED). Adds a public `AbilityCeiling` breakthrough-mutator to fw-core (AbilityCeiling is not in the canonical `MatchState` encoding). Supersedes: completes the "real `redraw_ceiling` write is T4-2.5d" deferral noted in the "**2026-05-29 — T4-2.5a — Gene→family PA/CA bridge …**" entry (T4-2.5a shipped `apply_family_delta` integer-clamp only; T4-2.5d adds the ceiling write); refines `docs/design/career-roster-layer.md` §2 (`PlayerInstance` gains a `genes` field + an age default, which §2's field list omitted).

- **2026-06-02 — T4-2.5f — scouting observes a re-derived source `PlayerBio`; single-scout, XI-only, cache-read IPC.** Context: T4-2.5f wires pillar 4 by running `fw_scouting::observe_player(&Scout, &PlayerBio, career_seed, observation_id) -> ScoutReport` per match-day and caching the result on `PlayerInstance`. Three gaps surfaced. (1) **Bio source.** `observe_player` reads `PlayerBio.internal_gene_snapshot` + `scout_labels` + `player_id`, but a rostered `PlayerInstance` carries only `genes` (round-robined off the bio pool at career-start, T4-2.5d) — no bio identity, no `scout_labels`. **Decision:** re-derive the SOURCE bio at observe time via the career-start round-robin index `global_idx = player_id.raw() - ROSTER_PLAYER_ID_BASE`, indexing `content.player_bios.values()` (`global_idx % pool_len`). This is provably the same bio whose genes are on the instance — the code `assert!`s `bio.internal_gene_snapshot == instance.genes` (fires in release per Sim/RULES §11; fails loud if the `build_roster_from_league` round-robin formula ever drifts from the derivation). Rationale: re-derivation recovers real `scout_labels`, so overall confidence (mean of label confidences) is non-degenerate; a synthesized genes-only bio would have empty labels → confidence frozen at the 0.5 `NO_LABEL_DEFAULT_CONFIDENCE` → every player reads "a tentative read", hollowing the pillar. It also avoids adding a second persisted field to `PlayerInstance` (only `last_scout_report` is added → minimal SaveV4 surface). A stored `source_bio_id`/`IdKind` was rejected (no-speculative-abstraction; the index re-derivation + gene-match assert give the same guarantee). T4.5-E1's procedural bios give each player its own bio and retire the re-derivation. Empty bio pool → observation skipped with a `log::warn!` per call (mirrors `build_roster_from_league`'s empty-pool branch). (2) **Cadence.** Observe the starting XI (roster indices 0..11) of BOTH clubs of each played fixture, in `advance_week_inner`'s post-loop atomic harvest block; each observation reads `observation_id = observation_count` BEFORE incrementing (first obs uses id 0) and OVERWRITES `last_scout_report` with the latest report. Single-scout (`Scout::basic_uncertainty()`); `observe_player` is independent per-`observation_id` noise (NOT a narrowing running average) — multi-scout disagreement + accumulating track-records (convergence) stay DEFERRED to T4-2.5m/n/p. Bench (indices 11-21) unobserved this increment (matches the appearance harvest's XI-only mutation). (3) **`get_scout_report` contract.** The IPC command is CACHE-READ (reads `PlayerInstance.last_scout_report`, does not observe on call); a never-observed player → new `IpcError::NotYetObserved { player_id }` (honest "no data yet" over a fabricated zero-observation report); non-roster ids → `PlayerNotFound` (scouting is a roster-player feature). **Consequence:** NON-canonical (scouting reports + `observation_count` live only in `CareerState`; both match pins UNCHANGED). `last_scout_report: Option<ScoutReport>` is added to `PlayerInstance` with `#[serde(default)]` (JSON-path only; bincode forward-compat is the SaveV3→V4 migration, same as `genes`) — T4-2.5g must persist it, and a flagged follow-up there: `ScoutReport` carries a redundant constant `scout_archetype_id` + a duplicate `player_id` string (consider strip-and-reconstruct on load to avoid per-instance save bloat). Supersedes: refines `docs/design/career-roster-layer.md` §"Pillar 4" (line 91) — the bio that `observe_player` reads is re-derived from the roster id, not carried on `PlayerInstance`; the design's `PlayerInstance` sketch (line 52-53) is met (`last_scout_report` + the existing `observation_count`, no `scout_labels` field added).

- **2026-06-02 — External dual review (Claude + Codex) — re-prioritise the career layer to correct → persistent → visible BEFORE more match-engine work; two confirmed correctness bugs gate SaveV4.** Context: an independent read-only dual review (two AI reviewers outside the `/next` stream; memo `docs/audits/2026-06-02-external-dual-review-claude-codex.md`, committed as a tracked advisory artifact) found the highest-value problems are in the career/save/scouting data layer (the product differentiator), not the match engine (the recent focus). Two findings were independently re-verified against the cited lines by the main session and are REAL: **F1** — `evaluate()` seeds the breakthrough-delta RNG with `tick_for_rng = event.event_id.0` (`fw-memory/src/breakthrough.rs:984` positive, `:1049` regressive), but `filter_new_events_for_player` (`fw-tauri/src/season.rs`) re-`append`s the per-player events into a fresh `MemoryLedger` and `append` (`ledger.rs:169`) overwrites `event_id` from 0 → the RNG `tick` is the within-batch index, not the global career `EventId`, so a given `(player, family)` at batch-position k draws the IDENTICAL delta every season it occurs (the `site` already includes `player_id`, so it is NOT cross-player; it is a within-player cross-season correlation). Off the canonical-hash path (fw-memory), so CI never caught it; the `season.rs:643-648` comment claiming id-preservation is false. **F2** — `observe_player` hardcodes the RNG `site` to `0` (`fw-scouting/src/observe.rs:39`), so scout noise is keyed only by `observation_id`, not the player → two roster instances sharing a round-robin bio + `observation_count` produce byte-identical reports (defeats the uncertainty pillar); and `ScoutReport.player_id` stores the content-bio id (`observe.rs:57`) not the roster `PlayerId` (latent today because the T4-2.5f DTO projects `target_pid`, but persisting the report verbatim would freeze the wrong id). **Decision:** (1) the career layer is taken **correct → persistent → visible**: fix **T4-F1** (breakthrough RNG → global career `EventId`, preserving ids through the per-player filter, both gates) and **T4-F2** (scout RNG `site` ← roster player id; `ScoutReport.player_id` ← roster id) BEFORE **T4-2.5g** (SaveV4) persists `BreakthroughState`/scout reports — otherwise live careers bake in buggy progression/identity needing a migration to undo; (2) T4-2.5g additionally requires non-empty roster/scout/breakthrough migration fixtures + an explicit V3→V4 identity map (V3 `breakthrough_states` keyed by content-bio `PlayerId`; roster uses the `ROSTER_PLAYER_ID_BASE` range); (3) T4-F4 + T4-2.5h then make the pillars player-VISIBLE (roster/scout/breakthrough UI + the missing TS `IpcError::notYetObserved` variant), with T4-I1 (a DEV-gated, fail-loud fixture `invoke` shim) in parallel to make 1-4 browser-iterable; (4) match-engine integration + calibration + the per-fixture-sim policy (F5/F6) remain the long-term moat but are SECOND to the pillar layer right now. **C1 (CI posture):** the review CORRECTS an earlier external recommendation — do NOT drop macOS from the determinism gate (the local pre-commit hook does NOT gate the canonical hash; it is convenience-only/bypassable/fails-open, and DESIGN_DOC §"determinism" + ADR-0012 require all three OSes to agree). KEEP macOS on `determinism-gate.yml`; move only the expensive macOS `ci.yml` full build-test to path-filtered/nightly/phase-gate; apply when the repo flips private. The main session AGREES with the re-prioritisation after verifying F1/F2 (these are advisory findings, not commands; the call is the human's + main session's). F3/F4/F5/F6/I1/C1 carry the memo's `[RELAYED]` tag where not independently re-read — verify against the cited lines at pick time. Supersedes: refines the post-T4-2.5f plan ordering (the "T4-2.5g is next" pointer) by inserting T4-F1 → T4-F2 as correctness prerequisites; no prior DECISION content is reversed.

- **2026-06-03 — T4-2.5g (pre-task) — SaveV4 persists a MUTABLE-SUBSET `SavedPlayerInstance` + regenerates the base roster from the seed on load (Option A); `last_scout_report` + genes are NOT persisted.** Context: SaveV4 must persist the career roster, but `PlayerInstance` lives in `fw-tauri` and `fw-save` sits below it — so `fw-save` needs its own roster row type. The design (`docs/design/career-roster-layer.md:113`) names a `SavedPlayerInstance` "save-stable subset — mutable state only; PlayerBio is reloaded from content at load," which CONTRADICTED the `roster.rs` docstrings (T4-2.5d/f) that said genes are persisted by SaveV4. The fork (a FOREVER wire format) was put to the user (Option A mutable-subset+regenerate vs Option B persist-full-instance); **user chose Option A.** **Decision:** (1) **`SaveV4`** (SaveEnvelope wire tag `0x04`) = the `SaveV3` fields (`career_seed`, `content_pack_version`, `ledger`, `season_number`, `season: Option<SeasonState>`) MINUS the obsolete standalone `breakthrough_states` map, PLUS `roster: BTreeMap<ClubId, Vec<SavedPlayerInstance>>` + `breakthrough_eval_watermark: usize`. (2) **`SavedPlayerInstance`** (NEW, in `fw-save`) carries ONLY the mutable career deltas that are NOT deterministically reconstructible from `(career_seed + content)`: `player_id`, `club_id`, `slot`, `ceiling` (breakthrough-mutated), `breakthrough_state`, `season_stats`, `career_apps`, `observation_count`. It does NOT carry `display_name`, `genes`, `attributes`, `signature_candidates`, or `last_scout_report` — all re-derived at load. This keeps the forever-format lean and AVOIDS a new `fw-save → fw-scouting` dependency edge (no `ScoutReport` in the save — also sidesteps the F2/T4-2.5f-flagged `scout_archetype_id`/`player_id` bloat). (3) **`PlayerSeasonStats` moves `fw-tauri` → `fw-core`** (pure float-free data: u16/u32/i64 counters + a Q32-numerator rating; no behavior) so `fw-save`'s `SavedPlayerInstance` can reference it; `fw-tauri` re-exports/uses the `fw-core` type (serde wire-compatible — same field order). (4) **`migrate_v3_to_v4`** is a PURE `fw-save` fn (no content access): SaveV3 → SaveV4 carrying seed/content_pack_version/ledger/season_number/season, with `roster = BTreeMap::new()` (empty) + `breakthrough_eval_watermark = 0`; the V3 standalone `breakthrough_states` map is DROPPED (always-empty in practice — no production save path ever populated it; it is content-bio-`PlayerId`-keyed and cannot attach to the `ROSTER_PLAYER_ID_BASE` roster id space; the new home is per-instance `breakthrough_state`). An EMPTY roster signals "migrated from < V4 — caller MUST regenerate," exactly mirroring V3's `season = None` caller-responsibility. (5) **Load path** (`load_career` in `fw-tauri`, which HAS the content store): `load_envelope` → SaveV4; regenerate the base roster from `career_seed` via `build_roster_from_league` (re-deriving display_name/genes/attributes/signature_candidates deterministically — same seed → byte-identical base); then OVERLAY the persisted `SavedPlayerInstance` deltas onto the base by `player_id`; if the persisted roster is empty (migrated), use the regenerated base as a fresh career. `last_scout_report` is `None` on load (re-observed on the next match-day; `observation_count` IS persisted, so the count is preserved even though the cached report is rebuilt). (6) **`save_career`** projects `CareerState.roster` (`PlayerInstance`) → `Vec<SavedPlayerInstance>`, encodes SaveV4, writes to a NEW career-save path on `AppState` (mirrors the T4-6a `settings_path` pattern). **Consequence:** lean forever-format; no `fw-save → fw-scouting` edge; load is regenerate-base-then-overlay (more logic, covered by the 4 mandatory migration tests + a save→load→roster-count-stable integration test + a non-empty-roster frozen fixture). LOSSLESS today because breakthroughs mutate only the `ceiling` (persisted), not the 55 `attributes`; when T4.5-E1 starts mutating attributes, a SaveV5 bump adds `attributes` to `SavedPlayerInstance` (expected). Content drift between save+load shifts the regenerated base — guarded separately by the planned `mod_load_fingerprint`-on-load check (Content/RULES §6), out of scope here. Follows ADR-0010 (bincode 2, forward-only migration, byte-identical round-trip); no ADR-0010 amendment needed (SaveV4 conforms to the existing format policy). Supersedes: the "**2026-06-02 — T4-2.5f**" entry's stance that "T4-2.5g must persist `last_scout_report`" + its "consider strip-and-reconstruct on load" follow-up (RESOLVED: `last_scout_report` is NOT persisted — re-observed on load); refines the "**2026-06-02 — External dual review**" entry's "explicit V3→V4 identity map" requirement (the V3 `breakthrough_states` map is empty-in-practice and id-space-incompatible, so the migration DROPS it and the caller regenerates the roster — no usable identity map exists); corrects the `crates/fw-tauri/src/roster.rs` docstrings (T4-2.5d/f) that stated genes are persisted by SaveV4 (they are RE-DERIVED at load — the docstrings will be fixed in the T4-2.5g implementation).

- **2026-06-03 — T4-2.5h — Squad shows a default-club stand-in; per-player stats accrue as apps/goals/minutes (minutes = 90/appearance); rating deferred.** Context: T4-2.5h begins the external review's "visible" phase (surface real per-player stats), but the career model has NO "managed club" / "your club" concept (career-start club selection does not exist). The fork — what the Squad screen shows — was put to the user (default-club stand-in vs a league-wide 440-player table vs defer-UI); **user chose the default-club stand-in.** **Decision:** (1) **Squad shows the DEFAULT club** = the lowest `ClubId` in `CareerState.roster` (`roster.keys().next()`, deterministic), surfaced via a new `get_squad_roster` IPC → `SquadRosterDto { club_id, club_name, players: Vec<PlayerRosterDto> }`, and labelled in the UI as a placeholder ("No club selected yet — showing {club}"). This is a stand-in until career-start club-selection lands (a later feature, NOT built here); the eventual managed-club simply replaces the default-resolution. `Squad.tsx` switches from `get_squad` (the content-bio pool) to `get_squad_roster`; columns become Player/Role/Apps/Goals/Minutes (Region/Traits drop off — they live on `PlayerBio`, surfaced on the Player detail page). (2) **Stat accrual model** (first increment): `harvest_match_memory_events` accrues `season_stats` — `appearances += 1` and `minutes_played += 90` per appearance (a NOTIONAL full match; no substitutions modeled, and decoupled from the placeholder 600-tick season-match budget — real per-minute tracking needs subs + the full 5400-tick match, a T5-5b concern), and `goals += 1` for the `LegacyGoal` scorer (reusing the T4-2.5e scorer→slot mapping). `assists` stay 0 (the sim's `MatchEvent::Goal` carries no assister). Per-match RATING is DEFERRED — `average_rating_numerator`/`rating_sample_count` stay 0 until a real per-match-performance model exists (not in this row's scope). (3) **Stat scope:** `season_stats` is PER-SEASON — `advance_season_inner` resets it to default at the rollover; `career_apps` is career-long and is NOT reset. (4) **Role-from-slot for a single-club roster:** squad slots 0-10 map to the 4-3-3 formation (GK/DEF/MID/FWD); slots 11-21 are bench/depth and render `"Sub"` — NOT a fabricated formation position (a single club's roster is not two teams, so there is NO match-slot `slot-11` "away XI" shift; self-review P1 fix). **Consequence:** NON-canonical (stats + roster live in `CareerState`; both match-state pins UNCHANGED). The Squad screen is now stats-bearing but shows a placeholder club until club-selection exists. Supersedes: none (first decision on Squad-screen data source + stat-accrual model + the default-club stand-in; refines `docs/design/career-roster-layer.md` §"Stats" line 94, which named `update_player_stats_from_match` without specifying the minutes model or the no-managed-club Squad behaviour).

- **2026-06-03 — T4-F4 — "scouting view" = a Player-page scout-report SECTION (not a standalone board); close both `notYetObserved` + `leagueGenerationFailed` in the TS IpcError union.** Context: T4-F4 ("make the pillars player-visible") said "surface … in Squad + Player + a scouting view." Squad was already done (T4-2.5h). Run in AUTONOMOUS mode (the user's away /loop), so the "what is the scouting view" fork was resolved by recommended default rather than a user prompt. **Decision:** (1) the scout surface is a SECTION on the Player page (you view a player → you see the scout's uncertain read: overall + 3 category `UncertaintyBand` labels + label estimates, as band TEXT only — no raw stat numbers). A standalone scouting-BOARD screen is DEFERRED to the post-EA multi-scout track (T4-2.5m/n/p) — it is design-heavy and depends on multi-scout disagreement, which is itself deferred. (2) The scout fetch is a second `createResource` isolated from the player-detail fetch, with a `ScoutOutcome` discriminant: a real report renders bands; `notYetObserved` (roster player not yet scouted) → a graceful note, NOT an error; `playerNotFound` (content-bio/non-roster id) → the section is OMITTED (scouting is roster-only); genuine errors still surface. (3) Close BOTH missing TS `IpcError` variants now — `notYetObserved` (Rust added at T4-2.5f) AND the pre-existing `leagueGenerationFailed` (emitted by `get_squad_roster`/`advance_week`/`get_fixtures`; the long-standing gap a prior task spawned a follow-up for) — across the union, `KNOWN_IPC_ERROR_KINDS`, `describeRouteError`, and all per-route local kind lists. (4) `CategoryEstimateDto.category` is a closed TS union `"Physical"|"Mental"|"Technical"` (mirrors Rust `GeneCategory`), guard-enforced. **Consequence:** the differentiator is now visible on the Player page; frontend-only (zero Rust; both match-state pins UNCHANGED). **Surfaced + filed (NOT decided here):** the roster Squad has no player-name → Player navigation (T4-2.5h's `rosterColumns` dropped the link; the roster `PlayerRosterDto` carries only a u32, so a navigable content-pack-qualified id is needed) — so the scout section, while correct, is not yet reachable for a roster player in the running app; the fix needs a small Rust DTO addition and is a separate follow-up task (out of this frontend-only row). Supersedes: refines `docs/design/career-roster-layer.md` §"Pillar 4" / the T4-F4 row's "a scouting view" wording (= a Player-page section; standalone board deferred).
- **2026-06-03 — T4-I1 — the dev fixture `invoke` shim is DEV-only, dual opt-in, fail-loud, and routes the current read commands only.** Context: the T4-2.5h Squad-stats + T4-F4 Player-scout UI cannot be browser-previewed because the Tauri routes call `invoke`, which is undefined outside the Tauri runtime (Step-5.1 preview was logged "n/a"). Run in AUTONOMOUS mode (the user's away /loop), so the shim's scoping was resolved by recommended default rather than a user prompt. **Decision:** (1) the shim lives at the single `safeInvoke` chokepoint, so every IPC read benefits and the SAME runtime shape guard validates the fixture — a fixture that doesn't match the guard fails exactly like a bad `invoke` response would. (2) DUAL opt-in: env `VITE_FW_BROWSER_BACKEND=fixtures` (for `pnpm dev`/preview sessions) OR `?backend=fixtures` URL param (for ad-hoc per-tab toggling); a dedicated `fixtureBackendActive()` reads both. (3) Production-safe by construction: `fixtureBackendActive()` returns `false` immediately on `!import.meta.env.DEV`, so a production Vite build dead-code-eliminates the entire shim branch and the live `invoke` path is byte-identical to before — the shim can NEVER ship to players. (4) STRICTLY fail-loud: a missing/404/network-failed/malformed-JSON fixture throws `IpcShapeError`, never a silent fallback to `invoke` and never a default value — a broken fixture is a loud dev error, matching the project's no-silent-failure posture. (5) `fixtureBackendActive()` is a SEPARATE concept from `isTauri()` (runtime detection) — they are not conflated; `isTauri()` semantics are untouched. (6) Scope = the current read commands only (`get_settings`/`get_squad_roster`/`get_player_detail`/`get_scout_report`/`get_standings`/`get_fixtures`/`get_career_overview`), each with a committed guard-passing fixture under `frontend/public/dev-fixtures/`; the 2D board already previews on real `dump_frames` fixture data via `/dev/board-preview`, so its commands are intentionally left for later extension rather than shimmed now. **Consequence:** the read routes render in plain Chrome, closing the Step-5.1 preview gap for the career-layer UI; frontend dev-infra only (zero Rust; both match-state pins UNCHANGED). **Deferred (cosmetic, no gate impact):** the `import type` block currently sits mid-file between `loadFixture` and the `IpcShapeError` class — a reorder filed as a follow-up; it compiles + lints today because the type reference resolves at call time. Supersedes: none.
